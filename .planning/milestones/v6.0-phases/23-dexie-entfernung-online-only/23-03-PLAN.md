---
phase: 23-dexie-entfernung-online-only
plan: 03
type: execute
wave: 2
depends_on:
  - 23-01
  - 23-02
files_modified:
  - client/src/features/pos/useSaleComplete.ts
  - client/src/features/admin/reports/DailyReport.tsx
  - client/src/hooks/useLowStockCount.ts
  - client/src/features/pos/POSScreen.tsx
  - client/vite.config.ts
  - client/package.json
autonomous: true
requirements:
  - ONL-01
  - ONL-02
  - ONL-03

must_haves:
  truths:
    - "Ein Verkauf sendet direkt an den Server — bei Fehler sieht die Nutzerin eine Fehlermeldung, kein Outbox-Eintrag"
    - "DailyReport lädt Verkäufe per TanStack Query vom Server, nicht mehr aus Dexie"
    - "useLowStockCount nutzt die TQ-gecachten Produkte aus useProducts, nicht mehr useLiveQuery"
    - "Ohne Internetverbindung zeigt POSScreen ein vollbildiges Overlay mit Hinweistext"
    - "dexie und dexie-react-hooks sind aus package.json entfernt"
    - "idb-keyval ist aus package.json entfernt"
    - "Der Service Worker cached keine /api/*-Responses mehr"
  artifacts:
    - path: "client/src/features/pos/useSaleComplete.ts"
      provides: "Online-Only completeSale + completeWithdrawal"
      contains: "throw new Error"
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "Server-basierter Tagesbericht per TanStack Query"
      contains: "useQuery"
    - path: "client/src/hooks/useLowStockCount.ts"
      provides: "useLowStockCount basierend auf useProducts"
      contains: "useProducts"
    - path: "client/src/features/pos/POSScreen.tsx"
      provides: "Vollbild-Offline-Overlay bei fehlendem Internet"
      contains: "isOnline"
    - path: "client/vite.config.ts"
      provides: "Service Worker ohne runtimeCaching für /api/*"
    - path: "client/package.json"
      provides: "package.json ohne dexie, dexie-react-hooks, idb-keyval"
  key_links:
    - from: "client/src/features/pos/useSaleComplete.ts"
      to: "/api/sync"
      via: "fetch POST"
      pattern: "fetch.*api/sync"
    - from: "client/src/features/admin/reports/DailyReport.tsx"
      to: "/api/sales"
      via: "useQuery queryFn fetch"
      pattern: "api/sales"
    - from: "client/src/hooks/useLowStockCount.ts"
      to: "useProducts"
      via: "filter auf products"
      pattern: "useProducts"
---

<objective>
Abschluss der Dexie-Entfernung: useSaleComplete auf Online-Only umstellen (kein Outbox-Fallback), DailyReport auf TanStack Query umstellen, useLowStockCount auf useProducts-Daten umstellen, POSScreen Offline-Overlay implementieren, Service Worker API-Caching entfernen, Dexie/idb-keyval aus package.json entfernen.

Purpose: Dieser Plan schließt Phase 23 ab. Nach Plan 01 und 02 gibt es keine Dexie-Aufrufe mehr in Hooks und Auth — dieser Plan bereinigt die letzten verbleibenden Stellen und entfernt die Packages selbst.

Output: Die App ist vollständig online-only. npm run build schlägt fehl wenn noch Dexie-Imports vorhanden sind. Offline-Nutzerin sieht sofort einen klaren Hinweis.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/23-dexie-entfernung-online-only/23-01-SUMMARY.md
@.planning/phases/23-dexie-entfernung-online-only/23-02-SUMMARY.md

<interfaces>
<!-- TanStack Query Muster für DailyReport (nach Plan 01 — useProducts als Referenz) -->

Existierendes Muster aus useProducts.ts (nach Plan 01):
```typescript
import { useQuery } from '@tanstack/react-query';
import { getShopId } from '../../db/index.js';
import { getAuthHeaders } from '../../features/auth/serverAuth.js';

export function useSales(dayStart: number, dayEnd: number) {
  const shopId = getShopId();
  return useQuery<Sale[]>({
    queryKey: ['sales', shopId, dayStart, dayEnd],
    queryFn: async () => {
      const headers = await getAuthHeaders();
      const res = await fetch(`/api/sales?shopId=${shopId}&from=${dayStart}&to=${dayEnd}`, { headers });
      if (!res.ok) throw new Error('Verkäufe konnten nicht geladen werden');
      return await res.json() as Sale[];
    },
  });
}
```

<!-- Server API für Sales -->
Der Server hat einen GET /api/sales Endpoint (aus Phase 19-20).
Query-Parameter: shopId, from (Unix-ms), to (Unix-ms).
Antwort: Sale[] (camelCase — Server gibt bereits camelCase zurück oder snake_case wie bei products).

PRÜFEN: Ob der Server-Endpoint /api/sales bereits existiert und welche Query-Parameter er erwartet.
Falls snake_case: Mapping analog zu mapServerProduct in useProducts nötig.

<!-- POSScreen Offline-Overlay -->
POSScreen hat bereits isOnline-State:
```typescript
const [isOnline, setIsOnline] = useState(navigator.onLine);
// online/offline Event-Listener bereits vorhanden
```
Das Overlay muss über der gesamten UI erscheinen — position: fixed, z-50, fullscreen.

<!-- useSaleComplete — neues Muster nach Umbau -->
completeSale() und completeWithdrawal() werden zu reinen Online-Funktionen:
- kein db.transaction, kein db.products.modify, kein db.sales.add
- kein db.outbox.add
- Nur: fetch POST /api/sync — bei Fehler throw new Error
- EK-Preise für completeWithdrawal: kommen nicht mehr aus Dexie, müssen aus den TQ-gecachten Produkten kommen

WICHTIG für completeWithdrawal: Da kein Dexie-Zugriff mehr möglich ist, muss der Caller (POSScreen) die enrichedItems übergeben — oder completeWithdrawal bekommt die products als Parameter. Einfachste Lösung: POSScreen übergibt useProducts-Daten an handleWithdrawal.

Alternativ: completeWithdrawal bekommt bereits angereicherte items (mit purchasePrice) — der Caller muss das Anreichern übernehmen.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: useSaleComplete Online-Only + DailyReport auf TanStack Query + useLowStockCount auf useProducts</name>
  <files>
    client/src/features/pos/useSaleComplete.ts,
    client/src/features/admin/reports/DailyReport.tsx,
    client/src/hooks/useLowStockCount.ts
  </files>
  <read_first>
    - client/src/features/pos/useSaleComplete.ts (vollständig lesen — aktueller Stand nach Plan 01)
    - client/src/features/admin/reports/DailyReport.tsx (vollständig lesen)
    - client/src/hooks/useLowStockCount.ts (vollständig lesen)
    - client/src/features/pos/POSScreen.tsx (lesen um zu verstehen wie handleWithdrawal aufgerufen wird)
    - client/src/hooks/api/useProducts.ts (nach Plan 01 — prüfen welche Typen exportiert werden)
    - client/src/db/index.ts (nach Plan 01 — prüfen welche Typen exportiert werden)
    - server/src/routes/sales.ts (prüfen: GET /api/sales — Query-Parameter, Response-Format, snake_case oder camelCase)
  </read_first>
  <action>
    1. USSALECOMPLETE.TS: client/src/features/pos/useSaleComplete.ts

    Imports: Entfernen: `import { db, getShopId } from '../../db/index.js';`
    Behalten/Ändern: `import type { CartItem, Sale, Product } from '../../db/index.js';`
    Behalten: `import { getShopId } from '../../db/index.js';`
    Behalten: `import { getAuthHeaders } from '../auth/serverAuth.js';`

    `completeSale()` — neuer Inhalt:
    ```typescript
    export async function completeSale(
      items: CartItem[],
      paidCents: number,
      changeCents: number
    ): Promise<Sale> {
      const totalCents = items.reduce((sum, i) => sum + i.salePrice * i.quantity, 0);
      if (paidCents < totalCents) throw new Error(`Bezahlt (${paidCents}) ist weniger als Gesamtpreis (${totalCents})`);
      const donationCents = paidCents - totalCents - changeCents;
      if (donationCents < 0) throw new Error(`Wechselgeld (${changeCents}) größer als Differenz (${paidCents - totalCents})`);

      const sale: Sale = {
        id: crypto.randomUUID(),
        shopId: getShopId(),
        items,
        totalCents,
        paidCents,
        changeCents,
        donationCents,
        type: 'sale',
        createdAt: Date.now(),
      };

      const headers = await getAuthHeaders();
      const res = await fetch('/api/sync', {
        method: 'POST',
        headers,
        body: JSON.stringify({
          entries: [{ operation: 'SALE_COMPLETE', payload: sale, shopId: getShopId(), createdAt: Date.now(), attempts: 0 }],
        }),
      });
      if (!res.ok) throw new Error('Verkauf konnte nicht gesendet werden. Bitte Internetverbindung prüfen.');
      return sale;
    }
    ```

    `completeWithdrawal()` — neuer Inhalt (items kommen bereits mit purchasePrice vom Caller):
    ```typescript
    export async function completeWithdrawal(
      items: CartItem[],
      products: Product[],
      reason?: string
    ): Promise<Sale> {
      // EK-Preise aus den übergebenen products anreichern
      const productMap = new Map(products.map(p => [p.id, p]));
      const enrichedItems = items.map(item => ({
        ...item,
        purchasePrice: productMap.get(item.productId)?.purchasePrice ?? 0,
      }));

      const totalCents = enrichedItems.reduce((sum, i) => sum + (i.purchasePrice ?? 0) * i.quantity, 0);

      const sale: Sale = {
        id: crypto.randomUUID(),
        shopId: getShopId(),
        items: enrichedItems,
        totalCents,
        paidCents: 0,
        changeCents: 0,
        donationCents: 0,
        type: 'withdrawal',
        withdrawalReason: reason?.trim() || undefined,
        createdAt: Date.now(),
      };

      const headers = await getAuthHeaders();
      const res = await fetch('/api/sync', {
        method: 'POST',
        headers,
        body: JSON.stringify({
          entries: [{ operation: 'SALE_COMPLETE', payload: sale, shopId: getShopId(), createdAt: Date.now(), attempts: 0 }],
        }),
      });
      if (!res.ok) throw new Error('Entnahme konnte nicht gesendet werden. Bitte Internetverbindung prüfen.');
      return sale;
    }
    ```

    HINWEIS zu POSScreen.tsx: Da `completeWithdrawal` jetzt `products` als zweiten Parameter braucht, muss POSScreen.tsx in Task 2 angepasst werden. POSScreen hat bereits `useProducts` — die Daten sind dort verfügbar.

    2. DAILYREPORT.TSX: client/src/features/admin/reports/DailyReport.tsx

    Import ersetzen:
    - Entfernen: `import { useLiveQuery } from 'dexie-react-hooks';`
    - Entfernen: `import { db, getShopId } from '../../../db/index.js';`
    - Hinzufügen: `import { useQuery } from '@tanstack/react-query';`
    - Hinzufügen: `import { getShopId } from '../../../db/index.js';`
    - Hinzufügen: `import { getAuthHeaders } from '../../auth/serverAuth.js';`
    - Behalten: `import type { Sale } from '../../../db/index.js';`

    `useLiveQuery(db.sales...)` durch `useQuery` ersetzen:
    ```typescript
    const { data: sales = [] } = useQuery<Sale[]>({
      queryKey: ['sales', getShopId(), dayStart, dayEnd],
      queryFn: async () => {
        const shopId = getShopId();
        const headers = await getAuthHeaders();
        const res = await fetch(
          `/api/sales?shopId=${shopId}&from=${dayStart}&to=${dayEnd}`,
          { headers }
        );
        if (!res.ok) throw new Error('Verkäufe konnten nicht geladen werden');
        const data = await res.json() as Record<string, unknown>[];
        // Server gibt snake_case zurück — mappen
        return data.map(s => ({
          id: s.id as string,
          shopId: (s.shop_id ?? s.shopId) as string,
          items: (s.items ?? []) as Sale['items'],
          totalCents: (s.total_cents ?? s.totalCents ?? 0) as number,
          paidCents: (s.paid_cents ?? s.paidCents ?? 0) as number,
          changeCents: (s.change_cents ?? s.changeCents ?? 0) as number,
          donationCents: (s.donation_cents ?? s.donationCents ?? 0) as number,
          type: (s.type ?? 'sale') as Sale['type'],
          withdrawalReason: (s.withdrawal_reason ?? s.withdrawalReason) as string | undefined,
          createdAt: (s.created_at ?? s.createdAt) as number,
          cancelledAt: (s.cancelled_at ?? s.cancelledAt) as number | undefined,
          returnedItems: (s.returned_items ?? s.returnedItems) as string[] | undefined,
        }));
      },
    });
    ```

    Die `useMemo`-Berechnungen für `dayStart`, `dayEnd`, `stats` bleiben unverändert.
    Die JSX-Ausgabe bleibt vollständig unverändert.

    WICHTIG: Server prüfen (read_first server/src/routes/sales.ts) — falls der Endpoint `/api/sales?from=&to=` nicht existiert, stattdessen den tatsächlichen Endpoint-Pfad und die Parameter anpassen.

    3. USLOWSTOCKCOUNT.TS: client/src/hooks/useLowStockCount.ts

    Komplett neu schreiben:
    ```typescript
    import { useProducts } from './api/useProducts.js';

    export function useLowStockProducts() {
      const { data: products = [] } = useProducts();
      return products.filter(p => p.active && p.minStock > 0 && p.stock <= p.minStock);
    }

    export function useLowStockCount(): number {
      return useLowStockProducts().length;
    }
    ```

    Entfernen: `import { useLiveQuery } from 'dexie-react-hooks';`, `import { db, getShopId } from '../db/index.js';`
  </action>
  <verify>
    <automated>grep -n "dexie\|useLiveQuery\|db\." client/src/features/pos/useSaleComplete.ts client/src/features/admin/reports/DailyReport.tsx client/src/hooks/useLowStockCount.ts 2>/dev/null</automated>
    Erwartet: 0 Treffer
  </verify>
  <done>
    - useSaleComplete.ts: kein Dexie, completeSale wirft bei Server-Fehler, completeWithdrawal hat neuen products-Parameter
    - DailyReport.tsx: useQuery statt useLiveQuery, Daten vom Server
    - useLowStockCount.ts: nutzt useProducts, kein Dexie
  </done>
  <acceptance_criteria>
    - `grep "dexie\|useLiveQuery\|OutboxEntry\|db\." client/src/features/pos/useSaleComplete.ts` gibt 0 Treffer
    - `grep "dexie-react-hooks\|useLiveQuery" client/src/features/admin/reports/DailyReport.tsx` gibt 0 Treffer
    - `grep "useQuery" client/src/features/admin/reports/DailyReport.tsx` gibt mindestens 1 Treffer
    - `grep "dexie\|useLiveQuery\|db\." client/src/hooks/useLowStockCount.ts` gibt 0 Treffer
    - `grep "useProducts" client/src/hooks/useLowStockCount.ts` gibt mindestens 1 Treffer
    - `grep "throw new Error" client/src/features/pos/useSaleComplete.ts` gibt mindestens 2 Treffer (completeSale + completeWithdrawal)
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: POSScreen Offline-Overlay + vite.config.ts API-Caching entfernen + package.json Dexie/idb-keyval entfernen</name>
  <files>
    client/src/features/pos/POSScreen.tsx,
    client/vite.config.ts,
    client/package.json
  </files>
  <read_first>
    - client/src/features/pos/POSScreen.tsx (vollständig lesen — isOnline-State, handleWithdrawal)
    - client/src/hooks/api/useProducts.ts (nach Plan 01 — prüfen was exportiert wird)
    - client/vite.config.ts (vollständig lesen)
    - client/package.json (vollständig lesen)
  </read_first>
  <action>
    1. POSSCREEN.TSX: client/src/features/pos/POSScreen.tsx

    A) Offline-Overlay hinzufügen:

    Import: `import { useProducts } from '../../hooks/api/useProducts.js';` hinzufügen (für completeWithdrawal).

    In der Komponente:
    ```typescript
    const { data: products = [] } = useProducts();
    ```

    Das Offline-Overlay als erste Renderbedingung hinzufügen — DIREKT nach den useState/useEffect-Hooks, VOR allen View-Checks:

    ```typescript
    // Offline-Overlay — überschreibt gesamte UI bei fehlender Verbindung
    if (!isOnline) {
      return (
        <div className="fixed inset-0 z-50 bg-sky-50 flex flex-col items-center justify-center gap-6 p-8">
          <WifiOff size={64} className="text-sky-300" />
          <div className="text-center">
            <h2 className="text-2xl font-bold text-slate-700 mb-2">Keine Internetverbindung</h2>
            <p className="text-slate-500 text-sm max-w-xs">
              Die Fairstand Kasse benötigt eine aktive Internetverbindung.
              Bitte WLAN oder Mobilfunk aktivieren.
            </p>
          </div>
        </div>
      );
    }
    ```

    B) handleWithdrawal anpassen — completeWithdrawal hat jetzt products-Parameter:
    ```typescript
    async function handleWithdrawal() {
      const reason = window.prompt('Grund der Entnahme (optional):', '');
      if (reason === null) return;
      try {
        setError(null);
        const sale = await completeWithdrawal(cart.items, products, reason);
        setLastSale(sale);
        setView('summary');
        setIsCartOpen(false);
      } catch (err) {
        setError(err instanceof Error ? err.message : 'Fehler bei der Entnahme.');
      }
    }
    ```

    C) WifiOff ist bereits importiert aus 'lucide-react' — prüfen und ggf. zum Import hinzufügen.

    2. VITE.CONFIG.TS: client/vite.config.ts

    Im `VitePWA`-Plugin den `workbox.runtimeCaching`-Array vollständig entfernen:

    ```typescript
    // VORHER:
    workbox: {
      globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
      runtimeCaching: [
        {
          urlPattern: /^https:\/\/.*\/api\/.*/,
          handler: 'NetworkFirst',
          options: { cacheName: 'api-cache', networkTimeoutSeconds: 5 },
        },
      ],
    },

    // NACHHER:
    workbox: {
      globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
    },
    ```

    Der `globPatterns`-Eintrag bleibt — er cached die App-Shell (HTML, JS, CSS, Icons) für PWA-Installation. Nur das API-Caching wird entfernt.

    3. PACKAGE.JSON: client/package.json

    ```bash
    cd client && npm uninstall dexie dexie-react-hooks idb-keyval
    ```

    Diesen Befehl ausführen. npm aktualisiert package.json und package-lock.json automatisch.

    Nach dem Uninstall: Prüfen ob der TypeScript-Build Fehler wirft:
    ```bash
    cd client && npm run build 2>&1 | head -50
    ```

    Falls Build-Fehler: Diese zeigen verbleibende Dexie-Imports an — diese beheben bevor der Plan als abgeschlossen gilt.
  </action>
  <verify>
    <automated>grep -n "dexie\|idb-keyval" client/package.json 2>/dev/null</automated>
    Erwartet: 0 Treffer
  </verify>
  <done>
    - POSScreen.tsx: Offline-Overlay bei !isOnline als erstes Render-Return, useProducts für completeWithdrawal
    - vite.config.ts: runtimeCaching-Array entfernt, nur App-Shell-Precaching bleibt
    - package.json: dexie, dexie-react-hooks, idb-keyval nicht mehr in dependencies
    - npm run build schlägt nicht wegen Dexie-Importen fehl
  </done>
  <acceptance_criteria>
    - `grep "dexie\|idb-keyval" client/package.json` gibt 0 Treffer
    - `grep "runtimeCaching" client/vite.config.ts` gibt 0 Treffer
    - `grep "globPatterns" client/vite.config.ts` gibt mindestens 1 Treffer (App-Shell-Caching bleibt)
    - `grep "isOnline.*return\|!isOnline" client/src/features/pos/POSScreen.tsx` gibt mindestens 1 Treffer (Offline-Guard)
    - `grep "WifiOff" client/src/features/pos/POSScreen.tsx` gibt mindestens 2 Treffer (Import + JSX)
    - `grep "dexie" client/node_modules/.package-lock.json 2>/dev/null | grep '"name": "dexie"'` gibt 0 Treffer (nach npm uninstall)
    - `cd client && npm run build 2>&1 | grep "error TS"` gibt 0 Treffer (Build sauber)
  </acceptance_criteria>
</task>

</tasks>

<verification>
Gesamtprüfung nach allen drei Plänen:
- `grep -r "from 'dexie'\|from \"dexie\"\|dexie-react-hooks" client/src/ 2>/dev/null` → 0 Treffer
- `grep -r "idb-keyval" client/src/ 2>/dev/null` → 0 Treffer
- `grep -r "db\.outbox\|db\.products\|db\.sales\|db\.cartItems\|db\.categories" client/src/ 2>/dev/null` → 0 Treffer
- `test ! -f client/src/db/schema.ts && test ! -f client/src/sync/engine.ts && test ! -f client/src/sync/triggers.ts` → Exit 0
- `cd client && npm run build` → erfolgreich, keine TS-Fehler
</verification>

<success_criteria>
- useSaleComplete: Online-Only POST, kein Offline-Fallback
- DailyReport: TanStack Query vom Server, kein useLiveQuery
- useLowStockCount: basiert auf useProducts-Daten
- POSScreen: Vollbild-Overlay bei fehlender Verbindung
- vite.config.ts: kein API-Caching
- package.json: Dexie und idb-keyval entfernt, Build sauber
</success_criteria>

<output>
Nach Completion: .planning/phases/23-dexie-entfernung-online-only/23-03-SUMMARY.md erstellen.
</output>
