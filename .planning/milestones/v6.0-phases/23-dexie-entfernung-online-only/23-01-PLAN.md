---
phase: 23-dexie-entfernung-online-only
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/db/schema.ts
  - client/src/db/index.ts
  - client/src/sync/engine.ts
  - client/src/sync/triggers.ts
  - client/src/hooks/api/useProducts.ts
  - client/src/hooks/api/useCategories.ts
  - client/src/features/pos/useCart.ts
  - client/src/App.tsx
autonomous: true
requirements:
  - DEX-01
  - DEX-02
  - DEX-03
  - DEX-04

must_haves:
  truths:
    - "Kein Import von 'dexie' oder 'dexie-react-hooks' existiert mehr in der gesamten Codebase"
    - "Warenkorb hält seinen Stand per React-State zwischen Tab-Wechseln, startet leer nach Reload"
    - "useProducts und useCategories holen Daten nur noch vom Server — kein Dexie-Fallback"
    - "App.tsx ruft keine Sync-Trigger mehr auf"
  artifacts:
    - path: "client/src/db/index.ts"
      provides: "shopId-State ohne Dexie — nur getShopId/setShopId Funktionen"
      contains: "export function getShopId"
    - path: "client/src/hooks/api/useProducts.ts"
      provides: "Server-only useProducts ohne Dexie write-through"
      contains: "useQuery"
    - path: "client/src/hooks/api/useCategories.ts"
      provides: "Server-only useCategories ohne Dexie write-through"
      contains: "useQuery"
    - path: "client/src/features/pos/useCart.ts"
      provides: "Reiner React-State Warenkorb"
      contains: "useReducer"
  key_links:
    - from: "client/src/hooks/api/useProducts.ts"
      to: "/api/products"
      via: "fetch in queryFn"
      pattern: "fetch.*api/products"
    - from: "client/src/features/pos/useCart.ts"
      to: "useReducer"
      via: "cartReducer"
      pattern: "useReducer"
---

<objective>
Dexie-Kern aus allen Hooks und API-Dateien entfernen. Das betrifft die DB-Layer-Dateien selbst (db/schema.ts wird geleert, db/index.ts auf shopId-only reduziert), die Sync-Dateien (engine.ts und triggers.ts gelöscht), sowie alle Hooks die Dexie schreiben oder lesen.

Purpose: Dexie ist nach Phase 22 (PostgreSQL) keine Notwendigkeit mehr — Server ist Single Source of Truth. Dieser Plan entfernt alle Dexie-Abhängigkeiten außer den UI-Komponenten (die kommen in Plan 03).

Output: db/index.ts ohne FairstandDB-Instanz, useProducts/useCategories ohne Dexie write-through/fallback, useCart als reiner React-State.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aktueller Zustand der zu ändernden Dateien (vor Plan-Ausführung) -->

client/src/db/index.ts (AKTUELL):
```typescript
import { FairstandDB } from './schema.js';
export type { Product, Sale, SaleItem, CartItem, PersistedCartItem, OutboxEntry, Category } from './schema.js';
export const db = new FairstandDB();
let _shopId: string | null = null;
export function setShopId(id: string): void { _shopId = id; }
export function getShopId(): string {
  if (!_shopId) throw new Error('Shop nicht gesetzt — zuerst einloggen');
  return _shopId;
}
```

client/src/hooks/api/useProducts.ts (AKTUELL — Dexie write-through und fallback vorhanden):
```typescript
import { db, getShopId } from '../../db/index.js';
// In queryFn: db.transaction('rw', db.products, ...) write-through
// In catch: db.products.where('shopId').equals(shopId).toArray() fallback
```

client/src/hooks/api/useCategories.ts (AKTUELL — gleiche Struktur):
```typescript
import { db, getShopId } from '../../db/index.js';
// write-through und catch-fallback identisch zu useProducts
```

client/src/features/pos/useCart.ts (AKTUELL):
```typescript
import { db, getShopId } from '../../db/index.js';
// Effect 1: lädt aus db.cartItems beim Mount
// Effect 2: persistiert nach jeder Reducer-Aktion in db.cartItems
// updateQuantity: db.products.get(productId) für Stock-Check
```

client/src/App.tsx (AKTUELL):
```typescript
import { registerSyncTriggers } from './sync/triggers.js';
// in UnlockedApp:
useEffect(() => { registerSyncTriggers(queryClient); }, [queryClient]);
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: db/schema.ts löschen, db/index.ts auf shopId-only reduzieren, Sync-Dateien löschen</name>
  <files>
    client/src/db/schema.ts,
    client/src/db/index.ts,
    client/src/sync/engine.ts,
    client/src/sync/triggers.ts
  </files>
  <read_first>
    - client/src/db/schema.ts (vollständig lesen — wird komplett gelöscht)
    - client/src/db/index.ts (vollständig lesen — wird radikal vereinfacht)
    - client/src/sync/engine.ts (vollständig lesen — wird gelöscht)
    - client/src/sync/triggers.ts (vollständig lesen — wird gelöscht)
    - client/src/features/auth/useAuth.ts (prüfen ob es getShopId oder setShopId importiert)
    - client/src/hooks/useWebSocket.ts (prüfen ob db importiert)
  </read_first>
  <action>
    1. LÖSCHEN: client/src/db/schema.ts komplett entfernen (enthält FairstandDB-Klasse und alle Dexie-Typen).

    2. ERSETZEN: client/src/db/index.ts — neuen Inhalt schreiben ohne jeden Dexie-Bezug:

    ```typescript
    // Typen — direkt hier definiert (kein Dexie mehr)
    export interface Category {
      id: string;
      shopId: string;
      name: string;
      sortOrder: number;
      createdAt: number;
    }

    export interface Product {
      id: string;
      shopId: string;
      articleNumber: string;
      name: string;
      category: string;
      purchasePrice: number;
      salePrice: number;
      vatRate: number;
      stock: number;
      minStock: number;
      active: boolean;
      imageUrl?: string;
      updatedAt: number;
    }

    export interface SaleItem {
      productId: string;
      articleNumber: string;
      name: string;
      salePrice: number;
      purchasePrice?: number;
      quantity: number;
    }

    export type CartItem = SaleItem;

    export interface Sale {
      id: string;
      shopId: string;
      items: SaleItem[];
      totalCents: number;
      paidCents: number;
      changeCents: number;
      donationCents: number;
      type?: 'sale' | 'withdrawal';
      withdrawalReason?: string;
      createdAt: number;
      syncedAt?: number;
      cancelledAt?: number;
      returnedItems?: string[];
    }

    // shopId-State — wird nach Login gesetzt
    let _shopId: string | null = null;

    export function setShopId(id: string): void {
      _shopId = id;
    }

    export function getShopId(): string {
      if (!_shopId) throw new Error('Shop nicht gesetzt — zuerst einloggen');
      return _shopId;
    }
    ```

    Wichtig: KEIN `export const db`, kein Dexie-Import, kein `OutboxEntry`, kein `PersistedCartItem` (nicht mehr benötigt).

    3. LÖSCHEN: client/src/sync/engine.ts komplett entfernen.

    4. LÖSCHEN: client/src/sync/triggers.ts komplett entfernen.

    HINWEIS: TypeScript-Fehler wegen fehlender Imports werden in Task 2 und in Plan 02/03 behoben. Dieser Task bricht bewusst andere Dateien — das ist der Plan.
  </action>
  <verify>
    <automated>grep -r "from.*dexie" client/src/db/ 2>/dev/null | grep -v ".d.ts" | wc -l</automated>
    Erwartet: 0 (keine Dexie-Imports mehr in db/)
  </verify>
  <done>
    - client/src/db/schema.ts existiert nicht mehr
    - client/src/db/index.ts enthält kein Dexie-Import, exportiert Product, Sale, SaleItem, CartItem, Category, getShopId, setShopId
    - client/src/sync/engine.ts existiert nicht mehr
    - client/src/sync/triggers.ts existiert nicht mehr
  </done>
  <acceptance_criteria>
    - `grep -r "import.*dexie" client/src/db/` gibt 0 Treffer
    - `test ! -f client/src/db/schema.ts` gibt Exit 0
    - `test ! -f client/src/sync/engine.ts` gibt Exit 0
    - `test ! -f client/src/sync/triggers.ts` gibt Exit 0
    - `grep "export function getShopId" client/src/db/index.ts` gibt einen Treffer
    - `grep "export interface Product" client/src/db/index.ts` gibt einen Treffer
    - `grep "export interface Sale " client/src/db/index.ts` gibt einen Treffer
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: useProducts, useCategories Dexie-Code entfernen + useCart auf reinen React-State + App.tsx Sync-Trigger entfernen</name>
  <files>
    client/src/hooks/api/useProducts.ts,
    client/src/hooks/api/useCategories.ts,
    client/src/features/pos/useCart.ts,
    client/src/App.tsx
  </files>
  <read_first>
    - client/src/hooks/api/useProducts.ts (vollständig lesen)
    - client/src/hooks/api/useCategories.ts (vollständig lesen)
    - client/src/features/pos/useCart.ts (vollständig lesen)
    - client/src/App.tsx (vollständig lesen)
  </read_first>
  <action>
    1. USEPRODUCTS: client/src/hooks/api/useProducts.ts

    Import ändern: `import { getShopId } from '../../db/index.js';` (kein `db` mehr).
    Import von `type { Product }` aus `'../../db/index.js'` bleibt.

    In `queryFn` von `useProducts()`:
    - Das `try/catch`-Block-Muster durch direkten Fetch ersetzen (kein Fallback mehr):
    ```typescript
    queryFn: async () => {
      const headers = await getAuthHeaders();
      const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
      if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
      const data = await res.json() as Record<string, unknown>[];
      return data.map(mapServerProduct);
    },
    ```
    - Die `db.transaction(...)` write-through-Zeilen entfernen (5 Zeilen nach `bulkPut`).
    - Das `catch`-Block mit `db.products.where(...)` komplett entfernen.

    In `useAdjustStock()`: import `db` wird nicht mehr gebraucht — bereits erledigt durch Import-Änderung oben.

    2. USECATEGORIES: client/src/hooks/api/useCategories.ts

    Import ändern: `import { getShopId } from '../../db/index.js';` (kein `db`).
    Import von `type { Category }` bleibt.

    In `queryFn` von `useCategories()` gleiche Änderung:
    ```typescript
    queryFn: async () => {
      const headers = await getAuthHeaders();
      const res = await fetch(`/api/categories?shopId=${shopId}`, { headers });
      if (!res.ok) throw new Error('Kategorien konnten nicht geladen werden');
      const data = await res.json() as Record<string, unknown>[];
      return data.map(c => ({
        id: c.id as string,
        shopId: (c.shop_id ?? c.shopId) as string,
        name: c.name as string,
        sortOrder: (c.sort_order ?? c.sortOrder ?? 0) as number,
        createdAt: (c.created_at ?? c.createdAt ?? Date.now()) as number,
      }));
    },
    ```
    - Dexie write-through und catch-Fallback entfernen.

    3. USECART: client/src/features/pos/useCart.ts

    Imports: Entfernen: `import { db, getShopId } from '../../db/index.js';`
    Behalten: `import type { CartItem, Product } from '../../db/index.js';`

    `CartAction`-Typ: `{ type: 'LOAD'; items: CartItem[] }` entfernen (wird nicht mehr gebraucht).

    Im `cartReducer`: `case 'LOAD':` Block entfernen.

    Hook `useCart()`:
    - `useState(false)` für `loaded` entfernen — wird nicht mehr gebraucht.
    - Effect 1 (loadAndValidate aus Dexie) komplett entfernen.
    - Effect 2 (persist nach Reducer-Aktion) komplett entfernen.
    - `invalidItems` und `clearInvalidItems` BEHALTEN (logische Validation bleibt, nur kein Dexie).
    - `updateQuantity()`: Dexie-Abfrage `db.products.get(productId)` entfernen. Stock-Check jetzt ohne Dexie — da kein lokaler DB-Zugriff möglich, Funktion einfach dispatch aufrufen ohne Stock-Check:
      ```typescript
      async function updateQuantity(productId: string, quantity: number): Promise<{ blocked?: boolean; stock?: number }> {
        dispatch({ type: 'UPDATE_QUANTITY', productId, quantity });
        return {};
      }
      ```
      (Stock-Check bei addItem bleibt über useProducts-Daten die der Caller übergibt)

    4. APP.TSX: client/src/App.tsx

    Import entfernen: `import { registerSyncTriggers } from './sync/triggers.js';`

    In `UnlockedApp`:
    - `useEffect(() => { registerSyncTriggers(queryClient); }, [queryClient]);` entfernen.
    - `const queryClient = useQueryClient();` nur entfernen wenn es nur für den Sync-Trigger gebraucht wurde — prüfen ob `queryClient` sonst noch genutzt wird (z.B. in useWebSocket). Falls nicht mehr gebraucht, auch diese Zeile entfernen.

    Der `navigator.storage.persist()` useEffect in `AppInner` BLEIBT (ist nützlich, kein Dexie-Bezug).
  </action>
  <verify>
    <automated>grep -n "from.*dexie\|import.*dexie\|db\." client/src/hooks/api/useProducts.ts client/src/hooks/api/useCategories.ts client/src/features/pos/useCart.ts client/src/App.tsx 2>/dev/null</automated>
    Erwartet: keine Treffer (alle Dexie-Bezüge entfernt)
  </verify>
  <done>
    - useProducts.ts: kein db-Import, queryFn wirft Error bei Netzwerkfehler statt Dexie-Fallback
    - useCategories.ts: kein db-Import, gleiche Änderung
    - useCart.ts: kein db-Import, kein loaded-State, keine Persistierung, updateQuantity ohne Stock-Check
    - App.tsx: kein registerSyncTriggers-Import, kein registerSyncTriggers-Aufruf
  </done>
  <acceptance_criteria>
    - `grep "import.*db" client/src/hooks/api/useProducts.ts` gibt 0 Treffer
    - `grep "import.*db" client/src/hooks/api/useCategories.ts` gibt 0 Treffer
    - `grep "import.*db\|from.*db/index" client/src/features/pos/useCart.ts` gibt nur Type-Imports (kein `db.`)
    - `grep "db\." client/src/features/pos/useCart.ts` gibt 0 Treffer
    - `grep "registerSyncTriggers\|sync/triggers" client/src/App.tsx` gibt 0 Treffer
    - `grep "db\.transaction\|db\.products\|db\.categories\|db\.cartItems" client/src/hooks/api/useProducts.ts client/src/hooks/api/useCategories.ts` gibt 0 Treffer
  </acceptance_criteria>
</task>

</tasks>

<verification>
Nach beiden Tasks:
- `grep -r "from 'dexie'" client/src/ 2>/dev/null` → 0 Treffer
- `grep -r "from \"dexie\"" client/src/ 2>/dev/null` → 0 Treffer
- `grep -r "dexie-react-hooks" client/src/ 2>/dev/null` → 0 Treffer (noch nicht entfernt in diesem Plan, aber in DailyReport/useLowStockCount — das ist Plan 03)
- `test ! -f client/src/sync/engine.ts && test ! -f client/src/sync/triggers.ts && test ! -f client/src/db/schema.ts` → Exit 0
</verification>

<success_criteria>
- db/schema.ts, sync/engine.ts, sync/triggers.ts existieren nicht mehr
- db/index.ts: reine Typen + getShopId/setShopId, kein Dexie-Code
- useProducts + useCategories: Server-only, kein Dexie-Import
- useCart: reiner useReducer-State, keine Persistierung
- App.tsx: kein Sync-Trigger-Aufruf
</success_criteria>

<output>
Nach Completion: .planning/phases/23-dexie-entfernung-online-only/23-01-SUMMARY.md erstellen.
</output>
