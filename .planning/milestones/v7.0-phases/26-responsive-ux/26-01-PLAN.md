---
phase: 26-responsive-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/admin/settings/SettingsForm.tsx
autonomous: true
requirements:
  - UX-01

must_haves:
  truths:
    - "Admin sieht in den Einstellungen eine neue Sektion 'Warenkorb-Layout' mit Checkbox"
    - "Toggle-Status wird per PUT /api/settings mit key='cart_sidebar_enabled' gespeichert"
    - "Beim Laden der Settings-Seite wird der gespeicherte Wert aus der API geladen und die Checkbox korrekt vorbelegt"
    - "Feedback 'Gespeichert' erscheint kurz nach dem Umschalten"
  artifacts:
    - path: "client/src/features/admin/settings/SettingsForm.tsx"
      provides: "Settings-UI mit cart_sidebar_enabled Toggle"
      contains: "cart_sidebar_enabled"
  key_links:
    - from: "SettingsForm.tsx"
      to: "/api/settings"
      via: "PUT fetch mit body { key: 'cart_sidebar_enabled', value: 'true'/'false' }"
      pattern: "cart_sidebar_enabled"
---

<objective>
Fügt dem Admin-Bereich (SettingsForm) eine neue Einstellung "Warenkorb-Layout" hinzu: ein Checkbox-Toggle, der steuert ob der Warenkorb auf breiten Screens als fixe Spalte erscheint. Der Wert wird per PUT /api/settings als key='cart_sidebar_enabled' persistiert.

Purpose: Diese Einstellung ist Voraussetzung für Plan 02 (POSScreen Sidebar-Modus). Der Admin muss die Spalten-Ansicht pro Shop aktivieren können.
Output: SettingsForm.tsx mit neuem "Warenkorb-Layout" Abschnitt und Toggle.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Settings-API (server/src/routes/settings.ts) -->
<!-- GET /api/settings  → Setting[] { key, value, shopId } -->
<!-- PUT /api/settings  → body: { key: string, value: string } — Upsert via (key, shopId) -->

<!-- Bestehendes SettingsForm-Pattern (client/src/features/admin/settings/SettingsForm.tsx) -->
<!-- saveSetting(key: string, value: string): Promise<void> — existiert bereits, PUT /api/settings -->
<!-- savedKey state: string | null — steuert "Gespeichert"-Anzeige -->
<!-- Settings laden per fetch GET /api/settings in useEffect -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: cart_sidebar_enabled Toggle in SettingsForm</name>
  <files>client/src/features/admin/settings/SettingsForm.tsx</files>

  <read_first>
    - client/src/features/admin/settings/SettingsForm.tsx (aktueller Stand — Muster für saveSetting, State-Pattern und UI-Blöcke)
  </read_first>

  <action>
Füge in SettingsForm.tsx folgende Änderungen ein:

1. **Neuer State** (nach reportYearly State):
```tsx
const [cartSidebarEnabled, setCartSidebarEnabled] = useState(false);
```

2. **Laden in useEffect** — im bestehenden `.then((rows: Setting[]) => { ... })` Block ergänzen:
```tsx
if (row.key === 'cart_sidebar_enabled') setCartSidebarEnabled(row.value === 'true');
```

3. **Handler-Funktion** (nach handleYearlyChange):
```tsx
function handleCartSidebarChange(checked: boolean) {
  setCartSidebarEnabled(checked);
  saveSetting('cart_sidebar_enabled', checked ? 'true' : 'false');
}
```

4. **Neuer UI-Block** — als erste Sektion in `<div className="flex flex-col gap-6">` (vor den Schnellbeträgen), damit er prominent sichtbar ist:
```tsx
{/* Warenkorb-Layout */}
<div className="bg-white rounded-xl shadow-sm p-6 flex flex-col gap-4">
  <h3 className="text-sm font-semibold text-sky-700 flex items-center gap-2">
    <LayoutPanelRight size={16} />
    Warenkorb-Layout
  </h3>

  <label className="flex items-center gap-3 cursor-pointer min-h-[44px]">
    <input
      type="checkbox"
      checked={cartSidebarEnabled}
      onChange={e => handleCartSidebarChange(e.target.checked)}
      className="w-6 h-6 accent-sky-500"
    />
    <span className="text-sm text-slate-700">
      Warenkorb als feste Spalte auf breiten Screens anzeigen
    </span>
  </label>

  <p className="text-xs text-slate-500">
    Auf iPad im Querformat (ab 1024px Breite) wird der Warenkorb als feste rechte Spalte angezeigt.
    Auf iPhone und iPad im Hochformat bleibt das Slide-In Panel aktiv.
  </p>

  {savedKey === 'cart_sidebar_enabled' && !saving && (
    <span className="text-xs text-green-600">Gespeichert</span>
  )}
</div>
```

5. **Import** ergänzen: `LayoutPanelRight` aus `lucide-react` importieren (bestehende `import { Mail, Send, Info, Banknote, Plus, X }` Zeile erweitern).

Hinweis: `saving` State ist bereits vorhanden und wird von `saveSetting` gesetzt — kein neuer Zustand nötig.
  </action>

  <verify>
    <automated>grep -n "cart_sidebar_enabled" /Users/simonluthe/Documents/fairstand/client/src/features/admin/settings/SettingsForm.tsx</automated>
  </verify>

  <done>
    - `cart_sidebar_enabled` kommt mindestens 3x vor (State init, useEffect load, saveSetting call)
    - `LayoutPanelRight` ist importiert
    - Neue Sektion "Warenkorb-Layout" ist im JSX vor den Schnellbeträgen
    - handleCartSidebarChange ruft saveSetting('cart_sidebar_enabled', ...) auf
  </done>
</task>

</tasks>

<verification>
grep -n "cart_sidebar_enabled" client/src/features/admin/settings/SettingsForm.tsx | wc -l
# Erwartet: >= 3 Treffer

grep -n "LayoutPanelRight" client/src/features/admin/settings/SettingsForm.tsx
# Erwartet: import + JSX-Verwendung
</verification>

<success_criteria>
Admin kann in den Einstellungen den "Warenkorb als feste Spalte"-Toggle umschalten. Der Wert wird per PUT /api/settings mit key='cart_sidebar_enabled' gespeichert. Beim Neuladen der Seite ist der zuletzt gespeicherte Wert vorbelegt.
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/26-responsive-ux/26-01-SUMMARY.md` erstellen
</output>
