---
phase: 24-master-shop-administration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/db/schema.ts
  - server/migrations/0003_add_master_shop_fields.sql
  - server/src/db/seed.ts
  - server/src/routes/auth.ts
autonomous: true
requirements:
  - SHOP-01
  - SHOP-02

must_haves:
  truths:
    - "Ein deaktivierter Shop (active: false) kann sich nicht per PIN einloggen"
    - "Der Master-Shop St. Secundus hat is_master: true in der DB"
    - "Die Auth-Antwort enthält das isMaster-Flag"
    - "Die shops-Tabelle hat is_master und active Spalten mit korrekten Defaults"
  artifacts:
    - path: "server/src/db/schema.ts"
      provides: "shops-Tabelle mit is_master + active Feldern"
      contains: "isMaster"
    - path: "server/migrations/0003_add_master_shop_fields.sql"
      provides: "PostgreSQL-Migration für neue Spalten"
      contains: "is_master"
    - path: "server/src/routes/auth.ts"
      provides: "active-Check + isMaster in Response"
      contains: "active"
  key_links:
    - from: "server/src/routes/auth.ts"
      to: "server/src/db/schema.ts"
      via: "Drizzle select auf shops-Tabelle"
      pattern: "shop\\.isMaster"
---

<objective>
Erweiterung des Server-Fundaments: shops-Tabelle bekommt is_master und active Spalten,
Migration wird erstellt, Seed setzt St. Secundus als Master, Auth-Route prüft active-Flag
und gibt isMaster in der Response zurück.

Purpose: Legt die Datenbankgrundlage und Auth-Logik, auf der Plan 02 (Shops-API + Client) aufbaut.
Output: Erweitertes Schema, Migration, angepasster Seed, angepasste Auth-Route.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende shops-Tabelle (server/src/db/schema.ts) — ERWEITERN, nicht ersetzen -->
```typescript
export const shops = pgTable('shops', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull().unique(),
  name: text('name').notNull(),
  pin: text('pin').notNull(),
  createdAt: integer('created_at').notNull(),
  // NEU hinzufügen:
  // isMaster: boolean('is_master').notNull().default(false),
  // active: boolean('active').notNull().default(true),
});
```

<!-- Bestehende Auth-Response (server/src/routes/auth.ts) — ERWEITERN -->
```typescript
// Aktuell:
return reply.send({ shopId: shop.shopId, shopName: shop.name, token });
// Neu:
return reply.send({ shopId: shop.shopId, shopName: shop.name, token, isMaster: shop.isMaster });
```

<!-- Bestehender Seed (server/src/db/seed.ts) — ensureShopSeeded() anpassen -->
// St. Secundus beim INSERT: isMaster: true setzen
// Für bereits existierende Shops: UPDATE shops SET is_master = true WHERE shop_id = 'st-secundus-hennstedt'

<!-- Migration-Muster (aus 0002_add_sale_type.sql) -->
// PostgreSQL-Syntax mit --> statement-breakpoint zwischen Statements
// Migrations-Datei: server/migrations/0003_add_master_shop_fields.sql
// Drizzle meta-Verzeichnis: server/migrations/meta/ — NICHT manuell anfassen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema erweitern + Migration erstellen</name>
  <files>server/src/db/schema.ts, server/migrations/0003_add_master_shop_fields.sql</files>
  <read_first>
    - server/src/db/schema.ts (shops-Tabelle, bestehende Felder)
    - server/migrations/0002_add_sale_type.sql (Migration-Format verstehen)
    - server/drizzle.config.ts (out-Pfad: ./migrations, dialect: postgresql)
  </read_first>
  <action>
    1. In server/src/db/schema.ts die shops-Tabelle um zwei Felder erweitern:
       ```typescript
       isMaster: boolean('is_master').notNull().default(false),
       active: boolean('active').notNull().default(true),
       ```
       Import: boolean ist bereits aus 'drizzle-orm/pg-core' importiert (in products-Tabelle genutzt) — kein neuer Import nötig.

    2. Neue Migrations-Datei anlegen: server/migrations/0003_add_master_shop_fields.sql
       Inhalt (PostgreSQL-Syntax, KEIN SQLite):
       ```sql
       ALTER TABLE "shops" ADD COLUMN "is_master" boolean DEFAULT false NOT NULL;
       --> statement-breakpoint
       ALTER TABLE "shops" ADD COLUMN "active" boolean DEFAULT true NOT NULL;
       ```
       WICHTIG: Kein Drizzle-Kit generate aufrufen — manuelle Migration, da drizzle-kit im Container läuft. Die Meta-Dateien in server/migrations/meta/ NICHT anfassen.

    3. Prüfen ob die Migration beim Server-Start automatisch angewendet wird. Grep nach "migrate" in server/src/ um zu verstehen ob ein Migrations-Runner existiert. Falls ja, sicherstellen dass 0003 beim nächsten Start läuft.
  </action>
  <verify>
    <automated>grep -n "isMaster\|is_master\|active" /Users/simonluthe/Documents/fairstand/server/src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/src/db/schema.ts enthält "isMaster: boolean('is_master').notNull().default(false)"
    - server/src/db/schema.ts enthält "active: boolean('active').notNull().default(true)"
    - server/migrations/0003_add_master_shop_fields.sql existiert und enthält "ALTER TABLE"
    - server/migrations/0003_add_master_shop_fields.sql enthält "is_master" und "active"
  </acceptance_criteria>
  <done>Schema hat neue Felder, PostgreSQL-Migration liegt bereit.</done>
</task>

<task type="auto">
  <name>Task 2: Seed anpassen + Auth-Route erweitern</name>
  <files>server/src/db/seed.ts, server/src/routes/auth.ts</files>
  <read_first>
    - server/src/db/seed.ts (ensureShopSeeded-Funktion, INSERT-Pattern)
    - server/src/routes/auth.ts (vollständige Route, bestehende Response)
    - server/src/lib/sessions.ts (Session-Interface, createSession-Signatur)
  </read_first>
  <action>
    1. In server/src/db/seed.ts die ensureShopSeeded-Funktion anpassen:

       a) Im INSERT beim Neuanlegen: isMaster: true hinzufügen:
          ```typescript
          await db.insert(shops).values({
            id: crypto.randomUUID(),
            shopId: SHOP_ID,
            name: SHOP_NAME,
            pin: pinHash,
            createdAt: now,
            isMaster: true,  // NEU
          });
          ```

       b) Für bestehende Shops (Idempotenz): Nach dem "if (existing) return;" NICHT einfach returnen.
          Stattdessen prüfen ob is_master bereits gesetzt ist, und falls nicht, updaten:
          ```typescript
          const [existing] = await db.select().from(shops).where(eq(shops.shopId, SHOP_ID));
          if (existing) {
            // Sicherstellen dass is_master gesetzt ist (Migration für bestehende Instanzen)
            if (!existing.isMaster) {
              await db.update(shops).set({ isMaster: true }).where(eq(shops.shopId, SHOP_ID));
            }
            return;
          }
          ```
          Import: update aus 'drizzle-orm' hinzufügen falls noch nicht vorhanden.

    2. In server/src/routes/auth.ts:

       a) active-Check hinzufügen — nach dem "if (!shop)" Check:
          ```typescript
          if (!shop) return reply.status(401).send({ error: 'Falscher PIN' });
          if (!shop.active) return reply.status(403).send({ error: 'Dieser Shop ist deaktiviert' });
          ```

       b) isMaster in Response mitgeben:
          ```typescript
          return reply.send({
            shopId: shop.shopId,
            shopName: shop.name,
            token,
            isMaster: shop.isMaster,
          });
          ```

       Kein anderer Code in auth.ts ändern — Rate-Limiting, PIN-Hashing etc. bleiben unverändert.
  </action>
  <verify>
    <automated>grep -n "isMaster\|active\|deaktiviert" /Users/simonluthe/Documents/fairstand/server/src/routes/auth.ts /Users/simonluthe/Documents/fairstand/server/src/db/seed.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/src/routes/auth.ts enthält "shop.active" Prüfung vor Token-Erstellung
    - server/src/routes/auth.ts enthält "isMaster: shop.isMaster" in der reply.send()
    - server/src/routes/auth.ts gibt 403 für deaktivierte Shops (nicht 401)
    - server/src/db/seed.ts enthält "isMaster: true" im INSERT
    - server/src/db/seed.ts prüft ob isMaster bereits gesetzt ist (Idempotenz-Update)
    - grep "update" in server/src/db/seed.ts findet den Drizzle-Update-Call
  </acceptance_criteria>
  <done>Auth lehnt deaktivierte Shops ab, gibt isMaster zurück, Seed setzt St. Secundus als Master.</done>
</task>

</tasks>

<verification>
Nach Plan-01-Ausführung (TypeScript-Kompilierung im Server prüfen):
```bash
cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit 2>&1 | head -20
```
Keine TypeScript-Fehler erwartet.

Migration-Datei vorhanden:
```bash
ls /Users/simonluthe/Documents/fairstand/server/migrations/0003_add_master_shop_fields.sql
```
</verification>

<success_criteria>
- TypeScript kompiliert ohne Fehler
- shops-Tabelle hat isMaster + active in Schema
- Migration 0003 liegt für PostgreSQL bereit
- Auth-Route gibt 403 für active: false Shops
- Auth-Route gibt isMaster in der Response
- Seed setzt is_master: true für St. Secundus (bei Neuanlage + bei Bestandsupdate)
</success_criteria>

<output>
Nach Completion: `.planning/phases/24-master-shop-administration/24-01-SUMMARY.md` erstellen.
</output>
