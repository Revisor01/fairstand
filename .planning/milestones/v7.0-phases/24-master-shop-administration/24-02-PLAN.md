---
phase: 24-master-shop-administration
plan: 02
type: execute
wave: 2
depends_on:
  - 24-01
files_modified:
  - server/src/routes/shops.ts
  - server/src/index.ts
  - client/src/features/admin/shops/ShopsManager.tsx
  - client/src/features/admin/AdminScreen.tsx
  - client/src/features/auth/serverAuth.ts
autonomous: true
requirements:
  - SHOP-02
  - SHOP-03

must_haves:
  truths:
    - "Master-Shop sieht einen 'Shops'-Tab im Admin-Bereich, normale Shops nicht"
    - "Über den Shops-Tab können neue Shops angelegt werden (Name + PIN)"
    - "Über den Shops-Tab können Shops deaktiviert werden"
    - "Nicht-Master-Shops erhalten 403 wenn sie /api/shops aufrufen"
    - "isMaster wird nach Login in localStorage gespeichert und steht der App zur Verfügung"
  artifacts:
    - path: "server/src/routes/shops.ts"
      provides: "CRUD-Endpunkte für Shops, Master-only geschützt"
      exports: ["shopsRoutes"]
    - path: "client/src/features/admin/shops/ShopsManager.tsx"
      provides: "Shop-Verwaltungs-UI mit Liste, Anlegen-Form, Deaktivieren-Button"
      contains: "ShopsManager"
    - path: "client/src/features/admin/AdminScreen.tsx"
      provides: "Bedingter Shops-Tab nur für Master"
      contains: "isMaster"
    - path: "client/src/features/auth/serverAuth.ts"
      provides: "isMaster in StoredSession"
      contains: "isMaster"
  key_links:
    - from: "client/src/features/admin/shops/ShopsManager.tsx"
      to: "/api/shops"
      via: "TanStack Query + fetch"
      pattern: "useQuery.*shops\|api/shops"
    - from: "server/src/routes/shops.ts"
      to: "server/src/db/schema.ts shops-Tabelle"
      via: "Drizzle db.select/insert/update"
      pattern: "db\\..*shops"
    - from: "client/src/features/admin/AdminScreen.tsx"
      to: "client/src/features/auth/serverAuth.ts"
      via: "getStoredSession().isMaster"
      pattern: "isMaster"
---

<objective>
Shops-API-Route auf dem Server (Master-only CRUD) und vollständige Client-Integration:
ShopsManager-Komponente, bedingter Shops-Tab im AdminScreen, isMaster in der Session.

Purpose: Schließt die Phase ab — Master-Shop kann Shops anlegen und deaktivieren.
Output: Neue Route shops.ts, neue Komponente ShopsManager.tsx, erweiterte AdminScreen + serverAuth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/24-master-shop-administration/24-01-SUMMARY.md

<interfaces>
<!-- Plan-01 hat diese Felder hinzugefügt — diese sind jetzt verfügbar: -->

<!-- server/src/db/schema.ts shops-Tabelle (nach Plan 01): -->
```typescript
export const shops = pgTable('shops', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull().unique(),
  name: text('name').notNull(),
  pin: text('pin').notNull(),
  createdAt: integer('created_at').notNull(),
  isMaster: boolean('is_master').notNull().default(false),
  active: boolean('active').notNull().default(true),
});
```

<!-- Auth-Response (nach Plan 01) — Client empfängt jetzt: -->
```typescript
{ shopId: string; shopName: string; token: string; isMaster: boolean }
```

<!-- Session-Pattern (client/src/features/auth/serverAuth.ts): -->
```typescript
export interface StoredSession {
  shopId: string;
  shopName: string;
  token: string;
  lastActivity: number;
  // NEU hinzufügen: isMaster: boolean;
}
```

<!-- Route-Pattern (server/src/routes/settings.ts): -->
```typescript
const session = (request as any).session as { shopId: string };
// Für Master-Check:
const session = (request as any).session as { shopId: string; isMaster: boolean };
// ABER: session enthält aktuell nur shopId — Master-Check muss über DB gehen:
// const [shop] = await db.select().from(shops).where(eq(shops.shopId, session.shopId));
// if (!shop.isMaster) return reply.status(403).send({ error: 'Nur für Master-Shops' });
```

<!-- Auth-Headers für Client-Requests (aus serverAuth.ts): -->
```typescript
export async function getAuthHeaders(): Promise<Record<string, string>> {
  // gibt { 'Content-Type': 'application/json', 'Authorization': 'Bearer TOKEN' } zurück
}
```

<!-- TanStack Query Pattern (aus bestehenden Features): -->
```typescript
const { data, isLoading } = useQuery({
  queryKey: ['shops'],
  queryFn: async () => {
    const headers = await getAuthHeaders();
    const res = await fetch('/api/shops', { headers });
    if (!res.ok) throw new Error('Fehler beim Laden');
    return res.json() as Promise<ShopItem[]>;
  },
});
const mutation = useMutation({
  mutationFn: async (data: CreateShopInput) => { ... },
  onSuccess: () => queryClient.invalidateQueries({ queryKey: ['shops'] }),
});
```

<!-- AdminScreen Tab-Pattern (aus AdminScreen.tsx): -->
```typescript
type AdminTab = 'products' | 'reports' | 'settings' | 'import' | 'categories';
// Erweiterung: | 'shops'
const tabs = [
  { key: 'products', label: 'Produkte', icon: Package },
  // ...
  // Shops-Tab NUR für isMaster:
  // ...(isMaster ? [{ key: 'shops', label: 'Shops', icon: Store }] : []),
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Server-Route /api/shops (Master-only CRUD)</name>
  <files>server/src/routes/shops.ts, server/src/index.ts</files>
  <read_first>
    - server/src/routes/settings.ts (Route-Pattern, session-Zugriff)
    - server/src/routes/auth.ts (hashPin Import-Pattern)
    - server/src/db/schema.ts (shops-Tabelle mit neuen Feldern aus Plan 01)
    - server/src/lib/crypto.ts (hashPin Signatur)
    - server/src/index.ts (Route-Registrierung, import-Zeilen)
  </read_first>
  <action>
    Neue Datei server/src/routes/shops.ts erstellen mit folgenden Endpunkten:

    ```typescript
    import type { FastifyInstance } from 'fastify';
    import { z } from 'zod';
    import { eq } from 'drizzle-orm';
    import { db } from '../db/index.js';
    import { shops } from '../db/schema.js';
    import { hashPin } from '../lib/crypto.js';

    // Master-Guard: Prüft ob der anfragende Shop is_master = true hat
    async function requireMaster(request: any, reply: any): Promise<boolean> {
      const session = (request as any).session as { shopId: string };
      const [shop] = await db.select().from(shops).where(eq(shops.shopId, session.shopId));
      if (!shop?.isMaster) {
        reply.status(403).send({ error: 'Nur für Master-Shops verfügbar' });
        return false;
      }
      return true;
    }

    const CreateShopSchema = z.object({
      name: z.string().min(2).max(60),
      shopId: z.string().min(3).max(40).regex(/^[a-z0-9-]+$/),
      pin: z.string().min(4).max(8),
    });

    const UpdateShopSchema = z.object({
      active: z.boolean(),
    });

    export async function shopsRoutes(fastify: FastifyInstance) {
      // GET /shops — Liste aller Shops (ohne PIN-Hash)
      fastify.get('/shops', async (request, reply) => {
        if (!await requireMaster(request, reply)) return;
        const rows = await db.select({
          id: shops.id,
          shopId: shops.shopId,
          name: shops.name,
          isMaster: shops.isMaster,
          active: shops.active,
          createdAt: shops.createdAt,
        }).from(shops).orderBy(shops.createdAt);
        return reply.send(rows);
      });

      // POST /shops — Neuen Shop anlegen
      fastify.post('/shops', async (request, reply) => {
        if (!await requireMaster(request, reply)) return;
        const result = CreateShopSchema.safeParse(request.body);
        if (!result.success) return reply.status(400).send({ error: result.error.flatten() });
        const { name, shopId, pin } = result.data;

        // shopId darf nicht bereits existieren
        const [existing] = await db.select().from(shops).where(eq(shops.shopId, shopId));
        if (existing) return reply.status(409).send({ error: 'Shop-ID bereits vergeben' });

        const pinHash = await hashPin(pin);
        await db.insert(shops).values({
          id: crypto.randomUUID(),
          shopId,
          name,
          pin: pinHash,
          isMaster: false,
          active: true,
          createdAt: Date.now(),
        });
        return reply.status(201).send({ shopId, name, active: true });
      });

      // PATCH /shops/:shopId — Shop aktivieren/deaktivieren
      fastify.patch('/shops/:shopId', async (request, reply) => {
        if (!await requireMaster(request, reply)) return;
        const { shopId } = request.params as { shopId: string };
        const result = UpdateShopSchema.safeParse(request.body);
        if (!result.success) return reply.status(400).send({ error: result.error.flatten() });

        // Master-Shop darf nicht deaktiviert werden
        const [target] = await db.select().from(shops).where(eq(shops.shopId, shopId));
        if (!target) return reply.status(404).send({ error: 'Shop nicht gefunden' });
        if (target.isMaster && !result.data.active) {
          return reply.status(400).send({ error: 'Master-Shop kann nicht deaktiviert werden' });
        }

        await db.update(shops).set({ active: result.data.active }).where(eq(shops.shopId, shopId));
        return reply.send({ ok: true });
      });
    }
    ```

    In server/src/index.ts:
    - Import hinzufügen: `import { shopsRoutes } from './routes/shops.js';`
    - Route registrieren nach bestehenden Registrierungen:
      `await fastify.register(shopsRoutes, { prefix: '/api' });`
      (vor dem fastifySchedule-Register einfügen)
  </action>
  <verify>
    <automated>grep -n "shopsRoutes\|shops\.ts" /Users/simonluthe/Documents/fairstand/server/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/src/routes/shops.ts existiert und exportiert shopsRoutes
    - server/src/index.ts importiert shopsRoutes
    - server/src/index.ts registriert shopsRoutes mit prefix '/api'
    - grep "requireMaster" in server/src/routes/shops.ts findet die Guard-Funktion
    - grep "PATCH\|patch" in server/src/routes/shops.ts findet den Update-Endpunkt
    - grep "409\|Konflikt\|bereits vergeben" in server/src/routes/shops.ts findet den Duplikat-Check
    - TypeScript-Check: cd server && npx tsc --noEmit gibt keine Fehler fuer shops.ts
  </acceptance_criteria>
  <done>
    GET /api/shops, POST /api/shops, PATCH /api/shops/:shopId sind registriert.
    Nicht-Master-Shops erhalten 403. Master-Shop kann nicht deaktiviert werden.
  </done>
</task>

<task type="auto">
  <name>Task 2: Client — Session + ShopsManager + AdminScreen-Tab</name>
  <files>
    client/src/features/auth/serverAuth.ts,
    client/src/features/admin/shops/ShopsManager.tsx,
    client/src/features/admin/AdminScreen.tsx
  </files>
  <read_first>
    - client/src/features/auth/serverAuth.ts (StoredSession Interface, serverLogin, getStoredSession)
    - client/src/features/admin/AdminScreen.tsx (Tab-System, tabs-Array, State-Management)
    - client/src/features/admin/settings/CategoryManager.tsx (TanStack Query Pattern im Admin)
    - client/src/features/admin/products/ProductList.tsx (useMutation Pattern falls vorhanden)
  </read_first>
  <action>
    **A. client/src/features/auth/serverAuth.ts — isMaster ergänzen:**

    1. StoredSession Interface erweitern:
       ```typescript
       export interface StoredSession {
         shopId: string;
         shopName: string;
         token: string;
         lastActivity: number;
         isMaster: boolean;  // NEU
       }
       ```

    2. In serverLogin(): Response-Typ erweitern und isMaster in Session speichern:
       ```typescript
       const data = await res.json() as { shopId: string; shopName: string; token: string; isMaster: boolean };
       const session: StoredSession = {
         shopId: data.shopId,
         shopName: data.shopName,
         token: data.token,
         lastActivity: Date.now(),
         isMaster: data.isMaster,  // NEU
       };
       ```

    Keine anderen Funktionen in serverAuth.ts ändern.

    **B. client/src/features/admin/shops/ShopsManager.tsx — Neue Datei erstellen:**

    Verzeichnis anlegen: client/src/features/admin/shops/

    Inhalt ShopsManager.tsx:
    ```typescript
    import { useState } from 'react';
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
    import { getAuthHeaders } from '../../auth/serverAuth.js';
    import { Plus, PowerOff, Power } from 'lucide-react';

    interface ShopItem {
      id: string;
      shopId: string;
      name: string;
      isMaster: boolean;
      active: boolean;
      createdAt: number;
    }

    interface CreateShopInput {
      name: string;
      shopId: string;
      pin: string;
    }

    export function ShopsManager() {
      const queryClient = useQueryClient();
      const [showForm, setShowForm] = useState(false);
      const [form, setForm] = useState<CreateShopInput>({ name: '', shopId: '', pin: '' });
      const [formError, setFormError] = useState<string | null>(null);

      const { data: shops = [], isLoading } = useQuery({
        queryKey: ['shops'],
        queryFn: async () => {
          const headers = await getAuthHeaders();
          const res = await fetch('/api/shops', { headers });
          if (!res.ok) throw new Error('Fehler beim Laden der Shops');
          return res.json() as Promise<ShopItem[]>;
        },
      });

      const createMutation = useMutation({
        mutationFn: async (input: CreateShopInput) => {
          const headers = await getAuthHeaders();
          const res = await fetch('/api/shops', {
            method: 'POST',
            headers,
            body: JSON.stringify(input),
          });
          if (!res.ok) {
            const err = await res.json() as { error: string };
            throw new Error(typeof err.error === 'string' ? err.error : 'Fehler beim Anlegen');
          }
          return res.json();
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['shops'] });
          setForm({ name: '', shopId: '', pin: '' });
          setShowForm(false);
          setFormError(null);
        },
        onError: (err: Error) => setFormError(err.message),
      });

      const toggleMutation = useMutation({
        mutationFn: async ({ shopId, active }: { shopId: string; active: boolean }) => {
          const headers = await getAuthHeaders();
          const res = await fetch(`/api/shops/${shopId}`, {
            method: 'PATCH',
            headers,
            body: JSON.stringify({ active }),
          });
          if (!res.ok) {
            const err = await res.json() as { error: string };
            throw new Error(typeof err.error === 'string' ? err.error : 'Fehler');
          }
        },
        onSuccess: () => queryClient.invalidateQueries({ queryKey: ['shops'] }),
      });

      // shopId-Vorschlag aus Name generieren
      const suggestShopId = (name: string) =>
        name.toLowerCase().replace(/[^a-z0-9]+/g, '-').replace(/^-|-$/g, '');

      return (
        <div className="space-y-4">
          <div className="flex items-center justify-between">
            <h2 className="text-lg font-semibold text-slate-700">Shops verwalten</h2>
            <button
              onPointerDown={() => setShowForm(v => !v)}
              className="flex items-center gap-1.5 bg-sky-500 active:bg-sky-600 text-white px-4 py-2 rounded-lg text-sm font-semibold min-h-[44px]"
            >
              <Plus size={16} />
              Neuer Shop
            </button>
          </div>

          {showForm && (
            <div className="bg-white rounded-xl border border-sky-100 p-4 space-y-3">
              <h3 className="font-semibold text-slate-700 text-sm">Neuen Shop anlegen</h3>
              {formError && (
                <p className="text-red-600 text-sm bg-red-50 rounded-lg p-2">{formError}</p>
              )}
              <div className="space-y-2">
                <input
                  type="text"
                  placeholder="Name (z.B. Gemeinde Heide)"
                  value={form.name}
                  onChange={e => setForm(f => ({
                    ...f,
                    name: e.target.value,
                    shopId: suggestShopId(e.target.value),
                  }))}
                  className="w-full border border-slate-200 rounded-lg px-3 py-2 text-sm min-h-[44px]"
                />
                <input
                  type="text"
                  placeholder="Shop-ID (z.B. gemeinde-heide)"
                  value={form.shopId}
                  onChange={e => setForm(f => ({ ...f, shopId: e.target.value }))}
                  className="w-full border border-slate-200 rounded-lg px-3 py-2 text-sm min-h-[44px] font-mono"
                />
                <input
                  type="password"
                  placeholder="PIN (4–8 Stellen)"
                  value={form.pin}
                  onChange={e => setForm(f => ({ ...f, pin: e.target.value }))}
                  className="w-full border border-slate-200 rounded-lg px-3 py-2 text-sm min-h-[44px]"
                />
              </div>
              <div className="flex gap-2">
                <button
                  onPointerDown={() => {
                    setFormError(null);
                    createMutation.mutate(form);
                  }}
                  disabled={createMutation.isPending}
                  className="flex-1 bg-sky-500 active:bg-sky-600 disabled:opacity-50 text-white font-semibold py-2 rounded-lg text-sm min-h-[44px]"
                >
                  {createMutation.isPending ? 'Wird angelegt…' : 'Anlegen'}
                </button>
                <button
                  onPointerDown={() => { setShowForm(false); setFormError(null); }}
                  className="px-4 py-2 bg-slate-100 active:bg-slate-200 text-slate-600 rounded-lg text-sm min-h-[44px]"
                >
                  Abbrechen
                </button>
              </div>
            </div>
          )}

          {isLoading && <p className="text-slate-400 text-sm">Lädt…</p>}

          <div className="space-y-2">
            {shops.map(shop => (
              <div
                key={shop.shopId}
                className={`bg-white rounded-xl border p-4 flex items-center justify-between gap-3 ${
                  shop.active ? 'border-sky-100' : 'border-slate-200 opacity-60'
                }`}
              >
                <div>
                  <p className={`font-semibold text-sm ${shop.active ? 'text-slate-700' : 'text-slate-400'}`}>
                    {shop.name}
                    {shop.isMaster && (
                      <span className="ml-2 text-[10px] bg-sky-100 text-sky-700 px-2 py-0.5 rounded-full font-bold uppercase">
                        Master
                      </span>
                    )}
                  </p>
                  <p className="text-slate-400 text-xs font-mono mt-0.5">{shop.shopId}</p>
                </div>
                {!shop.isMaster && (
                  <button
                    onPointerDown={() => toggleMutation.mutate({ shopId: shop.shopId, active: !shop.active })}
                    disabled={toggleMutation.isPending}
                    className={`flex items-center gap-1.5 px-3 py-2 rounded-lg text-xs font-semibold min-h-[44px] ${
                      shop.active
                        ? 'bg-red-50 text-red-600 active:bg-red-100'
                        : 'bg-green-50 text-green-700 active:bg-green-100'
                    }`}
                  >
                    {shop.active ? <PowerOff size={14} /> : <Power size={14} />}
                    {shop.active ? 'Deaktivieren' : 'Aktivieren'}
                  </button>
                )}
              </div>
            ))}
          </div>
        </div>
      );
    }
    ```

    **C. client/src/features/admin/AdminScreen.tsx — Shops-Tab für Master:**

    1. Import hinzufügen: Store aus lucide-react (bestehende Icon-Import-Zeile erweitern)
    2. Import: ShopsManager hinzufügen
    3. AdminTab-Type erweitern: `| 'shops'`
    4. State: `const [isMaster, setIsMaster] = useState(false);`
    5. Im useEffect wo shopName geladen wird, auch isMaster laden:
       ```typescript
       useEffect(() => {
         getStoredSession().then(s => {
           if (s) {
             setShopName(s.shopName);
             setIsMaster(s.isMaster ?? false);
           }
         });
       }, []);
       ```
    6. tabs-Array: Den Shops-Tab bedingt einfügen. Statisches Array bleibt, aber beim Rendern
       wird ein dynamisches Array gebaut:
       ```typescript
       const visibleTabs = isMaster
         ? [...tabs, { key: 'shops' as AdminTab, label: 'Shops', icon: Store }]
         : tabs;
       ```
       Dann in der JSX `visibleTabs.map(...)` statt `tabs.map(...)` verwenden.
    7. Im Tab-Inhalt-Bereich hinzufügen:
       `{tab === 'shops' && <ShopsManager />}`
  </action>
  <verify>
    <automated>grep -rn "isMaster\|ShopsManager\|shops" /Users/simonluthe/Documents/fairstand/client/src/features/admin/AdminScreen.tsx /Users/simonluthe/Documents/fairstand/client/src/features/auth/serverAuth.ts</automated>
  </verify>
  <acceptance_criteria>
    - client/src/features/auth/serverAuth.ts enthält "isMaster: boolean" im StoredSession Interface
    - client/src/features/auth/serverAuth.ts enthält "isMaster: data.isMaster" im serverLogin()
    - client/src/features/admin/shops/ShopsManager.tsx existiert und enthält "export function ShopsManager"
    - client/src/features/admin/shops/ShopsManager.tsx enthält "useQuery" und "useMutation"
    - client/src/features/admin/AdminScreen.tsx importiert ShopsManager
    - client/src/features/admin/AdminScreen.tsx enthält "isMaster" State
    - client/src/features/admin/AdminScreen.tsx enthält "visibleTabs" oder bedingtes Tab-Array
    - client/src/features/admin/AdminScreen.tsx enthält "tab === 'shops'"
    - TypeScript-Check: cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit gibt keine Fehler
  </acceptance_criteria>
  <done>
    isMaster wird nach Login gespeichert. AdminScreen zeigt Shops-Tab nur für Master.
    ShopsManager ermöglicht Shop-Anlegen und Deaktivieren.
  </done>
</task>

</tasks>

<verification>
TypeScript-Checks für Client und Server:
```bash
cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit 2>&1 | head -20
cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit 2>&1 | head -20
```

Manuelle Verifikation nach Deployment:
1. Mit St. Secundus PIN einloggen → Admin-Tab "Shops" erscheint
2. Neuen Shop anlegen → Shop erscheint in Liste
3. Shop deaktivieren → Shop ist ausgegraut
4. Mit deaktiviertem Shop PIN einloggen → Fehlermeldung "deaktiviert"
5. Mit anderem (nicht-Master) Shop einloggen → kein "Shops"-Tab sichtbar
</verification>

<success_criteria>
- Server kompiliert ohne TypeScript-Fehler
- Client kompiliert ohne TypeScript-Fehler
- GET /api/shops gibt 403 für Nicht-Master-Shops
- POST /api/shops legt neuen Shop an (Master-only)
- PATCH /api/shops/:shopId aktiviert/deaktiviert einen Shop
- AdminScreen zeigt "Shops"-Tab nur wenn isMaster: true in der Session
- ShopsManager zeigt alle Shops mit Status und Aktions-Buttons
- Deaktivierter Shop kann sich nicht einloggen (403)
</success_criteria>

<output>
Nach Completion: `.planning/phases/24-master-shop-administration/24-02-SUMMARY.md` erstellen.
</output>
