---
phase: 15-datenintegrit-t
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/db/schema.ts
  - client/src/features/pos/useCart.ts
  - client/src/features/pos/POSScreen.tsx
autonomous: true
requirements:
  - DAT-03
  - VAL-01

must_haves:
  truths:
    - "Nach einem Page-Reload sind die Cart-Artikel noch vorhanden"
    - "Cart-Artikel gehören zum aktuellen Shop — andere Shops sehen ihren eigenen Cart"
    - "Ungültige Artikel (nicht mehr existent oder deaktiviert) werden beim Laden aus Dexie entfernt"
    - "Wenn ungültige Artikel entfernt wurden, sieht die Mitarbeiterin einen Toast mit Namen der entfernten Artikel"
    - "Der Preis-Snapshot der Cart-Artikel wird beim Reload nicht aus der DB überschrieben"
  artifacts:
    - path: "client/src/db/schema.ts"
      provides: "Dexie version(7) mit cartItems-Tabelle, PersistedCartItem-Interface"
      contains: "cartItems"
    - path: "client/src/features/pos/useCart.ts"
      provides: "LOAD-Action im Reducer, loadAndValidate-Effect, persist-Effect mit loaded-Flag"
      contains: "LOAD"
    - path: "client/src/features/pos/POSScreen.tsx"
      provides: "invalidItems-State + Toast-Anzeige wenn Artikel entfernt wurden"
      contains: "invalidItems"
  key_links:
    - from: "client/src/features/pos/useCart.ts"
      to: "client/src/db/schema.ts FairstandDB.cartItems"
      via: "db.cartItems.where('shopId').equals(shopId)"
      pattern: "db\\.cartItems"
    - from: "client/src/features/pos/useCart.ts"
      to: "client/src/db/schema.ts FairstandDB.products"
      via: "db.products.where('id').anyOf(productIds)"
      pattern: "db\\.products.*anyOf"
    - from: "client/src/features/pos/POSScreen.tsx"
      to: "client/src/features/pos/useCart.ts useCart()"
      via: "cart.invalidItems für Toast-Anzeige"
      pattern: "invalidItems"
---

<objective>
Drei Änderungen für Warenkorb-Persistenz und Validierung:

1. **schema.ts**: Dexie version(7) mit neuer `cartItems`-Tabelle + `PersistedCartItem`-Interface
2. **useCart.ts**: `LOAD`-Action im Reducer + `loadAndValidate`-Effect beim Mount + `persist`-Effect mit `loaded`-Flag + `invalidItems` im Return-Wert
3. **POSScreen.tsx**: Toast-Anzeige wenn ungültige Artikel beim Laden entfernt wurden

Purpose: DAT-03 (Warenkorb überlebt Reload) und VAL-01 (Geister-Artikel aus Cart entfernen) — aktuell ist der Cart reiner In-Memory-State der bei jedem Reload verloren geht.
Output: Cart wird in Dexie persistiert, beim Mount geladen, validiert und ungültige Artikel mit Feedback entfernt.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Interfaces aus client/src/db/schema.ts — Executor braucht diese direkt -->

```typescript
// CartItem = SaleItem (kein shopId-Feld)
export type CartItem = SaleItem;

export interface SaleItem {
  productId: string;
  articleNumber: string;
  name: string;
  salePrice: number; // Cent-Integer — Snapshot zum Kaufzeitpunkt
  quantity: number;
}

// Aktuelle Dexie-Klasse (Version 6)
export class FairstandDB extends Dexie {
  products!: EntityTable<Product, 'id'>;
  sales!: EntityTable<Sale, 'id'>;
  outbox!: EntityTable<OutboxEntry, 'id'>;
}

// Product hat: id, shopId, active, stock, name, ...
export interface Product {
  id: string;
  shopId: string;
  active: boolean;
  stock: number;
  // ...
}
```

<!-- Bestehende Imports in useCart.ts -->
```typescript
import { useReducer } from 'react';
import type { CartItem, Product } from '../../db/index.js';
```

<!-- POSScreen.tsx nutzt useCart so: -->
```typescript
const cart = useCart();
// cart.items, cart.total, cart.addItem, cart.removeItem, cart.updateQuantity, cart.clear
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie version(7) mit cartItems-Tabelle in schema.ts</name>
  <files>client/src/db/schema.ts</files>
  <read_first>
    - client/src/db/schema.ts (vollständig lesen — alle bestehenden versions() verstehen)
  </read_first>
  <action>
In `client/src/db/schema.ts` zwei Änderungen vornehmen:

**1. Neues Interface `PersistedCartItem` nach dem bestehenden `CartItem = SaleItem`-Alias einfügen:**
```typescript
export interface PersistedCartItem extends SaleItem {
  shopId: string; // nötig für Shop-Isolation in Dexie-Index
}
```

**2. `FairstandDB`-Klasse erweitern:**
- Neue Property deklarieren: `cartItems!: EntityTable<PersistedCartItem, 'productId'>;`
- Am Ende des Konstruktors `version(7)` hinzufügen (kein `.upgrade()` nötig — neue Tabelle startet leer):

```typescript
this.version(7).stores({
  products: 'id, shopId, category, active, [shopId+active]',
  sales: 'id, shopId, createdAt, syncedAt, cancelledAt',
  outbox: '++id, shopId, createdAt, operation',
  cartItems: 'productId, shopId',  // productId = PK, shopId = Index für Shop-Isolation
});
```

**Wichtig:** Alle vorherigen `version().stores()`-Einträge beibehalten — Dexie benötigt vollständige Schema-History für Migration bestehender DBs. Version(7) ist additiv und braucht kein `.upgrade()`.

**Wichtig:** `productId` ist der Primary Key (`productId, shopId` — erstes Feld = PK in Dexie). Ein Produkt kann nur einmal im Cart sein — Quantity-Tracking über das `quantity`-Feld in `PersistedCartItem`.
  </action>
  <verify>
    <automated>grep -n "cartItems\|PersistedCartItem\|version(7)" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `PersistedCartItem`-Interface ist exportiert und erweitert `SaleItem` mit `shopId: string`
    - `FairstandDB` hat `cartItems!: EntityTable<PersistedCartItem, 'productId'>` deklariert
    - `version(7).stores()` enthält `cartItems: 'productId, shopId'`
    - Alle bestehenden version(1) bis version(6) sind unverändert erhalten
    - TypeScript-Check: `cd client && npx tsc --noEmit` ohne Fehler in schema.ts
  </acceptance_criteria>
  <done>Dexie-Schema version(7) mit cartItems-Tabelle bereit für Cart-Persistenz</done>
</task>

<task type="auto">
  <name>Task 2: useCart.ts — LOAD-Action, loadAndValidate, persist-Effect</name>
  <files>client/src/features/pos/useCart.ts</files>
  <read_first>
    - client/src/features/pos/useCart.ts (vollständig lesen)
    - client/src/db/schema.ts (PersistedCartItem und FairstandDB nach Task 1)
    - client/src/features/auth/serverAuth.ts (getShopId() Signatur prüfen)
  </read_first>
  <action>
`useCart.ts` in vier Schritten erweitern — bestehende `checkStockBeforeAdd`, Reducer-Cases und Hook-Methoden bleiben unverändert:

**Schritt 1: Imports erweitern**
```typescript
import { useReducer, useState, useEffect } from 'react';
import type { CartItem, Product, PersistedCartItem } from '../../db/index.js';
import { db } from '../../db/index.js';
import { getShopId } from '../auth/serverAuth.js';
```

**Schritt 2: LOAD-Action zum CartAction-Typ und Reducer hinzufügen**
```typescript
type CartAction =
  | { type: 'ADD_ITEM'; product: Product }
  | { type: 'REMOVE_ITEM'; productId: string }
  | { type: 'UPDATE_QUANTITY'; productId: string; quantity: number }
  | { type: 'CLEAR' }
  | { type: 'LOAD'; items: CartItem[] };  // NEU — für Mount aus Dexie
```

Im Reducer nach `case 'CLEAR':` einfügen:
```typescript
case 'LOAD':
  return { items: action.items };
```

**Schritt 3: Hook-Signatur und State erweitern**
Im `useCart()`-Hook nach `const [state, dispatch] = useReducer(...)`:
```typescript
const [loaded, setLoaded] = useState(false);
const [invalidItems, setInvalidItems] = useState<string[]>([]);
```

**Schritt 4: Zwei Effects nach dem `total`-Ausdruck einfügen**

*Effect 1 — Mount: Laden und Validieren*
```typescript
useEffect(() => {
  async function loadAndValidate() {
    let shopId: string;
    try {
      shopId = getShopId();
    } catch {
      setLoaded(true);
      return; // nicht eingeloggt — leerer Cart ist korrekt
    }

    const stored = await db.cartItems
      .where('shopId').equals(shopId).toArray();

    if (stored.length === 0) {
      setLoaded(true);
      return;
    }

    const productIds = stored.map(i => i.productId);
    const currentProducts = await db.products
      .where('id').anyOf(productIds).toArray();
    const productMap = new Map(currentProducts.map(p => [p.id, p]));

    const valid: CartItem[] = [];
    const removed: string[] = [];

    for (const item of stored) {
      const p = productMap.get(item.productId);
      if (!p || !p.active) {
        removed.push(item.name); // nicht mehr existent oder deaktiviert
        continue;
      }
      // Preis-Snapshot beibehalten — per Phase-01-Entscheidung NICHT aus DB aktualisieren
      const { shopId: _shopId, ...cartItem } = item; // shopId aus PersistedCartItem entfernen
      valid.push(cartItem);
    }

    if (valid.length > 0) {
      dispatch({ type: 'LOAD', items: valid });
    }
    if (removed.length > 0) {
      setInvalidItems(removed);
    }
    setLoaded(true);
  }
  loadAndValidate();
}, []); // eslint-disable-line react-hooks/exhaustive-deps
```

*Effect 2 — Persistieren nach jeder Reducer-Aktion*
```typescript
useEffect(() => {
  if (!loaded) return; // loaded-Flag: verhindert Löschen vor erstem Laden

  let shopId: string;
  try {
    shopId = getShopId();
  } catch {
    return; // nicht eingeloggt — nicht persistieren
  }

  async function persist() {
    await db.transaction('rw', db.cartItems, async () => {
      await db.cartItems.where('shopId').equals(shopId).delete();
      if (state.items.length > 0) {
        const toStore: PersistedCartItem[] = state.items.map(i => ({ ...i, shopId }));
        await db.cartItems.bulkPut(toStore);
      }
    });
  }
  persist();
}, [state.items, loaded]); // eslint-disable-line react-hooks/exhaustive-deps
```

**Schritt 5: Return-Wert des Hooks um `invalidItems` und `clearInvalidItems` erweitern**
```typescript
return {
  items: state.items,
  total,
  addItem,
  removeItem,
  updateQuantity,
  clear,
  invalidItems,
  clearInvalidItems: () => setInvalidItems([]),
};
```

**Wichtig:** `loaded`-Flag ist zwingend — ohne es würde der persist-Effect mit leerem initialState feuern und Dexie sofort leeren bevor `loadAndValidate` fertig ist. Das ist der häufigste Pitfall bei diesem Pattern (aus RESEARCH.md Pitfall 1).

**Wichtig:** `getShopId()` wirft wenn kein Shop gesetzt ist — try/catch ist nötig.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `LOAD`-Action ist im `CartAction`-Typ und im Reducer implementiert
    - `loaded`-State-Variable ist deklariert und der persist-Effect prüft `if (!loaded) return`
    - `loadAndValidate`-Effect läuft beim Mount (leeres Dependency-Array `[]`)
    - persist-Effect hat `[state.items, loaded]` als Dependencies
    - `invalidItems: string[]` ist im Return-Wert von `useCart()`
    - `clearInvalidItems` ist im Return-Wert von `useCart()`
    - `db.cartItems` wird korrekt importiert aus `../../db/index.js`
    - TypeScript: `npx tsc --noEmit` ohne Fehler
  </acceptance_criteria>
  <done>useCart persistiert Cart in Dexie, lädt beim Mount, validiert ungültige Artikel und meldet sie zurück</done>
</task>

<task type="auto">
  <name>Task 3: POSScreen.tsx — Toast für entfernte ungültige Cart-Artikel</name>
  <files>client/src/features/pos/POSScreen.tsx</files>
  <read_first>
    - client/src/features/pos/POSScreen.tsx (vollständig lesen)
    - client/src/features/pos/useCart.ts (nach Task 2 — invalidItems-API verstehen)
  </read_first>
  <action>
`POSScreen.tsx` um Toast-Anzeige für entfernte ungültige Cart-Artikel ergänzen — minimal-invasiv:

**Schritt 1: invalidItems aus useCart destructuren**
```typescript
const cart = useCart();
```
bleibt, aber direkt nach der Zeile einen neuen Effect für ungültige Artikel einfügen:

**Schritt 2: useEffect für invalidItems-Toast nach dem bestehenden shopName-Effect einfügen**
```typescript
useEffect(() => {
  if (cart.invalidItems.length > 0) {
    setStockError(
      `Artikel nicht mehr verfügbar und aus Warenkorb entfernt: ${cart.invalidItems.join(', ')}`
    );
    cart.clearInvalidItems();
    // Toast nach 4 Sekunden ausblenden (längere Anzeigezeit als normale Stock-Fehler)
    const t = setTimeout(() => setStockError(null), 4000);
    return () => clearTimeout(t);
  }
}, [cart.invalidItems]); // eslint-disable-line react-hooks/exhaustive-deps
```

**Wichtig:** `cart.invalidItems` ist ein neuer Array bei jedem Render wenn Items vorhanden — der Effect feuert nur wenn `invalidItems.length > 0`. `clearInvalidItems()` setzt den Array zurück um doppeltes Feuern zu verhindern.

**Kein zweiter stockError-State nötig** — der bestehende `stockError`-State in POSScreen wird wiederverwendet. Der Toast ist identisch styled wie der Stock-Fehler-Toast.
  </action>
  <verify>
    <automated>grep -n "invalidItems\|clearInvalidItems" /Users/simonluthe/Documents/fairstand/client/src/features/pos/POSScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `cart.invalidItems` wird in POSScreen.tsx referenziert
    - `cart.clearInvalidItems()` wird nach dem Toast-Setzen aufgerufen
    - Der useEffect hat `[cart.invalidItems]` als Dependency
    - Kein neuer State deklariert — bestehender `stockError`-State wird wiederverwendet
    - TypeScript: `cd client && npx tsc --noEmit` ohne Fehler in POSScreen.tsx
  </acceptance_criteria>
  <done>POSScreen zeigt Toast wenn ungültige Artikel beim Cart-Laden entfernt wurden — DAT-03 und VAL-01 vollständig implementiert</done>
</task>

</tasks>

<verification>
```bash
# schema.ts: version(7) und PersistedCartItem vorhanden
grep -n "version(7)\|PersistedCartItem\|cartItems" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts

# useCart.ts: LOAD-Action und loaded-Flag vorhanden
grep -n "LOAD\|loaded\|invalidItems\|db\.cartItems" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useCart.ts

# POSScreen.tsx: Toast-Handler vorhanden
grep -n "invalidItems\|clearInvalidItems" /Users/simonluthe/Documents/fairstand/client/src/features/pos/POSScreen.tsx

# TypeScript-Build frontend
cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit
```
</verification>

<success_criteria>
- Dexie-Schema hat version(7) mit cartItems-Tabelle (productId PK, shopId Index)
- useCart lädt Cart beim Mount aus Dexie und validiert Artikel gegen aktuelle Produktdaten
- Ungültige (nicht existent/deaktiviert) Artikel werden entfernt und als `invalidItems[]` zurückgegeben
- POSScreen zeigt Toast für entfernte Artikel (4 Sekunden sichtbar)
- Preis-Snapshot bleibt beim Reload unverändert (keine Preis-Aktualisierung aus DB)
- loaded-Flag verhindert Race-Condition zwischen Lade-Effect und Persist-Effect
- TypeScript-Build ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/15-datenintegrit-t/15-02-SUMMARY.md` erstellen mit:
- Dexie-Version und neue Tabellen-Definition
- Geänderte Dateien und Kern-Änderungen
- Bestätigung TypeScript-Build erfolgreich
</output>
