---
phase: 27-preis-history-bestandsverlauf
plan: 03
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - server/src/routes/sync.ts
  - server/src/routes/priceHistory.ts
  - server/src/index.ts
autonomous: true
requirements:
  - INV-04
must_haves:
  truths:
    - "Nach SALE_COMPLETE in POST /sync enthält stock_movements für jeden Artikel einen Eintrag mit type='sale', negativer Menge und referenceSaleId"
    - "Nach STOCK_ADJUST enthält stock_movements einen Eintrag mit type='adjustment'"
    - "Nach SALE_CANCEL enthält stock_movements für jeden Artikel einen Eintrag mit type='return' (Restock)"
    - "Nach ITEM_RETURN enthält stock_movements einen Eintrag mit type='return'"
    - "GET /api/products/:id/stock-movements gibt chronologische Liste aller Bewegungen für einen Artikel zurück"
    - "GET /api/products/:id/price-history gibt chronologische Liste aller Preis-Änderungen zurück"
  artifacts:
    - path: "server/src/routes/sync.ts"
      provides: "stock_movements Logging für alle 4 Sync-Operationen"
      contains: "stockMovements"
    - path: "server/src/routes/priceHistory.ts"
      provides: "GET /products/:id/stock-movements + GET /products/:id/price-history Endpoints"
      exports: ["priceHistoryRoutes"]
    - path: "server/src/index.ts"
      provides: "priceHistoryRoutes registriert"
      contains: "priceHistoryRoutes"
  key_links:
    - from: "server/src/routes/sync.ts"
      to: "stock_movements Tabelle"
      via: "tx.insert(stockMovements) in jeder Operation-Branch"
      pattern: "insert.*stockMovements"
    - from: "server/src/routes/priceHistory.ts"
      to: "price_histories + stock_movements Tabellen"
      via: "db.select() mit shopId-Check"
      pattern: "from.*priceHistories|from.*stockMovements"
---

<objective>
Ergänzt `sync.ts` um stock_movements Logging für alle vier Sync-Operationen (SALE_COMPLETE, STOCK_ADJUST, SALE_CANCEL, ITEM_RETURN) und erstellt einen neuen Route-Handler `priceHistory.ts` mit zwei GET-Endpoints für das Journal pro Artikel.

Purpose: INV-04 — User kann pro Artikel eine chronologische Liste aller Bestandsbewegungen abrufen. Die Sync-Route ist der zentrale Schreibpfad für alle Offline-Operationen.
Output: Erweitertes sync.ts + neue priceHistory.ts Route + Registrierung in index.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-preis-history-bestandsverlauf/27-01-SUMMARY.md

<interfaces>
<!-- Schema aus 27-01 und bestehende sync.ts Strukturen -->

Neue Tabellen aus 27-01 (server/src/db/schema.ts):
```typescript
export const priceHistories = pgTable('price_histories', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  field: text('field').notNull(),
  oldValue: integer('old_value').notNull(),
  newValue: integer('new_value').notNull(),
  changedAt: bigint('changed_at', { mode: 'number' }).notNull(),
});

export const stockMovements = pgTable('stock_movements', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  type: text('type').notNull(),
  quantity: integer('quantity').notNull(),
  referenceSaleId: text('reference_sale_id'), // nullable
  reason: text('reason'), // nullable
  movedAt: bigint('moved_at', { mode: 'number' }).notNull(),
});
```

Bestehende sync.ts Struktur (kritische Stellen):
- Zeile 5: `import { sales, products, outboxEvents } from '../db/schema.js';` — stockMovements ergänzen
- Zeile 94-134: SALE_COMPLETE Transaction — nach Stock-Delta (Zeilen 115-124) stockMovements INSERT einfügen
- Zeile 146-163: STOCK_ADJUST Transaction — nach products.update stockMovements INSERT einfügen
- Zeile 174-199: SALE_CANCEL Transaction — nach Restock-Loop für jedes Item stockMovements INSERT einfügen
- Zeile 209-228: ITEM_RETURN Transaction — nach products.update stockMovements INSERT einfügen

**Kritische Regel (Research Pitfall 2):** Für `movedAt` IMMER `entry.createdAt` verwenden, NICHT `Date.now()`. entry.createdAt ist der "offizielle" Zeitpunkt der Offline-Operation.

Bestehende index.ts Struktur — Route-Registrierung Pattern:
```typescript
// server/src/index.ts
import { productRoutes } from './routes/products.js';
fastify.register(productRoutes, { prefix: '/api' });
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: stock_movements Logging in sync.ts für alle 4 Operationen</name>
  <files>server/src/routes/sync.ts</files>
  <read_first>
    - server/src/routes/sync.ts — vollständige Datei lesen (alle 243 Zeilen), alle 4 Operation-Branches verstehen
    - server/src/db/schema.ts — stockMovements Export aus 27-01 prüfen
  </read_first>
  <action>
**Import erweitern** (Zeile 5):
```typescript
import { sales, products, outboxEvents, stockMovements } from '../db/schema.js';
```

**SALE_COMPLETE (innerhalb der Transaction, nach dem Stock-Delta-Loop, Zeilen 115-124):**
Nach dem bestehenden `await tx.update(products).set({stock: ...})` Block (innerhalb des `for (const item of sale.items)` Loops) einen stock_movements INSERT hinzufügen:

```typescript
for (const item of sale.items) {
  const [prod] = await tx.select().from(products).where(eq(products.id, item.productId)).limit(1);
  if (!prod || prod.shopId !== entry.shopId) {
    continue;
  }
  await tx.update(products)
    .set({ stock: sql`${products.stock} - ${item.quantity}` })
    .where(eq(products.id, item.productId));
  // Stock-Movement protokollieren
  await tx.insert(stockMovements).values({
    shopId: entry.shopId,
    productId: item.productId,
    type: 'sale',
    quantity: -item.quantity, // negativ = Ausgang
    referenceSaleId: sale.id,
    movedAt: entry.createdAt, // WICHTIG: entry.createdAt, nicht Date.now()
  });
}
```

**STOCK_ADJUST (innerhalb der Transaction, nach dem products.update):**
```typescript
await tx.update(products)
  .set({ stock: sql`${products.stock} + ${adj.delta}`, updatedAt: sql`${Date.now()}` })
  .where(eq(products.id, adj.productId));
await tx.insert(stockMovements).values({
  shopId: entry.shopId,
  productId: adj.productId,
  type: 'adjustment',
  quantity: adj.delta, // positiv oder negativ je nach Korrektur
  reason: adj.reason ?? null,
  movedAt: entry.createdAt, // WICHTIG: entry.createdAt
});
await tx.insert(outboxEvents).values({ ... }); // unverändert
```

**SALE_CANCEL (innerhalb der Transaction, innerhalb des Restock-Loops):**
```typescript
for (const item of cancel.items) {
  const [cancelProd] = await tx.select().from(products).where(eq(products.id, item.productId)).limit(1);
  if (!cancelProd || cancelProd.shopId !== entry.shopId) {
    continue;
  }
  await tx.update(products)
    .set({ stock: sql`${products.stock} + ${item.quantity}` })
    .where(eq(products.id, item.productId));
  // Stock-Movement protokollieren
  await tx.insert(stockMovements).values({
    shopId: entry.shopId,
    productId: item.productId,
    type: 'return',
    quantity: item.quantity, // positiv = Eingang (Restock nach Storno)
    referenceSaleId: cancel.saleId,
    movedAt: entry.createdAt, // WICHTIG: entry.createdAt
  });
}
```

**ITEM_RETURN (innerhalb der Transaction, nach dem products.update):**
```typescript
await tx.update(products)
  .set({ stock: sql`${products.stock} + ${ret.quantity}` })
  .where(eq(products.id, ret.productId));
await tx.insert(stockMovements).values({
  shopId: entry.shopId,
  productId: ret.productId,
  type: 'return',
  quantity: ret.quantity, // positiv = Eingang
  referenceSaleId: ret.saleId,
  movedAt: entry.createdAt, // WICHTIG: entry.createdAt
});
await tx.insert(outboxEvents).values({ ... }); // unverändert
```

**Alle anderen Teile der Datei bleiben unverändert** (Validierungslogik, ShopId-Checks, OutboxEvent-Inserts, broadcast-Aufruf).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && grep -c "insert(stockMovements)" src/routes/sync.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "insert(stockMovements)" server/src/routes/sync.ts` gibt 4 aus (je 1 pro Operation)
    - `grep "type: 'sale'" server/src/routes/sync.ts` findet 1 Treffer (SALE_COMPLETE)
    - `grep "type: 'adjustment'" server/src/routes/sync.ts` findet 1 Treffer (STOCK_ADJUST)
    - `grep -c "type: 'return'" server/src/routes/sync.ts` gibt 2 aus (SALE_CANCEL + ITEM_RETURN)
    - `grep "entry.createdAt" server/src/routes/sync.ts` findet mindestens 4 Treffer (movedAt in jedem Insert)
    - `grep "Date.now()" server/src/routes/sync.ts` findet KEINEN Treffer im movedAt-Kontext (nur in outboxEvents.processedAt und updatedAt)
    - `npx tsc --noEmit` gibt 0 Fehler aus
  </acceptance_criteria>
  <done>Alle 4 Sync-Operationen protokollieren Bestandsbewegungen in stock_movements mit entry.createdAt als Zeitstempel</done>
</task>

<task type="auto">
  <name>Task 2: Neue Route priceHistory.ts mit GET-Endpoints + Registrierung in index.ts</name>
  <files>server/src/routes/priceHistory.ts, server/src/index.ts</files>
  <read_first>
    - server/src/index.ts — vollständige Datei lesen, Route-Registrierungen verstehen und Einfügestelle finden
    - server/src/routes/reports.ts — Muster für bestehende Route-Dateien (FastifyInstance, session, shopId-Check)
    - server/src/db/schema.ts — priceHistories + stockMovements Export aus 27-01 prüfen
  </read_first>
  <action>
**Neue Datei erstellen: `server/src/routes/priceHistory.ts`**

```typescript
import type { FastifyInstance } from 'fastify';
import { eq, desc } from 'drizzle-orm';
import { db } from '../db/index.js';
import { products, priceHistories, stockMovements } from '../db/schema.js';

export async function priceHistoryRoutes(fastify: FastifyInstance) {
  // GET /products/:id/price-history — Alle Preis-Änderungen für einen Artikel
  fastify.get('/products/:id/price-history', async (request, reply) => {
    const { id } = request.params as { id: string };
    const session = (request as any).session as { shopId: string };

    // Ownership-Check: Produkt muss zum Shop der Session gehören
    const [product] = await db.select({ id: products.id, shopId: products.shopId })
      .from(products)
      .where(eq(products.id, id))
      .limit(1);

    if (!product || product.shopId !== session.shopId) {
      return reply.status(403).send({ error: 'Zugriff verweigert' });
    }

    const history = await db
      .select()
      .from(priceHistories)
      .where(eq(priceHistories.productId, id))
      .orderBy(desc(priceHistories.changedAt));

    return reply.send(history);
  });

  // GET /products/:id/stock-movements — Alle Bestandsbewegungen für einen Artikel
  fastify.get('/products/:id/stock-movements', async (request, reply) => {
    const { id } = request.params as { id: string };
    const session = (request as any).session as { shopId: string };

    // Ownership-Check: Produkt muss zum Shop der Session gehören
    const [product] = await db.select({ id: products.id, shopId: products.shopId })
      .from(products)
      .where(eq(products.id, id))
      .limit(1);

    if (!product || product.shopId !== session.shopId) {
      return reply.status(403).send({ error: 'Zugriff verweigert' });
    }

    const movements = await db
      .select()
      .from(stockMovements)
      .where(eq(stockMovements.productId, id))
      .orderBy(desc(stockMovements.movedAt));

    return reply.send(movements);
  });
}
```

**server/src/index.ts erweitern** — Import und Registrierung analog zu bestehenden Routen einfügen:

1. Import-Zeile hinzufügen (bei den anderen Route-Importen):
```typescript
import { priceHistoryRoutes } from './routes/priceHistory.js';
```

2. Route registrieren (bei den anderen `fastify.register(...)` Aufrufen):
```typescript
fastify.register(priceHistoryRoutes, { prefix: '/api' });
```

Position: Nach den bestehenden Route-Registrierungen, vor dem Server-Start.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && test -f src/routes/priceHistory.ts && grep -n "priceHistoryRoutes" src/index.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `test -f server/src/routes/priceHistory.ts` gibt Exit-Code 0
    - `grep "priceHistoryRoutes" server/src/index.ts` findet 2 Treffer (Import + register)
    - `grep "GET.*price-history" server/src/routes/priceHistory.ts || grep "price-history" server/src/routes/priceHistory.ts` findet 1 Treffer (Route-Definition)
    - `grep "GET.*stock-movements" server/src/routes/priceHistory.ts || grep "stock-movements" server/src/routes/priceHistory.ts` findet 1 Treffer (Route-Definition)
    - `grep "shopId !== session.shopId" server/src/routes/priceHistory.ts` findet 2 Treffer (ShopId-Check in beiden Endpoints)
    - `grep "orderBy(desc(" server/src/routes/priceHistory.ts` findet 2 Treffer (absteigende Sortierung)
    - `npx tsc --noEmit` gibt 0 Fehler aus
  </acceptance_criteria>
  <done>Beide GET-Endpoints existieren in priceHistory.ts, sind in index.ts registriert, haben ShopId-Checks und geben chronologisch sortierte Ergebnisse zurück</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

```bash
cd /Users/simonluthe/Documents/fairstand/server

# 1. sync.ts hat 4 stockMovements Inserts
grep -c "insert(stockMovements)" src/routes/sync.ts  # erwartet: 4

# 2. Neue Route vorhanden und registriert
test -f src/routes/priceHistory.ts && echo "EXISTS"
grep -c "priceHistoryRoutes" src/index.ts  # erwartet: 2

# 3. TypeScript fehlerfrei
npx tsc --noEmit 2>&1 | grep -c "error TS"  # erwartet: 0

# 4. movedAt verwendet entry.createdAt (nicht Date.now())
grep "movedAt: entry.createdAt" src/routes/sync.ts  # mindestens 4 Treffer
```
</verification>

<success_criteria>
- sync.ts loggt Bestandsbewegungen für alle 4 Operationen in stock_movements
- movedAt verwendet entry.createdAt (nicht Date.now()) für zeitlich korrekte Protokollierung
- GET /api/products/:id/price-history gibt price_histories-Einträge zurück (neueste zuerst)
- GET /api/products/:id/stock-movements gibt stock_movements-Einträge zurück (neueste zuerst)
- Beide Endpoints haben ShopId-Check (Ownership-Validierung)
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/27-preis-history-bestandsverlauf/27-03-SUMMARY.md` erstellen mit:
- Neue Route-Datei und Endpoints
- Logging-Coverage (welche Operationen in sync.ts)
- TypeScript-Status
- API-Endpunkte für Phase 28 (werden dort für Auswertungen genutzt)
</output>
