---
phase: 28-inventur-uebersicht-preis-auswertung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/reports.ts
autonomous: true
requirements:
  - INV-01
  - INV-02
  - INV-03
  - PRICE-03

must_haves:
  truths:
    - "GET /api/reports/inventory?year=XXXX gibt 200 zurück mit items-Array und total_stock_value_cents"
    - "Jedes item enthält id, name, article_number, current_stock, current_ek_cents, sold_qty, revenue_cents, cost_cents, ek_breakdown"
    - "ek_breakdown ist ein Array mit { ek_cents, qty } — enthält mehrere Einträge wenn Artikel mit verschiedenen EKs verkauft wurde"
    - "Artikel ohne Verkäufe im Jahreszeitraum erscheinen mit sold_qty=0 im Ergebnis"
    - "total_stock_value_cents ist Summe (stock * purchase_price) aller aktiven Produkte des Shops"
    - "NULL purchasePrice in Sale-Items wird per COALESCE auf aktuellen products.purchase_price aufgelöst"
  artifacts:
    - path: "server/src/routes/reports.ts"
      provides: "GET /api/reports/inventory Endpoint"
      contains: "fastify.get('/reports/inventory'"
  key_links:
    - from: "GET /api/reports/inventory"
      to: "products + sales JSONB"
      via: "LEFT JOIN + jsonb_array_elements"
      pattern: "jsonb_array_elements"
---

<objective>
Neuer Backend-Endpoint GET /api/reports/inventory in server/src/routes/reports.ts, der für ein gegebenes Jahr alle aktiven Produkte eines Shops mit Verkaufsmengen, VK-Umsatz, EK-Kosten und EK-Aufschlüsselung aggregiert.

Purpose: INV-01/02/03 und PRICE-03 — der Jahresbericht braucht eine Inventur-Ansicht mit korrekten EK-Kosten auch bei Preisänderungen im Jahresverlauf.
Output: Erweitertes reports.ts mit /inventory Endpoint; kein neues Routing nötig (bereits in server/src/index.ts als reportRoutes registriert).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@server/src/routes/reports.ts
@server/src/routes/priceHistory.ts
@server/src/index.ts
</context>

<interfaces>
<!-- Bestehende Muster in server/src/routes/reports.ts -->

Bestehende Route-Signatur (Vorlage für /inventory):
```typescript
fastify.get('/reports/yearly', async (request, reply) => {
  const session = (request as any).session as { shopId: string };
  const shopId = session.shopId;
  const { year } = request.query as { year: string };
  // ...
  return reply.send({ year: y, months: [...] });
});
```

Registrierung in server/src/index.ts:
```typescript
await fastify.register(reportRoutes, { prefix: '/api' });
// → Endpoints: /api/reports/inventory
```

Relevante Imports (bereits in reports.ts vorhanden):
```typescript
import type { FastifyInstance } from 'fastify';
import { sql } from 'drizzle-orm';
import { db } from '../db/index.js';
```

Erwartetes Response-Schema (TypeScript-Interface für Executor):
```typescript
interface InventoryItem {
  id: string;
  article_number: string;
  name: string;
  current_stock: number;
  current_ek_cents: number;
  sold_qty: number;
  revenue_cents: number;
  cost_cents: number;
  ek_breakdown: Array<{ ek_cents: number; qty: number }>;
}

interface InventoryResponse {
  year: number;
  items: InventoryItem[];
  total_stock_value_cents: number;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: GET /api/reports/inventory Endpoint in reports.ts</name>
  <files>server/src/routes/reports.ts</files>

  <read_first>
    - server/src/routes/reports.ts — vollständige Datei lesen, Pattern der bestehenden Routen übernehmen
    - server/src/routes/priceHistory.ts — Session-Ownership-Check-Muster
  </read_first>

  <action>
Am Ende der Funktion `reportRoutes` in server/src/routes/reports.ts einen neuen Endpoint `fastify.get('/reports/inventory', ...)` ergänzen.

**Wichtig: kein neues Import-Statement nötig** — `db`, `sql`, `FastifyInstance` sind bereits importiert.

Der Endpoint macht zwei SQL-Queries sequentiell:

**Query 1 — Inventur pro Artikel:**
```sql
SELECT
  p.id,
  p.article_number,
  p.name,
  p.stock as current_stock,
  p.purchase_price as current_ek_cents,
  COALESCE(SUM(
    CASE WHEN (sales.type IS NULL OR sales.type = 'sale')
    THEN (item->>'quantity')::integer ELSE 0 END
  ), 0) as sold_qty,
  COALESCE(SUM(
    CASE WHEN (sales.type IS NULL OR sales.type = 'sale')
    THEN (item->>'quantity')::integer * (item->>'salePrice')::integer ELSE 0 END
  ), 0) as revenue_cents,
  COALESCE(SUM(
    CASE WHEN (sales.type IS NULL OR sales.type = 'sale')
    THEN (item->>'quantity')::integer *
         COALESCE((item->>'purchasePrice')::integer, p.purchase_price)
    ELSE 0 END
  ), 0) as cost_cents
FROM products p
LEFT JOIN sales ON sales.shop_id = p.shop_id
                AND sales.created_at >= ${yearStart}
                AND sales.created_at < ${yearEnd}
                AND sales.cancelled_at IS NULL
LEFT JOIN jsonb_array_elements(sales.items) as item
  ON item->>'productId' = p.id
WHERE p.shop_id = ${shopId}
  AND p.active = true
GROUP BY p.id, p.name, p.article_number, p.stock, p.purchase_price
ORDER BY p.name
```

**Query 2 — Bestandswert-Summe:**
```sql
SELECT COALESCE(SUM(stock * purchase_price), 0) as total_stock_value_cents
FROM products
WHERE shop_id = ${shopId} AND active = true
```

**Query 3 — EK-Aufschlüsselung pro Artikel** (eine Query für alle Artikel auf einmal, nicht N+1):
```sql
SELECT
  item->>'productId' as product_id,
  COALESCE((item->>'purchasePrice')::integer, p.purchase_price) as ek_cents,
  SUM((item->>'quantity')::integer) as qty
FROM sales,
     jsonb_array_elements(sales.items) as item
JOIN products p ON p.id = item->>'productId'
WHERE sales.shop_id = ${shopId}
  AND sales.created_at >= ${yearStart}
  AND sales.created_at < ${yearEnd}
  AND sales.cancelled_at IS NULL
  AND (sales.type IS NULL OR sales.type = 'sale')
GROUP BY item->>'productId', COALESCE((item->>'purchasePrice')::integer, p.purchase_price)
ORDER BY product_id, ek_cents DESC
```

**Zusammenbau im TypeScript:**
- Query 3 Ergebnis in eine Map `ekMap: Map<string, Array<{ ek_cents: number; qty: number }>>` umwandeln
- Für jedes item aus Query 1: `ek_breakdown = ekMap.get(item.id) ?? []`
- Response: `{ year: y, items: itemsWithBreakdown, total_stock_value_cents: Number(stockValue) }`

**Jahr-Parameter:**
```typescript
const { year } = request.query as { year: string };
if (!year) return reply.status(400).send({ error: 'year required' });
const y = Number(year);
const yearStart = new Date(y, 0, 1).getTime();
const yearEnd = new Date(y + 1, 0, 1).getTime();
```

**Fehlerbehandlung:** Kein try/catch nötig — Fastify fängt unbehandelte Fehler automatisch. Aber: `year`-Validierung wie oben.

Beachte: `p.article_number` — prüfe im Schema wie das Feld heißt (`article_number` oder `articleNumber`). Drizzle gibt snake_case zurück — checke `server/src/db/schema.ts` kurz.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && curl -s -o /dev/null -w "%{http_code}" "http://localhost:3001/api/reports/inventory?year=2026" -H "Cookie: $(curl -s -c /tmp/cookies.txt -X POST http://localhost:3001/api/auth/login -H 'Content-Type: application/json' -d '{"pin":"0000"}' > /dev/null && cat /tmp/cookies.txt | grep session | awk '{print $7"="$8}')" 2>/dev/null || echo "Server not running — verify by checking TypeScript compiles: cd server && npx tsc --noEmit 2>&1 | head -20"</automated>
  </verify>

  <acceptance_criteria>
    - `GET /api/reports/inventory?year=2026` gibt 200 mit JSON-Body zurück
    - Response-Body enthält Felder: `year`, `items` (Array), `total_stock_value_cents` (Zahl)
    - Jedes item hat: `id`, `article_number`, `name`, `current_stock`, `current_ek_cents`, `sold_qty`, `revenue_cents`, `cost_cents`, `ek_breakdown`
    - `ek_breakdown` ist Array mit `{ ek_cents, qty }` Objekten
    - `GET /api/reports/inventory` ohne `year`-Parameter gibt 400 zurück
    - TypeScript kompiliert ohne Fehler: `cd server && npx tsc --noEmit` gibt keine Errors aus
  </acceptance_criteria>

  <done>
    GET /api/reports/inventory ist in reports.ts implementiert, gibt korrektes JSON zurück, TypeScript kompiliert sauber.
  </done>
</task>

</tasks>

<verification>
```bash
cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit
```
Muss ohne Fehler durchlaufen.

Manuelle Verifikation nach Server-Start:
```bash
# Login
curl -c /tmp/fs-cookies.txt -X POST http://localhost:3001/api/auth/login \
  -H 'Content-Type: application/json' -d '{"pin":"<shop-pin>"}'

# Inventory abrufen
curl -b /tmp/fs-cookies.txt "http://localhost:3001/api/reports/inventory?year=2026" | jq '.items[0]'
```
Erwartete Felder im ersten item: id, name, article_number, current_stock, current_ek_cents, sold_qty, revenue_cents, cost_cents, ek_breakdown.
</verification>

<success_criteria>
- GET /api/reports/inventory?year=XXXX antwortet mit HTTP 200 und korrektem JSON
- EK-Aufschlüsselung enthält pro Artikel wie viele Stück zu welchem EK verkauft wurden
- Bestandswert-Summe (total_stock_value_cents) ist Summe aller aktiven Produkte × aktueller EK
- TypeScript-Build sauber
</success_criteria>

<output>
Nach Abschluss `.planning/phases/28-inventur-uebersicht-preis-auswertung/28-01-SUMMARY.md` erstellen.
</output>
