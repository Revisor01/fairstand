---
phase: 28-inventur-uebersicht-preis-auswertung
plan: 02
type: execute
wave: 2
depends_on:
  - 28-01
files_modified:
  - client/src/features/admin/reports/MonthlyReport.tsx
  - client/src/features/admin/products/ProductStats.tsx
autonomous: false
requirements:
  - INV-01
  - INV-02
  - INV-03
  - PRICE-02

must_haves:
  truths:
    - "Im MonthlyReport gibt es einen dritten Tab 'Inventur' neben 'Monat' und 'Jahr'"
    - "Der Inventur-Tab zeigt eine Tabelle mit Spalten: Artikel, Bestand, Verkauft, VK-Umsatz, EK-Kosten"
    - "Artikel mit mehr als einer EK-Variante haben einen Expand-Button; Tap öffnet EK-Aufschlüsselung als Unterzeilen"
    - "Eine fette Summenzeile am Ende zeigt die Bestandswert-Summe (Menge × aktueller EK)"
    - "In ProductStats gibt es einen zweiten Tab 'Preis-History' neben dem Statistik-Bereich"
    - "Preis-History-Tab zeigt Tabelle: Datum, Feld (EK/VK), Alter Preis, Neuer Preis — neueste zuerst"
    - "Beide Tabs zeigen Hinweis 'Nur online verfügbar' wenn offline"
  artifacts:
    - path: "client/src/features/admin/reports/MonthlyReport.tsx"
      provides: "Inventur-Tab mit expandierbaren EK-Zeilen"
      contains: "activeTab.*inventur"
    - path: "client/src/features/admin/products/ProductStats.tsx"
      provides: "Preis-History-Tab"
      contains: "statsTab.*price-history"
  key_links:
    - from: "MonthlyReport.tsx Inventur-Tab"
      to: "/api/reports/inventory"
      via: "authFetch in useEffect"
      pattern: "authFetch.*inventory"
    - from: "ProductStats.tsx Preis-History-Tab"
      to: "/api/products/:id/price-history"
      via: "authFetch in useEffect"
      pattern: "authFetch.*price-history"
---

<objective>
UI-Erweiterung in zwei bestehenden Komponenten: (1) Inventur-Tab in MonthlyReport.tsx mit expandierbarer EK-Aufschlüsselung und Bestandswert-Summe, (2) Preis-History-Tab in ProductStats.tsx mit Zeitstrahl-Tabelle.

Purpose: INV-01/02/03 (Inventur-Ansicht im Jahresbericht) und PRICE-02 (Preis-History pro Artikel in Produktverwaltung).
Output: Erweiterte MonthlyReport.tsx und ProductStats.tsx — beide Komponenten bekommen neue Tabs mit inline-Datenfetching.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/28-inventur-uebersicht-preis-auswertung/28-01-SUMMARY.md

@client/src/features/admin/reports/MonthlyReport.tsx
@client/src/features/admin/products/ProductStats.tsx
</context>

<interfaces>
<!-- Aus Plan 28-01: Response-Schema des /api/reports/inventory Endpoints -->

```typescript
interface InventoryItem {
  id: string;
  article_number: string;
  name: string;
  current_stock: number;
  current_ek_cents: number;
  sold_qty: number;
  revenue_cents: number;
  cost_cents: number;
  ek_breakdown: Array<{ ek_cents: number; qty: number }>;
}

interface InventoryResponse {
  year: number;
  items: InventoryItem[];
  total_stock_value_cents: number;
}
```

<!-- Aus Phase 27: Response-Schema des /api/products/:id/price-history Endpoints -->

```typescript
// Aus server/src/routes/priceHistory.ts: Gibt priceHistories-Tabellen-Rows direkt zurück
interface PriceHistoryEntry {
  id: string;
  productId: string;
  field: string;          // 'purchase_price' oder 'sale_price'
  oldValue: number;       // Cents
  newValue: number;       // Cents
  changedAt: number;      // Timestamp (ms)
}
```

<!-- Bestehende Imports/Utils die bereits in den Komponenten vorhanden sind -->

Aus MonthlyReport.tsx (bereits importiert):
- `authFetch` aus `'../../auth/serverAuth.js'`
- `formatEur` aus `'../../pos/utils.js'`
- `format`, `de` aus `'date-fns'` / `'date-fns/locale'`
- `getShopId` aus `'../../../db/index.js'`

Aus ProductStats.tsx (bereits importiert):
- `authFetch` aus `'../../auth/serverAuth.js'`
- `formatEur` aus `'../../pos/utils.js'`
- `format`, `de` aus `'date-fns'` / `'date-fns/locale'`
- `getShopId` aus `'../../../db/index.js'`

Bestehende Tab-Typen in MonthlyReport.tsx:
- Aktuell KEIN activeTab-State — Monat und Jahr werden beide immer geladen
- Neuer State nötig: `activeTab: 'monat' | 'jahr' | 'inventur'`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Inventur-Tab in MonthlyReport.tsx</name>
  <files>client/src/features/admin/reports/MonthlyReport.tsx</files>

  <read_first>
    - client/src/features/admin/reports/MonthlyReport.tsx — vollständig lesen, bestehende State-Struktur und JSX-Struktur verstehen
    - client/src/features/admin/reports/ReportChart.tsx — nur kurz prüfen ob dort nochmal Tailwind-Patterns relevant sind
  </read_first>

  <action>
Bestehende MonthlyReport.tsx um Inventur-Tab erweitern. Die Datei vollständig lesen, dann gezielte Ergänzungen vornehmen:

**1. Neue Interfaces am Dateianfang (nach bestehenden Interfaces) ergänzen:**
```typescript
interface EkBreakdownEntry {
  ek_cents: number;
  qty: number;
}

interface InventoryItem {
  id: string;
  article_number: string;
  name: string;
  current_stock: number;
  current_ek_cents: number;
  sold_qty: number;
  revenue_cents: number;
  cost_cents: number;
  ek_breakdown: EkBreakdownEntry[];
}

interface InventoryResponse {
  year: number;
  items: InventoryItem[];
  total_stock_value_cents: number;
}
```

**2. Neuer State in MonthlyReport-Funktion (nach bestehenden States):**
```typescript
const [activeTab, setActiveTab] = useState<'monat' | 'jahr' | 'inventur'>('monat');
const [inventoryData, setInventoryData] = useState<InventoryResponse | null>(null);
const [loadingInventory, setLoadingInventory] = useState(false);
const [expandedProducts, setExpandedProducts] = useState<Set<string>>(new Set());
```

**3. Neuer useEffect für Inventur-Laden (nach bestehenden useEffects):**
```typescript
useEffect(() => {
  if (activeTab !== 'inventur' || !navigator.onLine) return;
  setLoadingInventory(true);
  setInventoryData(null);
  authFetch(`/api/reports/inventory?year=${year}`)
    .then(r => r.json())
    .then((data: InventoryResponse) => setInventoryData(data))
    .catch(() => setInventoryData(null))
    .finally(() => setLoadingInventory(false));
}, [year, activeTab]);
```

**4. Tab-Navigation:** Im JSX nach dem `<h2>Monats-/Jahresberichte</h2>` Abschnitt (oder an passender Stelle im bestehenden Layout) eine Tab-Leiste ergänzen:
```tsx
<div className="flex border-b border-sky-100 mt-4">
  {([
    { key: 'monat', label: 'Monat' },
    { key: 'jahr', label: 'Jahr' },
    { key: 'inventur', label: 'Inventur' },
  ] as const).map(tab => (
    <button
      key={tab.key}
      onPointerDown={() => setActiveTab(tab.key)}
      className={`px-5 py-3 text-sm font-medium border-b-2 transition-colors ${
        activeTab === tab.key
          ? 'border-sky-500 text-sky-700'
          : 'border-transparent text-slate-500 hover:text-slate-700'
      }`}
    >
      {tab.label}
    </button>
  ))}
</div>
```

**5. Bestehende Monat/Jahr-Inhalte mit Tab-Condition wrappen:**
```tsx
{activeTab !== 'inventur' && (
  // bestehender Monat/Jahr Inhalt bleibt unverändert
)}
```

Alternativ (sauberer): Separate Blöcke:
```tsx
{activeTab === 'monat' && ( /* Monats-Inhalt */ )}
{activeTab === 'jahr' && ( /* Jahres-Inhalt + Chart */ )}
{activeTab === 'inventur' && ( /* Inventur-Inhalt */ )}
```

**6. Inventur-Tab Inhalt:**
```tsx
{activeTab === 'inventur' && (
  <div className="flex flex-col gap-4 mt-4">
    {loadingInventory && (
      <div className="text-center py-12 text-slate-500">Laden...</div>
    )}
    {!loadingInventory && !inventoryData && (
      <div className="bg-slate-50 rounded-xl p-6 text-center text-slate-500">
        Keine Daten für {year}.
      </div>
    )}
    {!loadingInventory && inventoryData && (
      <div className="bg-white rounded-xl shadow-sm overflow-hidden">
        <table className="w-full text-sm">
          <thead>
            <tr className="bg-sky-50">
              <th className="text-left px-4 py-3 text-sky-700 font-medium">Artikel</th>
              <th className="text-right px-4 py-3 text-sky-700 font-medium">Bestand</th>
              <th className="text-right px-4 py-3 text-sky-700 font-medium">Verkauft</th>
              <th className="text-right px-4 py-3 text-sky-700 font-medium">VK-Umsatz</th>
              <th className="text-right px-4 py-3 text-sky-700 font-medium">EK-Kosten</th>
            </tr>
          </thead>
          <tbody>
            {inventoryData.items.map(item => {
              const isExpanded = expandedProducts.has(item.id);
              const hasVariants = item.ek_breakdown.length > 1;
              return (
                <React.Fragment key={item.id}>
                  <tr className="border-t border-slate-100 hover:bg-sky-50/50">
                    <td className="px-4 py-3 text-slate-800">
                      <div className="flex items-center gap-1">
                        {hasVariants && (
                          <button
                            onPointerDown={() => {
                              setExpandedProducts(prev => {
                                const next = new Set(prev);
                                if (isExpanded) next.delete(item.id);
                                else next.add(item.id);
                                return next;
                              });
                            }}
                            className="text-sky-500 text-xs w-4 shrink-0"
                            aria-label={isExpanded ? 'Zuklappen' : 'Aufklappen'}
                          >
                            {isExpanded ? '▼' : '▶'}
                          </button>
                        )}
                        <span>{item.name}</span>
                        {hasVariants && (
                          <span className="text-xs text-slate-400 ml-1">({item.ek_breakdown.length}×EK)</span>
                        )}
                      </div>
                    </td>
                    <td className="px-4 py-3 text-right text-slate-700">{item.current_stock}</td>
                    <td className="px-4 py-3 text-right text-slate-700">{item.sold_qty}</td>
                    <td className="px-4 py-3 text-right font-medium text-slate-800">{formatEur(item.revenue_cents)}</td>
                    <td className="px-4 py-3 text-right text-slate-700">{formatEur(item.cost_cents)}</td>
                  </tr>
                  {isExpanded && hasVariants && item.ek_breakdown.map((entry, idx) => (
                    <tr key={idx} className="bg-sky-50/70 border-t border-sky-100">
                      <td className="pl-10 pr-4 py-2 text-xs text-slate-500" colSpan={2}>
                        EK {formatEur(entry.ek_cents)}: {entry.qty} Stück
                      </td>
                      <td className="px-4 py-2 text-right text-xs text-slate-500">{entry.qty}</td>
                      <td className="px-4 py-2 text-right text-xs text-slate-500">{formatEur(entry.qty * item.current_ek_cents)}</td>
                      <td className="px-4 py-2 text-right text-xs text-slate-500">{formatEur(entry.qty * entry.ek_cents)}</td>
                    </tr>
                  ))}
                </React.Fragment>
              );
            })}
            {/* Summenzeile */}
            <tr className="border-t-2 border-slate-300 bg-slate-50">
              <td className="px-4 py-3 font-bold text-slate-800">Bestandswert-Summe</td>
              <td className="px-4 py-3 text-right font-bold text-slate-700">
                {inventoryData.items.reduce((s, i) => s + i.current_stock, 0)} Stück
              </td>
              <td className="px-4 py-3 text-right font-bold text-slate-700">
                {inventoryData.items.reduce((s, i) => s + i.sold_qty, 0)}
              </td>
              <td className="px-4 py-3 text-right font-bold text-slate-800">
                {formatEur(inventoryData.items.reduce((s, i) => s + i.revenue_cents, 0))}
              </td>
              <td className="px-4 py-3 text-right font-bold text-emerald-700">
                {formatEur(inventoryData.total_stock_value_cents)}
              </td>
            </tr>
          </tbody>
        </table>
        <p className="px-4 py-2 text-xs text-slate-400 border-t border-slate-100">
          * Bestandswert basiert auf aktuellem EK (Annäherung — kein FIFO-Verbrauch)
        </p>
      </div>
    )}
  </div>
)}
```

**Hinweis zu React.Fragment:** Prüfe ob `React` bereits importiert ist. Falls nicht: `import React from 'react';` ergänzen oder `<>...</>` statt `<React.Fragment>` verwenden (mit Key-Prop geht nur React.Fragment).

**Hinweis:** Der bestehende `!navigator.onLine` Early-Return am Anfang der Komponente bleibt unverändert — er gilt für alle Tabs.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - TypeScript kompiliert ohne Fehler: `cd client && npx tsc --noEmit`
    - MonthlyReport.tsx enthält `activeTab` State mit 'monat' | 'jahr' | 'inventur'
    - MonthlyReport.tsx enthält `inventoryData` State vom Typ InventoryResponse
    - MonthlyReport.tsx enthält `expandedProducts` State als Set
    - useEffect für Inventur-Laden ist an `year` und `activeTab` gebunden
    - Inventur-Tab JSX enthält Tabelle mit Summenzeile und expandierbaren EK-Zeilen
    - String "Bestandswert-Summe" ist im JSX vorhanden
    - String "authFetch" für `/api/reports/inventory` ist vorhanden
  </acceptance_criteria>

  <done>
    Inventur-Tab funktioniert in MonthlyReport.tsx: Tab-Navigation vorhanden, Inventur-Tabelle rendert korrekt, EK-Aufschlüsselung per Tap expandierbar, Summenzeile zeigt Bestandswert.
  </done>
</task>

<task type="auto">
  <name>Task 2: Preis-History-Tab in ProductStats.tsx</name>
  <files>client/src/features/admin/products/ProductStats.tsx</files>

  <read_first>
    - client/src/features/admin/products/ProductStats.tsx — vollständig lesen, bestehende State-Struktur und JSX-Struktur verstehen
  </read_first>

  <action>
Bestehende ProductStats.tsx um Preis-History-Tab erweitern. Die Datei vollständig lesen, dann gezielte Ergänzungen vornehmen:

**1. Neues Interface am Dateianfang (nach StatsData-Interface) ergänzen:**
```typescript
interface PriceHistoryEntry {
  id: string;
  productId: string;
  field: string;      // 'purchase_price' oder 'sale_price'
  oldValue: number;   // Cents
  newValue: number;   // Cents
  changedAt: number;  // Timestamp ms
}
```

**2. Neuer State in ProductStats-Funktion (nach `months`-State):**
```typescript
const [statsTab, setStatsTab] = useState<'stats' | 'price-history'>('stats');
const [priceHistory, setPriceHistory] = useState<PriceHistoryEntry[]>([]);
const [loadingHistory, setLoadingHistory] = useState(false);
```

**3. Neuer useEffect für Preis-History-Laden (nach bestehendem useEffect):**
```typescript
useEffect(() => {
  if (statsTab !== 'price-history') return;
  if (!navigator.onLine) {
    setPriceHistory([]);
    return;
  }
  setLoadingHistory(true);
  authFetch(`/api/products/${product.id}/price-history`)
    .then(r => r.ok ? r.json() : Promise.reject(r.status))
    .then((data: PriceHistoryEntry[]) => setPriceHistory(data))
    .catch(() => setPriceHistory([]))
    .finally(() => setLoadingHistory(false));
}, [product.id, statsTab]);
```

**4. Tab-Wechsel-UI:** Im bestehenden JSX nach der Zeitraum-Auswahl (`<div className="flex gap-2">` mit 1/3/6/12 Monat-Buttons) eine Tab-Leiste ergänzen:
```tsx
{/* Tab-Auswahl: Statistiken vs. Preis-History */}
<div className="flex border-b border-sky-100">
  {([
    { key: 'stats', label: 'Statistiken' },
    { key: 'price-history', label: 'Preis-History' },
  ] as const).map(tab => (
    <button
      key={tab.key}
      onPointerDown={() => setStatsTab(tab.key)}
      className={`px-5 py-3 text-sm font-medium border-b-2 transition-colors ${
        statsTab === tab.key
          ? 'border-sky-500 text-sky-700'
          : 'border-transparent text-slate-500 hover:text-slate-700'
      }`}
    >
      {tab.label}
    </button>
  ))}
</div>
```

**5. Bestehenden Stats-Inhalt wrappen:**
Den Block `{!loading && !error && stats && ( ... )}` (Kennzahlen-Grid + Zeitraum-Info) mit `{statsTab === 'stats' && ...}` wrappen — d.h. Stats-Inhalt nur bei aktivem 'stats'-Tab zeigen.

Die `loading`/`error`-States (die für den Stats-Fetch gelten) bleiben so — sie werden nur im 'stats'-Tab angezeigt.

**6. Preis-History-Tab Inhalt (nach dem Stats-Block):**
```tsx
{statsTab === 'price-history' && (
  <div className="flex flex-col gap-3">
    {loadingHistory && (
      <div className="text-center py-8 text-slate-500 text-sm">Laden...</div>
    )}
    {!loadingHistory && !navigator.onLine && (
      <div className="bg-amber-50 text-amber-700 px-4 py-3 rounded-xl text-sm">
        Preis-History ist nur mit Internetverbindung verfügbar.
      </div>
    )}
    {!loadingHistory && navigator.onLine && priceHistory.length === 0 && (
      <div className="bg-white rounded-xl shadow-sm p-6 text-center text-slate-500 text-sm">
        Keine Preisänderungen vorhanden.
      </div>
    )}
    {!loadingHistory && priceHistory.length > 0 && (
      <div className="bg-white rounded-xl shadow-sm overflow-hidden">
        <table className="w-full text-sm">
          <thead>
            <tr className="bg-sky-50">
              <th className="text-left px-4 py-3 text-sky-700 font-medium">Datum</th>
              <th className="text-left px-4 py-3 text-sky-700 font-medium">Feld</th>
              <th className="text-right px-4 py-3 text-sky-700 font-medium">Alter Preis</th>
              <th className="text-right px-4 py-3 text-sky-700 font-medium">Neuer Preis</th>
            </tr>
          </thead>
          <tbody>
            {priceHistory.map(entry => (
              <tr key={entry.id} className="border-t border-slate-100 hover:bg-sky-50/50">
                <td className="px-4 py-3 text-slate-700">
                  {format(new Date(entry.changedAt), 'd. MMM yyyy', { locale: de })}
                </td>
                <td className="px-4 py-3 text-slate-700">
                  {entry.field === 'purchase_price' ? 'EK' : 'VK'}
                </td>
                <td className="px-4 py-3 text-right text-slate-500">
                  {formatEur(entry.oldValue)}
                </td>
                <td className="px-4 py-3 text-right font-medium text-slate-800">
                  {formatEur(entry.newValue)}
                  {entry.newValue > entry.oldValue
                    ? <span className="text-rose-500 ml-1 text-xs">↑</span>
                    : entry.newValue < entry.oldValue
                    ? <span className="text-emerald-500 ml-1 text-xs">↓</span>
                    : null}
                </td>
              </tr>
            ))}
          </tbody>
        </table>
      </div>
    )}
  </div>
)}
```

**Hinweis:** `format` und `de` sind bereits importiert. `formatEur` ist bereits importiert. Keine neuen Imports nötig.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <acceptance_criteria>
    - TypeScript kompiliert ohne Fehler: `cd client && npx tsc --noEmit`
    - ProductStats.tsx enthält `statsTab` State mit 'stats' | 'price-history'
    - ProductStats.tsx enthält `priceHistory` State als PriceHistoryEntry[]
    - useEffect für Preis-History-Laden ist an `product.id` und `statsTab` gebunden
    - Preis-History-Tab JSX enthält Tabelle mit Spalten Datum, Feld, Alter Preis, Neuer Preis
    - Feld-Übersetzung: 'purchase_price' → 'EK', sonst → 'VK'
    - Aufwärts-Trend: ↑ in rose-500, Abwärts-Trend: ↓ in emerald-500
    - String "authFetch" für `/api/products/${product.id}/price-history` ist vorhanden
  </acceptance_criteria>

  <done>
    Preis-History-Tab funktioniert in ProductStats.tsx: Tab-Wechsel vorhanden, Tabelle zeigt Preisänderungen neueste zuerst, Richtungsindikator (↑↓) vorhanden, leerer Zustand korrekt behandelt.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Visuelle Verifikation beider neuer Tabs</name>
  <what-built>
    Inventur-Tab im Jahresbericht (MonthlyReport) und Preis-History-Tab in der Produktstatistik (ProductStats).
  </what-built>
  <how-to-verify>
    1. App starten (Frontend + Backend)
    2. Als Shop einloggen mit vorhandenem PIN
    3. Admin → Berichte öffnen → Tab "Inventur" antippen
       - Erwartung: Tabelle erscheint mit Artikel, Bestand, Verkauft, VK-Umsatz, EK-Kosten
       - Erwartung: Fette Summenzeile "Bestandswert-Summe" am Ende
       - Falls Artikel mit mehreren EK-Preisen: ▶ Button erscheint → antippen → EK-Aufschlüsselung sichtbar
    4. Admin → Produkte → Produkt antippen → Statistiken öffnen → Tab "Preis-History" antippen
       - Erwartung: Tabelle mit Datum, Feld (EK/VK), Alter Preis, Neuer Preis
       - Falls keine Preisänderungen: "Keine Preisänderungen vorhanden" erscheint
       - Falls Preiserhöhung: ↑ in Rot sichtbar; falls Senkung: ↓ in Grün
  </how-to-verify>
  <action>Manuell: App öffnen, Inventur-Tab und Preis-History-Tab gemäß how-to-verify testen.</action>
  <verify>
    <automated>HUMAN — visuelle Verifikation erforderlich</automated>
  </verify>
  <done>Beide Tabs zeigen korrekte Daten, keine UI-Fehler sichtbar.</done>
  <resume-signal>Tippe "approved" wenn beide Tabs korrekt angezeigt werden, oder beschreibe aufgetretene Fehler</resume-signal>
</task>

</tasks>

<verification>
TypeScript-Build des Clients muss sauber sein:
```bash
cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit
```

Visuelle Prüfung über Checkpoint-Task (letzter Task in diesem Plan).
</verification>

<success_criteria>
- MonthlyReport.tsx hat funktionierenden Inventur-Tab mit expandierbaren EK-Zeilen und Bestandswert-Summe
- ProductStats.tsx hat funktionierenden Preis-History-Tab mit korrekter Datumsformatierung und Richtungsindikator
- Beide Tabs zeigen korrekten Offline-Hinweis wenn kein Internet
- TypeScript-Build sauber
- Menschliche Verifikation über Checkpoint abgenommen
</success_criteria>

<output>
Nach Abschluss `.planning/phases/28-inventur-uebersicht-preis-auswertung/28-02-SUMMARY.md` erstellen.
</output>
