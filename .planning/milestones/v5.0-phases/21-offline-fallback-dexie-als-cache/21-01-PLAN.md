---
phase: 21-offline-fallback-dexie-als-cache
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/hooks/api/useProducts.ts
  - client/src/hooks/api/useCategories.ts
autonomous: true
requirements:
  - OFFL-01
must_haves:
  truths:
    - "POS lädt Produkte nach App-Neustart im Offline-Modus aus Dexie-Cache"
    - "Leerer TQ-Cache + Netzwerkfehler führt zu Dexie-Fallback, nicht zu Fehler-State"
    - "Write-Through befüllt Dexie bei jedem erfolgreichen Online-Fetch"
  artifacts:
    - path: "client/src/features/pos/ArticleGrid.tsx"
      provides: "queryFn mit try/catch Dexie-Fallback"
      contains: "db.products.where"
    - path: "client/src/hooks/api/useProducts.ts"
      provides: "useProducts queryFn mit Dexie-Fallback"
      contains: "db.products.where"
    - path: "client/src/hooks/api/useCategories.ts"
      provides: "useCategories queryFn mit Dexie-Fallback"
      contains: "db.categories.where"
  key_links:
    - from: "ArticleGrid.tsx queryFn"
      to: "db.products.where('shopId').equals(shopId).filter(p => p.active)"
      via: "catch block bei Netzwerkfehler"
      pattern: "db\\.products\\.where"
    - from: "useProducts queryFn"
      to: "db.products.where('shopId').equals(shopId).toArray()"
      via: "catch block"
      pattern: "db\\.products\\.where"
---

<objective>
Dexie als Cold-Start-Fallback für den POS sicherstellen. Nach einem App-Neustart im Offline-Modus ist der TQ-Cache leer — dann müssen ArticleGrid, useProducts und useCategories statt eines Fehlers die lokal gecachten Daten aus IndexedDB zurückgeben.

Purpose: OFFL-01 — Kasse funktioniert nach App-Neustart offline ohne Fehlermeldung.
Output: Drei queryFns mit try/catch Dexie-Fallback.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Relevante Typen und Exports. Kein Codebase-Erkunden nötig. -->

Aus client/src/db/index.ts:
```typescript
export type { Product, Sale, SaleItem, CartItem, PersistedCartItem, OutboxEntry, Category } from './schema.js';
export const db = new FairstandDB();
export function getShopId(): string; // wirft wenn nicht gesetzt
```

Aus client/src/hooks/api/useProducts.ts — aktuelle queryFn (kein Fallback):
```typescript
queryFn: async () => {
  const headers = await getAuthHeaders();
  const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
  if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
  const data = await res.json() as Record<string, unknown>[];
  const products = data.map(mapServerProduct);
  db.transaction('rw', db.products, async () => { /* write-through */ }).catch(() => {});
  return products;
},
```

Aus client/src/features/pos/ArticleGrid.tsx — aktuelle queryFn (kein Fallback):
```typescript
queryFn: async () => {
  const headers = await getAuthHeaders();
  const shopId = getShopId();
  const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
  if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
  const data = await res.json() as Record<string, unknown>[];
  return data.map(/* mapping */);
},
networkMode: 'offlineFirst',
staleTime: 60_000,
```

Aus client/src/hooks/api/useCategories.ts — aktuelle queryFn (kein Fallback):
```typescript
queryFn: async () => {
  const headers = await getAuthHeaders();
  const res = await fetch(`/api/categories?shopId=${shopId}`, { headers });
  if (!res.ok) throw new Error('Kategorien konnten nicht geladen werden');
  // ... mapping + write-through
},
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Dexie-Fallback in useProducts und useCategories</name>
  <files>client/src/hooks/api/useProducts.ts, client/src/hooks/api/useCategories.ts</files>
  <action>
In beiden Hooks die queryFn mit einem try/catch-Wrapper versehen, der bei Netzwerkfehler auf Dexie zurückfällt.

**useProducts.ts** — queryFn-Pattern:
```typescript
queryFn: async () => {
  try {
    const headers = await getAuthHeaders();
    const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
    if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
    const data = await res.json() as Record<string, unknown>[];
    const products = data.map(mapServerProduct);
    // Write-Through bleibt unverändert
    db.transaction('rw', db.products, async () => {
      await db.products.where('shopId').equals(shopId).delete();
      await db.products.bulkPut(products);
    }).catch(() => {});
    return products;
  } catch {
    // Offline oder Netzwerkfehler — Dexie-Cache als Fallback
    const cached = await db.products.where('shopId').equals(shopId).toArray();
    if (cached.length > 0) return cached;
    throw new Error('Keine Produkte verfügbar (offline, kein lokaler Cache)');
  }
},
```

**useCategories.ts** — queryFn-Pattern (analog):
```typescript
queryFn: async () => {
  try {
    const headers = await getAuthHeaders();
    const res = await fetch(`/api/categories?shopId=${shopId}`, { headers });
    if (!res.ok) throw new Error('Kategorien konnten nicht geladen werden');
    const data = await res.json() as Record<string, unknown>[];
    const categories = data.map(/* existing mapping */);
    db.transaction('rw', db.categories, async () => {
      await db.categories.where('shopId').equals(shopId).delete();
      await db.categories.bulkPut(categories);
    }).catch(() => {});
    return categories;
  } catch {
    const cached = await db.categories.where('shopId').equals(shopId).toArray();
    if (cached.length > 0) return cached;
    throw new Error('Keine Kategorien verfügbar (offline, kein lokaler Cache)');
  }
},
```

Hinweis: Die bestehende Mapping-Logik und Write-Through-Logik bleibt unverändert — sie wird nur in den try-Block eingeschlossen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npm run build --prefix client 2>&1 | tail -5</automated>
  </verify>
  <done>Build erfolgreich. Beide queryFns haben try/catch mit db.products.where / db.categories.where im catch-Block.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dexie-Fallback in ArticleGrid (POS-spezifisch)</name>
  <files>client/src/features/pos/ArticleGrid.tsx</files>
  <action>
Die inline queryFn in ArticleGrid.tsx um einen try/catch-Block erweitern. ArticleGrid ist POS-spezifisch — der Fallback filtert wie der Online-Pfad nur aktive Produkte.

```typescript
queryFn: async () => {
  try {
    const headers = await getAuthHeaders();
    const shopId = getShopId();
    const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
    if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
    const data = await res.json() as Record<string, unknown>[];
    return data.map(p => ({
      // ... bestehende Mapping-Felder unverändert
    })).filter(p => p.active);
  } catch {
    // Cold-Start offline: Dexie-Cache laden
    const shopId = getShopId();
    const cached = await db.products
      .where('shopId').equals(shopId)
      .filter(p => p.active)
      .toArray();
    if (cached.length > 0) return cached;
    throw new Error('Keine Produkte verfügbar (offline, kein lokaler Cache)');
  }
},
```

Import hinzufügen (oben in der Datei, falls noch nicht vorhanden):
```typescript
import { db, getShopId } from '../../db/index.js';
```

Prüfen ob `db` bereits importiert ist — falls ja, nicht doppelt importieren.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npm run build --prefix client 2>&1 | tail -5</automated>
  </verify>
  <done>Build erfolgreich. ArticleGrid hat db-Import und try/catch queryFn. grep bestätigt: `grep "db.products.where" client/src/features/pos/ArticleGrid.tsx` gibt einen Treffer.</done>
</task>

</tasks>

<verification>
```bash
grep -n "db\.products\.where" /Users/simonluthe/Documents/fairstand/client/src/features/pos/ArticleGrid.tsx
grep -n "db\.products\.where" /Users/simonluthe/Documents/fairstand/client/src/hooks/api/useProducts.ts
grep -n "db\.categories\.where" /Users/simonluthe/Documents/fairstand/client/src/hooks/api/useCategories.ts
```
Alle drei müssen mindestens einen Treffer im catch-Block liefern.
</verification>

<success_criteria>
- ArticleGrid, useProducts, useCategories haben alle einen Dexie-Fallback bei Netzwerkfehler
- TypeScript-Build ohne Fehler
- Kein isError-State im POS wenn offline und Dexie-Cache gefüllt
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/21-offline-fallback-dexie-als-cache/21-01-SUMMARY.md` erstellen.
</output>
