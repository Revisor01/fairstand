---
phase: 19-tanstack-query-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 19-01
files_modified:
  - client/src/features/admin/products/ProductList.tsx
  - client/src/features/admin/products/ProductForm.tsx
  - client/src/features/admin/products/StockAdjustModal.tsx
  - client/src/features/admin/settings/CategoryManager.tsx
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02

must_haves:
  truths:
    - "ProductList zeigt Produkte die per TQ vom Server kommen — kein useLiveQuery mehr"
    - "ProductForm speichert via useCreateProduct/useUpdateProduct Mutation, nicht fire-and-forget"
    - "Nach dem Speichern ist die Produktliste sofort aktualisiert (Query Invalidation)"
    - "StockAdjustModal nutzt useAdjustStock Mutation statt Dexie-Transaktion + Outbox"
    - "CategoryManager zeigt Kategorien via TQ, alle CRUD-Aktionen gehen über TQ Mutations"
    - "Kein useLiveQuery Import mehr in den vier migrierten Dateien"
  artifacts:
    - path: "client/src/features/admin/products/ProductList.tsx"
      provides: "Produktliste via TQ"
      contains: "useProducts"
    - path: "client/src/features/admin/products/ProductForm.tsx"
      provides: "Produktformular mit TQ Mutations"
      contains: "useCreateProduct"
    - path: "client/src/features/admin/products/StockAdjustModal.tsx"
      provides: "Bestandskorrektur via TQ"
      contains: "useAdjustStock"
    - path: "client/src/features/admin/settings/CategoryManager.tsx"
      provides: "Kategorieverwaltung via TQ"
      contains: "useCategories"
  key_links:
    - from: "client/src/features/admin/products/ProductList.tsx"
      to: "client/src/hooks/api/useProducts.ts"
      via: "useProducts() Hook Call"
      pattern: "useProducts\\(\\)"
    - from: "client/src/features/admin/products/ProductForm.tsx"
      to: "client/src/hooks/api/useProducts.ts"
      via: "useCreateProduct() / useUpdateProduct() Mutation"
      pattern: "useCreateProduct|useUpdateProduct"
    - from: "client/src/features/admin/products/StockAdjustModal.tsx"
      to: "client/src/hooks/api/useProducts.ts"
      via: "useAdjustStock() Mutation"
      pattern: "useAdjustStock"
    - from: "client/src/features/admin/settings/CategoryManager.tsx"
      to: "client/src/hooks/api/useCategories.ts"
      via: "useCategories() + Mutation Hooks"
      pattern: "useCategories\\(\\)"
---

<objective>
Die vier Admin-Komponenten von useLiveQuery/Dexie auf TanStack Query umstellen. Reads kommen jetzt direkt vom Server, Writes invalidieren den Query-Cache — Änderungen sind sofort sichtbar ohne manuellen Sync-Aufruf.

Purpose: LIVE-01 (Auto-Laden) und LIVE-02 (sofortige Aktualisierung nach Speichern) werden durch diese Migration erfüllt.
Output: ProductList, ProductForm, StockAdjustModal und CategoryManager ohne useLiveQuery, mit TQ Hooks aus Plan 01.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/19-tanstack-query-foundation/19-CONTEXT.md
@.planning/phases/19-tanstack-query-foundation/19-01-SUMMARY.md
</context>

<interfaces>
<!-- Hook-Contracts aus Plan 01 — Executor liest diese, nicht die Quelldateien -->

Aus client/src/hooks/api/useProducts.ts (erstellt in Plan 01):
```typescript
// Query
export function useProducts(): UseQueryResult<Product[]>
// data ist Product[] | undefined, isLoading: boolean, error: Error | null

// Mutations — alle geben UseMutationResult zurück
export function useCreateProduct(): UseMutationResult<Product, Error, Product>
// mutateAsync(productData: Product) → invalidiert ['products', shopId]

export function useUpdateProduct(): UseMutationResult<Product, Error, Product>
// mutateAsync(productData: Product) → invalidiert ['products', shopId]

export function useToggleProductActive(): UseMutationResult<..., Error, { productId: string; active: boolean }>
// mutateAsync({ productId, active }) → invalidiert ['products', shopId]

export function useAdjustStock(): UseMutationResult<void, Error, { productId: string; delta: number; reason?: string }>
// mutateAsync({ productId, delta, reason? }) → invalidiert ['products', shopId]
```

Aus client/src/hooks/api/useCategories.ts (erstellt in Plan 01):
```typescript
export function useCategories(): UseQueryResult<Category[]>
// data ist Category[] | undefined

export function useCreateCategory(): UseMutationResult<Category, Error, string>
// mutateAsync(name: string)

export function useRenameCategory(): UseMutationResult<void, Error, { id: string; name: string }>
// mutateAsync({ id, name }) → invalidiert ['categories', shopId] + ['products', shopId]

export function useDeleteCategory(): UseMutationResult<void, Error, string>
// mutateAsync(id: string) → wirft Error wenn 409 (Kategorie in Verwendung)
```

Bestehende Typen (unverändert):
```typescript
// client/src/db/index.ts
export type { Product, Category }
export function getShopId(): string

// client/src/features/pos/utils.ts
export function formatEur(cents: number): string
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ProductList.tsx auf useProducts() umstellen + handleToggleActive via Mutation</name>
  <files>client/src/features/admin/products/ProductList.tsx</files>
  <read_first>
    - client/src/features/admin/products/ProductList.tsx — vollständige aktuelle Implementierung lesen
    - client/src/hooks/api/useProducts.ts — Hook-Signaturen aus Plan 01
  </read_first>
  <action>
    ProductList.tsx migrieren:

    1. `useLiveQuery` Import entfernen, `dexie-react-hooks` Import entfernen
    2. `useProducts`, `useToggleProductActive` aus `../../hooks/api/useProducts.js` importieren
    3. `db` und `getShopId` Imports entfernen (werden nicht mehr direkt gebraucht)
    4. `downloadProducts` Import aus `../../../sync/engine.js` kann für den Sync-Button bleiben (wird in Phase 20 entfernt)

    Migration der Query:
    ```tsx
    // ALT:
    const products = useLiveQuery(
      () => db.products.where('shopId').equals(getShopId()).toArray()
        .then(arr => arr.sort((a, b) => a.name.localeCompare(b.name, 'de'))),
      []
    );

    // NEU:
    const { data: rawProducts, isLoading } = useProducts();
    const products = useMemo(
      () => rawProducts ? [...rawProducts].sort((a, b) => a.name.localeCompare(b.name, 'de')) : undefined,
      [rawProducts]
    );
    ```

    Migration handleToggleActive:
    ```tsx
    const toggleActive = useToggleProductActive();

    async function handleToggleActive(product: Product) {
      await toggleActive.mutateAsync({ productId: product.id, active: !product.active });
      // Kein db.products.update() mehr — TQ Invalidation holt frischen Stand
    }
    ```

    Lade-Zustand anpassen:
    ```tsx
    // ALT: if (!products)
    // NEU: if (isLoading)
    // Beides führt zum gleichen Render-Zweig
    ```

    networkMode für Admin: TQ nutzt Standard (online) — Produkte im Admin kommen nur bei Netzwerkverbindung. Das ist korrekt für LIVE-01 (networkMode: 'online' ist der TQ-Default wenn keine explizite Einstellung gesetzt).

    `getAuthHeaders` Import entfernen — die Mutation kümmert sich selbst um Auth-Headers.

    Sync-Button (handleDownloadSync) bleibt unverändert — wird in Phase 20 entfernt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | grep -E "error|Error|warning" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "useLiveQuery" client/src/features/admin/products/ProductList.tsx gibt KEINE Zeile zurück
    - grep -n "useProducts" client/src/features/admin/products/ProductList.tsx gibt mindestens eine Zeile zurück
    - grep -n "useToggleProductActive" client/src/features/admin/products/ProductList.tsx gibt eine Zeile zurück
    - Build läuft ohne Fehler
  </acceptance_criteria>
  <done>ProductList liest von Server via TQ, Toggle-Mutation invalidiert Cache</done>
</task>

<task type="auto">
  <name>Task 2: ProductForm, StockAdjustModal und CategoryManager auf TQ Mutations umstellen</name>
  <files>
    client/src/features/admin/products/ProductForm.tsx,
    client/src/features/admin/products/StockAdjustModal.tsx,
    client/src/features/admin/settings/CategoryManager.tsx
  </files>
  <read_first>
    - client/src/features/admin/products/ProductForm.tsx — vollständige aktuelle Implementierung
    - client/src/features/admin/products/StockAdjustModal.tsx — vollständige aktuelle Implementierung
    - client/src/features/admin/settings/CategoryManager.tsx — vollständige aktuelle Implementierung
    - client/src/hooks/api/useProducts.ts — Mutation-Signaturen
    - client/src/hooks/api/useCategories.ts — Mutation-Signaturen
  </read_first>
  <action>
    **ProductForm.tsx Migration:**

    1. `useLiveQuery` entfernen. Kategorien via `useCategories()` statt Dexie:
    ```tsx
    import { useCategories } from '../../../hooks/api/useCategories.js';
    import { useCreateProduct, useUpdateProduct } from '../../../hooks/api/useProducts.js';

    const { data: dbCategories } = useCategories();
    const createProduct = useCreateProduct();
    const updateProduct = useUpdateProduct();
    ```

    2. `downloadCategories` Import entfernen (wird nicht mehr gebraucht — TQ lädt automatisch)
    3. `db` Import entfernen — Dexie-Writes werden durch Server-Mutations ersetzt
    4. `getShopId` Import entfernen — shopId kommt über getShopId() in den Hooks intern

    Die useLiveQuery-Blöcke in ProductForm (Zeilen 58-70) entfernen:
    - `const dbCategories = useLiveQuery(...)` → durch `useCategories()` ersetzen
    - `useLiveQuery(async () => { const count = ... downloadCategories() })` → komplett entfernen, TQ übernimmt das Laden

    handleSave Migration — Edit-Fall:
    ```tsx
    // ALT: db.products.update() + fire-and-forget fetch
    // NEU:
    if (isEdit && product) {
      const updateData = { ...product, articleNumber: values.articleNumber.trim(), name: values.name.trim(),
        category: values.category.trim(), purchasePrice, salePrice, vatRate, minStock, updatedAt: Date.now() };
      await updateProduct.mutateAsync(updateData);
      // Image-Upload Logik bleibt (fetch direkt, kein TQ nötig für File-Uploads)
      if (pendingImageFile) {
        const authHeaders = await getAuthHeaders();
        const formData = new FormData();
        formData.append('image', pendingImageFile);
        await fetch(`/api/products/${product.id}/image`, {
          method: 'POST',
          headers: { 'Authorization': authHeaders['Authorization'] ?? '' },
          body: formData,
        });
        // Nach Image-Upload: Produkte neu laden
        // queryClient.invalidateQueries({ queryKey: ['products', getShopId()] }) wird durch
        // updateProduct.mutateAsync onSuccess bereits getriggert, aber für imageUrl brauchen
        // wir eine weitere Invalidation — daher useQueryClient() importieren und nochmal invalidieren
      }
    }
    ```

    handleSave Migration — Create-Fall:
    ```tsx
    // ALT: db.products.add() + fire-and-forget fetch
    // NEU:
    const stock = Math.max(0, parseInt(values.stock, 10) || 0);
    const productData = {
      id: crypto.randomUUID(),
      shopId: getShopId(),
      articleNumber: values.articleNumber.trim(),
      name: values.name.trim(),
      category: values.category.trim(),
      purchasePrice, salePrice, vatRate, stock, minStock, active: true, updatedAt: Date.now(),
    };
    await createProduct.mutateAsync(productData);
    // Image-Upload falls vorhanden (direkt via fetch, wie bisher — nur ohne db.products.update)
    ```

    WICHTIG: `getShopId()` aus `../../../db/index.js` Import bleibt für den Create-Fall (shopId im productData-Objekt).
    `getAuthHeaders()` aus `../../auth/serverAuth.js` bleibt für Image-Upload.
    `useQueryClient()` importieren und nach Image-Upload `invalidateQueries(['products', getShopId()])` aufrufen.

    `saving` State bleibt — durch `createProduct.isPending || updateProduct.isPending` ersetzen oder behalten (beides funktioniert).

    ---

    **StockAdjustModal.tsx Migration:**

    1. `db` und `getShopId` Imports entfernen
    2. `useAdjustStock` Hook importieren:
    ```tsx
    import { useAdjustStock } from '../../../hooks/api/useProducts.js';
    const adjustStock = useAdjustStock();
    ```

    handleSave Migration:
    ```tsx
    // ALT: db.transaction() mit db.products.modify() + db.outbox.add()
    // NEU:
    async function handleSave() {
      if (!isValidDelta) {
        setError('Bitte eine Anzahl eingeben (positiv = Zugang, negativ = Abgang).');
        return;
      }
      setSaving(true);
      try {
        await adjustStock.mutateAsync({
          productId: product.id,
          delta: parsedDelta,
          reason: reason.trim() || undefined,
        });
        onClose();
      } catch (err) {
        setError(err instanceof Error ? err.message : 'Fehler beim Speichern.');
      } finally {
        setSaving(false);
      }
    }
    ```

    ACHTUNG: Das Produkt-Objekt das als Prop übergeben wird (`product.stock`) zeigt den alten Stand — die Vorschau "Neuer Bestand: X" bleibt rechnerisch korrekt da sie lokal `product.stock + parsedDelta` berechnet. Die echte Änderung kommt erst nach Query-Invalidation aus dem Server.

    ---

    **CategoryManager.tsx Migration:**

    1. `useLiveQuery` entfernen, `dexie-react-hooks` Import entfernen
    2. `db` und `getShopId` Imports entfernen
    3. `downloadCategories` und `downloadProducts` Imports entfernen
    4. TQ Hooks importieren:
    ```tsx
    import { useCategories, useCreateCategory, useRenameCategory, useDeleteCategory } from '../../../hooks/api/useCategories.js';
    ```

    ```tsx
    const { data: categories, isLoading } = useCategories();
    const createCategory = useCreateCategory();
    const renameCategory = useRenameCategory();
    const deleteCategory = useDeleteCategory();
    ```

    handleAdd:
    ```tsx
    async function handleAdd() {
      const name = newCatName.trim();
      if (!name) return;
      try {
        await createCategory.mutateAsync(name);
        setNewCatName('');
      } catch (err) {
        alert(err instanceof Error ? err.message : 'Fehler beim Anlegen');
      }
    }
    ```

    handleRename:
    ```tsx
    async function handleRename(id: string, oldName: string) {
      const newName = window.prompt('Neuer Name:', oldName);
      if (!newName || !newName.trim() || newName.trim() === oldName) return;
      try {
        await renameCategory.mutateAsync({ id, name: newName.trim() });
        // downloadCategories() + downloadProducts() entfällt — TQ invalidiert beide Queries
      } catch (err) {
        alert(err instanceof Error ? err.message : 'Fehler beim Umbenennen');
      }
    }
    ```

    handleDelete:
    ```tsx
    async function handleDelete(id: string) {
      try {
        await deleteCategory.mutateAsync(id);
      } catch (err) {
        alert(err instanceof Error ? err.message : 'Fehler beim Löschen');
      }
    }
    ```

    Ladestate: `if (isLoading)` statt `if (!categories)` — oder beides beibehalten, da `!categories` bei undefined (während Ladevorgang) denselben Effekt hat.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -rn "useLiveQuery" client/src/features/admin/ gibt KEINE Zeile zurück
    - grep -n "useCreateProduct\|useUpdateProduct" client/src/features/admin/products/ProductForm.tsx gibt Zeilen zurück
    - grep -n "useAdjustStock" client/src/features/admin/products/StockAdjustModal.tsx gibt eine Zeile zurück
    - grep -n "useCategories\|useCreateCategory\|useRenameCategory\|useDeleteCategory" client/src/features/admin/settings/CategoryManager.tsx gibt Zeilen zurück
    - grep -n "db\." client/src/features/admin/products/StockAdjustModal.tsx gibt KEINE Zeile zurück
    - npm run build läuft ohne TypeScript-Fehler
  </acceptance_criteria>
  <done>Alle vier Admin-Komponenten lesen via TQ vom Server und schreiben via Mutations — useLiveQuery ist aus dem Admin-Kontext eliminiert</done>
</task>

</tasks>

<verification>
Gesamtprüfung nach Plan 02:
- `grep -rn "useLiveQuery" client/src/features/admin/` gibt keine Treffer
- `grep -rn "useProducts\|useCategories" client/src/features/admin/` zeigt Nutzung in allen 4 Dateien
- `npm run build` in client/ läuft ohne Fehler
- Manueller Check: App im Dev-Mode starten, Admin öffnen → Produkte erscheinen ohne manuellen Sync-Klick
</verification>

<success_criteria>
- ProductList, ProductForm, StockAdjustModal, CategoryManager: kein useLiveQuery mehr
- Admin-Komponenten zeigen Daten direkt vom Server (LIVE-01)
- Nach Speichern/Ändern ist die Liste sofort aktualisiert ohne Reload (LIVE-02)
- Build bleibt grün
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/19-tanstack-query-foundation/19-02-SUMMARY.md` erstellen.
</output>
