---
phase: 19-tanstack-query-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/package.json
  - client/src/App.tsx
  - client/src/hooks/api/useProducts.ts
  - client/src/hooks/api/useCategories.ts
autonomous: true
requirements:
  - LIVE-01
  - LIVE-02
  - LIVE-06

must_haves:
  truths:
    - "TanStack Query ist installiert und der QueryClientProvider wrapping die gesamte App"
    - "useProducts() gibt Product[] aus dem Server zurück (Query Key ['products', shopId])"
    - "useCategories() gibt Category[] aus dem Server zurück (Query Key ['categories', shopId])"
    - "useProducts() und useCategories() exportieren je eine useMutation für Writes"
    - "getAuthHeaders() wird in allen fetch-Calls der Hooks genutzt"
  artifacts:
    - path: "client/src/hooks/api/useProducts.ts"
      provides: "useProducts Query Hook + Mutation Hooks"
      exports:
        - "useProducts"
        - "useCreateProduct"
        - "useUpdateProduct"
        - "useToggleProductActive"
        - "useAdjustStock"
    - path: "client/src/hooks/api/useCategories.ts"
      provides: "useCategories Query Hook + Mutation Hooks"
      exports:
        - "useCategories"
        - "useCreateCategory"
        - "useRenameCategory"
        - "useDeleteCategory"
    - path: "client/src/App.tsx"
      provides: "QueryClientProvider Wrapper"
      contains: "QueryClientProvider"
  key_links:
    - from: "client/src/App.tsx"
      to: "@tanstack/react-query QueryClient"
      via: "QueryClientProvider client={queryClient}"
      pattern: "QueryClientProvider"
    - from: "client/src/hooks/api/useProducts.ts"
      to: "/api/products"
      via: "fetch in queryFn"
      pattern: "fetch.*api/products"
---

<objective>
TanStack Query installieren und die beiden zentralen API-Hooks erstellen, die alle Admin- und POS-Komponenten in Phase 19 nutzen werden.

Purpose: Ohne diese Basis können Plan 02 und Plan 03 nicht auf TQ umstellen — die Hook-Contracts müssen zuerst definiert sein, damit die Executors der Folge-Pläne keine Codebase-Erkundung brauchen.
Output: Installiertes @tanstack/react-query, QueryClientProvider in App.tsx, client/src/hooks/api/useProducts.ts, client/src/hooks/api/useCategories.ts mit vollständigen Query + Mutation Hooks.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/19-tanstack-query-foundation/19-CONTEXT.md
</context>

<interfaces>
<!-- Bestehende Typen die die Hooks verwenden -->

Aus client/src/db/index.ts:
```typescript
export const db = new FairstandDB();
export function getShopId(): string  // wirft wenn kein Shop gesetzt
export type { Product, Category }
```

Aus client/src/features/auth/serverAuth.ts:
```typescript
export async function getAuthHeaders(): Promise<Record<string, string>>
// Gibt zurück: { 'Content-Type': 'application/json', 'Authorization': 'Bearer <token>' }
// Wenn kein Token: nur Content-Type Header
```

Server API (bereits vorhanden):
```
GET    /api/products?shopId={shopId}   → Product[]  (snake_case keys von Server)
POST   /api/products                   → 201 (upsert, Body: Product-Objekt)
PATCH  /api/products/:id/activate      → 200
PATCH  /api/products/:id/deactivate    → 200
POST   /api/products/:id/image         → { imageUrl: string }
GET    /api/categories?shopId={shopId} → Category[]
POST   /api/categories                 → { id, name, shopId }
PATCH  /api/categories/:id             → 200
DELETE /api/categories/:id             → 200 | 409 { error: string }
POST   /api/sync  (STOCK_ADJUST in outbox)
```

Aus client/src/db/schema.ts (Client-Typen):
```typescript
export interface Product {
  id: string;
  shopId: string;
  articleNumber: string;
  name: string;
  category: string;
  purchasePrice: number; // Cent-Integer
  salePrice: number;     // Cent-Integer
  vatRate: number;
  stock: number;
  minStock: number;
  active: boolean;
  updatedAt: number;     // Unix-Timestamp ms
  imageUrl?: string;
}

export interface Category {
  id: string;
  shopId: string;
  name: string;
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: @tanstack/react-query installieren und QueryClientProvider in App.tsx einbinden</name>
  <files>client/package.json, client/src/App.tsx</files>
  <read_first>
    - client/package.json — bestehende Dependencies prüfen
    - client/src/App.tsx — bestehende Struktur lesen (UnlockedApp, QueryClientProvider Position)
  </read_first>
  <action>
    1. @tanstack/react-query v5 + @tanstack/react-query-devtools in client installieren:
       ```
       cd client && npm install @tanstack/react-query@^5 @tanstack/react-query-devtools@^5
       ```

    2. In client/src/App.tsx QueryClient erstellen und QueryClientProvider als äußersten Wrapper einfügen.
       QueryClient AUSSERHALB der Komponente auf Modul-Ebene erstellen (nicht in der Render-Funktion).
       staleTime: 30_000 (30s), gcTime: 5 * 60 * 1000 (5min), retry: 1.
       DevTools nur im dev-Build einbinden (`import.meta.env.DEV`).

       Endresultat App.tsx Struktur:
       ```tsx
       import { QueryClient, QueryClientProvider } from '@tanstack/react-query';
       import { ReactQueryDevtools } from '@tanstack/react-query-devtools';

       const queryClient = new QueryClient({
         defaultOptions: {
           queries: {
             staleTime: 30_000,
             gcTime: 5 * 60 * 1000,
             retry: 1,
           },
         },
       });

       export default function App() {
         return (
           <QueryClientProvider client={queryClient}>
             {/* bestehende App-Logik unverändert */}
             {import.meta.env.DEV && <ReactQueryDevtools initialIsOpen={false} />}
           </QueryClientProvider>
         );
       }
       ```

       WICHTIG: Die `UnlockedApp`-Komponente und gesamte Auth-Logik (useAuth, state-machine) bleibt exakt unverändert. QueryClientProvider ist nur äußerer Wrapper.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "QueryClientProvider" client/src/App.tsx gibt eine Zeile mit dem Provider zurück
    - grep "tanstack/react-query" client/package.json gibt eine Zeile zurück
    - npm run build läuft ohne TypeScript-Fehler durch
  </acceptance_criteria>
  <done>QueryClientProvider wrapping die App, Build grün</done>
</task>

<task type="auto">
  <name>Task 2: useProducts.ts und useCategories.ts Hook-Infrastruktur erstellen</name>
  <files>client/src/hooks/api/useProducts.ts, client/src/hooks/api/useCategories.ts</files>
  <read_first>
    - client/src/features/auth/serverAuth.ts — getAuthHeaders() Signatur
    - client/src/db/index.ts — getShopId() und Product/Category Typen
    - client/src/features/admin/products/ProductList.tsx — wie Products heute konsumiert werden
    - client/src/features/admin/settings/CategoryManager.tsx — wie Categories heute konsumiert werden
    - client/src/features/admin/products/StockAdjustModal.tsx — STOCK_ADJUST Payload-Struktur
  </read_first>
  <action>
    Verzeichnis client/src/hooks/api/ anlegen und zwei Hook-Dateien erstellen.

    **client/src/hooks/api/useProducts.ts:**
    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
    import { getShopId } from '../../db/index.js';
    import { getAuthHeaders } from '../../features/auth/serverAuth.js';
    import type { Product } from '../../db/index.js';

    // Server liefert snake_case — camelCase-Mapping nötig
    function mapServerProduct(p: Record<string, unknown>): Product {
      return {
        id: p.id as string,
        shopId: (p.shop_id ?? p.shopId) as string,
        articleNumber: (p.article_number ?? p.articleNumber) as string,
        name: p.name as string,
        category: (p.category ?? '') as string,
        purchasePrice: (p.purchase_price ?? p.purchasePrice ?? 0) as number,
        salePrice: (p.sale_price ?? p.salePrice) as number,
        vatRate: (p.vat_rate ?? p.vatRate ?? 7) as number,
        stock: (p.stock ?? 0) as number,
        minStock: (p.min_stock ?? p.minStock ?? 0) as number,
        active: (p.active ?? true) as boolean,
        updatedAt: (p.updated_at ?? p.updatedAt ?? Date.now()) as number,
        imageUrl: (p.image_url ?? p.imageUrl) as string | undefined,
      };
    }

    export function useProducts() {
      const shopId = getShopId();
      return useQuery<Product[]>({
        queryKey: ['products', shopId],
        queryFn: async () => {
          const headers = await getAuthHeaders();
          const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
          if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
          const data = await res.json() as Record<string, unknown>[];
          return data.map(mapServerProduct);
        },
        // networkMode wird vom Aufrufer pro Context überschrieben
      });
    }

    export function useCreateProduct() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async (productData: Product) => {
          const headers = await getAuthHeaders();
          const res = await fetch('/api/products', {
            method: 'POST',
            headers,
            body: JSON.stringify(productData),
          });
          if (!res.ok) throw new Error('Produkt konnte nicht gespeichert werden');
          return productData;
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['products', getShopId()] });
        },
      });
    }

    export function useUpdateProduct() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async (productData: Product) => {
          const headers = await getAuthHeaders();
          const res = await fetch('/api/products', {
            method: 'POST',
            headers,
            body: JSON.stringify({ ...productData, updatedAt: Date.now() }),
          });
          if (!res.ok) throw new Error('Produkt konnte nicht aktualisiert werden');
          return productData;
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['products', getShopId()] });
        },
      });
    }

    export function useToggleProductActive() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async ({ productId, active }: { productId: string; active: boolean }) => {
          const headers = await getAuthHeaders();
          const action = active ? 'activate' : 'deactivate';
          const res = await fetch(`/api/products/${productId}/${action}`, {
            method: 'PATCH',
            headers,
          });
          if (!res.ok) throw new Error('Status konnte nicht geändert werden');
          return { productId, active };
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['products', getShopId()] });
        },
      });
    }

    // STOCK_ADJUST: direkt via POST /api/sync mit STOCK_ADJUST OutboxEntry
    // (Server-Route sync.ts verarbeitet STOCK_ADJUST per Delta)
    export function useAdjustStock() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async ({
          productId,
          delta,
          reason,
        }: {
          productId: string;
          delta: number;
          reason?: string;
        }) => {
          const shopId = getShopId();
          const headers = await getAuthHeaders();
          const res = await fetch('/api/sync', {
            method: 'POST',
            headers,
            body: JSON.stringify({
              entries: [
                {
                  operation: 'STOCK_ADJUST',
                  payload: { productId, delta, reason, shopId },
                  shopId,
                  createdAt: Date.now(),
                  attempts: 0,
                },
              ],
            }),
          });
          if (!res.ok) throw new Error('Bestandskorrektur fehlgeschlagen');
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['products', getShopId()] });
        },
      });
    }
    ```

    **client/src/hooks/api/useCategories.ts:**
    ```typescript
    import { useQuery, useMutation, useQueryClient } from '@tanstack/react-query';
    import { getShopId } from '../../db/index.js';
    import { getAuthHeaders } from '../../features/auth/serverAuth.js';
    import type { Category } from '../../db/index.js';

    export function useCategories() {
      const shopId = getShopId();
      return useQuery<Category[]>({
        queryKey: ['categories', shopId],
        queryFn: async () => {
          const headers = await getAuthHeaders();
          const res = await fetch(`/api/categories?shopId=${shopId}`, { headers });
          if (!res.ok) throw new Error('Kategorien konnten nicht geladen werden');
          const data = await res.json() as Record<string, unknown>[];
          return data.map(c => ({
            id: c.id as string,
            shopId: (c.shop_id ?? c.shopId) as string,
            name: c.name as string,
          }));
        },
      });
    }

    export function useCreateCategory() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async (name: string) => {
          const headers = await getAuthHeaders();
          const res = await fetch('/api/categories', {
            method: 'POST',
            headers,
            body: JSON.stringify({ name }),
          });
          if (!res.ok) throw new Error('Kategorie konnte nicht angelegt werden');
          return await res.json() as Category;
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['categories', getShopId()] });
        },
      });
    }

    export function useRenameCategory() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async ({ id, name }: { id: string; name: string }) => {
          const headers = await getAuthHeaders();
          const res = await fetch(`/api/categories/${id}`, {
            method: 'PATCH',
            headers,
            body: JSON.stringify({ name }),
          });
          if (!res.ok) throw new Error('Umbenennen fehlgeschlagen');
        },
        onSuccess: () => {
          // Umbenennungen ändern auch Produktkategorien — beide invalidieren
          const shopId = getShopId();
          queryClient.invalidateQueries({ queryKey: ['categories', shopId] });
          queryClient.invalidateQueries({ queryKey: ['products', shopId] });
        },
      });
    }

    export function useDeleteCategory() {
      const queryClient = useQueryClient();
      return useMutation({
        mutationFn: async (id: string) => {
          const headers = await getAuthHeaders();
          const res = await fetch(`/api/categories/${id}`, {
            method: 'DELETE',
            headers,
          });
          if (res.status === 409) {
            const data = await res.json() as { error: string };
            throw new Error(data.error);
          }
          if (!res.ok) throw new Error('Kategorie konnte nicht gelöscht werden');
        },
        onSuccess: () => {
          queryClient.invalidateQueries({ queryKey: ['categories', getShopId()] });
        },
      });
    }
    ```

    WICHTIG bei useAdjustStock: Der Sync-Endpunkt erwartet `entries` als Array mit OutboxEntry-Objekten. Das Schema im Server (sync.ts) validiert per Zod: `{ entries: z.array(SyncEntrySchema) }`. Den `id`-Field nicht mitschicken — der Server ignoriert ihn für STOCK_ADJUST.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - ls client/src/hooks/api/ zeigt useProducts.ts und useCategories.ts
    - grep -n "export function useProducts" client/src/hooks/api/useProducts.ts gibt eine Zeile zurück
    - grep -n "export function useCategories" client/src/hooks/api/useCategories.ts gibt eine Zeile zurück
    - grep -n "export function useAdjustStock" client/src/hooks/api/useProducts.ts gibt eine Zeile zurück
    - grep -n "export function useDeleteCategory" client/src/hooks/api/useCategories.ts gibt eine Zeile zurück
    - npm run build läuft ohne Fehler durch
  </acceptance_criteria>
  <done>Beide Hook-Dateien existieren, alle Exports vorhanden, TypeScript-Build grün</done>
</task>

</tasks>

<verification>
Gesamtprüfung nach Plan 01:
- `npm run build` in /Users/simonluthe/Documents/fairstand/client läuft ohne Fehler
- `grep -rn "QueryClientProvider" client/src/App.tsx` zeigt Provider-Einbindung
- `ls client/src/hooks/api/` zeigt useProducts.ts und useCategories.ts
- `grep -n "useProducts\|useCreateProduct\|useUpdateProduct\|useToggleProductActive\|useAdjustStock" client/src/hooks/api/useProducts.ts` zeigt 5 Exports
- `grep -n "useCategories\|useCreateCategory\|useRenameCategory\|useDeleteCategory" client/src/hooks/api/useCategories.ts` zeigt 4 Exports
</verification>

<success_criteria>
- TanStack Query v5 ist in client/package.json installiert
- QueryClientProvider mit staleTime: 30s, gcTime: 5min, retry: 1 wrapping die App
- useProducts() und useCategories() lesen vom Server (nicht Dexie)
- Alle Mutations invalidieren den zugehörigen Query Key
- Build ist grün, keine TypeScript-Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/19-tanstack-query-foundation/19-01-SUMMARY.md` erstellen.
</output>
