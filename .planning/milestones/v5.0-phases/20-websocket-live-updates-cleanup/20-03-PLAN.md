---
phase: 20-websocket-live-updates-cleanup
plan: 03
type: execute
wave: 2
depends_on:
  - 20-01
  - 20-02
files_modified:
  - client/src/hooks/useWebSocket.ts
  - client/src/features/pos/POSScreen.tsx
  - client/src/features/admin/products/ProductList.tsx
  - client/src/sync/engine.ts
  - client/src/App.tsx
autonomous: true
requirements:
  - LIVE-07

must_haves:
  truths:
    - "Nach Login verbindet sich die App via WebSocket mit /api/ws?token=... und hält die Verbindung"
    - "Bei eingehender products_changed-Nachricht werden TQ-Queries ['products', shopId] invalidiert"
    - "Bei eingehender categories_changed-Nachricht wird ['categories', shopId] invalidiert"
    - "Bei eingehender stock_changed-Nachricht werden ['products', shopId] invalidiert"
    - "Trennt sich die Verbindung, wird mit Exponential Backoff (1s → 2s → 4s → max 30s) wieder verbunden"
    - "Der manuelle Sync-Button ist aus ProductList entfernt"
    - "downloadProducts() und downloadCategories() sind aus engine.ts entfernt"
    - "useSyncStatus ist aus POSScreen entfernt (Sync-Badge weg)"
  artifacts:
    - path: "client/src/hooks/useWebSocket.ts"
      provides: "useWebSocket() Hook — Verbindung, Auto-Reconnect, Invalidation"
      exports: ["useWebSocket"]
    - path: "client/src/features/pos/POSScreen.tsx"
      provides: "POS ohne Sync-Badge, mit useWebSocket-Hook"
    - path: "client/src/features/admin/products/ProductList.tsx"
      provides: "ProductList ohne Sync-Button und downloadProducts-Import"
    - path: "client/src/sync/engine.ts"
      provides: "flushOutbox() bleibt, downloadProducts/downloadCategories entfernt"
  key_links:
    - from: "client/src/hooks/useWebSocket.ts"
      to: "queryClient.invalidateQueries"
      via: "useQueryClient() + onmessage handler"
      pattern: "invalidateQueries"
    - from: "client/src/App.tsx"
      to: "client/src/hooks/useWebSocket.ts"
      via: "useWebSocket() im UnlockedApp-Kontext aufgerufen"
      pattern: "useWebSocket"
---

<objective>
WebSocket-Client-Hook erstellen und in die App integrieren, gleichzeitig altes Sync-System (Sync-Button, downloadProducts/downloadCategories, Sync-Badge) entfernen.

Purpose: Live-Updates über WebSocket erfordern einen dedizierten Client-Hook. Ohne Cleanup bleibt das veraltete Sync-System parallel aktiv und verwirrt Nutzerinnen mit einem überflüssigen Sync-Badge.
Output: useWebSocket.ts Hook mit Auto-Reconnect + TQ-Invalidation; alle Cleanup-Änderungen in ProductList, POSScreen und engine.ts.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/20-websocket-live-updates-cleanup/20-01-SUMMARY.md
@.planning/phases/20-websocket-live-updates-cleanup/20-02-SUMMARY.md

<interfaces>
<!-- WebSocket Message-Typen (aus 20-01 websocket.ts) -->
```typescript
type BroadcastMessage =
  | { type: 'products_changed'; shopId: string }
  | { type: 'categories_changed'; shopId: string }
  | { type: 'stock_changed'; shopId: string };
```

<!-- TanStack Query Invalidierung -->
```typescript
import { useQueryClient } from '@tanstack/react-query';
const queryClient = useQueryClient();
queryClient.invalidateQueries({ queryKey: ['products', shopId] });
queryClient.invalidateQueries({ queryKey: ['categories', shopId] });
```

<!-- Session für Token (client/src/features/auth/serverAuth.ts) -->
```typescript
export async function getStoredSession(): Promise<StoredSession | null>;
// StoredSession = { shopId, shopName, token, lastActivity }
```

<!-- getShopId (client/src/db/index.ts) -->
```typescript
export function getShopId(): string; // wirft wenn nicht gesetzt
```

<!-- Bestehende App.tsx Struktur -->
// UnlockedApp-Komponente (extrahiert in Phase 19) ist der richtige Ort für useWebSocket()
// Sie liegt innerhalb von QueryClientProvider
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useWebSocket.ts — Hook mit Auto-Reconnect und TQ-Invalidation</name>
  <files>
    client/src/hooks/useWebSocket.ts
    client/src/App.tsx
  </files>
  <read_first>
    - client/src/App.tsx (vollständig — UnlockedApp-Komponente finden, wo Hook eingebaut wird)
    - client/src/features/auth/serverAuth.ts (getStoredSession-Signatur + StoredSession-Interface)
    - client/src/db/index.ts (getShopId-Signatur)
  </read_first>
  <action>
**1. `client/src/hooks/useWebSocket.ts` erstellen:**

```typescript
import { useEffect, useRef } from 'react';
import { useQueryClient } from '@tanstack/react-query';
import { getStoredSession } from '../features/auth/serverAuth.js';
import { getShopId } from '../db/index.js';

const MIN_BACKOFF_MS = 1000;
const MAX_BACKOFF_MS = 30_000;

export function useWebSocket() {
  const queryClient = useQueryClient();
  const wsRef = useRef<WebSocket | null>(null);
  const backoffRef = useRef(MIN_BACKOFF_MS);
  const reconnectTimerRef = useRef<ReturnType<typeof setTimeout> | null>(null);
  const unmountedRef = useRef(false);

  useEffect(() => {
    unmountedRef.current = false;

    async function connect() {
      if (unmountedRef.current) return;

      const session = await getStoredSession();
      if (!session?.token) return; // Kein Token — nicht verbinden

      const protocol = window.location.protocol === 'https:' ? 'wss:' : 'ws:';
      const host = window.location.host;
      const url = `${protocol}//${host}/api/ws?token=${encodeURIComponent(session.token)}`;

      const ws = new WebSocket(url);
      wsRef.current = ws;

      ws.onopen = () => {
        backoffRef.current = MIN_BACKOFF_MS; // Erfolgreiche Verbindung → Backoff zurücksetzen
      };

      ws.onmessage = (event: MessageEvent) => {
        try {
          const msg = JSON.parse(event.data as string) as {
            type: 'products_changed' | 'categories_changed' | 'stock_changed';
            shopId: string;
          };
          const shopId = getShopId();

          // Nur Nachrichten für den eigenen Shop verarbeiten
          if (msg.shopId !== shopId) return;

          if (msg.type === 'products_changed' || msg.type === 'stock_changed') {
            queryClient.invalidateQueries({ queryKey: ['products', shopId] });
          }
          if (msg.type === 'categories_changed') {
            queryClient.invalidateQueries({ queryKey: ['categories', shopId] });
          }
        } catch {
          // Ungültige JSON-Nachricht — ignorieren
        }
      };

      ws.onclose = () => {
        if (unmountedRef.current) return;
        // Exponential Backoff mit Jitter
        const jitter = Math.random() * 500;
        const delay = Math.min(backoffRef.current + jitter, MAX_BACKOFF_MS);
        backoffRef.current = Math.min(backoffRef.current * 2, MAX_BACKOFF_MS);
        reconnectTimerRef.current = setTimeout(connect, delay);
      };

      ws.onerror = () => {
        ws.close(); // onclose wird dann reconnect triggern
      };
    }

    connect();

    return () => {
      unmountedRef.current = true;
      if (reconnectTimerRef.current) {
        clearTimeout(reconnectTimerRef.current);
      }
      wsRef.current?.close();
    };
  }, []); // eslint-disable-line react-hooks/exhaustive-deps
}
```

**2. `useWebSocket()` in App.tsx aufrufen:**

Den Hook in der `UnlockedApp`-Komponente aufrufen (die in Phase 19 extrahiert wurde und innerhalb von QueryClientProvider liegt):

```typescript
import { useWebSocket } from './hooks/useWebSocket.js';

// In UnlockedApp-Komponente:
function UnlockedApp({ ... }) {
  useWebSocket(); // Verbindung herstellen und aufrechterhalten
  // ... Rest der Komponente unverändert
}
```

Hinweis: Wenn `UnlockedApp` nicht existiert oder anders heißt, die Komponente identifizieren die innerhalb von `QueryClientProvider` gerendert wird und einen eingeloggten Zustand darstellt — dort den Hook aufrufen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `client/src/hooks/useWebSocket.ts` existiert mit exportierter `useWebSocket`-Funktion
    - `grep -n "useWebSocket" client/src/App.tsx` zeigt Import und Aufruf
    - `grep -n "invalidateQueries" client/src/hooks/useWebSocket.ts` findet mindestens 2 Treffer
    - `grep -n "onclose" client/src/hooks/useWebSocket.ts` zeigt Reconnect-Logik
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>useWebSocket() verbindet sich nach Login mit dem Server, invalidiert TQ-Queries bei Nachrichten und reconnected automatisch mit Exponential Backoff</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Cleanup — Sync-Button, Sync-Badge, downloadProducts/downloadCategories entfernen</name>
  <files>
    client/src/features/admin/products/ProductList.tsx
    client/src/features/pos/POSScreen.tsx
    client/src/sync/engine.ts
  </files>
  <read_first>
    - client/src/features/admin/products/ProductList.tsx (vollständig — Sync-Button-Block finden)
    - client/src/features/pos/POSScreen.tsx (vollständig — Sync-Badge-Block finden, useSyncStatus-Import)
    - client/src/sync/engine.ts (vollständig — downloadProducts + downloadCategories entfernen)
  </read_first>
  <action>
**1. ProductList.tsx — Sync-Button entfernen:**

Folgendes entfernen:
- Import: `import { downloadProducts } from '../../../sync/engine.js';`
- State: `const [syncing, setSyncing] = useState(false);`
- State: `const [syncResult, setSyncResult] = useState<string | null>(null);`
- Funktion: `async function handleDownloadSync() { ... }` (komplett)
- JSX: `<button onPointerDown={handleDownloadSync} ...>Sync</button>` (der Button mit RefreshCw-Icon)
- JSX: `{syncResult && ...}` (Ergebnis-Anzeige falls vorhanden)
- Import: `RefreshCw` aus lucide-react (falls danach nicht mehr verwendet)

Der Produktlisten-Header soll danach nur noch den "+ Neues Produkt" Button enthalten.

**2. POSScreen.tsx — Sync-Badge + useSyncStatus entfernen:**

Folgendes entfernen:
- Import: `import { useSyncStatus, resetFailedEntries } from '../../sync/useSyncStatus.js';`
- Hook: `const syncStatus = useSyncStatus();`
- JSX-Block im Header: der gesamte `{syncStatus !== 'synced' && ( <button ...>...</button> )}`-Block (Zeilen ca. 158-175 in der bisherigen Datei)
- Import: `RefreshCw` aus lucide-react (falls danach nicht mehr verwendet)

Der Header-Bereich behält: Lock-Button, Settings-Button, Cart-Button, ShopName-Anzeige.

**3. engine.ts — downloadProducts und downloadCategories entfernen:**

Entfernen:
- Interface `ServerProduct` (Zeilen 66-81)
- Funktion `downloadProducts()` (Zeilen 82-112)
- Funktion `downloadCategories()` (Zeilen 114-125)
- Import `Category` aus db/index.js falls nur in diesen Funktionen verwendet

Behalten: `flushOutbox()` und alle ihre Abhängigkeiten unverändert.

Auch den `downloadProducts()`-Aufruf am Ende von `flushOutbox()` entfernen (Zeile ~58: `downloadProducts().catch(() => {});`).

Prüfen ob `downloadProducts` oder `downloadCategories` noch irgendwo aufgerufen werden:
`grep -rn "downloadProducts\|downloadCategories" client/src/` — alle gefundenen Stellen bereinigen.

Nach Cleanup: `engine.ts` enthält nur noch `flushOutbox()`.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "downloadProducts\|downloadCategories" client/src/features/admin/products/ProductList.tsx` findet keinen Treffer
    - `grep -n "useSyncStatus\|syncStatus\|resetFailedEntries" client/src/features/pos/POSScreen.tsx` findet keinen Treffer
    - `grep -n "downloadProducts\|downloadCategories" client/src/sync/engine.ts` findet keinen Treffer
    - `grep -rn "downloadProducts\|downloadCategories" client/src/` — findet keine Treffer mehr
    - TypeScript-Kompilierung ohne Fehler
    - `grep -n "flushOutbox" client/src/sync/engine.ts` — flushOutbox bleibt erhalten
  </acceptance_criteria>
  <done>Sync-Button aus ProductList entfernt, Sync-Badge aus POSScreen entfernt, downloadProducts/downloadCategories aus engine.ts entfernt; flushOutbox() bleibt für Offline-Pfad erhalten</done>
</task>

</tasks>

<verification>
1. `cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json` — keine Fehler
2. `grep -rn "downloadProducts\|downloadCategories" client/src/` — 0 Treffer
3. `grep -n "useWebSocket" client/src/App.tsx` — Aufruf vorhanden
4. `grep -n "invalidateQueries" client/src/hooks/useWebSocket.ts` — mindestens 2 Treffer
5. `grep -n "useSyncStatus" client/src/features/pos/POSScreen.tsx` — 0 Treffer
</verification>

<success_criteria>
- useWebSocket.ts ist erstellt und in App.tsx eingebunden
- Auto-Reconnect mit Exponential Backoff implementiert
- TQ-Invalidation bei allen drei Message-Typen
- Sync-Button aus ProductList entfernt
- Sync-Badge aus POSScreen entfernt
- downloadProducts/downloadCategories aus engine.ts entfernt
- flushOutbox() und Outbox-Tabellen für Phase 21 erhalten
- TypeScript ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/20-websocket-live-updates-cleanup/20-03-SUMMARY.md` erstellen.
</output>
