---
phase: 20-websocket-live-updates-cleanup
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/package.json
  - server/src/routes/websocket.ts
  - server/src/index.ts
  - server/src/routes/products.ts
  - server/src/routes/categories.ts
  - server/src/routes/sync.ts
autonomous: true
requirements:
  - LIVE-04

must_haves:
  truths:
    - "Ein WebSocket-Client kann sich nach Login via /api/ws?token=... verbinden"
    - "Nach einer Produktänderung (POST /api/products) erhalten alle verbundenen Clients desselben Shops eine Nachricht { type: 'products_changed', shopId }"
    - "Nach einer Kategorienänderung erhalten alle verbundenen Clients eine Nachricht { type: 'categories_changed', shopId }"
    - "Nach einem Verkauf (SALE_COMPLETE in /api/sync) und nach STOCK_ADJUST erhalten alle verbundenen Clients { type: 'stock_changed', shopId }"
    - "Clients mit ungültigem oder fehlendem Token werden sofort getrennt (close 1008)"
  artifacts:
    - path: "server/src/routes/websocket.ts"
      provides: "WebSocket-Route + broadcast()-Hilfsfunktion"
      exports: ["websocketRoutes", "broadcast"]
    - path: "server/src/index.ts"
      provides: "Registrierung @fastify/websocket + websocketRoutes"
  key_links:
    - from: "server/src/routes/products.ts"
      to: "server/src/routes/websocket.ts"
      via: "broadcast() call nach jeder Mutation"
      pattern: "broadcast\\("
    - from: "server/src/routes/sync.ts"
      to: "server/src/routes/websocket.ts"
      via: "broadcast() call nach SALE_COMPLETE + STOCK_ADJUST"
      pattern: "broadcast\\("
---

<objective>
WebSocket-Infrastruktur auf dem Server aufbauen: @fastify/websocket installieren, Route /api/ws mit Token-Auth einrichten, broadcast()-Hilfsfunktion implementieren, und alle datenmutierenden Routen (products, categories, sync) mit Broadcast-Aufrufen versehen.

Purpose: Ohne Server-seitige WebSocket-Unterstützung können Clients keine Live-Updates empfangen. Dieser Plan legt die Grundlage für alle nachfolgenden Client-Integrationen.
Output: Laufender WebSocket-Endpoint; jede Datenmutation broadcastet ein Invalidierungs-Signal an alle Clients desselben Shops.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Sessions-API (server/src/lib/sessions.ts) -->
```typescript
interface Session {
  shopId: string;
  createdAt: number;
}
export function validateSession(token: string): Session | null;
```

<!-- Fastify Plugin-Registrierung (server/src/index.ts) -->
```typescript
// Muster für Route-Registrierung:
await fastify.register(productRoutes, { prefix: '/api' });
// WebSocket MUSS VOR Route-Registrierung registriert werden:
await fastify.register(websocketPlugin);
```

<!-- Bestehende Route-Signatur (server/src/routes/products.ts) -->
```typescript
export async function productRoutes(fastify: FastifyInstance) { ... }
// Mutationspunkte (broadcast NACH reply.send()):
// POST /products (Zeile 52): nach Upsert + reply.send(rows)
// PATCH /products/:id/activate + /deactivate (Ende jedes Handlers)
// DELETE /products/:id (falls vorhanden)
```

<!-- Bestehende Route-Signatur (server/src/routes/sync.ts) -->
// Mutationspunkte: nach Verarbeitung von SALE_COMPLETE und STOCK_ADJUST
// SaleComplete schreibt Bestand — broadcast({ type: 'stock_changed', shopId })
// StockAdjust schreibt Bestand — broadcast({ type: 'stock_changed', shopId })
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: @fastify/websocket installieren + websocket.ts mit broadcast()</name>
  <files>
    server/package.json
    server/src/routes/websocket.ts
    server/src/index.ts
  </files>
  <read_first>
    - server/src/index.ts (Plugin-Registrierungsreihenfolge verstehen)
    - server/src/lib/sessions.ts (validateSession-Signatur)
    - server/package.json (bestehende Dependencies prüfen)
  </read_first>
  <action>
1. In server/: `npm install @fastify/websocket` ausführen.

2. Datei `server/src/routes/websocket.ts` erstellen:

```typescript
import type { FastifyInstance } from 'fastify';
import websocketPlugin from '@fastify/websocket';
import type { WebSocket } from 'ws';
import { validateSession } from '../lib/sessions.js';

// shopId → Set von aktiven WebSocket-Verbindungen
const clients = new Map<string, Set<WebSocket>>();

export type BroadcastMessage =
  | { type: 'products_changed'; shopId: string }
  | { type: 'categories_changed'; shopId: string }
  | { type: 'stock_changed'; shopId: string };

/**
 * Sendet ein Invalidierungs-Signal an alle verbundenen Clients desselben Shops.
 * Wird von products.ts, categories.ts und sync.ts nach Mutationen aufgerufen.
 */
export function broadcast(msg: BroadcastMessage): void {
  const conns = clients.get(msg.shopId);
  if (!conns) return;
  const payload = JSON.stringify(msg);
  for (const ws of conns) {
    if (ws.readyState === 1 /* WebSocket.OPEN */) {
      ws.send(payload);
    }
  }
}

export async function websocketRoutes(fastify: FastifyInstance) {
  await fastify.register(websocketPlugin);

  fastify.get('/api/ws', { websocket: true }, (socket, request) => {
    // Token aus Query-Parameter holen: /api/ws?token=...
    const url = new URL(request.url, 'http://localhost');
    const token = url.searchParams.get('token');

    if (!token) {
      socket.close(1008, 'Token fehlt');
      return;
    }

    const session = validateSession(token);
    if (!session) {
      socket.close(1008, 'Ungültiger Token');
      return;
    }

    const { shopId } = session;

    // Client registrieren
    if (!clients.has(shopId)) {
      clients.set(shopId, new Set());
    }
    clients.get(shopId)!.add(socket);

    socket.on('close', () => {
      clients.get(shopId)?.delete(socket);
      if (clients.get(shopId)?.size === 0) {
        clients.delete(shopId);
      }
    });

    // Keepalive: Client kann pings senden, Server antwortet nicht explizit
    socket.on('error', () => {
      clients.get(shopId)?.delete(socket);
    });
  });
}
```

3. In `server/src/index.ts` websocketRoutes VOR den anderen Route-Registrierungen eintragen:

```typescript
import { websocketRoutes } from './routes/websocket.js';
// ...
// NACH fastify.register(cors) und fastify.register(rateLimit), VOR health/sync/product-Routes:
await fastify.register(websocketRoutes);
```

Wichtig: @fastify/websocket MUSS als erstes Plugin registriert werden (vor allen Routen die `{ websocket: true }` nutzen).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `server/src/routes/websocket.ts` existiert mit exportierten Funktionen `broadcast` und `websocketRoutes`
    - `grep -n "websocketRoutes" server/src/index.ts` findet den Import und die Registrierung
    - `grep -n "@fastify/websocket" server/package.json` findet den Eintrag in dependencies
    - TypeScript-Kompilierung läuft ohne Fehler
  </acceptance_criteria>
  <done>WebSocket-Route ist registriert, broadcast() ist aufrufbar, ungültige Tokens werden mit close(1008) abgelehnt</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Broadcast-Aufrufe in products.ts, categories.ts und sync.ts</name>
  <files>
    server/src/routes/products.ts
    server/src/routes/categories.ts
    server/src/routes/sync.ts
  </files>
  <read_first>
    - server/src/routes/products.ts (vollständig — alle Mutationspunkte identifizieren)
    - server/src/routes/categories.ts (vollständig)
    - server/src/routes/sync.ts (vollständig — SALE_COMPLETE + STOCK_ADJUST Stellen finden)
  </read_first>
  <action>
In jede der drei Route-Dateien `broadcast` importieren und nach jeder erfolgreichen Datenmutation aufrufen.

**server/src/routes/products.ts:**
```typescript
import { broadcast } from './websocket.js';
```
Nach jedem erfolgreichen POST /products (Upsert), PATCH /products/:id/activate, PATCH /products/:id/deactivate, DELETE /products/:id (sofern vorhanden):
```typescript
broadcast({ type: 'products_changed', shopId: session.shopId });
```
Der broadcast-Aufruf erfolgt NACH dem reply.send() — Reihenfolge: Daten schreiben → reply.send() → broadcast().

**server/src/routes/categories.ts:**
```typescript
import { broadcast } from './websocket.js';
```
Nach POST /categories, PATCH /categories/:id, DELETE /categories/:id:
```typescript
broadcast({ type: 'categories_changed', shopId: session.shopId });
```
Bei PATCH (Umbenennung) zusätzlich:
```typescript
broadcast({ type: 'products_changed', shopId: session.shopId });
```
(weil Produktkategorie-Felder mitgeändert werden)

**server/src/routes/sync.ts:**
```typescript
import { broadcast } from './websocket.js';
```
Nach Verarbeitung von SALE_COMPLETE-Einträgen:
```typescript
broadcast({ type: 'stock_changed', shopId });
```
Nach Verarbeitung von STOCK_ADJUST-Einträgen:
```typescript
broadcast({ type: 'stock_changed', shopId });
```
shopId ist bereits in der Outbox-Entry verfügbar als `entry.payload.shopId` oder aus der Session: `const session = (request as any).session as { shopId: string }`.

Wichtig: Einen broadcast pro Batch reicht — nicht pro Entry. Prüfen ob die Schleife SALE_COMPLETE oder STOCK_ADJUST-Einträge enthielt, dann einmal am Ende broadcasten.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "broadcast" server/src/routes/products.ts` zeigt mindestens 1 Treffer (nach POST-Handler)
    - `grep -n "broadcast" server/src/routes/categories.ts` zeigt mindestens 2 Treffer (nach POST + PATCH + DELETE)
    - `grep -n "broadcast" server/src/routes/sync.ts` zeigt mindestens 1 Treffer (nach SALE_COMPLETE/STOCK_ADJUST)
    - TypeScript-Kompilierung ohne Fehler
    - `grep -n "from './websocket.js'" server/src/routes/products.ts` findet den Import
  </acceptance_criteria>
  <done>Jede Datenmutation auf dem Server sendet ein Invalidierungs-Signal an alle verbundenen Clients desselben Shops</done>
</task>

</tasks>

<verification>
1. `cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit` — keine TypeScript-Fehler
2. `grep -rn "broadcast(" server/src/routes/` — zeigt Aufrufe in products.ts, categories.ts und sync.ts
3. `grep -n "websocketRoutes" server/src/index.ts` — Registrierung vorhanden und VOR anderen Routes
4. `grep -n "@fastify/websocket" server/package.json` — Dependency eingetragen
</verification>

<success_criteria>
- @fastify/websocket ist installiert und in package.json eingetragen
- /api/ws Route existiert, validiert Token aus Query-Param, schließt ungültige Clients
- broadcast() ist aus allen drei Routen-Dateien importiert und wird nach Mutationen aufgerufen
- TypeScript-Build läuft ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/20-websocket-live-updates-cleanup/20-01-SUMMARY.md` erstellen.
</output>
