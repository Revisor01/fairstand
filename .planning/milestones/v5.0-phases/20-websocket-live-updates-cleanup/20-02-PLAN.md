---
phase: 20-websocket-live-updates-cleanup
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/pos/useSaleComplete.ts
  - client/src/hooks/api/useProducts.ts
  - client/src/hooks/api/useCategories.ts
autonomous: true
requirements:
  - LIVE-03
  - LIVE-05

must_haves:
  truths:
    - "Ein Online-Verkauf wird direkt via POST /api/sync gesendet — kein OutboxEntry wird erstellt"
    - "Wenn navigator.onLine false ist, fällt completeSale() auf Outbox-Pfad zurück (unverändert)"
    - "Nach einem erfolgreichen useProducts()-Fetch werden die Produkte in Dexie geschrieben (Write-Through-Cache)"
    - "Nach einem erfolgreichen useCategories()-Fetch werden die Kategorien in Dexie geschrieben"
    - "Die Outbox und flushOutbox() bleiben für den Offline-Pfad erhalten"
  artifacts:
    - path: "client/src/features/pos/useSaleComplete.ts"
      provides: "completeSale() + completeWithdrawal() mit Online/Offline-Branch"
    - path: "client/src/hooks/api/useProducts.ts"
      provides: "useProducts() mit Dexie Write-Through nach fetch"
    - path: "client/src/hooks/api/useCategories.ts"
      provides: "useCategories() mit Dexie Write-Through nach fetch"
  key_links:
    - from: "client/src/features/pos/useSaleComplete.ts"
      to: "/api/sync"
      via: "direkter fetch() wenn navigator.onLine"
      pattern: "navigator\\.onLine"
    - from: "client/src/hooks/api/useProducts.ts"
      to: "db.products"
      via: "db.products.where('shopId').equals(shopId).delete() + bulkPut()"
      pattern: "bulkPut"
---

<objective>
Zwei unabhängige Client-Verbesserungen die zusammen passen: (1) completeSale/completeWithdrawal schicken Verkäufe online direkt an den Server ohne Outbox-Umweg. (2) useProducts und useCategories schreiben nach jedem erfolgreichen Fetch die Daten in Dexie als Write-Through-Cache für den Offline-Fallback in Phase 21.

Purpose: Online-Verkäufe erscheinen sofort auf dem Server (kein Sync-Delay). Dexie hat immer einen aktuellen Produktstand für Phase 21 (Offline-Fallback bei kaltem TQ-Cache).
Output: useSaleComplete.ts mit Online/Offline-Branch; useProducts/useCategories mit Dexie Write-Through.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehender completeSale (client/src/features/pos/useSaleComplete.ts) -->
```typescript
// Aktueller Ablauf:
// 1. Sale-Objekt erstellen
// 2. db.transaction: sale + stock-delta + outbox-entry in Dexie
// 3. Sale zurückgeben

// Neuer Ablauf (Online):
// 1. Sale-Objekt erstellen
// 2. Stock-delta in Dexie (immer — auch online, weil TQ cache noch nicht aktualisiert)
// 3. Sale in db.sales speichern (immer — für Verlauf/Storno)
// 4. Wenn online: direkt POST /api/sync, Sale.syncedAt setzen
// 5. Wenn offline: outbox-entry erstellen (wie bisher)
```

<!-- Dexie Write-Through Pattern -->
```typescript
// Nach fetch() in queryFn von useProducts:
const shopId = getShopId();
const products = data.map(mapServerProduct);
// Fire-and-forget — Fehler nicht nach oben propagieren
db.transaction('rw', db.products, async () => {
  await db.products.where('shopId').equals(shopId).delete();
  await db.products.bulkPut(products);
}).catch(() => {});
return products;
```

<!-- Auth-Headers Pattern -->
```typescript
import { getAuthHeaders } from '../../features/auth/serverAuth.js';
const headers = await getAuthHeaders();
// Headers enthalten: { Authorization: 'Bearer <token>', 'Content-Type': 'application/json' }
```

<!-- Outbox-Entry Struktur (behalten für Offline-Pfad) -->
```typescript
{
  operation: 'SALE_COMPLETE',
  payload: sale,
  shopId: getShopId(),
  createdAt: Date.now(),
  attempts: 0,
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: useSaleComplete.ts — Online-Direct-POST + Offline-Outbox-Fallback</name>
  <files>
    client/src/features/pos/useSaleComplete.ts
  </files>
  <read_first>
    - client/src/features/pos/useSaleComplete.ts (vollständig — beide Funktionen)
    - client/src/features/auth/serverAuth.ts (getAuthHeaders-Signatur)
    - client/src/db/index.ts (Dexie-Schema, OutboxEntry-Typ)
  </read_first>
  <action>
`useSaleComplete.ts` umschreiben. Beide Funktionen (`completeSale` und `completeWithdrawal`) bekommen denselben Online/Offline-Branch.

**Neues Muster für completeSale():**

```typescript
import { db, getShopId } from '../../db/index.js';
import type { CartItem, Sale, Product } from '../../db/index.js';
import { getAuthHeaders } from '../../features/auth/serverAuth.js';

export async function completeSale(
  items: CartItem[],
  paidCents: number,
  changeCents: number
): Promise<Sale> {
  const totalCents = items.reduce((sum, i) => sum + i.salePrice * i.quantity, 0);

  if (paidCents < totalCents) {
    throw new Error(`Bezahlt (${paidCents}) ist weniger als Gesamtpreis (${totalCents})`);
  }

  const donationCents = paidCents - totalCents - changeCents;

  if (donationCents < 0) {
    throw new Error(`Wechselgeld (${changeCents}) größer als Differenz (${paidCents - totalCents})`);
  }

  const sale: Sale = {
    id: crypto.randomUUID(),
    shopId: getShopId(),
    items,
    totalCents,
    paidCents,
    changeCents,
    donationCents,
    type: 'sale',
    createdAt: Date.now(),
  };

  // Sale + Stock-Delta immer in Dexie (Verlauf, Storno, Offline-Fallback)
  await db.transaction('rw', [db.sales, db.products], async () => {
    await db.sales.add(sale);
    for (const item of items) {
      await db.products
        .where('id')
        .equals(item.productId)
        .modify((p: { stock: number; updatedAt: number }) => {
          p.stock -= item.quantity;
          p.updatedAt = Date.now();
        });
    }
  });

  if (navigator.onLine) {
    // Online: direkt an Server senden
    try {
      const headers = await getAuthHeaders();
      const res = await fetch('/api/sync', {
        method: 'POST',
        headers,
        body: JSON.stringify({
          entries: [{
            operation: 'SALE_COMPLETE',
            payload: sale,
            shopId: getShopId(),
            createdAt: Date.now(),
            attempts: 0,
          }],
        }),
      });
      if (res.ok) {
        await db.sales.update(sale.id, { syncedAt: Date.now() });
      } else {
        // Server-Fehler → in Outbox für späteren Retry
        await db.outbox.add({
          operation: 'SALE_COMPLETE',
          payload: sale,
          shopId: getShopId(),
          createdAt: Date.now(),
          attempts: 0,
        });
      }
    } catch {
      // Netzwerkfehler → in Outbox für späteren Retry
      await db.outbox.add({
        operation: 'SALE_COMPLETE',
        payload: sale,
        shopId: getShopId(),
        createdAt: Date.now(),
        attempts: 0,
      });
    }
  } else {
    // Offline: in Outbox schreiben
    await db.outbox.add({
      operation: 'SALE_COMPLETE',
      payload: sale,
      shopId: getShopId(),
      createdAt: Date.now(),
      attempts: 0,
    });
  }

  return sale;
}
```

**completeWithdrawal()** erhält dasselbe Muster: Sale + Stock-Delta immer in Dexie, dann Online-Direct-POST oder Outbox-Fallback. Den EK-Preis-Enrichment-Teil (Zeilen 70-80 im Original) unverändert übernehmen.

Hinweis: `db.outbox` bleibt in der Transaktion NICHT mehr enthalten — Sale und Stock werden in eigener Transaktion gespeichert, Outbox-Insert danach separat. Das verhindert IDB-Transaktions-Timeout bei async fetch().
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "navigator.onLine" client/src/features/pos/useSaleComplete.ts` findet mindestens 1 Treffer in completeSale und 1 in completeWithdrawal
    - `grep -n "db.outbox" client/src/features/pos/useSaleComplete.ts` zeigt dass Outbox-Eintrag nur im else-Branch + catch-Block erstellt wird
    - TypeScript-Kompilierung ohne Fehler
    - Der Dexie-Transaktion-Block enthält NICHT mehr db.outbox (kein IDB-Timeout-Risiko)
  </acceptance_criteria>
  <done>Online-Verkäufe werden direkt gepostet; Offline-Verkäufe landen in der Outbox; beide Pfade speichern Sale + Stock-Delta in Dexie</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Dexie Write-Through in useProducts und useCategories</name>
  <files>
    client/src/hooks/api/useProducts.ts
    client/src/hooks/api/useCategories.ts
  </files>
  <read_first>
    - client/src/hooks/api/useProducts.ts (vollständig)
    - client/src/hooks/api/useCategories.ts (vollständig)
    - client/src/db/index.ts (db-Export, Product + Category Typen)
  </read_first>
  <action>
In beiden Hooks die queryFn so erweitern, dass nach einem erfolgreichen Fetch die Daten in Dexie geschrieben werden (fire-and-forget, Fehler nicht propagieren).

**useProducts.ts — queryFn erweitern:**

```typescript
import { db } from '../../db/index.js';  // bereits importiert über getShopId

// In queryFn von useProducts():
queryFn: async () => {
  const headers = await getAuthHeaders();
  const res = await fetch(`/api/products?shopId=${shopId}`, { headers });
  if (!res.ok) throw new Error('Produkte konnten nicht geladen werden');
  const data = await res.json() as Record<string, unknown>[];
  const products = data.map(mapServerProduct);

  // Write-Through: Dexie als Offline-Cache aktuell halten
  // Fire-and-forget — wirft keinen Fehler nach oben wenn IDB scheitert
  db.transaction('rw', db.products, async () => {
    await db.products.where('shopId').equals(shopId).delete();
    await db.products.bulkPut(products);
  }).catch(() => {});

  return products;
},
```

**useCategories.ts — queryFn erweitern:**

```typescript
import { db } from '../../db/index.js';

// In queryFn von useCategories():
queryFn: async () => {
  const headers = await getAuthHeaders();
  const res = await fetch(`/api/categories?shopId=${shopId}`, { headers });
  if (!res.ok) throw new Error('Kategorien konnten nicht geladen werden');
  const data = await res.json() as Record<string, unknown>[];
  const categories = data.map(c => ({
    id: c.id as string,
    shopId: (c.shop_id ?? c.shopId) as string,
    name: c.name as string,
    sortOrder: (c.sort_order ?? c.sortOrder ?? 0) as number,
    createdAt: (c.created_at ?? c.createdAt ?? Date.now()) as number,
  }));

  // Write-Through: Dexie als Offline-Cache
  db.transaction('rw', db.categories, async () => {
    await db.categories.where('shopId').equals(shopId).delete();
    await db.categories.bulkPut(categories);
  }).catch(() => {});

  return categories;
},
```

`db` ist über den bestehenden Import von `../../db/index.js` bereits zugänglich (dort wird `db` als Dexie-Instanz exportiert).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "bulkPut" client/src/hooks/api/useProducts.ts` findet einen Treffer im queryFn
    - `grep -n "bulkPut" client/src/hooks/api/useCategories.ts` findet einen Treffer im queryFn
    - `grep -n "\.catch" client/src/hooks/api/useProducts.ts` zeigt fire-and-forget Pattern
    - TypeScript-Kompilierung ohne Fehler
  </acceptance_criteria>
  <done>Nach jedem erfolgreichen TQ-Fetch werden Produkte/Kategorien in Dexie als Write-Through-Cache gespeichert — Phase 21 kann auf diese Daten offline zurückgreifen</done>
</task>

</tasks>

<verification>
1. `cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json` — keine Fehler
2. `grep -n "navigator.onLine" client/src/features/pos/useSaleComplete.ts` — 2 Treffer
3. `grep -n "bulkPut" client/src/hooks/api/useProducts.ts client/src/hooks/api/useCategories.ts` — je 1 Treffer
</verification>

<success_criteria>
- Online-Verkäufe in completeSale/completeWithdrawal nutzen direkten POST ohne Outbox
- Offline-Verkäufe landen in Outbox (Fallback unverändert)
- useProducts + useCategories schreiben nach fetch fire-and-forget in Dexie
- TypeScript ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/20-websocket-live-updates-cleanup/20-02-SUMMARY.md` erstellen.
</output>
