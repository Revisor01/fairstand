---
phase: 05-tech-debt-fixes
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/sync.ts
  - client/src/features/admin/products/ProductList.tsx
  - client/src/features/admin/reports/MonthlyReport.tsx
autonomous: true
requirements: [TD-01, TD-02, TD-04]

must_haves:
  truths:
    - "Produkt-Upsert im Sync-Handler nutzt Timestamp-Vergleich (LWW) statt onConflictDoNothing"
    - "Produkt-Deaktivierung/Aktivierung wird per direktem PATCH an den Server synchronisiert"
    - "extra_donation_cents wird im Monatsbericht als 6. Karte angezeigt"
  artifacts:
    - path: "server/src/routes/sync.ts"
      provides: "LWW-Upsert fuer Produkte im Sync-Handler"
      contains: "onConflictDoUpdate"
    - path: "client/src/features/admin/products/ProductList.tsx"
      provides: "Direkter PATCH-Aufruf bei Produkt-Aktivierung/Deaktivierung"
      contains: "/api/products/"
    - path: "client/src/features/admin/reports/MonthlyReport.tsx"
      provides: "Anzeige von extra_donation_cents"
      contains: "extra_donation_cents"
  key_links:
    - from: "client/src/features/admin/products/ProductList.tsx"
      to: "server/src/routes/products.ts"
      via: "Direkter PATCH /api/products/:id/deactivate bzw. /activate"
      pattern: "fetch.*api/products.*deactivate|activate"
    - from: "server/src/routes/sync.ts"
      to: "server/src/routes/products.ts"
      via: "Gleicher LWW-Pattern fuer onConflictDoUpdate"
      pattern: "CASE WHEN excluded.updated_at"
---

<objective>
Drei technische Schulden aus v1.0 beheben: LWW-Konfliktaufloesung im Sync-Handler, Server-Sync fuer Produkt-Deaktivierung per direktem PATCH, und extra_donation_cents im Monatsbericht anzeigen.

Purpose: Datenintegritaet bei paralleler Nutzung sicherstellen (LWW), Produkt-Status serverseitig konsistent halten (direkter PATCH), und Spendenbetraege vollstaendig im Reporting sichtbar machen.
Output: Korrigierte sync.ts mit LWW, direkter PATCH-Aufruf in ProductList.tsx, 6. Report-Karte.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@server/src/routes/sync.ts
@server/src/routes/products.ts
@client/src/features/admin/products/ProductList.tsx
@client/src/features/admin/reports/MonthlyReport.tsx

<interfaces>
<!-- LWW-Pattern aus products.ts (Zeilen 49-63) — dieses Pattern muss in sync.ts repliziert werden -->
From server/src/routes/products.ts:
```typescript
// POST /products — Produkt anlegen (LWW-Upsert)
db.insert(products).values({...}).onConflictDoUpdate({
  target: products.id,
  set: {
    articleNumber: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.article_number ELSE ${products.articleNumber} END`,
    name: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.name ELSE ${products.name} END`,
    // ... alle Felder gleich
    updatedAt: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.updated_at ELSE ${products.updatedAt} END`,
  },
}).run();

// PATCH /products/:id/deactivate — Soft-Delete (Zeile 67-72)
fastify.patch('/products/:id/deactivate', async (request, reply) => {
  const { id } = request.params as { id: string };
  db.update(products).set({ active: false, updatedAt: Date.now() }).where(eq(products.id, id)).run();
  return reply.send({ ok: true });
});

// PATCH /products/:id/activate — Reaktivieren (Zeile 74-79)
fastify.patch('/products/:id/activate', async (request, reply) => {
  const { id } = request.params as { id: string };
  db.update(products).set({ active: true, updatedAt: Date.now() }).where(eq(products.id, id)).run();
  return reply.send({ ok: true });
});
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: LWW-Upsert in sync.ts (TD-01)</name>
  <files>server/src/routes/sync.ts</files>
  <read_first>
    - server/src/routes/sync.ts (vollstaendig — aktueller Code mit onConflictDoNothing auf Zeile 94)
    - server/src/routes/products.ts (Zeilen 49-63 — LWW-Pattern als Vorlage)
    - server/src/db/schema.ts (products-Tabelle mit updatedAt-Spalte)
  </read_first>
  <action>
**TD-01: LWW-Fix (Zeile 82-94 in sync.ts):**
Ersetze den Produkt-Insert-Block (Zeilen 82-94) im SALE_COMPLETE-Handler. Aendere Zeile 94 von:
```typescript
}).onConflictDoNothing().run();
```
zu einem `onConflictDoUpdate` mit exakt dem gleichen LWW-Pattern wie in products.ts (Zeilen 49-63). Alle Felder muessen mit `CASE WHEN excluded.updated_at > products.updatedAt` geschuetzt werden:
- articleNumber, name, category, purchasePrice, salePrice, vatRate, stock, active, updatedAt

WICHTIG: Im sync.ts Kontext sind die Insert-Values eingeschraenkt (category='', purchasePrice=0 etc.), aber das LWW-Pattern stellt sicher, dass nur neuere Daten gewinnen. Das ist korrekt — wenn das Produkt auf dem Server schon existiert mit echten Werten, gewinnen die alten (besseren) Werte weil deren updatedAt neuer ist.

Fuer stock: Im LWW-Block NICHT stock per LWW setzen, sondern stock im LWW-CASE auf `${products.stock}` belassen (bestehender Wert bleibt). Der Stock-Delta wird danach separat in Zeile 97-100 angewendet. Aendere also im set-Objekt:
```typescript
stock: sql`${products.stock}`, // Stock bleibt — Delta kommt in Zeile 97-100
```

NUR den LWW-Fix vornehmen. KEIN PRODUCT_TOGGLE-Handler in sync.ts — TD-02 wird ueber direkte PATCH-Aufrufe geloest (siehe Task 2).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit --project server/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "onConflictDoUpdate" server/src/routes/sync.ts (LWW-Pattern statt onConflictDoNothing)
    - grep -q "CASE WHEN" server/src/routes/sync.ts (LWW-Timestamp-Vergleich)
    - KEIN grep -q "onConflictDoNothing" server/src/routes/sync.ts fuer den Produkt-Insert (nur noch fuer Sales)
    - KEIN PRODUCT_TOGGLE in sync.ts
  </acceptance_criteria>
  <done>
    - Produkt-Upsert im SALE_COMPLETE-Handler nutzt onConflictDoUpdate mit Timestamp-LWW
    - Stock bleibt beim LWW unberuehrt (Delta-Update danach)
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: Direkter PATCH fuer Produkt-Toggle (TD-02)</name>
  <files>client/src/features/admin/products/ProductList.tsx</files>
  <read_first>
    - client/src/features/admin/products/ProductList.tsx (handleToggleActive auf Zeile 39-44)
    - client/src/db/index.ts (db und SHOP_ID Export)
    - server/src/routes/products.ts (Zeilen 67-79 — PATCH /deactivate und /activate Endpoints)
  </read_first>
  <action>
**TD-02: Direkter PATCH statt Outbox (per REQUIREMENTS.md):**

handleToggleActive in ProductList.tsx soll PATCH /api/products/:id/deactivate bzw. /activate aufrufen. Die Endpoints existieren bereits in server/src/routes/products.ts (Zeilen 67-79).

Aendere die handleToggleActive-Funktion (Zeile 39-44) zu:
```typescript
async function handleToggleActive(product: Product) {
  const newActive = !product.active;
  const now = Date.now();
  // Lokal in Dexie sofort aktualisieren (offline-faehig)
  await db.products.update(product.id, {
    active: newActive,
    updatedAt: now,
  });
  // Fire-and-forget PATCH an Server wenn online
  if (navigator.onLine) {
    const action = newActive ? 'activate' : 'deactivate';
    fetch(`/api/products/${product.id}/${action}`, { method: 'PATCH' }).catch(() => {});
  }
}
```

Kein Outbox-Eintrag, kein PRODUCT_TOGGLE — einfacher direkter PATCH. Wenn offline, wird nur Dexie lokal aktualisiert. Der Server bekommt den aktuellen Status beim naechsten Download-Sync (TD-03, Phase 5 Plan 02) oder wenn die Nutzerin spaeter online den Toggle nochmal betaetigt.

WICHTIG: Keine Aenderungen an client/src/db/schema.ts (kein PRODUCT_TOGGLE im OutboxEntry-Typ noetig). Keine Aenderungen an client/src/sync/engine.ts.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "PATCH" client/src/features/admin/products/ProductList.tsx (direkter Server-Aufruf)
    - grep -q "deactivate" client/src/features/admin/products/ProductList.tsx (Endpoint-Pfad)
    - grep -q "activate" client/src/features/admin/products/ProductList.tsx (Endpoint-Pfad)
    - grep -q "navigator.onLine" client/src/features/admin/products/ProductList.tsx (Online-Check)
    - KEIN grep -q "PRODUCT_TOGGLE" client/src/features/admin/products/ProductList.tsx
    - KEIN grep -q "outbox" client/src/features/admin/products/ProductList.tsx
  </acceptance_criteria>
  <done>
    - handleToggleActive aktualisiert Dexie lokal sofort
    - Wenn online: fire-and-forget PATCH an /api/products/:id/deactivate bzw. /activate
    - Kein Outbox-Mechanismus, kein PRODUCT_TOGGLE — einfacher direkter Ansatz
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 3: extra_donation_cents im Monatsbericht anzeigen (TD-04)</name>
  <files>client/src/features/admin/reports/MonthlyReport.tsx</files>
  <read_first>
    - client/src/features/admin/reports/MonthlyReport.tsx (vollstaendig — Grid auf Zeile 138-159, MonthlySummary Interface auf Zeile 12-19)
  </read_first>
  <action>
**TD-04: 6. Karte im Grid hinzufuegen (MonthlyReport.tsx Zeile 138-159):**

1. Grid-Klasse aendern (Zeile 138): von `sm:grid-cols-5` auf `sm:grid-cols-3 lg:grid-cols-6` damit 6 Karten auf grossen Bildschirmen in eine Reihe passen und auf mittleren in 3er-Reihen umbrechen.

2. Neue Karte NACH der bestehenden Spenden-Karte (nach Zeile 158, vor dem schliessenden `</div>`) einfuegen:
```tsx
<div className="bg-white rounded-xl shadow-sm p-6 flex flex-col items-center gap-1">
  <span className="text-xl font-bold text-amber-500">{formatEur(summary.extra_donation_cents)}</span>
  <span className="text-xs text-slate-500 text-center">Ueberzahlung</span>
</div>
```

Die Karte nutzt `text-amber-500` als Akzentfarbe (unterscheidet sich von den bestehenden: slate, sky, slate, emerald, green). Label "Ueberzahlung" beschreibt praezise was extra_donation_cents bedeutet: Kunden zahlen mehr als den Gesamtpreis, die Differenz ist eine zusaetzliche Spende.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -c "extra_donation_cents" client/src/features/admin/reports/MonthlyReport.tsx</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "extra_donation_cents" client/src/features/admin/reports/MonthlyReport.tsx (Wert wird gerendert)
    - grep -q "Ueberzahlung" client/src/features/admin/reports/MonthlyReport.tsx (Label sichtbar)
    - grep -q "lg:grid-cols-6" client/src/features/admin/reports/MonthlyReport.tsx (Grid fuer 6 Karten)
    - grep -q "text-amber-500" client/src/features/admin/reports/MonthlyReport.tsx (Akzentfarbe)
  </acceptance_criteria>
  <done>
    - extra_donation_cents wird als 6. Karte "Ueberzahlung" im Monatsbericht angezeigt
    - Grid passt sich fuer 6 Karten an (lg:grid-cols-6)
    - Farbe amber-500 unterscheidet sich visuell von den anderen Karten
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project server/tsconfig.json` — Server kompiliert fehlerfrei
- `npx tsc --noEmit --project client/tsconfig.json` — Client kompiliert fehlerfrei
- `grep "onConflictDoUpdate" server/src/routes/sync.ts` — LWW-Pattern vorhanden
- `grep "PATCH" client/src/features/admin/products/ProductList.tsx` — Direkter PATCH-Aufruf
- `grep "deactivate" client/src/features/admin/products/ProductList.tsx` — Endpoint vorhanden
- `grep "extra_donation_cents" client/src/features/admin/reports/MonthlyReport.tsx` — Report-Karte vorhanden
</verification>

<success_criteria>
1. sync.ts Produkt-Upsert nutzt onConflictDoUpdate mit CASE WHEN Timestamp-Vergleich (kein onConflictDoNothing mehr fuer Produkte)
2. Produkt-Deaktivierung in ProductList.tsx ruft direkt PATCH /api/products/:id/deactivate bzw. /activate auf (per REQUIREMENTS.md TD-02)
3. Monatsbericht zeigt extra_donation_cents als 6. Karte mit Label "Ueberzahlung"
4. TypeScript kompiliert fehlerfrei (Client + Server)
</success_criteria>

<output>
After completion, create `.planning/phases/05-tech-debt-fixes/05-01-SUMMARY.md`
</output>
