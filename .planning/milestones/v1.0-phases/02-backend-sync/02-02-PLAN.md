---
phase: 02-backend-sync
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - client/src/sync/engine.ts
  - client/src/sync/triggers.ts
  - client/src/main.tsx
autonomous: true
requirements:
  - OFF-03
  - OFF-04

must_haves:
  truths:
    - "Nach einem Offline-Verkauf werden ausstehende Outbox-Eintraege beim naechsten Online-Kontakt automatisch an POST /api/sync gesendet"
    - "Sync-Trigger feuern bei window.online-Event und bei document.visibilitychange (sichtbar + online)"
    - "Beim App-Start wird sofort geflusht wenn navigator.onLine true ist"
    - "Nach erfolgreichem Sync wird syncedAt auf dem Sale-Datensatz in Dexie gesetzt"
    - "Nach 5 fehlgeschlagenen Versuchen wird ein Outbox-Eintrag nicht mehr retried"
    - "Fehlgeschlagener Sync loescht keine Outbox-Eintraege — sie bleiben fuer den naechsten Trigger"
  artifacts:
    - path: "client/src/sync/engine.ts"
      provides: "flushOutbox() — liest Dexie-Outbox, sendet an /api/sync, markiert syncedAt"
      exports: ["flushOutbox"]
    - path: "client/src/sync/triggers.ts"
      provides: "registerSyncTriggers() — registriert online + visibilitychange Events"
      exports: ["registerSyncTriggers"]
    - path: "client/src/main.tsx"
      provides: "registerSyncTriggers() beim App-Start aufrufen"
  key_links:
    - from: "client/src/sync/engine.ts"
      to: "/api/sync"
      via: "fetch('/api/sync', { method: 'POST', body: JSON.stringify({ entries: pending }) })"
      pattern: "fetch.*api/sync"
    - from: "client/src/sync/engine.ts"
      to: "client/src/db/index.ts"
      via: "import { db } from '../db/index.js'"
      pattern: "db\\.outbox"
    - from: "client/src/sync/triggers.ts"
      to: "client/src/sync/engine.ts"
      via: "import { flushOutbox } from './engine.js'"
      pattern: "flushOutbox"
    - from: "client/src/main.tsx"
      to: "client/src/sync/triggers.ts"
      via: "registerSyncTriggers() vor createRoot().render()"
      pattern: "registerSyncTriggers"
---

<objective>
Client-seitige Sync-Engine implementieren: flushOutbox() liest ausstehende Outbox-Eintraege aus Dexie, sendet sie batched an POST /api/sync und markiert erfolgreiche Eintraege. Triggers registrieren die Engine auf window.online und visibilitychange — iOS-kompatibel, kein Background Sync API.

Purpose: Schliesst die Sync-Schleife. Nach diesem Plan landen Offline-Verkaeufe automatisch auf dem Server sobald das iPad wieder online ist.
Output: Zwei neue Module (engine.ts, triggers.ts) und ein Einzeiler in main.tsx. Kein neuer UI-Code.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-backend-sync/02-RESEARCH.md
@.planning/phases/02-backend-sync/02-01-SUMMARY.md

<interfaces>
<!-- Vorhandene Dexie-Typen aus client/src/db/schema.ts -->

  OutboxEntry:
    id?: number           (auto-increment, vorhanden nach Dexie-Insert)
    operation: 'SALE_COMPLETE' | 'STOCK_ADJUST'
    payload: unknown      (bei SALE_COMPLETE: Sale-Objekt mit id: string)
    shopId: string
    createdAt: number
    attempts: number

  Sale:
    id: string
    syncedAt?: number     (undefined = ungesynct)

<!-- Dexie-DB-API aus client/src/db/index.ts -->

  export const db: FairstandDB
  export const SHOP_ID: string

  // Relevante Tabellen:
  db.outbox: EntityTable<OutboxEntry, 'id'>
  db.sales: EntityTable<Sale, 'id'>

  // Muster fuer "alle ungesyncten Outbox-Eintraege":
  db.outbox.where('createdAt').aboveOrEqual(0).filter(e => e.attempts < 5).toArray()

  // Muster fuer "syncedAt setzen":
  db.sales.update(saleId, { syncedAt: Date.now() })

  // Muster fuer "Outbox-Eintraege loeschen":
  db.outbox.bulkDelete(ids)

  // Muster fuer "attempts erhoehen":
  db.outbox.update(id, { attempts: entry.attempts + 1 })

<!-- POST /api/sync Request/Response (aus Plan 02-01) -->

  Request-Body: { entries: OutboxEntry[] }
  Response:     { processed: number, errors: Array<{ index: number, message: string }> }
  Idempotent:   Gleiche entries-id kann mehrfach gesendet werden

<!-- iOS/Safari-Constraint (aus RESEARCH.md) -->
  Background Sync API: nicht verfuegbar auf iOS
  Zuverlassige Trigger: window.online + document.visibilitychange
  Beides zusammen deckt: Netz-Reconnect aus Hintergrund + Zurueckkehren in App
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync-Engine (engine.ts) + Triggers (triggers.ts)</name>
  <files>client/src/sync/engine.ts, client/src/sync/triggers.ts</files>

  <read_first>
    - client/src/db/index.ts (db-Export, SHOP_ID, EntityTable-API)
    - client/src/db/schema.ts (OutboxEntry und Sale Interfaces — Typen fuer den Flush)
    - client/src/features/pos/useSaleComplete.ts (zeigt wie OutboxEntry geschrieben wird — gleiche Struktur beim Lesen)
  </read_first>

  <action>
Verzeichnis client/src/sync/ anlegen und zwei Dateien erstellen:

1. client/src/sync/engine.ts:

```typescript
import { db } from '../db/index.js';
import type { OutboxEntry } from '../db/index.js';

let flushing = false;

export async function flushOutbox(): Promise<void> {
  // Guard gegen parallele Ausfuehrung (Doppel-Trigger bei schnellem Reconnect)
  if (flushing) return;
  flushing = true;

  try {
    const pending = await db.outbox
      .where('createdAt')
      .aboveOrEqual(0)
      .filter((e: OutboxEntry) => (e.attempts ?? 0) < 5)
      .toArray();

    if (pending.length === 0) return;

    let res: Response;
    try {
      res = await fetch('/api/sync', {
        method: 'POST',
        headers: { 'Content-Type': 'application/json' },
        body: JSON.stringify({ entries: pending }),
      });
    } catch {
      // Netzwerkfehler (z.B. DNS nicht erreichbar) — Attempts erhoehen
      for (const entry of pending) {
        await db.outbox.update(entry.id!, { attempts: (entry.attempts ?? 0) + 1 });
      }
      return;
    }

    if (!res.ok) {
      // Server-Fehler (5xx) — Attempts erhoehen, naechster Trigger retried
      for (const entry of pending) {
        await db.outbox.update(entry.id!, { attempts: (entry.attempts ?? 0) + 1 });
      }
      return;
    }

    // Erfolg: Outbox-Eintraege loeschen
    const ids = pending.map((e: OutboxEntry) => e.id!);
    await db.outbox.bulkDelete(ids);

    // Sale.syncedAt setzen fuer alle SALE_COMPLETE-Eintraege
    const saleEntries = pending.filter((e: OutboxEntry) => e.operation === 'SALE_COMPLETE');
    for (const entry of saleEntries) {
      const payload = entry.payload as { id?: string };
      if (payload?.id) {
        await db.sales.update(payload.id, { syncedAt: Date.now() });
      }
    }
  } finally {
    flushing = false;
  }
}
```

2. client/src/sync/triggers.ts:

```typescript
import { flushOutbox } from './engine.js';

export function registerSyncTriggers(): void {
  // Sofortiger Flush beim App-Start wenn bereits online (Pitfall 3 aus RESEARCH.md)
  if (navigator.onLine) {
    void flushOutbox();
  }

  // Trigger 1: Netz-Reconnect
  window.addEventListener('online', () => {
    void flushOutbox();
  });

  // Trigger 2: Zurueckkehren in die App (iOS: online-Event fehlt oft nach Hintergrund)
  document.addEventListener('visibilitychange', () => {
    if (document.visibilityState === 'visible' && navigator.onLine) {
      void flushOutbox();
    }
  });
}
```

Kein Timer, kein setInterval — ausschliesslich Event-basiert (iOS-Anforderung aus RESEARCH.md).
Der flushing-Guard in engine.ts verhindert parallele Ausfuehrung wenn beide Events gleichzeitig feuern.
  </action>

  <verify>
    <automated>ls /Users/simonluthe/Documents/fairstand/client/src/sync/engine.ts /Users/simonluthe/Documents/fairstand/client/src/sync/triggers.ts &amp;&amp; echo "Beide Dateien vorhanden" &amp;&amp; grep -q "export async function flushOutbox" /Users/simonluthe/Documents/fairstand/client/src/sync/engine.ts &amp;&amp; grep -q "export function registerSyncTriggers" /Users/simonluthe/Documents/fairstand/client/src/sync/triggers.ts &amp;&amp; echo "Exports korrekt"</automated>
  </verify>

  <acceptance_criteria>
    - client/src/sync/engine.ts existiert und exportiert flushOutbox
    - client/src/sync/triggers.ts existiert und exportiert registerSyncTriggers
    - engine.ts liest db.outbox mit attempts-Filter (grep "attempts" client/src/sync/engine.ts gibt Treffer)
    - engine.ts verwendet bulkDelete nach Erfolg (grep "bulkDelete" client/src/sync/engine.ts gibt Treffer)
    - engine.ts setzt syncedAt nach Erfolg (grep "syncedAt" client/src/sync/engine.ts gibt Treffer)
    - triggers.ts registriert 'online' und 'visibilitychange' (grep -c "addEventListener" client/src/sync/triggers.ts ergibt 2)
    - triggers.ts prueft navigator.onLine beim Start (grep "navigator.onLine" client/src/sync/triggers.ts gibt Treffer)
  </acceptance_criteria>

  <done>Sync-Engine und Triggers als eigenstaendige Module implementiert. flushOutbox() kann unabhaengig aufgerufen werden. registerSyncTriggers() registriert alle iOS-kompatiblen Trigger.</done>
</task>

<task type="auto">
  <name>Task 2: Sync-Triggers in main.tsx einbinden + TypeScript-Build pruefen</name>
  <files>client/src/main.tsx</files>

  <read_first>
    - client/src/main.tsx (aktueller Inhalt — dort registerSyncTriggers() einfuegen)
    - client/src/sync/triggers.ts (aus Task 1 — Import-Pfad verifizieren)
    - client/package.json (TypeScript-Build-Befehl)
  </read_first>

  <action>
client/src/main.tsx anpassen — registerSyncTriggers() VOR dem React-Render aufrufen:

```typescript
import { StrictMode } from 'react'
import { createRoot } from 'react-dom/client'
import App from './App.tsx'
import { registerSyncTriggers } from './sync/triggers.js'

// Sync-Engine initialisieren — bevor React gerendert wird
// Registriert online + visibilitychange Event-Listener (iOS-kompatibel)
registerSyncTriggers();

createRoot(document.getElementById('root')!).render(
  <StrictMode>
    <App />
  </StrictMode>,
)
```

Der Aufruf steht bewusst VOR createRoot().render() — Sync ist infrastruktureller Code, nicht UI-Code.
Kein React-State involviert. Keine Aufraeumung noetig (Event-Listener laufen fuer die gesamte App-Lebensdauer).

Anschliessend TypeScript-Build fuer den Client ausfuehren:
```bash
cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit
```

Falls TypeScript-Fehler auftreten (z.B. fehlende Typen fuer OutboxEntry.id als number | undefined):
- engine.ts verwendet e.id! mit Non-Null-Assertion — korrekt, da bulkDelete nur nach erfolgreichem Fetch aufgerufen wird
- Kein void-Operator-Fehler erwartet, da flushOutbox als Promise<void> typisiert ist
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TypeScript-Build Client OK"</automated>
  </verify>

  <acceptance_criteria>
    - client/src/main.tsx importiert registerSyncTriggers (grep "registerSyncTriggers" client/src/main.tsx gibt zwei Treffer: import + Aufruf)
    - registerSyncTriggers() steht VOR createRoot().render() in main.tsx
    - npx tsc --noEmit im client/-Verzeichnis gibt keinen Fehler aus
    - npx tsc --noEmit im server/-Verzeichnis gibt keinen Fehler aus (Gesamtsystem-Check)
  </acceptance_criteria>

  <done>Sync-Engine wird beim App-Start initialisiert. TypeScript-Build fuer Client und Server fehlerfrei. Phase 2 vollstaendig implementiert.</done>
</task>

</tasks>

<verification>
Gesamtverifikation Phase 02-02 (End-to-End Sync):

1. TypeScript fehlerfrei: cd client && npx tsc --noEmit && cd ../server && npx tsc --noEmit
2. Sync-Engine-Dateien vorhanden: ls client/src/sync/
3. main.tsx registriert Triggers: grep "registerSyncTriggers" client/src/main.tsx
4. Manueller End-to-End-Test (optional, erfordert laufenden Server):
   a. Server starten: cd server && npm run dev
   b. App im Browser oeffnen (Vite Dev Server)
   c. Offline gehen (Chrome DevTools Network: Offline)
   d. Verkauf abschliessen — OutboxEntry in Dexie erscheint (DevTools > Application > IndexedDB)
   e. Online gehen — flushOutbox() feuert, OutboxEntry verschwindet aus Dexie
   f. Server-Log zeigt: processed: 1
</verification>

<success_criteria>
- client/src/sync/engine.ts und client/src/sync/triggers.ts existieren und sind fehlerfrei typisiert
- main.tsx ruft registerSyncTriggers() vor dem React-Render auf
- Nach erfolgreichem Sync: Outbox-Eintraege aus Dexie geloescht, Sale.syncedAt gesetzt
- Bei Netzwerkfehler: Outbox-Eintraege bleiben erhalten, attempts wird inkrementiert
- Kein Background Sync API — ausschliesslich online + visibilitychange Events (iOS-kompatibel)
</success_criteria>

<output>
Nach Abschluss .planning/phases/02-backend-sync/02-02-SUMMARY.md erstellen.
</output>
