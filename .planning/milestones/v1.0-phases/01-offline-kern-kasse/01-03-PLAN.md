---
phase: 01-offline-kern-kasse
plan: 03
type: execute
wave: 3
depends_on:
  - 01-01
  - 01-02
files_modified:
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/features/pos/CartPanel.tsx
  - client/src/features/pos/NumPad.tsx
  - client/src/features/pos/PaymentFlow.tsx
  - client/src/features/pos/SaleSummary.tsx
  - client/src/features/pos/useSaleComplete.ts
  - client/src/features/pos/useCart.ts
  - client/src/features/pos/POSScreen.tsx
  - client/src/App.tsx
autonomous: false
requirements:
  - POS-01
  - POS-02
  - POS-03
  - POS-04
  - POS-05
  - POS-06
  - POS-07
  - UX-01
  - UX-02
  - UX-03
  - UX-04

must_haves:
  truths:
    - "Mitarbeiterin sieht Artikel-Grid mit aktiven Produkten (Name + Preis), gefiltert nach Kategorie-Tabs"
    - "Antippen eines Artikels fügt ihn zum Warenkorb hinzu; Warenkorb slide-in von rechts"
    - "Warenkorb zeigt Einzelpreise, Menge (+/- + Direkteingabe) und Gesamtsumme"
    - "Bezahlt-Betrag wird per Numpad eingegeben; Differenz wird live berechnet"
    - "Mitarbeiterin tippt Wechselgeld-Betrag; Rest wird automatisch als Spende angezeigt"
    - "Verkaufsabschluss schreibt Sale, Bestandsänderung und OutboxEntry atomar in eine Dexie-Transaktion"
    - "Nach Abschluss: Zusammenfassung mit Umsatz, Spende, Wechselgeld und 'Nächster Kunde'-Button"
    - "Alle Tap-Targets mindestens 44×44px"
  artifacts:
    - path: "client/src/features/pos/useSaleComplete.ts"
      provides: "Atomare Dexie-Transaktion: Sale + Bestandsreduzierung + OutboxEntry"
      contains: "db.transaction('rw'"
    - path: "client/src/features/pos/useCart.ts"
      provides: "React-State für Warenkorb mit addItem, removeItem, updateQuantity, clear"
      contains: "CartItem"
    - path: "client/src/features/pos/ArticleGrid.tsx"
      provides: "Reaktives Produkt-Grid via useLiveQuery, Kategorie-Tabs"
      contains: "useLiveQuery"
    - path: "client/src/features/pos/PaymentFlow.tsx"
      provides: "Bezahlt-Betrag-Eingabe, Differenzberechnung, Wechselgeld/Spende-Flow"
      contains: "donationCents"
    - path: "client/src/features/pos/SaleSummary.tsx"
      provides: "Verkaufs-Zusammenfassung mit 'Nächster Kunde'-Button"
      contains: "Nächster Kunde"
  key_links:
    - from: "client/src/features/pos/ArticleGrid.tsx"
      to: "client/src/db/index.ts"
      via: "useLiveQuery(() => db.products.where('[shopId+active]').equals([SHOP_ID, 1]).toArray())"
      pattern: "useLiveQuery"
    - from: "client/src/features/pos/useSaleComplete.ts"
      to: "client/src/db/index.ts"
      via: "db.transaction('rw', [db.sales, db.products, db.outbox], ...)"
      pattern: "db.transaction"
    - from: "client/src/features/pos/POSScreen.tsx"
      to: "client/src/App.tsx"
      via: "App rendert POSScreen wenn state === 'unlocked'"
      pattern: "POSScreen"
---

<objective>
Vollständige Kassen-UI implementieren: Artikel-Grid mit Kategorie-Tabs, Slide-In-Warenkorb, Numpad-Betragseingabe, Wechselgeld/Spende-Flow, atomare Verkaufstransaktion und Zusammenfassungsscreen.

Purpose: Das ist der eigentliche Kern von Phase 1 — die Kasse, die am ersten Gottesdienst einsetzbar sein muss. Alle vorherigen Plans haben diesen Screen vorbereitet.
Output: Vollständig funktionierende Offline-Kasse auf dem iPad — artikel antippen, bezahlen, Wechselgeld und Spende differenzieren, abschließen.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-offline-kern-kasse/01-CONTEXT.md
@.planning/phases/01-offline-kern-kasse/01-RESEARCH.md
@.planning/phases/01-offline-kern-kasse/01-01-SUMMARY.md
@.planning/phases/01-offline-kern-kasse/01-02-SUMMARY.md

<interfaces>
<!-- Aus Plan 01-02 — diese Typen und Funktionen sind verfügbar -->

From client/src/db/schema.ts:
```typescript
export interface Product {
  id: string;
  shopId: string;
  articleNumber: string;
  name: string;
  category: string;
  purchasePrice: number;  // Cent-Integer
  salePrice: number;      // Cent-Integer
  vatRate: number;
  stock: number;
  active: boolean;
  updatedAt: number;
}

export interface SaleItem {
  productId: string;
  articleNumber: string;
  name: string;
  salePrice: number;  // Cent-Integer, Snapshot
  quantity: number;
}

export type CartItem = SaleItem;

export interface Sale {
  id: string; shopId: string; items: SaleItem[];
  totalCents: number; paidCents: number; changeCents: number; donationCents: number;
  createdAt: number; syncedAt?: number;
}

export interface OutboxEntry {
  id?: number;
  operation: 'SALE_COMPLETE' | 'STOCK_ADJUST';
  payload: unknown; shopId: string; createdAt: number; attempts: number;
}
```

From client/src/db/index.ts:
```typescript
export const db: FairstandDB;
export const SHOP_ID: string; // 'st-secundus-hennstedt'
```

Dexie useLiveQuery (Dexie 4.x — React-Integration):
```typescript
import { useLiveQuery } from 'dexie-react-hooks';
// ODER: import { useLiveQuery } from 'dexie'; // ab Dexie 4
```

Geldbetragsanzeige (aus RESEARCH.md Pattern 3):
```typescript
const formatEur = (cents: number) =>
  new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(cents / 100);
// → "7,89 €"
```

Atomare Transaktion (RESEARCH.md Pattern 2):
```typescript
await db.transaction('rw', [db.sales, db.products, db.outbox], async () => {
  await db.sales.add(sale);
  for (const item of items) {
    await db.products.where('id').equals(item.productId)
      .modify(p => { p.stock -= item.quantity; p.updatedAt = Date.now(); });
  }
  await db.outbox.add({ operation: 'SALE_COMPLETE', payload: sale, shopId, createdAt: Date.now(), attempts: 0 });
});
```

Tailwind-Farben (RESEARCH.md Pattern 6):
- sky-50: Seitenhintergrund
- sky-400: Primäre Aktions-Buttons, aktiver Tab
- sky-500: Hover-State
- sky-700: Text auf hellen Flächen
- slate-800: Artikel-Namen, Haupttext
- slate-500: Sekundärer Text
- emerald-500: Erfolgs-Feedback nach Abschluss
- rose-500: Fehler, Storno

Touch-Targets: min-h-[44px] min-w-[44px]; primäre Actions h-14 (56px)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Warenkorb-State (useCart) und atomare Verkaufstransaktion (useSaleComplete) implementieren</name>
  <files>
    client/src/features/pos/useCart.ts,
    client/src/features/pos/useSaleComplete.ts
  </files>
  <read_first>
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-RESEARCH.md (Pattern 2: Atomare Transaktion, Pattern 3: Cent-Integer)
    - /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts (CartItem, SaleItem, Sale, OutboxEntry)
    - /Users/simonluthe/Documents/fairstand/client/src/db/index.ts (db, SHOP_ID)
  </read_first>
  <behavior>
    useCart-Hook:
    - addItem(product): Wenn Artikel bereits im Warenkorb → quantity++; sonst neues CartItem hinzufügen
    - removeItem(productId): Entfernt Artikel komplett aus Warenkorb
    - updateQuantity(productId, quantity): Setzt Menge; bei quantity <= 0 → Artikel entfernen
    - clear(): Leert Warenkorb
    - total: number — Summe aller (salePrice * quantity) in Cent-Integer
    - items: CartItem[]

    completeSale(items, paidCents, changeCents):
    - donationCents = paidCents - total - changeCents
    - donationCents darf nicht negativ sein (Validation)
    - Schreibt in EINER Dexie-Transaktion: Sale, Bestandsreduzierung (als Delta), OutboxEntry
    - Gibt das gespeicherte Sale-Objekt zurück
    - Wirft Fehler wenn paidCents < totalCents
  </behavior>
  <action>
**client/src/features/pos/useCart.ts** — React-Hook mit useReducer:

Definiere CartAction-Typen: ADD_ITEM, REMOVE_ITEM, UPDATE_QUANTITY, CLEAR.
CartState: { items: CartItem[] }.

Berechne `total` als berechneter Wert: `items.reduce((sum, i) => sum + i.salePrice * i.quantity, 0)`.

WICHTIG: Preis im CartItem ist ein Snapshot des salePrice zum Zeitpunkt des Hinzufügens — nie nachträglich aus DB aktualisieren. Verhindert Preisänderungen mid-Sale.

Exportiere als Hook: `export function useCart(): { items: CartItem[], total: number, addItem, removeItem, updateQuantity, clear }`.

**client/src/features/pos/useSaleComplete.ts** — Exakt nach RESEARCH.md Pattern 2:

```typescript
import { db, SHOP_ID, type CartItem, type Sale } from '../../db/index.js';

export async function completeSale(
  items: CartItem[],
  paidCents: number,
  changeCents: number
): Promise<Sale> {
  const totalCents = items.reduce((sum, i) => sum + i.salePrice * i.quantity, 0);

  if (paidCents < totalCents) {
    throw new Error(`Bezahlt (${paidCents}) ist weniger als Gesamtpreis (${totalCents})`);
  }

  const donationCents = paidCents - totalCents - changeCents;

  if (donationCents < 0) {
    throw new Error(`Wechselgeld (${changeCents}) größer als Differenz (${paidCents - totalCents})`);
  }

  const sale: Sale = {
    id: crypto.randomUUID(),
    shopId: SHOP_ID,
    items,
    totalCents,
    paidCents,
    changeCents,
    donationCents,
    createdAt: Date.now(),
  };

  await db.transaction('rw', [db.sales, db.products, db.outbox], async () => {
    await db.sales.add(sale);

    for (const item of items) {
      await db.products
        .where('id').equals(item.productId)
        .modify((p: { stock: number; updatedAt: number }) => {
          p.stock -= item.quantity;
          p.updatedAt = Date.now();
        });
    }

    await db.outbox.add({
      operation: 'SALE_COMPLETE',
      payload: sale,
      shopId: SHOP_ID,
      createdAt: Date.now(),
      attempts: 0,
    });
  });

  return sale;
}
```

Nach Implementierung: `npm run build` in client/ muss ohne TypeScript-Fehler durchlaufen.
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/fairstand/client &amp;&amp; npm run build 2>&amp;1 | grep -E "^.*error TS" | wc -l | tr -d ' '
    </automated>
  </verify>
  <done>
    - useCart exportiert addItem, removeItem, updateQuantity, clear, items, total
    - completeSale ist eine async Funktion die eine Dexie-Transaktion über 3 Stores ausführt
    - Negative donationCents wird mit Fehler abgefangen
    - paidCents < totalCents wird mit Fehler abgefangen
    - TypeScript-Build ohne Fehler
  </done>
  <acceptance_criteria>
    - grep -q "db.transaction('rw'" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useSaleComplete.ts
    - grep -q "donationCents" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useSaleComplete.ts
    - grep -q "p.stock -= item.quantity" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useSaleComplete.ts
    - grep -q "SALE_COMPLETE" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useSaleComplete.ts
    - grep -q "useReducer\|useState" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useCart.ts
    - grep -q "salePrice \* i.quantity\|salePrice\*i.quantity" /Users/simonluthe/Documents/fairstand/client/src/features/pos/useCart.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Kassen-UI — ArticleGrid, Warenkorb, Numpad, Bezahl-Flow, Zusammenfassung und POSScreen zusammenbauen</name>
  <files>
    client/src/features/pos/ArticleGrid.tsx,
    client/src/features/pos/CartPanel.tsx,
    client/src/features/pos/NumPad.tsx,
    client/src/features/pos/PaymentFlow.tsx,
    client/src/features/pos/SaleSummary.tsx,
    client/src/features/pos/POSScreen.tsx,
    client/src/App.tsx
  </files>
  <read_first>
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-CONTEXT.md (Decisions: Kassen-Layout, Bezahl- und Spenden-Flow, alle UI-Entscheidungen)
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-RESEARCH.md (Pattern 6 Tailwind-Farben, UX-Anforderungen)
    - /Users/simonluthe/Documents/fairstand/client/src/features/pos/useCart.ts (verfügbare Hook-API)
    - /Users/simonluthe/Documents/fairstand/client/src/features/pos/useSaleComplete.ts (completeSale Signatur)
    - /Users/simonluthe/Documents/fairstand/client/src/db/index.ts (db, SHOP_ID)
    - /Users/simonluthe/Documents/fairstand/client/src/App.tsx (aktueller Zustand)
  </read_first>
  <action>
Implementiere alle Kassen-UI-Komponenten. Alle Entscheidungen aus CONTEXT.md sind verbindlich:

**Geldbetrags-Hilfsfunktion** (in einer separaten utils.ts oder oben in POSScreen):
```typescript
export const formatEur = (cents: number) =>
  new Intl.NumberFormat('de-DE', { style: 'currency', currency: 'EUR' }).format(cents / 100);
```

**client/src/features/pos/ArticleGrid.tsx** (per D aus CONTEXT.md):
- `useLiveQuery` aus dexie: `db.products.where('[shopId+active]').equals([SHOP_ID, 1]).toArray()`
- Horizontale Kategorie-Tabs oben: "Alle" als erster Tab, danach alphabetisch sortierte unique Kategorien aus den geladenen Produkten
- Aktiver Tab: `sky-400` Text/Unterstrich, inaktiver Tab: `sky-100`/`sky-700`
- Produkt-Kacheln: dynamisch fließend mit `flex flex-wrap gap-3` oder `grid grid-cols-[repeat(auto-fill,minmax(140px,1fr))]`
- Jede Kachel: weißer Hintergrund, shadow-sm, rounded-xl, min-h-[80px], Name (slate-800, font-medium) + Preis (sky-700, formatEur)
- Kein Bild — nur Name + Preis (Produktbilder sind v2-Feature, laut DEFERRED)
- Tap: `onPointerDown` → `onAddToCart(product)` aufrufen
- Bei leerem Produktliste: "Keine Produkte verfügbar"

```tsx
interface ArticleGridProps {
  onAddToCart: (product: Product) => void;
}
```

**client/src/features/pos/CartPanel.tsx** (per CONTEXT.md: Slide-In von rechts, liegt über Grid):
- Position: `fixed inset-y-0 right-0 w-80 md:w-96 bg-white shadow-2xl z-50`
- Animation: Tailwind `transform translate-x-full` (geschlossen) → `translate-x-0` (offen) mit `transition-transform duration-300`
- Header: "Warenkorb" + Schließen-Button (X, min 44×44px)
- Artikel-Liste: Jede Zeile zeigt Name, Preis, Menge (+/- Buttons + `<input type="number" inputMode="numeric">` für Direkteingabe), Entfernen-Button (rose-500)
- +/- Buttons: h-9 w-9 min, bg-sky-100, rounded-full
- Direkteingabe: `onPointerDown` auf Zahl öffnet Input-Focus
- Footer: Gesamtsumme (formatEur(total)), "Bezahlen"-Button (bg-sky-400, h-14, full width, slate-800 text-xl)
- "Bezahlen"-Button nur aktiv wenn items.length > 0

```tsx
interface CartPanelProps {
  isOpen: boolean;
  items: CartItem[];
  total: number;
  onClose: () => void;
  onUpdateQuantity: (productId: string, quantity: number) => void;
  onRemoveItem: (productId: string) => void;
  onCheckout: () => void;
}
```

**client/src/features/pos/NumPad.tsx** (per CONTEXT.md: Taschenrechner-Style):
- Taschenrechner-Grid: Ziffern 1-9, Löschen (⌫), 0, Dezimalpunkt (,), Bestätigen (✓)
- State: string-Buffer für eingetippte Ziffern; bei Bestätigen → in Cent konvertieren und `onConfirm(cents)` aufrufen
- Dezimalkomma: Europäisch (Komma statt Punkt); maximal 2 Nachkommastellen
- Konvertierung: `parseFloat(buffer.replace(',', '.')) * 100` → `Math.round()` → Cent-Integer
- Quick-Buttons oben: "5 €", "10 €", "20 €", "50 €" (aus Settings, in Phase 1 hardcoded als Standard: [500, 1000, 2000, 5000] Cent)
- Quick-Button-Tap → setzt Buffer direkt und ruft onConfirm auf
- Anzeige: großes Display oben mit aktuellem Buffer-Wert (mindestens 2rem Schriftgröße)
- Alle Ziffern-Buttons: h-16 w-full, bg-white, border border-sky-100, rounded-lg, text-2xl
- Löschen: bg-rose-50, text-rose-600
- Bestätigen: bg-sky-400, text-white, text-2xl

```tsx
interface NumPadProps {
  label: string;  // z.B. "Bezahlter Betrag"
  onConfirm: (cents: number) => void;
  onCancel: () => void;
}
```

**client/src/features/pos/PaymentFlow.tsx** (per CONTEXT.md: Bezahl- & Spenden-Flow):
Drei interne Schritte:
1. **Bezahlt-Eingabe**: NumPad anzeigen. Nach Bestätigung → paidCents setzen
2. **Wechselgeld-Eingabe**: Zeigt "Gesamtpreis: X €", "Bezahlt: Y €", "Differenz: Z €". Numpad für Wechselgeld-Betrag. Max-Wert = Differenz (paidCents - totalCents). Spende = Differenz - Wechselgeld live berechnen und anzeigen (emerald-500). "Abschließen"-Button.
3. Schritt 3 → `onComplete(paidCents, changeCents)` aufrufen

WICHTIG: Alle Beträge intern als Cent-Integer (Pattern 3). Anzeige immer via formatEur().
Wenn paidCents < totalCents: Fehlermeldung anzeigen "Bezahlter Betrag zu gering", Schritt 1 zurücksetzen.

```tsx
interface PaymentFlowProps {
  totalCents: number;
  onComplete: (paidCents: number, changeCents: number) => void;
  onCancel: () => void;
}
```

**client/src/features/pos/SaleSummary.tsx** (per CONTEXT.md: Zusammenfassung nach Abschluss):
- Erfolgs-Icon (großer Haken, emerald-500)
- Zeigt: Umsatz (totalCents), Wechselgeld (changeCents), Spende (donationCents) — alle via formatEur()
- "Nächster Kunde"-Button (h-14, bg-sky-400, full width) → `onNext()` aufrufen
- "Korrigieren"-Button (h-12, outline/rose-100, full width) → `onCorrect()` aufrufen (in Phase 1: öffnet Warenkorb mit gleichen Artikeln wieder; vollständige Storno-Logik ist DEFERRED laut CONTEXT.md)
- Kein Bon-Druck, keine externen Systeme (Out of Scope)

```tsx
interface SaleSummaryProps {
  sale: Sale;
  onNext: () => void;
  onCorrect: () => void;
}
```

**client/src/features/pos/POSScreen.tsx** — Orchestriert alle Komponenten:
```typescript
type POSView = 'pos' | 'payment' | 'summary';
```
- `pos`: ArticleGrid + CartPanel (slide-in state). Warenkorb-Button in Header (Anzahl-Badge sky-400)
- `payment`: PaymentFlow (fullscreen)
- `summary`: SaleSummary (fullscreen)

POSScreen verwaltet:
- `useCart()` Hook
- `currentView: POSView` State
- `lastSale: Sale | null` State (für SaleSummary)
- `isCartOpen: boolean` State

Ablauf:
1. Grid zeigt Produkte, Cart-Button öffnet Panel
2. "Bezahlen" im CartPanel → view: 'payment'
3. PaymentFlow.onComplete(paid, change) → completeSale aufrufen → view: 'summary'
4. SaleSummary.onNext() → cart.clear() + view: 'pos'
5. SaleSummary.onCorrect() → cart mit gleichen Artikeln neu befüllen + view: 'pos' (einfache Korrektur)

Fehlerbehandlung: try/catch um completeSale, bei Fehler → toast-ähnliche Fehlermeldung (einfaches State-Banner) + view bleibt 'payment'.

**client/src/App.tsx** — Ersetze Placeholder "Kassen-UI wird implementiert":
```tsx
// In unlocked-Branch:
return (
  <div className="min-h-screen bg-sky-50">
    <POSScreen onLock={lock} />
  </div>
);
```
POSScreen erhält `onLock` Callback für den Sperren-Button im Header.

Nach Implementierung: `npm run build` ohne TypeScript-Fehler.
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/fairstand/client &amp;&amp; npm run build 2>&amp;1 | grep -c "error TS"
    </automated>
  </verify>
  <done>
    - ArticleGrid lädt Produkte via useLiveQuery, Kategorie-Tabs funktionieren, "Alle" Tab vorhanden
    - CartPanel slide-in von rechts, +/- Buttons und Direkteingabe für Menge
    - NumPad mit Quick-Buttons (5€, 10€, 20€, 50€) und Cent-Integer-Konvertierung
    - PaymentFlow zeigt Differenz live und Spende = Differenz - Wechselgeld
    - SaleSummary zeigt Umsatz/Wechselgeld/Spende mit "Nächster Kunde"-Button
    - completeSale schreibt atomar in Dexie (Sale + Bestandsdelta + OutboxEntry)
    - npm run build läuft ohne TypeScript-Fehler
  </done>
  <acceptance_criteria>
    - grep -q "useLiveQuery" /Users/simonluthe/Documents/fairstand/client/src/features/pos/ArticleGrid.tsx
    - grep -q "shopId+active\|shopId\+active" /Users/simonluthe/Documents/fairstand/client/src/features/pos/ArticleGrid.tsx
    - grep -q "translate-x" /Users/simonluthe/Documents/fairstand/client/src/features/pos/CartPanel.tsx
    - grep -q "inputMode\|inputmode" /Users/simonluthe/Documents/fairstand/client/src/features/pos/CartPanel.tsx
    - grep -q "Quick\|quick\|5 €\|10 €\|5€\|10€" /Users/simonluthe/Documents/fairstand/client/src/features/pos/NumPad.tsx
    - grep -q "donationCents" /Users/simonluthe/Documents/fairstand/client/src/features/pos/PaymentFlow.tsx
    - grep -q "Nächster Kunde" /Users/simonluthe/Documents/fairstand/client/src/features/pos/SaleSummary.tsx
    - grep -q "POSScreen" /Users/simonluthe/Documents/fairstand/client/src/App.tsx
    - cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | grep "error TS" | wc -l | tr -d ' ' | grep "^0$"
  </acceptance_criteria>
</task>

</tasks>

<verification>
Manuelle Verifikation auf dem iPad (oder Chrome DevTools mit iPad-Simulation):
1. App öffnen → PIN-Setup erscheint (6 Ziffern)
2. PIN einrichten → Kassen-Ansicht erscheint mit Artikel-Grid
3. Artikel antippen → Warenkorb öffnet sich von rechts
4. Menge ändern (+/-) → Gesamtsumme aktualisiert sich
5. "Bezahlen" → Numpad erscheint
6. Betrag eingeben (z.B. 10€) → Differenz wird angezeigt
7. Wechselgeld eingeben (z.B. 0,11€) → Spende = Rest
8. Abschließen → Zusammenfassung erscheint
9. "Nächster Kunde" → Grid zurück, Warenkorb leer
10. In Chrome DevTools: Application → IndexedDB → fairstand-db → sales → Verkauf vorhanden

Automated Build-Check:
```bash
cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | grep "error TS" | wc -l
# → muss 0 sein
```
</verification>

<success_criteria>
- ArticleGrid lädt aktive Produkte via useLiveQuery (reaktiv, offline-fähig)
- Kategorie-Tabs filtern korrekt; "Alle" zeigt alle aktiven Produkte
- Slide-In-Warenkorb öffnet von rechts; +/-, Direkteingabe und Entfernen funktionieren
- Numpad mit Quick-Buttons (5€, 10€, 20€, 50€) gibt Cent-Integer-Werte zurück
- Spende = paidCents - totalCents - changeCents (live berechnet, nie negativ)
- completeSale schreibt Sale + Bestandsdelta + OutboxEntry in einer Dexie-Transaktion
- SaleSummary zeigt Umsatz/Wechselgeld/Spende; "Nächster Kunde" setzt State zurück
- Alle Tap-Targets mindestens 44×44px (primäre Actions h-14 = 56px)
- npm run build ohne TypeScript-Fehler
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen unter:
.planning/phases/01-offline-kern-kasse/01-03-SUMMARY.md
</output>
