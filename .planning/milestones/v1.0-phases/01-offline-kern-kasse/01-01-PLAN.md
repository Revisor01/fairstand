---
phase: 01-offline-kern-kasse
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - client/package.json
  - client/vite.config.ts
  - client/tsconfig.json
  - client/tsconfig.app.json
  - client/index.html
  - client/src/main.tsx
  - client/src/App.tsx
  - client/public/icons/icon-192.png
  - client/public/icons/icon-512.png
  - client/nginx.conf
  - client/Dockerfile
  - server/package.json
  - server/tsconfig.json
  - server/src/index.ts
  - server/src/routes/health.ts
  - server/drizzle.config.ts
  - server/Dockerfile
  - docker-compose.yml
  - .github/workflows/deploy.yml
  - .gitignore
autonomous: true
requirements:
  - DEP-01
  - DEP-02
  - DEP-03
  - AUTH-02

must_haves:
  truths:
    - "npm run dev startet den Vite-Dev-Server ohne Fehler"
    - "npm run build im client/-Verzeichnis erzeugt ein dist/-Verzeichnis"
    - "Der Fastify-Server startet mit GET /api/health und antwortet 200"
    - "docker-compose build baut beide Images ohne Fehler"
    - "GitHub Actions Workflow deployt automatisch nach Push auf main"
  artifacts:
    - path: "client/vite.config.ts"
      provides: "Vite-Konfiguration mit vite-plugin-pwa (registerType: 'prompt')"
      contains: "VitePWA"
    - path: "server/src/index.ts"
      provides: "Fastify-Server-Einstiegspunkt mit CORS und health-Route"
      contains: "fastify"
    - path: "docker-compose.yml"
      provides: "Docker-Stack mit client und server Services im traefik-Netzwerk"
      contains: "traefik"
    - path: ".github/workflows/deploy.yml"
      provides: "CI/CD Pipeline mit GHCR-Push und Portainer-Webhook"
      contains: "PORTAINER_WEBHOOK_URL"
  key_links:
    - from: "docker-compose.yml"
      to: "traefik-Netzwerk"
      via: "networks: traefik: external: true"
      pattern: "external: true"
    - from: ".github/workflows/deploy.yml"
      to: "Portainer Webhook"
      via: "curl -X POST secrets.PORTAINER_WEBHOOK_URL"
      pattern: "PORTAINER_WEBHOOK_URL"
---

<objective>
Monorepo-Gerüst mit Vite-React-Frontend, Fastify-Backend, Docker-Stack und GitHub Actions CI/CD aufbauen. Alle Verzeichnisse, Configs und Dockerfiles anlegen — kein Feature-Code, nur Fundament.

Purpose: Phase-1-Arbeit (Dexie-Schema, UI) baut auf diesem Gerüst auf. Ohne lauffähige Dev-Umgebung und funktionierende Docker-Pipeline ist nichts deploybar.
Output: Monorepo mit client/ + server/ + docker-compose.yml + .github/workflows/deploy.yml, alles commit-bereit und buildbar.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-offline-kern-kasse/01-CONTEXT.md
@.planning/phases/01-offline-kern-kasse/01-RESEARCH.md

<interfaces>
<!-- Dexie-Schema kommt in Plan 01-02, noch nicht vorhanden. -->
<!-- Dieser Plan definiert nur die Monorepo-Struktur und Einstiegspunkte. -->

Verzeichnisstruktur (aus RESEARCH.md):
```
fairstand/
├── client/                  # React PWA
│   ├── src/
│   │   ├── features/
│   │   │   ├── pos/
│   │   │   └── auth/
│   │   ├── db/
│   │   ├── components/
│   │   ├── App.tsx
│   │   └── main.tsx
│   ├── public/
│   │   └── icons/
│   ├── vite.config.ts
│   ├── nginx.conf
│   └── Dockerfile
├── server/
│   ├── src/
│   │   ├── routes/
│   │   │   └── health.ts
│   │   ├── db/
│   │   └── index.ts
│   ├── drizzle.config.ts
│   └── Dockerfile
├── docker-compose.yml
└── .github/
    └── workflows/
        └── deploy.yml
```

Docker-Compose-Netzwerk: `traefik` (external: true) — Domain fairstand.godsapp.de
Client-Service: nginx:alpine, Port 80, Traefik-Label für fairstand.godsapp.de
Server-Service: node:20-alpine, Port 3000, Traefik-Label für fairstand.godsapp.de/api

Fastify health-Route:
```typescript
// GET /api/health → { status: 'ok', timestamp: number }
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Client-Gerüst mit Vite, React 19, Tailwind 4 und vite-plugin-pwa anlegen</name>
  <files>
    client/package.json,
    client/vite.config.ts,
    client/tsconfig.json,
    client/tsconfig.app.json,
    client/index.html,
    client/src/main.tsx,
    client/src/App.tsx,
    client/public/icons/icon-192.png,
    client/public/icons/icon-512.png
  </files>
  <read_first>
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-RESEARCH.md (Zeilen 88-145: Standard Stack, Installationsbefehle, Pattern 5 vite-plugin-pwa Config, Pattern 6 Tailwind-Farben)
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-CONTEXT.md (Decisions: Monorepo-Struktur)
  </read_first>
  <action>
Erstelle das komplette client/-Verzeichnis mit folgendem Inhalt:

**client/package.json** — Name `fairstand-client`, Typ `module`. Dependencies:
- react@^19, react-dom@^19
- dexie@^4.3, idb-keyval (aktuell)
- react-router-dom@^7
- zod@^3
- date-fns@^4
- workbox-window@^7

DevDependencies:
- @vitejs/plugin-react@^4, vite@^6, typescript@^5
- vite-plugin-pwa@^1.2.0
- @tailwindcss/vite@^4, tailwindcss@^4
- vitest@^3, @vitest/ui@^3

Scripts: `dev`, `build`, `preview`, `test`.

**client/vite.config.ts** — Exakt nach RESEARCH.md Pattern 5:
```typescript
import { defineConfig } from 'vite';
import react from '@vitejs/plugin-react';
import { VitePWA } from 'vite-plugin-pwa';
import tailwindcss from '@tailwindcss/vite';

export default defineConfig({
  plugins: [
    react(),
    tailwindcss(),
    VitePWA({
      registerType: 'prompt',
      workbox: {
        globPatterns: ['**/*.{js,css,html,ico,png,svg,woff2}'],
        runtimeCaching: [
          {
            urlPattern: /^https:\/\/.*\/api\/.*/,
            handler: 'NetworkFirst',
            options: { cacheName: 'api-cache', networkTimeoutSeconds: 5 },
          },
        ],
      },
      manifest: {
        name: 'Fairstand Kasse',
        short_name: 'Fairstand',
        theme_color: '#38bdf8',
        background_color: '#f0f9ff',
        display: 'standalone',
        orientation: 'portrait',
        icons: [
          { src: '/icons/icon-192.png', sizes: '192x192', type: 'image/png' },
          { src: '/icons/icon-512.png', sizes: '512x512', type: 'image/png', purpose: 'any maskable' },
        ],
      },
      devOptions: { enabled: true },
    }),
  ],
});
```

WICHTIG: `registerType: 'prompt'` — NICHT `'autoUpdate'`. Sonst aktualisiert sich die App mid-Session und leert den Warenkorb (RESEARCH.md Anti-Patterns).

**client/tsconfig.json** — References auf tsconfig.app.json, strict: true.

**client/tsconfig.app.json** — target: ES2022, lib: [ES2022, DOM, DOM.Iterable], moduleResolution: bundler, strict: true, jsx: react-jsx.

**client/index.html** — Standard Vite HTML. Meta-Tags für iOS:
```html
<meta name="apple-mobile-web-app-capable" content="yes">
<meta name="apple-mobile-web-app-status-bar-style" content="black-translucent">
<meta name="apple-mobile-web-app-title" content="Fairstand">
```
Root-div id="root".

**client/src/main.tsx** — Minimaler Einstiegspunkt: ReactDOM.createRoot + StrictMode + `<App />`.

**client/src/App.tsx** — Placeholder:
```tsx
export default function App() {
  return <div className="min-h-screen bg-sky-50 flex items-center justify-center">
    <p className="text-sky-700 text-xl font-semibold">Fairstand Kasse — wird geladen...</p>
  </div>;
}
```
Tailwind-Klassen `bg-sky-50` und `text-sky-700` benutzen — verifiziert, dass Tailwind 4 korrekt konfiguriert ist.

**client/public/icons/** — Erstelle zwei einfache SVG-basierte PNG-Platzhalter (solid sky-400 Quadrat mit "F"-Text). Verwende einen einfachen Node-Script oder erstelle die Dateien mit einem minimalen Base64-PNG (1×1 Pixel reicht NICHT — muss 192×192 und 512×512 sein). Verwende ein einfaches Canvas-Script oder generiere die Icons mit einem Online-Tool-Äquivalent per Bash. Alternativ: Erstelle SVG-Icons und konvertiere per `sharp` oder `imagemagick` (wenn verfügbar). Falls kein Tool verfügbar: Erstelle JSON-konforme Icon-Dateien als Platzhalter, die später ersetzt werden, und markiere sie als TODO im SUMMARY.

Führe in client/ aus:
```bash
npm install
npm run build
```
Build muss ohne Fehler durchlaufen.
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&amp;1 | tail -5
    </automated>
  </verify>
  <done>
    - client/vite.config.ts enthält `registerType: 'prompt'` und VitePWA-Plugin
    - client/src/App.tsx enthält `bg-sky-50`
    - `npm run build` in client/ läuft ohne Fehler und erzeugt dist/
    - dist/manifest.webmanifest enthält `"theme_color": "#38bdf8"`
  </done>
  <acceptance_criteria>
    - grep -q "registerType: 'prompt'" /Users/simonluthe/Documents/fairstand/client/vite.config.ts
    - grep -q "bg-sky-50" /Users/simonluthe/Documents/fairstand/client/src/App.tsx
    - test -f /Users/simonluthe/Documents/fairstand/client/dist/manifest.webmanifest
    - grep -q "38bdf8" /Users/simonluthe/Documents/fairstand/client/dist/manifest.webmanifest
    - test -f /Users/simonluthe/Documents/fairstand/client/dist/index.html
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Fastify-Backend-Gerüst, Docker-Compose-Stack und GitHub Actions CI/CD anlegen</name>
  <files>
    server/package.json,
    server/tsconfig.json,
    server/src/index.ts,
    server/src/routes/health.ts,
    server/drizzle.config.ts,
    server/Dockerfile,
    client/nginx.conf,
    client/Dockerfile,
    docker-compose.yml,
    .github/workflows/deploy.yml,
    .gitignore,
    package.json
  </files>
  <read_first>
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-RESEARCH.md (Pattern 7 Docker-Compose-Stack, Pattern 8 GitHub Actions, nginx.conf, Client Dockerfile, Backend Installation)
    - /Users/simonluthe/Documents/fairstand/CLAUDE.md (Deployment-Ziel: server.godsapp.de, Traefik, KeyHelp)
  </read_first>
  <action>
Erstelle server/ und alle Root-Dateien:

**server/package.json** — Name `fairstand-server`, Typ `module`. Dependencies:
- fastify@^5.7
- @fastify/cors (aktuell)
- better-sqlite3 (aktuell)
- drizzle-orm (aktuell)
- zod@^3

DevDependencies:
- typescript@^5, @types/node@^20, @types/better-sqlite3
- drizzle-kit (aktuell)
- tsx@^4

Scripts: `dev: tsx src/index.ts`, `build: tsc`, `start: node dist/index.js`.

**server/tsconfig.json** — target: ES2022, module: NodeNext, moduleResolution: NodeNext, outDir: dist, strict: true, esModuleInterop: true.

**server/src/routes/health.ts**:
```typescript
import type { FastifyInstance } from 'fastify';

export async function healthRoutes(fastify: FastifyInstance) {
  fastify.get('/health', async () => {
    return { status: 'ok', timestamp: Date.now() };
  });
}
```

**server/src/index.ts**:
```typescript
import Fastify from 'fastify';
import cors from '@fastify/cors';
import { healthRoutes } from './routes/health.js';

const fastify = Fastify({ logger: true });

await fastify.register(cors, {
  origin: process.env.CORS_ORIGIN ?? '*',
});

await fastify.register(healthRoutes, { prefix: '/api' });

const port = Number(process.env.PORT ?? 3000);
await fastify.listen({ port, host: '0.0.0.0' });
```

**server/drizzle.config.ts** — Placeholder für spätere Phase-2-Nutzung:
```typescript
import { defineConfig } from 'drizzle-kit';
export default defineConfig({
  schema: './src/db/schema.ts',
  out: './migrations',
  dialect: 'sqlite',
  dbCredentials: { url: process.env.DB_PATH ?? './data/fairstand.db' },
});
```

**server/Dockerfile**:
```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS production
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY --from=build /app/dist ./dist
RUN mkdir -p /app/data
EXPOSE 3000
CMD ["node", "dist/index.js"]
```

**client/nginx.conf** — Exakt nach RESEARCH.md nginx.conf (Service Worker no-cache + SPA Fallback):
```nginx
server {
  listen 80;
  root /usr/share/nginx/html;
  index index.html;

  location = /sw.js {
    add_header Cache-Control "no-store, no-cache, must-revalidate";
    add_header Service-Worker-Allowed "/";
    try_files $uri =404;
  }

  location / {
    try_files $uri $uri/ /index.html;
  }
}
```

**client/Dockerfile** — Multi-Stage exakt nach RESEARCH.md Pattern 7:
```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM nginx:alpine AS production
COPY --from=build /app/dist /usr/share/nginx/html
COPY nginx.conf /etc/nginx/conf.d/default.conf
EXPOSE 80
```

**docker-compose.yml** — Exakt nach RESEARCH.md Pattern 7. Domain: `fairstand.godsapp.de`. Beide Services im traefik-Netzwerk (external: true). SQLite-Volume für server:
```yaml
services:
  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.fairstand.rule=Host(`fairstand.godsapp.de`)"
      - "traefik.http.services.fairstand.loadbalancer.server.port=80"
    networks:
      - traefik
    restart: unless-stopped

  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    volumes:
      - sqlite-data:/app/data
    environment:
      - PORT=3000
      - CORS_ORIGIN=https://fairstand.godsapp.de
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.fairstand-api.rule=Host(`fairstand.godsapp.de`) && PathPrefix(`/api`)"
      - "traefik.http.services.fairstand-api.loadbalancer.server.port=3000"
    networks:
      - traefik
    restart: unless-stopped

volumes:
  sqlite-data:

networks:
  traefik:
    external: true
```

**.github/workflows/deploy.yml** — Exakt nach RESEARCH.md Pattern 8:
```yaml
name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push client
        uses: docker/build-push-action@v6
        with:
          context: ./client
          push: true
          tags: ghcr.io/${{ github.repository }}/client:latest

      - name: Build and push server
        uses: docker/build-push-action@v6
        with:
          context: ./server
          push: true
          tags: ghcr.io/${{ github.repository }}/server:latest

      - name: Trigger Portainer Webhook
        run: curl -f -X POST "${{ secrets.PORTAINER_WEBHOOK_URL }}"
```

**Root package.json** — Workspace-Root (kein eigenes Module):
```json
{
  "name": "fairstand",
  "private": true,
  "scripts": {
    "dev:client": "npm run dev --workspace=client",
    "dev:server": "npm run dev --workspace=server"
  }
}
```

**.gitignore**:
```
node_modules/
dist/
.env
*.db
data/
client/dist/
```

Führe in server/ aus: `npm install`
Führe im Root aus: `docker-compose config` — muss ohne Fehler parsen.
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/fairstand && docker-compose config --quiet 2>&amp;1 &amp;&amp; echo "docker-compose config OK" || echo "FEHLER in docker-compose.yml"
    </automated>
  </verify>
  <done>
    - server/src/index.ts startet Fastify auf Port 3000 mit /api/health
    - docker-compose.yml hat beide Services (client, server) mit traefik-Netzwerk
    - .github/workflows/deploy.yml hat PORTAINER_WEBHOOK_URL als Secret-Referenz
    - client/nginx.conf hat Service-Worker-Cache-Control-Header
  </done>
  <acceptance_criteria>
    - grep -q "PORTAINER_WEBHOOK_URL" /Users/simonluthe/Documents/fairstand/.github/workflows/deploy.yml
    - grep -q "external: true" /Users/simonluthe/Documents/fairstand/docker-compose.yml
    - grep -q "fairstand.godsapp.de" /Users/simonluthe/Documents/fairstand/docker-compose.yml
    - grep -q "no-store" /Users/simonluthe/Documents/fairstand/client/nginx.conf
    - grep -q "Service-Worker-Allowed" /Users/simonluthe/Documents/fairstand/client/nginx.conf
    - test -f /Users/simonluthe/Documents/fairstand/server/src/routes/health.ts
    - grep -q "status.*ok" /Users/simonluthe/Documents/fairstand/server/src/routes/health.ts
  </acceptance_criteria>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
1. `cd /Users/simonluthe/Documents/fairstand/client && npm run build` — kein Fehler, dist/ vorhanden
2. `cd /Users/simonluthe/Documents/fairstand/server && npx tsx src/index.ts &amp;` — Server startet, `curl localhost:3000/api/health` antwortet `{"status":"ok"}`
3. `grep -q "registerType: 'prompt'" client/vite.config.ts` — pass
4. `grep -q "external: true" docker-compose.yml` — pass
5. `grep -q "PORTAINER_WEBHOOK_URL" .github/workflows/deploy.yml` — pass
</verification>

<success_criteria>
- Monorepo mit client/ und server/ angelegt und beide npm installiert
- `npm run build` in client/ erzeugt dist/ mit manifest.webmanifest (theme_color: #38bdf8)
- Fastify health-Route in server/src/routes/health.ts vorhanden
- docker-compose.yml baut beide Services im traefik-Netzwerk (external: true)
- GitHub Actions Workflow mit GHCR-Push und Portainer-Webhook-Trigger vorhanden
- .gitignore schließt node_modules/, dist/, .env, *.db aus
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen unter:
.planning/phases/01-offline-kern-kasse/01-01-SUMMARY.md
</output>
