---
phase: 08-bestandspruefung-verkaufshistorie
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - client/src/features/admin/reports/DailyReport.tsx
  - client/src/features/admin/reports/SaleDetailModal.tsx
  - client/src/features/admin/products/ProductStats.tsx
  - client/src/features/admin/products/ProductList.tsx
  - server/src/routes/reports.ts
autonomous: true
requirements:
  - HIST-01
  - HIST-02
must_haves:
  truths:
    - "In der Tagesübersicht ist jede Zeile in der Verkaufsliste antippbar"
    - "Ein Tipp auf einen Verkauf öffnet eine Detailansicht mit Artikeln, Mengen und Preisen"
    - "Die Detailansicht funktioniert offline (Daten kommen aus Dexie)"
    - "Pro Artikel gibt es eine Statistikseite: Anzahl verkauft, Gesamtumsatz, Zeitraum (letzter Monat)"
    - "Die Statistik ist aus der Produktliste erreichbar"
  artifacts:
    - path: "client/src/features/admin/reports/SaleDetailModal.tsx"
      provides: "Modal mit Artikeldetails eines Verkaufs"
      contains: "SaleItem"
    - path: "client/src/features/admin/products/ProductStats.tsx"
      provides: "Artikel-Statistikansicht"
      contains: "total_qty"
    - path: "server/src/routes/reports.ts"
      provides: "GET /reports/product/:id/stats Endpoint"
      contains: "product_stats"
  key_links:
    - from: "DailyReport.tsx"
      to: "SaleDetailModal.tsx"
      via: "selectedSale state + onPointerDown auf Tabellenzeile"
      pattern: "selectedSale"
    - from: "ProductList.tsx"
      to: "ProductStats.tsx"
      via: "view state 'stats' + openStats(product)"
      pattern: "openStats"
    - from: "ProductStats.tsx"
      to: "/api/reports/product/:id/stats"
      via: "fetch mit shopId + productId"
      pattern: "fetch.*reports/product"
---

<objective>
Anklickbare Tagesübersicht und Artikel-Statistik einbauen.

Purpose: Mitarbeiterinnen können Verkäufe des Tages einsehen und für jeden Artikel nachvollziehen, wie oft er verkauft wurde.
Output: DailyReport mit tippbaren Zeilen + SaleDetailModal, ProductStats-Komponente + Server-Endpoint.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-bestandspruefung-verkaufshistorie/08-01-SUMMARY.md

<interfaces>
<!-- Aus client/src/db/schema.ts -->
```typescript
export interface Sale {
  id: string;
  shopId: string;
  items: SaleItem[];      // Array mit allen Artikeln des Verkaufs
  totalCents: number;
  paidCents: number;
  changeCents: number;
  donationCents: number;
  createdAt: number;      // Unix-Timestamp ms
  syncedAt?: number;
}

export interface SaleItem {
  productId: string;
  articleNumber: string;
  name: string;
  salePrice: number;      // Cent-Integer — Snapshot zum Kaufzeitpunkt
  quantity: number;
}
```

<!-- Aus client/src/features/admin/reports/DailyReport.tsx -->
```typescript
// Bestehende Tabellenzeile (bisher nicht klickbar):
// <tr key={sale.id} className="border-b border-slate-50 last:border-0">
//   <td>{format(new Date(sale.createdAt), 'HH:mm')}</td>
//   <td>{formatEur(sale.totalCents)}</td>
//   <td>{donationCents > 0 ? formatEur(...) : '—'}</td>
// </tr>
// sales kommen von useLiveQuery auf db.sales (offline-fähig)
```

<!-- Aus server/src/routes/reports.ts — bestehende Query-Muster für neue Route -->
```typescript
// Muster für items-JSON-Extraktion (bereits in monthly verwendet):
// FROM sales, json_each(sales.items) as item
// WHERE sales.shop_id = ${shopId}
//   AND json_extract(item.value, '$.productId') = ${productId}
// SUM(CAST(json_extract(item.value, '$.quantity') AS INTEGER))
// SUM(qty * CAST(json_extract(item.value, '$.salePrice') AS INTEGER))
```

<!-- Aus client/src/features/admin/products/ProductList.tsx -->
```typescript
// Bestehende view-States: 'list' | 'form' | 'stock'
// Muster für neuen 'stats'-State:
type ProductListView = 'list' | 'form' | 'stock' | 'stats';
// selectedProduct: Product | undefined
// openStats(product) → setSelectedProduct(product); setView('stats')
// handleClose() → setSelectedProduct(undefined); setView('list')
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: SaleDetailModal + DailyReport tippbare Zeilen</name>
  <files>
    client/src/features/admin/reports/SaleDetailModal.tsx
    client/src/features/admin/reports/DailyReport.tsx
  </files>
  <read_first>
    - client/src/features/admin/reports/DailyReport.tsx (vollständig)
    - client/src/db/schema.ts (Sale und SaleItem)
    - client/src/features/pos/utils.js (formatEur)
  </read_first>
  <action>
    **SaleDetailModal.tsx** (neue Datei erstellen):

    Zeigt Details eines Verkaufs in einem Modal/Overlay. Kein eigenes Dexie-Query nötig — Sale-Objekt wird als Prop übergeben.

    ```tsx
    import { format } from 'date-fns';
    import { de } from 'date-fns/locale';
    import type { Sale } from '../../../db/index.js';
    import { formatEur } from '../../pos/utils.js';

    interface SaleDetailModalProps {
      sale: Sale;
      onClose: () => void;
    }

    export function SaleDetailModal({ sale, onClose }: SaleDetailModalProps) {
      return (
        // Overlay: festes Overlay, dunkel halbtransparent, zentriert
        <div
          className="fixed inset-0 bg-black/40 flex items-end sm:items-center justify-center z-50 p-4"
          onPointerDown={onClose}
        >
          <div
            className="bg-white rounded-2xl shadow-xl w-full max-w-md max-h-[80vh] flex flex-col"
            onPointerDown={e => e.stopPropagation()}
          >
            {/* Header */}
            <div className="flex items-center justify-between px-5 py-4 border-b border-slate-100">
              <div>
                <h3 className="font-semibold text-slate-800">
                  Verkauf {format(new Date(sale.createdAt), 'HH:mm', { locale: de })} Uhr
                </h3>
                <p className="text-xs text-slate-500 mt-0.5">
                  {format(new Date(sale.createdAt), 'EEEE, d. MMMM yyyy', { locale: de })}
                </p>
              </div>
              <button
                onPointerDown={onClose}
                className="min-w-[44px] min-h-[44px] flex items-center justify-center text-slate-400 hover:text-slate-600 rounded-xl hover:bg-slate-100 transition-colors"
                aria-label="Schließen"
              >
                ✕
              </button>
            </div>

            {/* Artikel-Liste */}
            <div className="overflow-y-auto flex-1">
              <table className="w-full text-sm">
                <thead>
                  <tr className="bg-sky-50 border-b border-sky-100">
                    <th className="text-left px-5 py-3 text-sky-700 font-semibold">Artikel</th>
                    <th className="text-right px-4 py-3 text-sky-700 font-semibold">Menge</th>
                    <th className="text-right px-5 py-3 text-sky-700 font-semibold">Preis</th>
                  </tr>
                </thead>
                <tbody>
                  {sale.items.map((item, idx) => (
                    <tr key={idx} className="border-b border-slate-50 last:border-0">
                      <td className="px-5 py-3 text-slate-800">{item.name}</td>
                      <td className="px-4 py-3 text-right text-slate-600">{item.quantity}×</td>
                      <td className="px-5 py-3 text-right font-medium text-slate-800">
                        {formatEur(item.salePrice * item.quantity)}
                      </td>
                    </tr>
                  ))}
                </tbody>
              </table>
            </div>

            {/* Footer: Summen */}
            <div className="border-t border-slate-100 px-5 py-4 flex flex-col gap-1">
              <div className="flex justify-between text-sm text-slate-600">
                <span>Gesamt</span>
                <span className="font-semibold text-slate-800">{formatEur(sale.totalCents)}</span>
              </div>
              {sale.donationCents > 0 && (
                <div className="flex justify-between text-sm text-green-600">
                  <span>davon Spende</span>
                  <span className="font-semibold">{formatEur(sale.donationCents)}</span>
                </div>
              )}
            </div>
          </div>
        </div>
      );
    }
    ```

    **DailyReport.tsx** anpassen:

    1. Import hinzufügen: `import { SaleDetailModal } from './SaleDetailModal.js';`
    2. State hinzufügen: `const [selectedSale, setSelectedSale] = useState<Sale | null>(null);`
       Import erweitern: `import type { Sale } from '../../../db/index.js';`
    3. Tabellenzeilen: `<tr>` → `<tr ... onPointerDown={() => setSelectedSale(sale)} className="... cursor-pointer hover:bg-sky-50 active:bg-sky-100">`
    4. Modal einblenden wenn selectedSale gesetzt:
       ```tsx
       {selectedSale && (
         <SaleDetailModal sale={selectedSale} onClose={() => setSelectedSale(null)} />
       )}
       ```
       (Außerhalb der Tabelle, am Ende des Return-Statements)
    5. Optischer Hinweis auf Klickbarkeit: Chevron-Symbol (→) oder `text-sky-400` in der Zeile — diskret, nicht störend.
       Einfachste Lösung: eine vierte Spalte mit `›` Symbol:
       ```tsx
       <td className="px-4 py-3 text-slate-300 text-lg">›</td>
       ```
       Entsprechend `<th></th>` im thead ergänzen.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - SaleDetailModal.tsx existiert und exportiert `SaleDetailModal`
    - DailyReport.tsx importiert und rendert SaleDetailModal bei selectedSale !== null
    - Tabellenzeilen haben `cursor-pointer` und `onPointerDown`
    - Modal zeigt Artikelname, Menge, Einzelpreis × Menge für jeden SaleItem
    - Modal zeigt Gesamtbetrag und ggf. Spende im Footer
    - Tipp außerhalb des Modal-Cards schließt das Modal
    - TypeScript-Build schlägt nicht fehl
  </acceptance_criteria>
  <done>DailyReport hat tippbare Zeilen, SaleDetailModal zeigt Artikel-Breakdown eines Verkaufs</done>
</task>

<task type="auto">
  <name>Task 2: Server-Endpoint Artikel-Statistik + ProductStats-Komponente + ProductList-Integration</name>
  <files>
    server/src/routes/reports.ts
    client/src/features/admin/products/ProductStats.tsx
    client/src/features/admin/products/ProductList.tsx
  </files>
  <read_first>
    - server/src/routes/reports.ts (vollständig — für SQL-Muster)
    - client/src/features/admin/products/ProductList.tsx (vollständig — für view-State Muster)
    - client/src/db/schema.ts (Product Interface)
  </read_first>
  <action>
    **server/src/routes/reports.ts** — neuen Endpoint anhängen:

    Füge am Ende der `reportRoutes`-Funktion (vor der letzten `}`) hinzu:

    ```typescript
    // GET /reports/product/:id/stats?shopId=xxx&months=3
    fastify.get('/reports/product/:id/stats', async (request, reply) => {
      const { id } = request.params as { id: string };
      const { shopId, months } = request.query as { shopId: string; months?: string };
      if (!shopId) return reply.status(400).send({ error: 'shopId required' });

      const monthsBack = Math.min(Math.max(Number(months ?? '3'), 1), 24);
      const since = new Date();
      since.setMonth(since.getMonth() - monthsBack);
      const sinceTs = since.getTime();

      const result = db.all(sql`
        SELECT
          COUNT(DISTINCT sales.id) as sale_count,
          COALESCE(SUM(CAST(json_extract(item.value, '$.quantity') AS INTEGER)), 0) as total_qty,
          COALESCE(SUM(
            CAST(json_extract(item.value, '$.quantity') AS INTEGER) *
            CAST(json_extract(item.value, '$.salePrice') AS INTEGER)
          ), 0) as revenue_cents,
          MIN(sales.created_at) as first_sale_at,
          MAX(sales.created_at) as last_sale_at
        FROM sales, json_each(sales.items) as item
        WHERE sales.shop_id = ${shopId}
          AND json_extract(item.value, '$.productId') = ${id}
          AND sales.created_at >= ${sinceTs}
      `);

      const row = (result[0] as Record<string, unknown>) ?? {};
      return reply.send({
        productId: id,
        period_months: monthsBack,
        since_ts: sinceTs,
        sale_count: Number(row.sale_count ?? 0),
        total_qty: Number(row.total_qty ?? 0),
        revenue_cents: Number(row.revenue_cents ?? 0),
        first_sale_at: row.first_sale_at ? Number(row.first_sale_at) : null,
        last_sale_at: row.last_sale_at ? Number(row.last_sale_at) : null,
      });
    });
    ```

    **client/src/features/admin/products/ProductStats.tsx** (neue Datei erstellen):

    Zeigt Artikel-Statistik. Fetcht vom Server — zeigt Hinweis wenn offline.

    ```tsx
    import { useState, useEffect } from 'react';
    import type { Product } from '../../../db/index.js';
    import { getShopId } from '../../../db/index.js';
    import { formatEur } from '../../pos/utils.js';
    import { format } from 'date-fns';
    import { de } from 'date-fns/locale';

    interface ProductStatsProps {
      product: Product;
      onClose: () => void;
    }

    interface StatsData {
      productId: string;
      period_months: number;
      since_ts: number;
      sale_count: number;
      total_qty: number;
      revenue_cents: number;
      first_sale_at: number | null;
      last_sale_at: number | null;
    }

    export function ProductStats({ product, onClose }: ProductStatsProps) {
      const [stats, setStats] = useState<StatsData | null>(null);
      const [loading, setLoading] = useState(true);
      const [error, setError] = useState<string | null>(null);
      const [months, setMonths] = useState(3);

      useEffect(() => {
        if (!navigator.onLine) {
          setLoading(false);
          setError('offline');
          return;
        }
        setLoading(true);
        setError(null);
        fetch(`/api/reports/product/${product.id}/stats?shopId=${getShopId()}&months=${months}`)
          .then(r => r.ok ? r.json() : Promise.reject(r.status))
          .then((data: StatsData) => setStats(data))
          .catch(() => setError('fetch_failed'))
          .finally(() => setLoading(false));
      }, [product.id, months]);

      return (
        <div className="flex flex-col gap-4">
          {/* Header */}
          <div className="flex items-center justify-between">
            <div>
              <h2 className="text-lg font-semibold text-sky-800">{product.name}</h2>
              <p className="text-sm text-slate-500">Art.-Nr. {product.articleNumber} · {product.category}</p>
            </div>
            <button
              onPointerDown={onClose}
              className="bg-slate-100 hover:bg-slate-200 text-slate-700 font-medium px-4 py-2 rounded-lg h-11 text-sm transition-colors"
            >
              Zurück
            </button>
          </div>

          {/* Zeitraum-Auswahl */}
          <div className="flex gap-2">
            {[1, 3, 6, 12].map(m => (
              <button
                key={m}
                onPointerDown={() => setMonths(m)}
                className={`px-4 py-2 rounded-full text-sm font-medium h-11 transition-colors ${
                  months === m
                    ? 'bg-sky-500 text-white'
                    : 'bg-sky-100 text-sky-700 hover:bg-sky-200'
                }`}
              >
                {m === 1 ? '1 Monat' : `${m} Monate`}
              </button>
            ))}
          </div>

          {/* Inhalt */}
          {loading && (
            <div className="text-center py-12 text-slate-500">Laden...</div>
          )}

          {error === 'offline' && (
            <div className="bg-amber-50 text-amber-700 px-4 py-3 rounded-xl text-sm">
              Statistik ist nur mit Internetverbindung verfügbar.
            </div>
          )}

          {error === 'fetch_failed' && (
            <div className="bg-rose-50 text-rose-700 px-4 py-3 rounded-xl text-sm">
              Statistik konnte nicht geladen werden.
            </div>
          )}

          {!loading && !error && stats && (
            <>
              {/* Kennzahlen */}
              <div className="grid grid-cols-3 gap-3">
                <div className="bg-white rounded-xl shadow-sm p-4 flex flex-col items-center justify-center gap-1">
                  <span className="text-3xl font-bold text-slate-800">{stats.total_qty}</span>
                  <span className="text-xs text-slate-500 text-center">Stück verkauft</span>
                </div>
                <div className="bg-white rounded-xl shadow-sm p-4 flex flex-col items-center justify-center gap-1">
                  <span className="text-2xl font-bold text-sky-600">{formatEur(stats.revenue_cents)}</span>
                  <span className="text-xs text-slate-500 text-center">Umsatz</span>
                </div>
                <div className="bg-white rounded-xl shadow-sm p-4 flex flex-col items-center justify-center gap-1">
                  <span className="text-3xl font-bold text-slate-800">{stats.sale_count}</span>
                  <span className="text-xs text-slate-500 text-center">Transaktionen</span>
                </div>
              </div>

              {/* Zeitraum-Info */}
              <div className="bg-white rounded-xl shadow-sm p-4 text-sm text-slate-600">
                <p>Zeitraum: letzte {stats.period_months} Monate (ab {format(new Date(stats.since_ts), 'd. MMM yyyy', { locale: de })})</p>
                {stats.last_sale_at && (
                  <p className="mt-1">Letzter Verkauf: {format(new Date(stats.last_sale_at), 'd. MMM yyyy, HH:mm', { locale: de })} Uhr</p>
                )}
                {stats.total_qty === 0 && (
                  <p className="mt-2 text-slate-400">In diesem Zeitraum keine Verkäufe.</p>
                )}
              </div>
            </>
          )}
        </div>
      );
    }
    ```

    **client/src/features/admin/products/ProductList.tsx** anpassen:

    1. Import: `import { ProductStats } from './ProductStats.js';`
    2. Type erweitern: `type ProductListView = 'list' | 'form' | 'stock' | 'stats';`
    3. Funktion `openStats` hinzufügen (analog zu `openStockAdjust`):
       ```typescript
       function openStats(product: Product) {
         setSelectedProduct(product);
         setView('stats');
       }
       ```
    4. Render-Block: nach dem `view === 'stock'`-Block hinzufügen:
       ```tsx
       if (view === 'stats' && selectedProduct) {
         return <ProductStats product={selectedProduct} onClose={handleClose} />;
       }
       ```
    5. In der Produktzeile: "Statistik"-Button neben "Bearbeiten" hinzufügen:
       ```tsx
       <button
         onPointerDown={() => openStats(product)}
         className="bg-sky-100 hover:bg-sky-200 active:bg-sky-300 text-sky-700 font-medium px-3 py-2 rounded-lg h-11 text-sm transition-colors"
       >
         Statistik
       </button>
       ```
       Einfügen zwischen "Bearbeiten"- und "Bestand"-Button.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - `GET /api/reports/product/:id/stats?shopId=xxx` antwortet mit `{ total_qty, revenue_cents, sale_count, ... }`
    - `GET /api/reports/product/:id/stats?shopId=xxx&months=6` antwortet mit Daten für 6-Monats-Zeitraum
    - Produktliste zeigt "Statistik"-Button pro Artikel
    - Tipp auf "Statistik" öffnet ProductStats-Ansicht
    - ProductStats zeigt Kennzahlen (Stück, Umsatz, Transaktionen) in drei Kacheln
    - Bei offline: Hinweis "Statistik ist nur mit Internetverbindung verfügbar"
    - TypeScript-Build schlägt nicht fehl
    - Zeitraum-Buttons (1, 3, 6, 12 Monate) laden neue Daten
  </acceptance_criteria>
  <done>Server-Endpoint liefert Artikel-Statistik, ProductStats-Komponente zeigt sie in der Admin-Verwaltung</done>
</task>

</tasks>

<verification>
Nach Plan-Ausführung manuell verifizieren:
1. Tagesübersicht in Admin → Verkaufszeile antippen → Modal öffnet sich mit Artikel-Breakdown
2. Modal zeigt: Artikelname, Menge, Preis, Gesamtsumme, ggf. Spende
3. Tipp außerhalb des Modals schließt es
4. Produktverwaltung → "Statistik"-Button → Statistikseite öffnet sich
5. Statistik zeigt Kennzahlen und Zeitraum-Auswahl
6. Zeitraum wechseln lädt neue Daten
</verification>

<success_criteria>
- HIST-01: DailyReport-Zeilen sind antippbar, SaleDetailModal zeigt Artikel/Mengen/Preise
- HIST-02: ProductStats-Komponente mit Server-Endpoint liefert Stückzahl, Umsatz, Transaktionen pro Zeitraum
- Offline-Handling: DailyReport funktioniert offline (Dexie), ProductStats zeigt Hinweis
- Build ohne TypeScript-Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-bestandspruefung-verkaufshistorie/08-02-SUMMARY.md` erstellen.
</output>
