---
phase: 08-bestandspruefung-verkaufshistorie
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/features/pos/useCart.ts
  - client/src/features/pos/POSScreen.tsx
autonomous: true
requirements:
  - BEST-01
  - BEST-02
must_haves:
  truths:
    - "Jede Artikelkachel zeigt den aktuellen Bestand als kleine Zahl"
    - "Kacheln mit stock <= 0 sind grau, nicht tippbar und zeigen 'Ausverkauft'"
    - "Kacheln mit stock > 0 aber <= minStock werden mit orangefarbener Bestandszahl dargestellt"
    - "Wer einen ausverkauften Artikel antippt, bekommt kein Feedback (Button ist disabled)"
    - "Wer einen Artikel antippt, dessen Bestand durch vorherige Cart-Einträge erschöpft ist, bekommt keinen weiteren Eintrag"
  artifacts:
    - path: "client/src/features/pos/ArticleGrid.tsx"
      provides: "Produkt-Grid mit Bestandsanzeige und disabled-State"
      contains: "stock"
    - path: "client/src/features/pos/useCart.ts"
      provides: "addItem mit Bestandsprüfung"
      contains: "addItemResult"
  key_links:
    - from: "ArticleGrid.tsx"
      to: "useCart.ts addItem"
      via: "onAddToCart prop in POSScreen"
      pattern: "cart\\.addItem"
    - from: "useCart addItem"
      to: "product.stock"
      via: "cartItems quantity sum check"
      pattern: "stock"
---

<objective>
Bestandsanzeige und Überverkauf-Blockierung in der Kasse einbauen.

Purpose: Mitarbeiterinnen sehen auf einen Blick, wie viele Stück noch verfügbar sind, und können nichts verkaufen, was nicht mehr im Lager liegt.
Output: Artikelkacheln zeigen Bestand, ausverkaufte Artikel sind blockiert, addItem prüft Bestand vor Eintrag.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aus client/src/db/schema.ts -->
```typescript
export interface Product {
  id: string;
  shopId: string;
  articleNumber: string;
  name: string;
  category: string;
  purchasePrice: number;
  salePrice: number;
  vatRate: number;
  stock: number;        // Aktueller Bestand (kann 0 sein)
  minStock: number;     // 0 = kein Mindestbestand (Opt-in)
  active: boolean;
  updatedAt: number;
}

export type CartItem = SaleItem;
// CartItem hat: productId, articleNumber, name, salePrice, quantity
```

<!-- Aus client/src/features/pos/useCart.ts -->
```typescript
// Aktuelles addItem — kein Stock-Check:
function addItem(product: Product): void
// dispatch({ type: 'ADD_ITEM', product })

// POSScreen ruft auf:
cart.addItem(product);
setIsCartOpen(true);
// in: <ArticleGrid onAddToCart={product => { cart.addItem(product); setIsCartOpen(true); }} />
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: useCart — addItem mit Bestandsprüfung und Rückgabewert</name>
  <files>client/src/features/pos/useCart.ts</files>
  <read_first>
    - client/src/features/pos/useCart.ts (vollständig)
    - client/src/db/schema.ts (Product und CartItem Interface)
  </read_first>
  <behavior>
    - addItem({ stock: 3 }, cartItems=[]) → returns { added: true }
    - addItem({ stock: 1 }, cartItems=[{ productId: same, quantity: 1 }]) → returns { added: false, reason: 'out_of_stock' }
    - addItem({ stock: 0 }, cartItems=[]) → returns { added: false, reason: 'out_of_stock' }
    - addItem({ stock: 5 }, cartItems=[{ productId: same, quantity: 3 }]) → returns { added: true } (5 > 3, noch Platz)
    - addItem({ stock: 5 }, cartItems=[{ productId: same, quantity: 5 }]) → returns { added: false, reason: 'out_of_stock' }
  </behavior>
  <action>
    Ändere `addItem` so, dass es ein `AddItemResult`-Objekt zurückgibt:

    ```typescript
    export type AddItemResult = { added: true } | { added: false; reason: 'out_of_stock' };
    ```

    Bestandsprüfung im Reducer ist NICHT möglich (Reducer kennt den Bestand nicht ohne Product-Referenz, und CartItem enthält kein `stock`-Feld).
    Stattdessen: Prüfung in der `addItem`-Funktion BEFORE dem dispatch:

    ```typescript
    function addItem(product: Product): AddItemResult {
      // Wie viele Stück sind bereits im Warenkorb?
      const existing = state.items.find(i => i.productId === product.id);
      const inCart = existing ? existing.quantity : 0;
      if (product.stock <= 0 || inCart >= product.stock) {
        return { added: false, reason: 'out_of_stock' };
      }
      dispatch({ type: 'ADD_ITEM', product });
      return { added: true };
    }
    ```

    Wichtig: `state` ist im `useCart`-Hook über `useReducer` verfügbar — `addItem` ist eine Closure über `state`. Das funktioniert, weil `addItem` innerhalb der Hook-Funktion definiert wird.

    Exportiere `AddItemResult` als Named Export damit POSScreen es importieren kann.

    Der `useCart`-Return-Typ ändert sich: `addItem` hat jetzt Rückgabetyp `AddItemResult`.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx vitest run --reporter=verbose 2>&1 | tail -30</automated>
  </verify>
  <acceptance_criteria>
    - `addItem` gibt `{ added: true }` zurück wenn stock > Menge im Warenkorb
    - `addItem` gibt `{ added: false, reason: 'out_of_stock' }` zurück wenn stock === 0
    - `addItem` gibt `{ added: false, reason: 'out_of_stock' }` zurück wenn inCart >= stock
    - TypeScript kompiliert ohne Fehler: `npx tsc --noEmit` im client-Verzeichnis
  </acceptance_criteria>
  <done>useCart.ts exportiert AddItemResult, addItem prüft Bestand vor dispatch und gibt Ergebnis zurück</done>
</task>

<task type="auto">
  <name>Task 2: ArticleGrid — Bestandsanzeige und disabled-State, POSScreen Fehler-Feedback</name>
  <files>
    client/src/features/pos/ArticleGrid.tsx
    client/src/features/pos/POSScreen.tsx
  </files>
  <read_first>
    - client/src/features/pos/ArticleGrid.tsx (vollständig)
    - client/src/features/pos/POSScreen.tsx (vollständig, besonders ArticleGrid-Aufruf)
    - client/src/features/pos/useCart.ts (nach Task 1, für AddItemResult-Import)
  </read_first>
  <action>
    **ArticleGrid.tsx:**

    1. Props erweitern: `onAddToCart` bekommt Rückgabewert `AddItemResult` (oder `void` bleibt als Fallback für Rückwärtskompatibilität):
       ```typescript
       interface ArticleGridProps {
         onAddToCart: (product: Product) => void;
       }
       ```
       Props-Interface bleibt `void` — der Return-Wert wird NICHT in ArticleGrid ausgewertet. Die Blockierung passiert direkt in der Kachel.

    2. Jede Produktkachel bekommt:
       - `disabled={product.stock <= 0}` auf dem button-Element
       - Bestandsanzeige unter dem Preis: kleiner Text

    3. Visuelles Design der Kacheln (per D-Diskretionsbereich):
       - **Ausverkauft** (stock <= 0): Button `opacity-50 cursor-not-allowed`, Bestandstext `text-rose-500 text-xs font-medium`, Inhalt "Ausverkauft"
       - **Niedrig** (minStock > 0 && stock <= minStock): Bestandstext `text-amber-600 text-xs`, Inhalt z.B. "Noch 2"
       - **Normal** (stock > minStock oder minStock === 0): Bestandstext `text-slate-400 text-xs`, Inhalt z.B. "2 Stk."

    4. `onPointerDown` bei disabled-Buttons NICHT aufrufen (ist durch `disabled` automatisch blockiert bei pointer events nicht — daher explizit prüfen oder `pointer-events-none` per Tailwind auf disabled hinzufügen):
       ```typescript
       onPointerDown={() => { if (product.stock > 0) onAddToCart(product); }}
       ```

    Konkretes Markup für Kachel (Ersetze bestehenden button-Inhalt):
    ```tsx
    <button
      key={product.id}
      onPointerDown={() => { if (product.stock > 0) onAddToCart(product); }}
      disabled={product.stock <= 0}
      className={`
        bg-white shadow-sm rounded-xl min-h-[80px] p-3
        flex flex-col justify-between items-start
        transition-colors text-left
        ${product.stock <= 0
          ? 'opacity-50 cursor-not-allowed'
          : 'active:bg-sky-50'
        }
      `}
    >
      <span className="text-slate-800 font-medium text-sm leading-tight line-clamp-3">
        {product.name}
      </span>
      <div className="flex items-baseline gap-2 mt-1 w-full justify-between">
        <span className="text-sky-700 font-semibold text-sm">
          {formatEur(product.salePrice)}
        </span>
        <span className={`text-xs font-medium ${
          product.stock <= 0
            ? 'text-rose-500'
            : product.minStock > 0 && product.stock <= product.minStock
              ? 'text-amber-600'
              : 'text-slate-400'
        }`}>
          {product.stock <= 0
            ? 'Ausverkauft'
            : product.minStock > 0 && product.stock <= product.minStock
              ? `Noch ${product.stock}`
              : `${product.stock} Stk.`
          }
        </span>
      </div>
    </button>
    ```

    **POSScreen.tsx:**

    Der `onAddToCart`-Handler prüft den Rückgabewert von `cart.addItem`. Wenn `added === false`, zeigt er kurz eine Fehlermeldung. Kein dauerhaftes Error-State nötig — nur kurzes Feedback.

    Importiere `AddItemResult` aus useCart:
    ```typescript
    import type { AddItemResult } from './useCart.js';
    ```

    Füge state hinzu:
    ```typescript
    const [stockError, setStockError] = useState<string | null>(null);
    ```

    Ändere den ArticleGrid-Handler:
    ```tsx
    <ArticleGrid onAddToCart={product => {
      const result = cart.addItem(product);
      if (!result.added) {
        setStockError(`"${product.name}" ist nicht mehr auf Lager.`);
        setTimeout(() => setStockError(null), 2500);
      } else {
        setStockError(null);
        setIsCartOpen(true);
      }
    }} />
    ```

    Zeige `stockError` als Toast unterhalb des Headers (vor dem Artikel-Grid):
    ```tsx
    {stockError && (
      <div className="mx-4 mt-2 px-4 py-3 bg-rose-50 text-rose-700 text-sm rounded-xl shadow-sm">
        {stockError}
      </div>
    )}
    ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npm run build 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - Jede Artikelkachel zeigt Bestandszahl (klein, rechts unten)
    - Kacheln mit stock <= 0 zeigen "Ausverkauft" und sind visuell gedimmt (opacity-50)
    - Tipp auf ausverkaufte Kachel öffnet NICHT den Warenkorb
    - Kacheln mit stock <= minStock (und minStock > 0) zeigen "Noch X" in Amber
    - Nach Bestandserschöpfung durch Cart (addItem gibt added:false zurück) erscheint rose-farbener Toast
    - `npm run build` (im client-Verzeichnis) oder `npx tsc --noEmit` schlägt nicht fehl
  </acceptance_criteria>
  <done>Artikelkacheln zeigen Bestand, ausverkaufte Artikel sind blockiert, Toast erscheint bei Überverkaufsversuch</done>
</task>

</tasks>

<verification>
Nach Plan-Ausführung manuell verifizieren:
1. `npm run dev` starten, in die Kasse navigieren
2. Artikelkacheln zeigen Bestandszahl (klein, rechts unten auf Kachel)
3. Artikel mit stock=0 sind grau und lassen sich nicht antippen
4. Artikel mit stock=1 und 1 Stk. im Warenkorb: erneutes Antippen zeigt Toast "nicht mehr auf Lager"
</verification>

<success_criteria>
- BEST-01: Jede Kachel zeigt Bestand als kleine Zahl neben/unter dem Preis
- BEST-02: Stock-Check in addItem verhindert Überverkauf; disabled-State auf Kachel verhindert Tipp auf leeren Artikel
- Build ohne TypeScript-Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/08-bestandspruefung-verkaufshistorie/08-01-SUMMARY.md` erstellen.
</output>
