---
phase: 09-storno-rueckgabe
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/db/schema.ts
  - server/src/db/schema.ts
  - server/src/routes/sync.ts
  - client/src/features/admin/reports/SaleDetailModal.tsx
  - client/src/features/admin/reports/DailyReport.tsx
autonomous: true
requirements:
  - STOR-01
  - STOR-02

must_haves:
  truths:
    - "Aus der Tagesübersicht kann ein Verkauf vollständig storniert werden — alle enthaltenen Artikel werden im Bestand zurückgebucht"
    - "Einzelne Artikel eines Verkaufs können als Rückgabe verbucht werden — nur der betroffene Artikel wird zurückgebucht"
    - "Storno/Rückgabe funktioniert offline — Outbox-Einträge werden bei Reconnect synchronisiert"
    - "Stornierte Verkäufe sind in der Tagesübersicht visuell erkennbar (durchgestrichen)"
    - "Rückgegebene Artikel sind im SaleDetailModal als zurückgegeben markiert"
  artifacts:
    - path: "client/src/db/schema.ts"
      provides: "Dexie v4: cancelledAt auf Sale, returnedItems auf Sale, OutboxEntry.operation um SALE_CANCEL + ITEM_RETURN erweitert"
    - path: "server/src/db/schema.ts"
      provides: "Drizzle: cancelledAt-Spalte in sales-Tabelle"
    - path: "server/src/routes/sync.ts"
      provides: "Handler für SALE_CANCEL (sale.cancelledAt setzen + Stock +quantity) und ITEM_RETURN (Stock +quantity für einzelnen Artikel)"
    - path: "client/src/features/admin/reports/SaleDetailModal.tsx"
      provides: "Stornieren-Button + per-Item Rückgabe-Buttons mit Outbox-Logik"
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "Stornierte Verkäufe visuell mit Durchstreichung und roter Markierung"
  key_links:
    - from: "SaleDetailModal.tsx (Stornieren-Button)"
      to: "db.outbox (SALE_CANCEL)"
      via: "db.outbox.add({ operation: 'SALE_CANCEL', payload: { saleId, items } })"
      pattern: "SALE_CANCEL"
    - from: "SaleDetailModal.tsx (Rückgabe-Button)"
      to: "db.outbox (ITEM_RETURN)"
      via: "db.outbox.add({ operation: 'ITEM_RETURN', payload: { saleId, productId, quantity } })"
      pattern: "ITEM_RETURN"
    - from: "server/src/routes/sync.ts"
      to: "products.stock"
      via: "sql`${products.stock} + ${quantity}` für SALE_CANCEL und ITEM_RETURN"
      pattern: "stock.*\\+"
---

<objective>
Storno und Rückgabe implementieren: vollständige Verkaufsstornierung (STOR-01) und Einzelartikel-Rückgabe (STOR-02). Beide Operationen schreiben offline in die Outbox und synchronisieren den Bestand beim nächsten Reconnect.

Purpose: Fehlgebuchte Verkäufe korrigierbar machen — Kassenmitarbeiterinnen können Tippfehler und Irrtümer ohne Admin-Eingriff beheben
Output: Storno-Button im SaleDetailModal, per-Artikel-Rückgabe-Buttons, visuelle Markierung in DailyReport, Server-Handler für SALE_CANCEL + ITEM_RETURN
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Typen und Contracts — Executor benötigt keine zusätzliche Codebase-Exploration -->

Aus client/src/db/schema.ts:
```typescript
export interface Sale {
  id: string;
  shopId: string;
  items: SaleItem[];
  totalCents: number;
  paidCents: number;
  changeCents: number;
  donationCents: number;
  createdAt: number;  // Unix-Timestamp ms
  syncedAt?: number;
  // NEU (Task 1): cancelledAt?: number; returnedItems?: string[];
}

export interface OutboxEntry {
  id?: number;
  operation: 'SALE_COMPLETE' | 'STOCK_ADJUST'; // NEU: | 'SALE_CANCEL' | 'ITEM_RETURN'
  payload: unknown;
  shopId: string;
  createdAt: number;
  attempts: number;
}
```

Aus server/src/db/schema.ts (Drizzle):
```typescript
// sales-Tabelle hat KEIN cancelledAt — muss in Task 1 ergänzt werden
export const sales = sqliteTable('sales', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  // ... weitere Felder
  syncedAt: integer('synced_at'),
  // NEU (Task 1): cancelledAt: integer('cancelled_at'),
});
```

Aus server/src/routes/sync.ts (bestehendes Muster für neue Operationen):
```typescript
// Muster für neuen Operation-Handler (nach STOCK_ADJUST einfügen):
if (entry.operation === 'SALE_CANCEL') {
  // Schema validieren, dann db.transaction(() => { ... })
}

const StockAdjustSchema = z.object({ productId, delta, reason?, shopId });
// Analoges Schema für SALE_CANCEL und ITEM_RETURN definieren (auf Modul-Ebene!)
```

Aus client/src/features/admin/reports/SaleDetailModal.tsx:
```typescript
// Props-Interface bleibt stabil, Callbacks ergänzen:
interface SaleDetailModalProps {
  sale: Sale;
  onClose: () => void;
  // NEU (Task 2): onCancelSale?: () => void;  (optional für Rückwärtskompatibilität)
}
// Storno-Button: onPointerDown, nicht onClick (iOS-Konvention dieser App)
// Rückgabe-Buttons: pro item in der Tabelle, disabled wenn item in sale.returnedItems
```

Aus client/src/features/admin/reports/DailyReport.tsx:
```typescript
// DailyReport rendert SaleDetailModal:
{selectedSale && (
  <SaleDetailModal sale={selectedSale} onClose={() => setSelectedSale(null)} />
)}
// NEU (Task 2): onCancelSale-Callback übergeben, der selectedSale auf null setzt
// Stornierte Zeilen: sale.cancelledAt !== undefined → line-through + red styling
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Schemas erweitern — Dexie v4, Drizzle, OutboxEntry-Typen</name>
  <files>
    client/src/db/schema.ts
    server/src/db/schema.ts
  </files>

  <read_first>
    - client/src/db/schema.ts (vollständig — aktuelles Dexie-Schema, Version 3)
    - server/src/db/schema.ts (vollständig — aktuelles Drizzle-Schema)
  </read_first>

  <action>
**client/src/db/schema.ts:**

1. Sale-Interface erweitern:
   ```typescript
   cancelledAt?: number;     // Unix-Timestamp ms, undefined = aktiv
   returnedItems?: string[]; // productIds die zurückgegeben wurden
   ```

2. OutboxEntry.operation um neue Typen erweitern:
   ```typescript
   operation: 'SALE_COMPLETE' | 'STOCK_ADJUST' | 'SALE_CANCEL' | 'ITEM_RETURN';
   ```

3. Dexie-Version auf 4 erhöhen mit Upgrade-Migration:
   ```typescript
   this.version(4).stores({
     products: 'id, shopId, category, active, [shopId+active]',
     sales: 'id, shopId, createdAt, syncedAt, cancelledAt',
     outbox: '++id, shopId, createdAt, operation',
   }).upgrade(tx => {
     // Neue optionale Felder brauchen kein aktives Upgrade — Dexie füllt undefined
     return Promise.resolve();
   });
   ```
   WICHTIG: `cancelledAt` muss im Dexie-Index stehen damit `filter(s => !s.cancelledAt)` effizient läuft.

**server/src/db/schema.ts:**

4. sales-Tabelle: `cancelledAt`-Spalte ergänzen:
   ```typescript
   cancelledAt: integer('cancelled_at'), // nullable — kein .notNull()
   ```

   Diese Spalte erfordert eine Drizzle-Migration. Da `drizzle-kit migrate` lokal nicht läuft (arm64 macOS, Phase-03-Entscheidung), wird die Migration im Docker-Container beim Deployment ausgeführt. Das SQL dafür lautet:
   ```sql
   ALTER TABLE sales ADD COLUMN cancelled_at INTEGER;
   ```
   Executor erstellt dazu `server/src/db/migrations/0004_add_cancelled_at.sql` mit genau diesem Statement — die Migration wird via `drizzle-kit` im Container-Entrypoint aufgenommen.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
    - Sale-Interface hat cancelledAt?: number und returnedItems?: string[]
    - OutboxEntry.operation enthält 'SALE_CANCEL' | 'ITEM_RETURN'
    - Dexie-Version ist 4, Index für cancelledAt ist definiert
    - server/src/db/schema.ts hat cancelledAt: integer('cancelled_at') ohne notNull
    - Migration-SQL-Datei existiert unter server/src/db/migrations/0004_add_cancelled_at.sql
    - TypeScript-Compile ohne neue Fehler
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Server-Handler — SALE_CANCEL und ITEM_RETURN in sync.ts</name>
  <files>
    server/src/routes/sync.ts
  </files>

  <read_first>
    - server/src/routes/sync.ts (vollständig — bestehende Handler als Muster)
    - server/src/db/schema.ts (nach Task 1 — cancelledAt ist jetzt vorhanden)
  </read_first>

  <action>
Nach dem STOCK_ADJUST-Block zwei neue Operationen einfügen. Schemas immer auf Modul-Ebene definieren (nicht im Handler-Body — Phase-03-Entscheidung).

**1. Schemas auf Modul-Ebene ergänzen** (nach StockAdjustSchema):
```typescript
const SaleCancelSchema = z.object({
  saleId: z.string(),
  shopId: z.string(),
  items: z.array(SaleItemSchema), // Items für Stock-Reversal
  cancelledAt: z.number().int(),
});

const ItemReturnSchema = z.object({
  saleId: z.string(),
  shopId: z.string(),
  productId: z.string(),
  quantity: z.number().int().positive(),
  returnedAt: z.number().int(),
});
```

**2. SALE_CANCEL-Handler** (nach dem STOCK_ADJUST-Block):
```typescript
if (entry.operation === 'SALE_CANCEL') {
  const cancelResult = SaleCancelSchema.safeParse(entry.payload);
  if (!cancelResult.success) {
    errors.push({ index: i, message: 'Ungültiger SALE_CANCEL-Payload: ' + JSON.stringify(cancelResult.error.flatten()) });
    continue;
  }
  const cancel = cancelResult.data;
  db.transaction((tx) => {
    // Sale als storniert markieren (idempotent via cancelledAt)
    tx.update(sales)
      .set({ cancelledAt: cancel.cancelledAt })
      .where(eq(sales.id, cancel.saleId))
      .run();

    // Bestand für alle Artikel zurückbuchen (Delta +quantity)
    for (const item of cancel.items) {
      tx.update(products)
        .set({ stock: sql`${products.stock} + ${item.quantity}` })
        .where(eq(products.id, item.productId))
        .run();
    }

    // OutboxEvent protokollieren
    tx.insert(outboxEvents).values({
      shopId: entry.shopId,
      operation: entry.operation,
      payload: entry.payload,
      processedAt: Date.now(),
      createdAt: entry.createdAt,
    }).run();
  });
  processed++;
}
```

**3. ITEM_RETURN-Handler** (nach SALE_CANCEL):
```typescript
if (entry.operation === 'ITEM_RETURN') {
  const returnResult = ItemReturnSchema.safeParse(entry.payload);
  if (!returnResult.success) {
    errors.push({ index: i, message: 'Ungültiger ITEM_RETURN-Payload: ' + JSON.stringify(returnResult.error.flatten()) });
    continue;
  }
  const ret = returnResult.data;
  db.transaction((tx) => {
    // Bestand für zurückgegebenen Artikel zurückbuchen
    tx.update(products)
      .set({ stock: sql`${products.stock} + ${ret.quantity}` })
      .where(eq(products.id, ret.productId))
      .run();

    // OutboxEvent protokollieren
    tx.insert(outboxEvents).values({
      shopId: entry.shopId,
      operation: entry.operation,
      payload: entry.payload,
      processedAt: Date.now(),
      createdAt: entry.createdAt,
    }).run();
  });
  processed++;
}
```

WICHTIG: `cancelledAt` muss im Drizzle-Import aus `../db/schema.js` verfügbar sein — sicherstellen dass `sales` im destructuring vollständig bleibt.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>

  <done>
    - SaleCancelSchema und ItemReturnSchema auf Modul-Ebene definiert (nicht im Handler)
    - SALE_CANCEL-Handler setzt sales.cancelledAt und addiert Stock für alle Items
    - ITEM_RETURN-Handler addiert Stock für einen einzelnen Artikel
    - Beide Handler protokollieren in outboxEvents
    - TypeScript-Compile ohne neue Fehler
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: UI — SaleDetailModal (Buttons) + DailyReport (visuelle Markierung)</name>
  <files>
    client/src/features/admin/reports/SaleDetailModal.tsx
    client/src/features/admin/reports/DailyReport.tsx
  </files>

  <read_first>
    - client/src/features/admin/reports/SaleDetailModal.tsx (vollständig)
    - client/src/features/admin/reports/DailyReport.tsx (vollständig)
    - client/src/db/schema.ts (nach Task 1 — Sale hat cancelledAt, returnedItems)
    - client/src/db/index.ts (für db-Import in SaleDetailModal)
  </read_first>

  <action>
**SaleDetailModal.tsx:**

Props-Interface erweitern (optional, rückwärtskompatibel):
```typescript
interface SaleDetailModalProps {
  sale: Sale;
  onClose: () => void;
  onSaleChanged?: () => void; // Callback nach Storno/Rückgabe — DailyReport refresht
}
```

Storno-Button im Footer-Bereich (unterhalb der Summen, vor dem Schließen-Button):
- Nur zeigen wenn `!sale.cancelledAt` (Sale noch nicht storniert)
- Confirmation via `window.confirm('Verkauf stornieren? Alle Artikel werden zurückgebucht.')` — einfach, kein Custom-Modal nötig
- onPointerDown (iOS-Konvention)
- Bei Bestätigung:
  1. `await db.sales.update(sale.id, { cancelledAt: Date.now() })` — lokaler Soft-Delete
  2. Bestand lokal für alle Items zurückbuchen:
     ```typescript
     for (const item of sale.items) {
       await db.products
         .where('id')
         .equals(item.productId)
         .modify(p => { p.stock += item.quantity; });
     }
     ```
  3. Outbox-Eintrag erstellen:
     ```typescript
     await db.outbox.add({
       operation: 'SALE_CANCEL',
       payload: {
         saleId: sale.id,
         shopId: sale.shopId,
         items: sale.items,
         cancelledAt: Date.now(),
       },
       shopId: sale.shopId,
       createdAt: Date.now(),
       attempts: 0,
     });
     ```
  4. `onSaleChanged?.()` aufrufen, dann `onClose()` — Modal schließt

Per-Artikel Rückgabe-Buttons (neue Spalte "Rückgabe" in der items-Tabelle):
- Button-Label: "Zurück" (kurz für Touch)
- Disabled wenn: `sale.cancelledAt` (ganzer Verkauf storniert) ODER `sale.returnedItems?.includes(item.productId)` (bereits zurückgegeben)
- Kein confirm bei Einzelartikel — Aktion ist kleiner
- Bei Klick:
  1. `await db.sales.update(sale.id, { returnedItems: [...(sale.returnedItems ?? []), item.productId] })` — lokale Markierung
  2. Bestand lokal zurückbuchen: `db.products.where('id').equals(item.productId).modify(p => { p.stock += item.quantity; })`
  3. Outbox:
     ```typescript
     await db.outbox.add({
       operation: 'ITEM_RETURN',
       payload: {
         saleId: sale.id,
         shopId: sale.shopId,
         productId: item.productId,
         quantity: item.quantity,
         returnedAt: Date.now(),
       },
       shopId: sale.shopId,
       createdAt: Date.now(),
       attempts: 0,
     });
     ```
  4. `onSaleChanged?.()` (kein onClose — Modal bleibt offen für weitere Rückgaben)

Wenn Sale bereits storniert: roter Banner oben im Modal — "Storniert" + Datum/Uhrzeit.

Wenn Artikel zurückgegeben: Tabellenzeile mit `opacity-50 line-through text-slate-400` + "Zurück"-Button grün/ausgegraut mit Checkmark.

**DailyReport.tsx:**

1. Stornierte Tabellenzeilen stylen:
   ```tsx
   className={`border-b border-slate-50 last:border-0 cursor-pointer transition-colors ${
     sale.cancelledAt
       ? 'bg-red-50 hover:bg-red-100 active:bg-red-100 opacity-60'
       : 'hover:bg-sky-50 active:bg-sky-100'
   }`}
   ```

2. Uhrzeit-Zelle bei stornierten Verkäufen mit Durchstreichung:
   ```tsx
   <td className={`px-4 py-3 ${sale.cancelledAt ? 'line-through text-red-400' : 'text-slate-600'}`}>
     {format(new Date(sale.createdAt), 'HH:mm')}
     {sale.cancelledAt && <span className="ml-1 text-xs font-medium text-red-500">Storno</span>}
   </td>
   ```

3. SaleDetailModal-Aufruf: `onSaleChanged`-Callback übergeben der `setSelectedSale(null)` aufruft (Modal schließt, DailyReport livequery refresht automatisch via Dexie useLiveQuery):
   ```tsx
   <SaleDetailModal
     sale={selectedSale}
     onClose={() => setSelectedSale(null)}
     onSaleChanged={() => setSelectedSale(null)}
   />
   ```

4. Kennzahlen-Stats: stornierte Verkäufe aus count, totalCents, donationCents herausrechnen:
   ```typescript
   const stats = useMemo(() => {
     if (!sales) return { count: 0, totalCents: 0, donationCents: 0 };
     const active = sales.filter(s => !s.cancelledAt);
     return {
       count: active.length,
       totalCents: active.reduce((sum, s) => sum + s.totalCents, 0),
       donationCents: active.reduce((sum, s) => sum + s.donationCents, 0),
     };
   }, [sales]);
   ```
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - SaleDetailModal zeigt "Stornieren"-Button wenn sale.cancelledAt undefined
    - Stornieren erzeugt Outbox-Eintrag mit operation='SALE_CANCEL' in IndexedDB
    - Jede Artikel-Zeile hat "Zurück"-Button, disabled wenn returnedItems enthält productId
    - Rückgabe erzeugt Outbox-Eintrag mit operation='ITEM_RETURN' in IndexedDB
    - Stornierter Sale zeigt roten Banner "Storniert" im Modal
    - DailyReport-Zeile eines stornierten Verkaufs hat line-through + "Storno"-Label
    - Kennzahlen-Kacheln zählen stornierte Verkäufe nicht mit
    - TypeScript-Compile ohne neue Fehler
  </acceptance_criteria>

  <done>
    - Storno-Button funktioniert, Modal schließt, DailyReport-Zeile wird rot/durchgestrichen
    - Rückgabe-Button funktioniert, Modal bleibt offen, Artikel-Zeile wird gedimmt
    - Beide Aktionen landen in db.outbox mit korrektem Schema
    - Lokaler Bestand wird sofort angepasst (items.modify stock + quantity)
    - TypeScript-Compile grün
  </done>
</task>

</tasks>

<verification>
Gesamtprüfung nach allen Tasks:

1. `npx tsc --noEmit` im Projektwurzel — keine TypeScript-Fehler
2. In der App: Tagesübersicht aufrufen → Verkauf antippen → Modal zeigt "Stornieren"-Button und "Zurück"-Buttons pro Artikel
3. Storno: Bestätigen → Modal schließt → DailyReport-Zeile rot + "Storno"-Label
4. IndexedDB prüfen (DevTools → Application → IndexedDB → fairstand-db → outbox): Eintrag mit operation='SALE_CANCEL' vorhanden
5. Rückgabe: Artikel-Button → Modal bleibt offen → Artikel-Zeile gedimmt → Outbox-Eintrag mit operation='ITEM_RETURN' vorhanden
6. Server (wenn online): POST /api/sync mit dem Outbox-Batch → Response enthält processed: 1, errors: []
</verification>

<success_criteria>
STOR-01 erfüllt wenn:
- Stornieren-Button in SaleDetailModal für nicht-stornierte Verkäufe sichtbar
- Klick + Bestätigung setzt sale.cancelledAt lokal und erstellt SALE_CANCEL-Outbox-Eintrag
- Server verarbeitet SALE_CANCEL: sales.cancelledAt gesetzt + products.stock + quantity für alle Items
- DailyReport markiert stornierte Verkäufe visuell (Durchstreichung, rotes Badge)
- Kennzahlen zählen stornierte Verkäufe nicht

STOR-02 erfüllt wenn:
- Pro Artikel-Zeile im SaleDetailModal ein "Zurück"-Button sichtbar
- Bereits zurückgegebene Artikel haben deaktivierten Button
- Klick setzt sale.returnedItems lokal und erstellt ITEM_RETURN-Outbox-Eintrag
- Server verarbeitet ITEM_RETURN: products.stock + quantity für den zurückgegebenen Artikel
</success_criteria>

<output>
Nach Abschluss `.planning/phases/09-storno-rueckgabe/09-01-SUMMARY.md` erstellen.
</output>
