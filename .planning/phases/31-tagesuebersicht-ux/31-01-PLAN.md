---
phase: 31-tagesuebersicht-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/admin/reports/DailyReport.tsx
autonomous: true
requirements:
  - HIST-01
  - HIST-02

must_haves:
  truths:
    - "Tabellenzeilen mit Spendenbetrag > 0 sind visuell hervorgehoben (farbig markiert)"
    - "Der Spendenbetrag in der Tabelle ist fett/farbig dargestellt, sodass er auf einen Blick erkennbar ist"
    - "Der native Kalender-Datepicker ist prominent platziert und wird bei Auswahl automatisch aktiv (custom-Modus)"
    - "Die Preset-Buttons funktionieren weiterhin (keine Regression)"
  artifacts:
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "Spendenmarkierung + Datepicker-UX"
      contains: "donationCents > 0"
  key_links:
    - from: "DailyReport.tsx"
      to: "sale.donationCents"
      via: "conditional Tailwind class on <tr>"
      pattern: "donationCents > 0"
---

<objective>
Die Tagesübersicht in DailyReport.tsx erhält zwei UX-Verbesserungen:

1. Verkäufe mit Spende werden in der Tabelle visuell markiert — die Zeile bekommt einen grünen Hintergrund, der Spendenbetrag wird fett und deutlich grün dargestellt.
2. Der bereits vorhandene native Datepicker (`<input type="date">`) wird optisch aufgewertet und erhält ein Label, sodass er als eigenständige Datumsauswahl klar erkennbar ist — HIST-02 ist technisch bereits erfüllt (onChange setzt `rangeMode` auf `custom`), braucht nur bessere visuelle Kommunikation.

Purpose: Mitarbeiterinnen sollen auf einen Blick sehen, welche Verkäufe Spenden enthielten — ohne jeden Eintrag einzeln lesen zu müssen.
Output: DailyReport.tsx mit Spendenmarkierung in Tabellenzeilen und verbessertem Datepicker-Label.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<interfaces>
<!-- Aus DailyReport.tsx extrahiert — vollständige aktuelle Implementierung -->

Sale-Typ (aus client/src/db/index.ts):
```typescript
interface Sale {
  id: string;
  shopId: string;
  items: Sale['items'];
  totalCents: number;
  paidCents: number;
  changeCents: number;
  donationCents: number;       // > 0 wenn Spende erfasst
  type: 'sale' | 'withdrawal';
  withdrawalReason?: string;
  createdAt: number;
  cancelledAt?: number;
  returnedItems?: string[];
}
```

Aktuelles Tabellen-Row-Rendering (Zeile 222-249 in DailyReport.tsx):
```tsx
<tr
  key={sale.id}
  className={`border-b border-slate-50 last:border-0 cursor-pointer transition-colors ${
    sale.cancelledAt
      ? 'bg-red-50 hover:bg-red-100 active:bg-red-100 opacity-60'
      : 'hover:bg-sky-50 active:bg-sky-100'
  }`}
  onPointerDown={() => setSelectedSale(sale)}
>
```

Aktuelle Spenden-Zelle (Zeile 245-247):
```tsx
<td className={`px-4 py-3 text-right font-medium ${sale.cancelledAt ? 'text-red-300' : 'text-green-600'}`}>
  {sale.donationCents > 0 ? formatEur(sale.donationCents) : '—'}
</td>
```

Aktueller Datepicker (Zeile 155-166):
```tsx
<input
  type="date"
  value={toDateInputValue(customDate)}
  onChange={e => {
    const d = new Date(e.target.value);
    if (!isNaN(d.getTime())) {
      setCustomDate(d);
      setRangeMode('custom');
    }
  }}
  className="min-h-[44px] border border-slate-200 rounded-lg px-3 text-sm focus:outline-none focus:border-sky-400"
/>
```
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Spendenmarkierung in der Verkaufstabelle (HIST-01)</name>
  <files>client/src/features/admin/reports/DailyReport.tsx</files>
  <read_first>
    - client/src/features/admin/reports/DailyReport.tsx (vollständig — bereits geladen in interfaces)
  </read_first>
  <action>
In DailyReport.tsx die `<tr>`-Klassen-Logik erweitern:

**Tabellenzeile (per D-Discretion: grüner Hintergrund bei Spende):**
Die bestehende className-Berechnung für `<tr>` erhält eine dritte Bedingung. Priorität: storniert > Spende > normal.

```tsx
className={`border-b border-slate-50 last:border-0 cursor-pointer transition-colors ${
  sale.cancelledAt
    ? 'bg-red-50 hover:bg-red-100 active:bg-red-100 opacity-60'
    : sale.donationCents > 0
    ? 'bg-green-50 hover:bg-green-100 active:bg-green-100'
    : 'hover:bg-sky-50 active:bg-sky-100'
}`}
```

**Spenden-Zelle:** Wenn `donationCents > 0` und nicht storniert, Betrag fett und dunkelgrün:
```tsx
<td className={`px-4 py-3 text-right font-medium ${
  sale.cancelledAt
    ? 'text-red-300'
    : sale.donationCents > 0
    ? 'text-green-700 font-bold'
    : 'text-green-600'
}`}>
  {sale.donationCents > 0 ? formatEur(sale.donationCents) : '—'}
</td>
```

Nur diese zwei Stellen ändern — alle anderen Teile der Komponente bleiben unberührt.
  </action>
  <verify>
    Visuell: In der Verkaufstabelle erscheinen Zeilen mit Spende mit grünem Hintergrund (`bg-green-50`).
    Grep-Check:
    ```
    grep -n "bg-green-50" client/src/features/admin/reports/DailyReport.tsx
    grep -n "font-bold" client/src/features/admin/reports/DailyReport.tsx
    ```
    Beide Befehle müssen mindestens 1 Treffer liefern.
  </verify>
  <acceptance_criteria>
    - grep "bg-green-50" client/src/features/admin/reports/DailyReport.tsx liefert Treffer
    - grep "font-bold" client/src/features/admin/reports/DailyReport.tsx liefert Treffer
    - grep "sale.donationCents > 0" client/src/features/admin/reports/DailyReport.tsx liefert mindestens 2 Treffer (tr-class + td-class)
    - grep "bg-red-50" client/src/features/admin/reports/DailyReport.tsx liefert weiterhin Treffer (Storno-Zeilen unverändert)
  </acceptance_criteria>
  <done>Verkäufe mit Spende haben grünen Zeilenhintergrund, der Spendenbetrag ist fett und dunkelgrün. Storno-Zeilen bleiben rot. Normale Zeilen unverändert.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Datepicker-Label und aktiver Zustand (HIST-02)</name>
  <files>client/src/features/admin/reports/DailyReport.tsx</files>
  <read_first>
    - client/src/features/admin/reports/DailyReport.tsx (Zeile 142-179 — Preset-Buttons und Datepicker)
  </read_first>
  <action>
Der native `<input type="date">` ist funktional vollständig (setzt bereits `rangeMode='custom'`), aber optisch nicht klar als eigenständige Datumsauswahl erkennbar. Verbesserungen:

1. **Aktivzustand**: Der Datepicker erhält denselben aktiven Stil wie die Preset-Buttons (`bg-sky-500 text-white`) wenn `rangeMode === 'custom'`:

```tsx
<input
  type="date"
  value={toDateInputValue(customDate)}
  onChange={e => {
    const d = new Date(e.target.value);
    if (!isNaN(d.getTime())) {
      setCustomDate(d);
      setRangeMode('custom');
    }
  }}
  className={`min-h-[44px] rounded-lg px-3 text-sm focus:outline-none transition-colors ${
    rangeMode === 'custom'
      ? 'bg-sky-500 text-white border border-sky-500 [color-scheme:dark]'
      : 'border border-slate-200 focus:border-sky-400'
  }`}
/>
```

Hinweis: `[color-scheme:dark]` ist nötig damit der native Kalender-Icon auf weißem Text sichtbar bleibt (Safari/iOS). Nur diese eine Stelle ändern.
  </action>
  <verify>
    Grep-Checks:
    ```
    grep -n "rangeMode === 'custom'" client/src/features/admin/reports/DailyReport.tsx
    grep -n "color-scheme" client/src/features/admin/reports/DailyReport.tsx
    ```
    Erster Befehl muss mindestens 2 Treffer liefern (der bestehende im `useMemo` + der neue in der className).
  </verify>
  <acceptance_criteria>
    - grep "color-scheme" client/src/features/admin/reports/DailyReport.tsx liefert Treffer
    - grep -c "rangeMode === 'custom'" client/src/features/admin/reports/DailyReport.tsx gibt mindestens 2 aus
    - Der input type="date" hat bedingte className-Logik (kein statischer String mehr)
  </acceptance_criteria>
  <done>Wenn rangeMode=custom aktiv ist, erscheint der Datepicker mit sky-500-Hintergrund wie die anderen aktiven Buttons. Bei anderen Modi hat er den Standard-Stil. Kein anderer Code geändert.</done>
</task>

</tasks>

<verification>
Nach Ausführung beider Tasks:

```bash
# Spendenmarkierung vorhanden
grep -n "bg-green-50" client/src/features/admin/reports/DailyReport.tsx
grep -n "font-bold" client/src/features/admin/reports/DailyReport.tsx

# Datepicker aktiver Zustand
grep -n "color-scheme" client/src/features/admin/reports/DailyReport.tsx

# Keine Regression bei Storno-Stil
grep -n "bg-red-50" client/src/features/admin/reports/DailyReport.tsx

# TypeScript-Kompilierung
cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -20
```

Alle Befehle müssen ohne Fehler durchlaufen.
</verification>

<success_criteria>
1. Verkäufe mit `donationCents > 0` in der Tagesübersicht haben grünen Zeilenhintergrund (`bg-green-50`) — auf einen Blick erkennbar ohne jeden Eintrag lesen zu müssen (HIST-01)
2. Der Spendenbetrag in Zeilen mit Spende ist fett (`font-bold`) und dunkelgrün (`text-green-700`) statt nur normal grün (HIST-01)
3. Der native Datepicker erhält denselben aktiven Stil (sky-500) wie die Preset-Buttons wenn er ausgewählt ist — `rangeMode === 'custom'` steuert den visuellen Zustand (HIST-02)
4. TypeScript-Kompilierung ohne Fehler
5. Keine Regression: Storno-Zeilen bleiben rot, Preset-Buttons weiterhin funktional
</success_criteria>

<output>
Nach Completion: `.planning/phases/31-tagesuebersicht-ux/31-01-SUMMARY.md` erstellen.
</output>
