---
phase: 15-datenintegrit-t
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/reports.ts
autonomous: true
requirements:
  - DAT-01
  - DAT-02

must_haves:
  truths:
    - "Ein Monatsbericht zählt stornierte Verkäufe nicht in sale_count"
    - "Ein Monatsbericht berechnet Umsatz, EK-Kosten und Marge ohne stornierte Verkäufe"
    - "Die Top-5-Artikel-Rangliste enthält keine Artikel aus stornierten Verkäufen"
    - "Die Jahresübersicht pro Monat rechnet stornierte Verkäufe nicht mit"
    - "Produkt-Statistiken zählen keine Transaktionen aus stornierten Verkäufen"
  artifacts:
    - path: "server/src/routes/reports.ts"
      provides: "Alle fünf SQL-Queries mit AND cancelled_at IS NULL gefiltert"
      contains: "AND cancelled_at IS NULL"
  key_links:
    - from: "server/src/routes/reports.ts"
      to: "SQLite sales.cancelled_at"
      via: "WHERE sales.cancelled_at IS NULL"
      pattern: "cancelled_at IS NULL"
---

<objective>
Alle fünf SQL-Queries in reports.ts um `AND cancelled_at IS NULL` (bzw. `AND sales.cancelled_at IS NULL`) erweitern, damit stornierte Verkäufe nicht mehr in Umsatz, EK-Kosten, Marge, Spenden, Top-Artikel-Ranking und Produktstatistiken einfließen.

Purpose: DAT-01 (Marge/EK korrekt) und DAT-02 (Stornos herausgerechnet) — aktuell fließen alle stornierten Verkäufe unkontrolliert in die Berichte ein.
Output: reports.ts mit korrekten SQL-Filtern in allen Endpunkten.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Monatsbericht-Queries um Storno-Filter ergänzen</name>
  <files>server/src/routes/reports.ts</files>
  <read_first>
    - server/src/routes/reports.ts (vollständig lesen — alle drei Endpunkte)
  </read_first>
  <action>
In `reportRoutes` (GET /reports/monthly) drei SQL-Queries anpassen:

**1. summary-Query (Zeile ~17):** Nach `AND created_at < ${monthEnd}` einfügen:
```sql
AND cancelled_at IS NULL
```

**2. costResult-Query (Zeile ~31):** Nach `AND sales.created_at < ${monthEnd}` einfügen:
```sql
AND sales.cancelled_at IS NULL
```

**3. topArticles-Query (Zeile ~44):** Nach `AND sales.created_at < ${monthEnd}` einfügen:
```sql
AND sales.cancelled_at IS NULL
```

**Wichtig:** `cancelled_at` in SQLite wird als INTEGER-NULL gespeichert (nicht als 0) — `IS NULL` ist korrekt, `= 0` wäre falsch. Das Drizzle-Schema speichert `cancelledAt?: number` als NULL wenn undefined.
  </action>
  <verify>
    <automated>grep -n "cancelled_at IS NULL" /Users/simonluthe/Documents/fairstand/server/src/routes/reports.ts | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - grep "cancelled_at IS NULL" reports.ts liefert mindestens 5 Treffer (je eine Zeile pro Query)
    - summary-Query enthält `AND cancelled_at IS NULL` direkt nach dem created_at-Filter
    - costResult-Query enthält `AND sales.cancelled_at IS NULL`
    - topArticles-Query enthält `AND sales.cancelled_at IS NULL`
  </acceptance_criteria>
  <done>Alle drei Queries des Monatsberichts filtern stornierte Verkäufe korrekt heraus</done>
</task>

<task type="auto">
  <name>Task 2: Jahresbericht- und Produktstatistik-Queries um Storno-Filter ergänzen</name>
  <files>server/src/routes/reports.ts</files>
  <read_first>
    - server/src/routes/reports.ts (vollständig lesen — Endpunkte /reports/yearly und /reports/product/:id/stats)
  </read_first>
  <action>
In `reportRoutes` zwei weitere SQL-Queries anpassen:

**4. months-Query in GET /reports/yearly (Zeile ~87):** Nach `AND created_at < ${yearEnd}` einfügen:
```sql
AND cancelled_at IS NULL
```

**5. monthlyCosts-Query in GET /reports/yearly (Zeile ~102):** Nach `AND sales.created_at < ${yearEnd}` einfügen:
```sql
AND sales.cancelled_at IS NULL
```

**6. result-Query in GET /reports/product/:id/stats (Zeile ~147):** Nach `AND sales.created_at >= ${sinceTs}` einfügen:
```sql
AND sales.cancelled_at IS NULL
```

Nach allen Änderungen: TypeScript-Build prüfen um sicherzustellen dass keine Syntax-Fehler eingeführt wurden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - grep "cancelled_at IS NULL" reports.ts liefert mindestens 6 Treffer (5 Queries + ggf. product stats hat ebenfalls sales.-Präfix)
    - `npx tsc --noEmit` im server-Verzeichnis meldet 0 Fehler
    - Alle 5 Endpunkte (summary, costResult, topArticles, months, monthlyCosts, product stats) haben den Filter
    - Kein `AND cancelled_at = 0` — ausschließlich `IS NULL` verwenden
  </acceptance_criteria>
  <done>Alle SQL-Queries in reports.ts filtern stornierte Verkäufe heraus — DAT-01 und DAT-02 erfüllt</done>
</task>

</tasks>

<verification>
```bash
# Alle cancelled_at IS NULL Filter zählen — erwartet: >= 6
grep -c "cancelled_at IS NULL" /Users/simonluthe/Documents/fairstand/server/src/routes/reports.ts

# TypeScript-Compiler ohne Fehler
cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit

# Kein alter Fehler-Pattern vorhanden (kein Filter fehlt)
grep -n "AND created_at <\|AND sales.created_at <" /Users/simonluthe/Documents/fairstand/server/src/routes/reports.ts
# Jede dieser Zeilen muss eine Folgezeile mit cancelled_at IS NULL haben
```
</verification>

<success_criteria>
- grep "cancelled_at IS NULL" in reports.ts liefert 6 Treffer (summary, costResult, topArticles, months, monthlyCosts, product stats)
- TypeScript-Build ist fehlerfrei
- DailyReport.tsx (clientseitig) bleibt unverändert — filtert bereits korrekt mit `filter(s => !s.cancelledAt)`
</success_criteria>

<output>
Nach Abschluss `.planning/phases/15-datenintegrit-t/15-01-SUMMARY.md` erstellen mit:
- Welche Queries geändert wurden (Zeilennummern)
- Anzahl der eingefügten Filter
- Bestätigung TypeScript-Build erfolgreich
</output>
