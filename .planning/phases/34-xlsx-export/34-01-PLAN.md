---
phase: 34-xlsx-export
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/reports.ts
  - client/src/features/admin/reports/InventurTab.tsx
  - client/src/features/admin/reports/DailyReport.tsx
autonomous: true
requirements:
  - EXP-04
  - EXP-05

must_haves:
  truths:
    - "Im Inventur-Tab gibt es neben 'Inventur CSV' und 'Inventur PDF' einen dritten Button 'Inventur Excel'"
    - "Im Tagesübersicht-Tab gibt es neben dem CSV-Button einen weiteren Button 'Excel'"
    - "Der Download erzeugt eine .xlsx-Datei die in Excel und Numbers fehlerfrei öffnet"
    - "Deutsche Umlaute (ä, ö, ü, Ä, Ö, Ü, ß) erscheinen korrekt in der Excel-Datei"
    - "Alle Spalten aus der Inventur-Tabellenansicht sind im XLSX vorhanden"
    - "Alle Spalten aus der Verkaufshistorie-CSV sind im XLSX vorhanden"
  artifacts:
    - path: "server/src/routes/reports.ts"
      provides: "GET /api/reports/inventory-xlsx und GET /api/reports/sales-xlsx Endpoints"
      contains: "inventory-xlsx"
    - path: "client/src/features/admin/reports/InventurTab.tsx"
      provides: "Excel-Download-Button neben bestehenden CSV/PDF-Buttons"
      contains: "handleInventurXlsxDownload"
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "Excel-Download-Button neben bestehendem CSV-Button"
      contains: "handleXlsxDownload"
  key_links:
    - from: "client/src/features/admin/reports/InventurTab.tsx"
      to: "/api/reports/inventory-xlsx"
      via: "authFetch in handleInventurXlsxDownload"
      pattern: "inventory-xlsx"
    - from: "client/src/features/admin/reports/DailyReport.tsx"
      to: "/api/reports/sales-xlsx"
      via: "authFetch in handleXlsxDownload"
      pattern: "sales-xlsx"
---

<objective>
XLSX-Export für Inventur und Verkaufshistorie hinzufügen — zwei neue Server-Endpoints und zwei neue Download-Buttons im Frontend.

Purpose: Mitarbeiterinnen können Inventur und Verkaufshistorie als Excel-Datei herunterladen, die direkt in Numbers auf dem iPad oder Excel öffnet — ohne CSV-Import-Aufwand.
Output: GET /api/reports/inventory-xlsx, GET /api/reports/sales-xlsx, je ein neuer Button in InventurTab und DailyReport.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

@client/src/features/admin/reports/InventurTab.tsx
@client/src/features/admin/reports/DailyReport.tsx
@server/src/routes/reports.ts
</context>

<interfaces>
<!-- Bestehende Export-Pattern aus dem Codebase — Executor braucht keine weitere Exploration -->

Aus server/src/routes/reports.ts — CSV-Pattern (zu replizieren für XLSX):
```typescript
// Authentifizierung
const session = (request as any).session as { shopId: string };
const shopId = session.shopId;

// Header-Pattern für Datei-Downloads
reply.header('Content-Type', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');
reply.header('Content-Disposition', `attachment; filename="inventur-${year}.xlsx"`);
```

Aus client/src/features/admin/reports/InventurTab.tsx — Download-Handler-Pattern:
```typescript
const [downloadingInventurCsv, setDownloadingInventurCsv] = useState(false);

async function handleInventurCsvDownload() {
  setDownloadingInventurCsv(true);
  try {
    const res = await authFetch(`/api/reports/inventory-csv?year=${year}`);
    if (!res.ok) throw new Error(`${res.status}`);
    const blob = await res.blob();
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = `inventur-${year}.csv`;
    a.click();
    URL.revokeObjectURL(a.href);
  } catch (err) {
    alert('CSV-Download fehlgeschlagen. Bitte erneut versuchen.');
  } finally {
    setDownloadingInventurCsv(false);
  }
}
```

Button-Pattern in InventurTab (grün = CSV, blau = PDF → neu: grün-dunkel für XLSX):
```tsx
<button
  onClick={handleInventurPdfDownload}
  disabled={downloadingInventurPdf}
  className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
>
  {downloadingInventurPdf ? (
    <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
  ) : <span>↓</span>}
  Inventur PDF
</button>
```

Inventur-SQL (aus inventory-csv, identisch wiederverwendbar):
Die SQL-Query aus `/reports/inventory-csv` ist identisch — Ergebnis enthält:
`id, article_number, name, current_stock, current_ek_cents, sold_qty, withdrawn_qty, revenue_cents, cost_cents, withdrawal_cost_cents`

Sales-SQL (aus sales-csv, identisch wiederverwendbar):
Die SQL-Query aus `/reports/sales-csv` ist identisch — Ergebnis enthält:
`id, items (JSONB-Array), total_cents, paid_cents, change_cents, donation_cents, created_at`
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: xlsx-Bibliothek installieren und Server-Endpoints erstellen</name>
  <files>server/src/routes/reports.ts</files>

  <read_first>
    - server/src/routes/reports.ts (Zeilen 545-667 für inventory-csv Pattern, Zeilen 473-543 für sales-csv Pattern)
    - server/package.json (um xlsx-Dependency korrekt einzutragen)
  </read_first>

  <action>
**Schritt 1: xlsx installieren**

Im Verzeichnis `/Users/simonluthe/Documents/fairstand/server/` installieren:
```bash
npm install xlsx
```

`xlsx` (SheetJS Community Edition, aktuell 0.18.x) ist die Standard-XLSX-Bibliothek — keine Alternativen nötig. Keine @types nötig (types inklusive).

**Schritt 2: Import hinzufügen**

In `server/src/routes/reports.ts` am Anfang der Imports hinzufügen:
```typescript
import * as XLSX from 'xlsx';
```

**Schritt 3: GET /reports/inventory-xlsx Endpoint**

In `reportRoutes()` nach dem `inventory-csv` Endpoint (nach Zeile 667) einfügen:

```typescript
// GET /reports/inventory-xlsx?year=... — Inventur als Excel (EXP-04)
fastify.get('/reports/inventory-xlsx', async (request, reply) => {
  const session = (request as any).session as { shopId: string };
  const shopId = session.shopId;
  const { year } = request.query as { year?: string };
  if (!year) return reply.status(400).send({ error: 'year required' });

  const y = Number(year);
  const yearStart = new Date(y, 0, 1).getTime();
  const yearEnd = new Date(y + 1, 0, 1).getTime();

  const inventoryResult = await db.execute(sql`
    SELECT
      p.id,
      p.article_number,
      p.name,
      p.stock as current_stock,
      p.purchase_price as current_ek_cents,
      COALESCE(SUM(
        CASE WHEN (sales.type IS NULL OR sales.type = 'sale')
        THEN (item->>'quantity')::integer ELSE 0 END
      ), 0) as sold_qty,
      COALESCE(SUM(
        CASE WHEN sales.type = 'withdrawal'
        THEN (item->>'quantity')::integer ELSE 0 END
      ), 0) as withdrawn_qty,
      COALESCE(SUM(
        CASE WHEN (sales.type IS NULL OR sales.type = 'sale')
        THEN (item->>'quantity')::integer * (item->>'salePrice')::integer ELSE 0 END
      ), 0) as revenue_cents,
      COALESCE(SUM(
        CASE WHEN (sales.type IS NULL OR sales.type = 'sale')
        THEN (item->>'quantity')::integer *
             COALESCE((item->>'purchasePrice')::integer, p.purchase_price)
        ELSE 0 END
      ), 0) as cost_cents,
      COALESCE(SUM(
        CASE WHEN sales.type = 'withdrawal'
        THEN (item->>'quantity')::integer *
             COALESCE((item->>'purchasePrice')::integer, p.purchase_price)
        ELSE 0 END
      ), 0) as withdrawal_cost_cents
    FROM products p
    LEFT JOIN sales ON sales.shop_id = p.shop_id
                    AND sales.created_at >= ${yearStart}
                    AND sales.created_at < ${yearEnd}
                    AND sales.cancelled_at IS NULL
    LEFT JOIN jsonb_array_elements(sales.items) as item
      ON item->>'productId' = p.id
    WHERE p.shop_id = ${shopId}
      AND p.active = true
    GROUP BY p.id, p.name, p.article_number, p.stock, p.purchase_price
    ORDER BY p.name
  `);

  const stockValueResult = await db.execute(sql`
    SELECT COALESCE(SUM(stock * purchase_price), 0) as total_stock_value_cents
    FROM products
    WHERE shop_id = ${shopId} AND active = true
  `);

  const items = (inventoryResult.rows as Record<string, unknown>[]).map(row => ({
    name: String(row.name),
    article_number: String(row.article_number),
    current_stock: Number(row.current_stock),
    current_ek_cents: Number(row.current_ek_cents),
    sold_qty: Number(row.sold_qty),
    withdrawn_qty: Number(row.withdrawn_qty),
    revenue_cents: Number(row.revenue_cents),
    cost_cents: Number(row.cost_cents),
    withdrawal_cost_cents: Number(row.withdrawal_cost_cents),
  }));

  const stockValueRow = (stockValueResult.rows[0] as Record<string, unknown>) ?? {};
  const totalStockValueCents = Number(stockValueRow.total_stock_value_cents ?? 0);
  const totalRevenue = items.reduce((s, i) => s + i.revenue_cents, 0);
  const totalWithdrawalCost = items.reduce((s, i) => s + i.withdrawal_cost_cents, 0);
  const totalCost = items.reduce((s, i) => s + i.cost_cents + i.withdrawal_cost_cents, 0);
  const margin = totalRevenue + totalWithdrawalCost - totalCost;

  // Worksheet-Daten aufbauen
  const wsData: (string | number)[][] = [
    ['Artikelname', 'Artikelnummer', 'Bestand', 'Verkauft', 'Entnahme', 'VK-Umsatz (EUR)', 'Entnahme EK (EUR)', 'EK-Kosten (EUR)', 'Bestandswert (EUR)'],
  ];

  for (const item of items) {
    wsData.push([
      item.name,
      item.article_number,
      item.current_stock,
      item.sold_qty,
      item.withdrawn_qty,
      Number((item.revenue_cents / 100).toFixed(2)),
      Number((item.withdrawal_cost_cents / 100).toFixed(2)),
      Number(((item.cost_cents + item.withdrawal_cost_cents) / 100).toFixed(2)),
      Number((item.current_stock * item.current_ek_cents / 100).toFixed(2)),
    ]);
  }

  // Leerzeile + Bilanz
  wsData.push(['', '', '', '', '', '', '', '', '']);
  wsData.push(['BILANZ', '', '', '', '', '', '', '', '']);
  wsData.push(['Einnahmen aus Verkauf (VK)', '', '', '', '', Number((totalRevenue / 100).toFixed(2)), '', '', '']);
  wsData.push(['Einnahmen aus Entnahme (EK)', '', '', '', '', '', Number((totalWithdrawalCost / 100).toFixed(2)), '', '']);
  wsData.push(['Gesamt EK-Kosten', '', '', '', '', '', '', Number((totalCost / 100).toFixed(2)), '']);
  wsData.push(['Marge', '', '', '', '', Number((margin / 100).toFixed(2)), '', '', Number((totalStockValueCents / 100).toFixed(2))]);

  const ws = XLSX.utils.aoa_to_sheet(wsData);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, `Inventur ${year}`);

  const buf = XLSX.write(wb, { type: 'buffer', bookType: 'xlsx' });

  reply.header('Content-Type', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');
  reply.header('Content-Disposition', `attachment; filename="inventur-${year}.xlsx"`);
  return reply.send(buf);
});
```

**Schritt 4: GET /reports/sales-xlsx Endpoint**

Nach dem neuen inventory-xlsx Endpoint einfügen:

```typescript
// GET /reports/sales-xlsx?from=...&to=... — Verkaufshistorie als Excel (EXP-05)
fastify.get('/reports/sales-xlsx', async (request, reply) => {
  const session = (request as any).session as { shopId: string };
  const shopId = session.shopId;
  const { from, to } = request.query as { from?: string; to?: string };
  if (!from || !to) return reply.status(400).send({ error: 'from and to (Unix-ms) required' });

  const fromTs = Number(from);
  const toTs = Number(to);
  if (isNaN(fromTs) || isNaN(toTs)) return reply.status(400).send({ error: 'from and to must be numbers' });

  const salesResult = await db.execute(sql`
    SELECT id, items, total_cents, paid_cents, change_cents, donation_cents, created_at
    FROM sales
    WHERE shop_id = ${shopId}
      AND created_at >= ${fromTs}
      AND created_at <= ${toTs}
      AND cancelled_at IS NULL
      AND (type IS NULL OR type = 'sale')
    ORDER BY created_at ASC
  `);

  const rows = salesResult.rows as Array<Record<string, unknown>>;
  const today = new Date().toISOString().slice(0, 10);

  const wsData: (string | number)[][] = [
    ['Datum', 'Uhrzeit', 'Artikel', 'Menge', 'VK (EUR)', 'EK (EUR)', 'Summe Artikel (EUR)', 'Gesamtsumme (EUR)', 'Bezahlt (EUR)', 'Wechselgeld (EUR)', 'Spende (EUR)'],
  ];

  for (const sale of rows) {
    const items = (sale.items ?? []) as Array<{ name: string; quantity: number; salePrice: number; purchasePrice?: number }>;
    const createdAt = Number(sale.created_at);
    const dateStr = new Date(createdAt).toLocaleDateString('de-DE');
    const timeStr = new Date(createdAt).toLocaleTimeString('de-DE', { hour: '2-digit', minute: '2-digit' });
    const totalCents = Number(sale.total_cents);
    const paidCents = Number(sale.paid_cents);
    const changeCents = Number(sale.change_cents);
    const donationCents = Number(sale.donation_cents);

    items.forEach((item, idx) => {
      const isFirst = idx === 0;
      wsData.push([
        dateStr,
        timeStr,
        item.name,
        item.quantity,
        Number((item.salePrice / 100).toFixed(2)),
        Number(((item.purchasePrice ?? 0) / 100).toFixed(2)),
        Number((item.salePrice * item.quantity / 100).toFixed(2)),
        isFirst ? Number((totalCents / 100).toFixed(2)) : '',
        isFirst ? Number((paidCents / 100).toFixed(2)) : '',
        isFirst ? Number((changeCents / 100).toFixed(2)) : '',
        isFirst ? Number((donationCents / 100).toFixed(2)) : '',
      ]);
    });
  }

  const ws = XLSX.utils.aoa_to_sheet(wsData);
  const wb = XLSX.utils.book_new();
  XLSX.utils.book_append_sheet(wb, ws, 'Verkaufshistorie');

  const buf = XLSX.write(wb, { type: 'buffer', bookType: 'xlsx' });

  reply.header('Content-Type', 'application/vnd.openxmlformats-officedocument.spreadsheetml.sheet');
  reply.header('Content-Disposition', `attachment; filename="verkaufshistorie-${today}.xlsx"`);
  return reply.send(buf);
});
```
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && grep -c "inventory-xlsx\|sales-xlsx" src/routes/reports.ts && grep "import \* as XLSX" src/routes/reports.ts</automated>
  </verify>

  <acceptance_criteria>
    - `grep "inventory-xlsx" server/src/routes/reports.ts` findet mindestens 2 Treffer (Route-Definition + Comment)
    - `grep "sales-xlsx" server/src/routes/reports.ts` findet mindestens 2 Treffer
    - `grep "import \* as XLSX" server/src/routes/reports.ts` findet genau 1 Treffer
    - `grep '"xlsx"' server/package.json` findet einen Eintrag (npm install hat dependency eingetragen)
    - `cd server && npx tsc --noEmit` schlägt nicht fehl (TypeScript-Compilation sauber)
  </acceptance_criteria>

  <done>Beide XLSX-Endpoints sind in reports.ts implementiert, xlsx ist als Dependency installiert, TypeScript compiliert fehlerfrei.</done>
</task>

<task type="auto">
  <name>Task 2: Excel-Download-Buttons im Frontend hinzufügen</name>
  <files>
    client/src/features/admin/reports/InventurTab.tsx
    client/src/features/admin/reports/DailyReport.tsx
  </files>

  <read_first>
    - client/src/features/admin/reports/InventurTab.tsx (vollständig — Button-Gruppe Zeilen 109-131, State-Variablen Zeilen 46-47)
    - client/src/features/admin/reports/DailyReport.tsx (vollständig — Button Zeilen 171-183, State Zeile 24)
  </read_first>

  <action>
**InventurTab.tsx — 3 Änderungen:**

1. State-Variable hinzufügen (neben `downloadingInventurPdf`):
```typescript
const [downloadingInventurXlsx, setDownloadingInventurXlsx] = useState(false);
```

2. Handler-Funktion hinzufügen (nach `handleInventurPdfDownload`):
```typescript
async function handleInventurXlsxDownload() {
  setDownloadingInventurXlsx(true);
  try {
    const res = await authFetch(`/api/reports/inventory-xlsx?year=${year}`);
    if (!res.ok) throw new Error(`${res.status}`);
    const blob = await res.blob();
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = `inventur-${year}.xlsx`;
    a.click();
    URL.revokeObjectURL(a.href);
  } catch (err) {
    alert('Excel-Download fehlgeschlagen. Bitte erneut versuchen.');
    console.error('Inventur XLSX error:', err);
  } finally {
    setDownloadingInventurXlsx(false);
  }
}
```

3. Button in der Button-Gruppe hinzufügen (nach dem "Inventur PDF" Button, in der `<div className="flex gap-2 mb-4">` Gruppe):
```tsx
<button
  onClick={handleInventurXlsxDownload}
  disabled={downloadingInventurXlsx}
  className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-green-700 text-white hover:bg-green-800 disabled:opacity-50 disabled:cursor-not-allowed"
>
  {downloadingInventurXlsx ? (
    <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
  ) : <span>↓</span>}
  Inventur Excel
</button>
```

Farbe `bg-green-700` (dunkleres Grün als CSV `bg-emerald-600`) damit die drei Buttons visuell unterscheidbar bleiben.

**DailyReport.tsx — 3 Änderungen:**

1. State-Variable hinzufügen (neben `downloadingCsv`):
```typescript
const [downloadingXlsx, setDownloadingXlsx] = useState(false);
```

2. Handler-Funktion hinzufügen (nach `handleCsvDownload`):
```typescript
async function handleXlsxDownload() {
  setDownloadingXlsx(true);
  try {
    const res = await authFetch(`/api/reports/sales-xlsx?from=${rangeStart}&to=${rangeEnd}`);
    if (!res.ok) throw new Error(`${res.status}`);
    const blob = await res.blob();
    const date = new Date().toISOString().slice(0, 10);
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = `verkaufshistorie-${date}.xlsx`;
    a.click();
    URL.revokeObjectURL(a.href);
  } catch (err) {
    alert('Excel-Download fehlgeschlagen. Bitte erneut versuchen.');
    console.error('XLSX download error:', err);
  } finally {
    setDownloadingXlsx(false);
  }
}
```

3. Button direkt nach dem bestehenden CSV-Button einfügen (in der Button-Gruppe neben den Zeitraum-Buttons):
```tsx
<button
  onClick={handleXlsxDownload}
  disabled={downloadingXlsx || sales.length === 0}
  className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-green-700 text-white hover:bg-green-800 disabled:opacity-50 disabled:cursor-not-allowed"
>
  {downloadingXlsx ? (
    <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
  ) : (
    <span>↓</span>
  )}
  Excel
</button>
```

Gleiche `disabled`-Bedingung wie CSV-Button (`sales.length === 0`) — kein Export ohne Daten.
  </action>

  <verify>
    <automated>grep -n "handleInventurXlsxDownload\|downloadingInventurXlsx\|Inventur Excel" /Users/simonluthe/Documents/fairstand/client/src/features/admin/reports/InventurTab.tsx && grep -n "handleXlsxDownload\|downloadingXlsx" /Users/simonluthe/Documents/fairstand/client/src/features/admin/reports/DailyReport.tsx</automated>
  </verify>

  <acceptance_criteria>
    - `grep "downloadingInventurXlsx" client/src/features/admin/reports/InventurTab.tsx` findet mindestens 3 Treffer (useState, setTrue, setFalse)
    - `grep "handleInventurXlsxDownload" client/src/features/admin/reports/InventurTab.tsx` findet mindestens 2 Treffer (Definition + onClick)
    - `grep "Inventur Excel" client/src/features/admin/reports/InventurTab.tsx` findet genau 1 Treffer (Button-Label)
    - `grep "downloadingXlsx" client/src/features/admin/reports/DailyReport.tsx` findet mindestens 3 Treffer
    - `grep "handleXlsxDownload" client/src/features/admin/reports/DailyReport.tsx` findet mindestens 2 Treffer
    - `cd client && npx tsc --noEmit` schlägt nicht fehl
  </acceptance_criteria>

  <done>Beide Komponenten haben Excel-Download-Buttons mit identischem Ladezustand und Fehlerbehandlung wie die bestehenden CSV/PDF-Buttons. TypeScript compiliert fehlerfrei.</done>
</task>

</tasks>

<verification>
Nach beiden Tasks prüfen:

1. Server startet ohne Fehler: `cd /Users/simonluthe/Documents/fairstand && npm run dev:server`
2. TypeScript sauber: `cd server && npx tsc --noEmit` (keine Fehler)
3. Client TypeScript sauber: `cd client && npx tsc --noEmit` (keine Fehler)
4. xlsx in server/package.json vorhanden: `grep '"xlsx"' server/package.json`
5. Beide Endpoints registriert: `grep -c "inventory-xlsx\|sales-xlsx" server/src/routes/reports.ts` (mindestens 4)
</verification>

<success_criteria>
- GET /api/reports/inventory-xlsx?year=2026 gibt eine .xlsx-Datei zurück (Content-Type: application/vnd.openxmlformats-officedocument.spreadsheetml.sheet)
- GET /api/reports/sales-xlsx?from=...&to=... gibt eine .xlsx-Datei zurück
- InventurTab zeigt drei Buttons: "Inventur CSV" (emerald), "Inventur PDF" (blau), "Inventur Excel" (green-700)
- DailyReport zeigt zwei Download-Buttons: "CSV" (emerald) und "Excel" (green-700)
- Umlaute in Artikelnamen erscheinen korrekt (xlsx nutzt intern UTF-8, kein BOM-Problem)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/34-xlsx-export/34-01-SUMMARY.md` erstellen mit tatsächlich geänderten Dateien, implementierten Endpoints und etwaigen Abweichungen vom Plan.
</output>
