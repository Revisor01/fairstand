---
phase: 03-warenwirtschaft-berichte
plan: 02
type: execute
wave: 2
depends_on: ["03-01"]
files_modified:
  - client/src/features/admin/products/ProductList.tsx
  - client/src/features/admin/products/ProductForm.tsx
  - client/src/features/admin/products/StockAdjustModal.tsx
  - client/src/features/admin/AdminScreen.tsx
  - client/src/features/admin/reports/DailyReport.tsx
  - client/src/features/pos/POSScreen.tsx
  - client/src/features/pos/LowStockBanner.tsx
autonomous: true
requirements: [WAR-01, WAR-02, WAR-03, WAR-05, WAR-06, POS-08]

must_haves:
  truths:
    - "Neue Produkte koennen angelegt werden mit allen Pflichtfeldern"
    - "Bestehende Produkte koennen bearbeitet werden"
    - "Produkte koennen deaktiviert werden und verschwinden aus dem Kassen-Grid"
    - "Warenbestand kann manuell korrigiert werden mit Zugang/Abgang und Grund"
    - "Mindestbestand-Warnung erscheint als Banner im POS-Screen"
    - "Tagesuebersicht zeigt Anzahl Verkaeufe, Gesamtumsatz und Gesamtspenden"
  artifacts:
    - path: "client/src/features/admin/products/ProductList.tsx"
      provides: "Alphabetisch sortierte Produktliste mit Kategorie-Filter"
    - path: "client/src/features/admin/products/ProductForm.tsx"
      provides: "Formular fuer Produkt-CRUD (Anlegen + Bearbeiten)"
    - path: "client/src/features/admin/products/StockAdjustModal.tsx"
      provides: "Modal fuer Bestandskorrektur mit Delta und Grund"
    - path: "client/src/features/pos/LowStockBanner.tsx"
      provides: "Banner-Komponente fuer Mindestbestand-Warnung"
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "Tagesuebersicht aus lokalen Dexie-Daten"
  key_links:
    - from: "client/src/features/admin/products/StockAdjustModal.tsx"
      to: "client/src/db/schema.ts"
      via: "db.outbox.add mit STOCK_ADJUST operation"
      pattern: "STOCK_ADJUST"
    - from: "client/src/features/pos/LowStockBanner.tsx"
      to: "client/src/db/schema.ts"
      via: "useLiveQuery filtert products mit stock <= minStock"
      pattern: "minStock"
    - from: "client/src/features/admin/reports/DailyReport.tsx"
      to: "client/src/db/schema.ts"
      via: "db.sales.where('createdAt').between()"
      pattern: "sales.*where.*createdAt"
---

<objective>
Produktverwaltungs-UI, Bestandskorrektur, Mindestbestand-Warnung und Tagesuebersicht.

Purpose: Verwalterin kann Produkte pflegen, Bestaende korrigieren und nach dem Gottesdienst die Tagesuebersicht sehen. Mindestbestand-Warnung warnt auf dem POS-Screen.
Output: Vollstaendige Produktverwaltung im Admin-Bereich, Mindestbestand-Banner im POS-Screen, Tagesuebersicht.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-warenwirtschaft-berichte/03-CONTEXT.md
@.planning/phases/03-warenwirtschaft-berichte/03-RESEARCH.md
@.planning/phases/03-warenwirtschaft-berichte/03-01-SUMMARY.md

<interfaces>
<!-- Nach Plan 01 verfuegbare Interfaces -->

From client/src/db/schema.ts (nach Plan 01):
```typescript
export interface Product {
  id: string; shopId: string; articleNumber: string; name: string;
  category: string; purchasePrice: number; salePrice: number;
  vatRate: number; stock: number; minStock: number; active: boolean; updatedAt: number;
}
export interface Sale {
  id: string; shopId: string; items: SaleItem[];
  totalCents: number; paidCents: number; changeCents: number;
  donationCents: number; createdAt: number; syncedAt?: number;
}
export interface OutboxEntry {
  id?: number; operation: 'SALE_COMPLETE' | 'STOCK_ADJUST';
  payload: unknown; shopId: string; createdAt: number; attempts: number;
}
```

From client/src/db/index.ts:
```typescript
export const db = new FairstandDB();
export const SHOP_ID = 'st-secundus-hennstedt';
```

From client/src/features/pos/utils.ts:
```typescript
export function formatEur(cents: number): string;
```

From client/src/features/admin/AdminScreen.tsx (nach Plan 01):
```typescript
// Tab-Navigation: 'products' | 'reports' | 'settings'
// Platzhalter-Content pro Tab — hier ersetzen
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Produktverwaltung — ProductList + ProductForm + StockAdjustModal</name>
  <files>
    client/src/features/admin/products/ProductList.tsx,
    client/src/features/admin/products/ProductForm.tsx,
    client/src/features/admin/products/StockAdjustModal.tsx,
    client/src/features/admin/AdminScreen.tsx
  </files>
  <read_first>
    client/src/db/schema.ts,
    client/src/db/index.ts,
    client/src/features/pos/ArticleGrid.tsx,
    client/src/features/pos/utils.ts,
    client/src/features/admin/AdminScreen.tsx,
    client/src/features/pos/useSaleComplete.ts
  </read_first>
  <action>
**1. client/src/features/admin/products/ProductList.tsx:**

Zeigt alle Produkte des aktuellen Shops, alphabetisch sortiert nach Name, mit Kategorie-Filter (per CONTEXT.md: "Produkte alphabetisch sortiert mit Kategorie-Filter").

```tsx
import { useState } from 'react';
import { useLiveQuery } from 'dexie-react-hooks';
import { db, SHOP_ID } from '../../../db/index.js';
import type { Product } from '../../../db/index.js';
import { formatEur } from '../../pos/utils.js';
```

- useLiveQuery auf `db.products.where('shopId').equals(SHOP_ID).toArray()` — ALLE Produkte (auch inaktive)
- Kategorie-Filter oben (gleiche Tab-Logik wie ArticleGrid: "Alle" + unique Kategorien)
- Jedes Produkt als Zeile/Karte mit: Name, Artikelnummer, VK-Preis (formatEur), Bestand, Status (aktiv/inaktiv)
- Bestand-Zelle: rot eingefaerbt wenn `product.minStock > 0 && product.stock <= product.minStock`
- Buttons pro Produkt: "Bearbeiten" (oeffnet ProductForm), "Bestand" (oeffnet StockAdjustModal), "Deaktivieren"/"Aktivieren" Toggle
- "Neues Produkt" Button oben rechts
- Deaktivieren/Aktivieren: `db.products.update(product.id, { active: !product.active, updatedAt: Date.now() })`
- Touch-optimiert: Buttons mindestens `h-11 px-4`, ausreichend Abstand

**2. client/src/features/admin/products/ProductForm.tsx:**

Formular fuer Anlegen und Bearbeiten. Props: `product?: Product` (undefined = Anlegen), `onClose: () => void`.

Felder:
- `articleNumber` — Text-Input, required
- `name` — Text-Input, required
- `category` — Text-Input (Freitext, kein Dropdown)
- `purchasePrice` — Numerisch, Anzeige in Euro (Eingabe in Euro, intern Umrechnung in Cent: `Math.round(parseFloat(input) * 100)`)
- `salePrice` — Numerisch, gleiche Logik
- `vatRate` — Select mit Optionen `7` und `19` (Standard: 7)
- `stock` — Numerisch, Integer (nur beim Anlegen editierbar, danach ueber StockAdjust)
- `minStock` — Numerisch, Integer, default 0, Label: "Mindestbestand (0 = keine Warnung)"

Speichern:
- Anlegen: `db.products.add({ id: crypto.randomUUID(), shopId: SHOP_ID, ...formValues, active: true, updatedAt: Date.now() })`
- Bearbeiten: `db.products.update(product.id, { ...formValues, updatedAt: Date.now() })`
- KEIN stock-Update beim Bearbeiten (nur ueber StockAdjustModal)

Nach dem Speichern: `onClose()` aufrufen.

Validation: Name und Artikelnummer muessen ausgefuellt sein, Preise >= 0.

Styling: Modal/Overlay oder eigene Seite im Admin-Tab. Formular-Inputs mit `h-12 text-lg` fuer Touch.

**3. client/src/features/admin/products/StockAdjustModal.tsx:**

Modal fuer Bestandskorrektur (per CONTEXT.md: "manuelles Eingabefeld fuer Zugang/Abgang mit Grund").

Props: `product: Product`, `onClose: () => void`.

- Zeigt aktuellen Bestand des Produkts
- Eingabefeld fuer Delta (positiv = Zugang, negativ = Abgang), Typ: number
- Eingabefeld fuer Grund (Text, optional aber empfohlen), z.B. "Lieferung", "Schwund", "Inventur"
- "Speichern" Button:
  1. Dexie: `db.products.where('id').equals(product.id).modify(p => { p.stock += delta; p.updatedAt = Date.now(); })`
  2. Outbox: `db.outbox.add({ operation: 'STOCK_ADJUST', payload: { productId: product.id, delta, reason, shopId: SHOP_ID }, shopId: SHOP_ID, createdAt: Date.now(), attempts: 0 })`
  3. `onClose()`
- WICHTIG: Delta-Prinzip — nie Absolutwert senden (per OFF-04, CONTEXT.md Specifics)

**4. AdminScreen.tsx updaten:**

Ersetze den Platzhalter-Content fuer den "products"-Tab durch `<ProductList />`. Die ProductList managed intern ihre Sub-Views (Liste, Formular, Modal) via eigenem useState.

Importiere ProductList:
```tsx
import { ProductList } from './products/ProductList.js';
```

Im Tab-Content fuer `tab === 'products'`: `<ProductList />`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - client/src/features/admin/products/ProductList.tsx existiert und enthaelt `useLiveQuery`
    - client/src/features/admin/products/ProductList.tsx enthaelt `alphabetisch` oder `.sort((a, b) => a.name.localeCompare(b.name))`
    - client/src/features/admin/products/ProductForm.tsx existiert und enthaelt `crypto.randomUUID()`
    - client/src/features/admin/products/ProductForm.tsx enthaelt `minStock`
    - client/src/features/admin/products/ProductForm.tsx enthaelt `Math.round` fuer Cent-Konvertierung
    - client/src/features/admin/products/StockAdjustModal.tsx existiert und enthaelt `STOCK_ADJUST`
    - client/src/features/admin/products/StockAdjustModal.tsx enthaelt `db.outbox.add`
    - client/src/features/admin/AdminScreen.tsx importiert `ProductList`
    - TypeScript-Build fehlerfrei
  </acceptance_criteria>
  <done>
    Produktliste zeigt alle Produkte alphabetisch sortiert mit Kategorie-Filter. Neues Produkt anlegen mit allen Feldern (WAR-01). Bearbeiten oeffnet gefuelltes Formular (WAR-02). Deaktivieren-Toggle setzt active-Flag (WAR-03). Bestandskorrektur via StockAdjustModal mit Delta und Grund, schreibt Outbox-Entry (WAR-05). Bestand-Zelle visuell markiert bei Unterschreitung des Mindestbestands (WAR-06 Teil 1).
  </done>
</task>

<task type="auto">
  <name>Task 2: Mindestbestand-Banner im POS + Tagesuebersicht</name>
  <files>
    client/src/features/pos/LowStockBanner.tsx,
    client/src/features/pos/POSScreen.tsx,
    client/src/features/admin/reports/DailyReport.tsx,
    client/src/features/admin/AdminScreen.tsx
  </files>
  <read_first>
    client/src/features/pos/POSScreen.tsx,
    client/src/features/pos/ArticleGrid.tsx,
    client/src/features/admin/AdminScreen.tsx,
    client/src/db/schema.ts,
    client/src/db/index.ts,
    client/src/features/pos/utils.ts
  </read_first>
  <action>
**1. client/src/features/pos/LowStockBanner.tsx (NEU):**

Banner-Komponente die auf dem POS-Screen erscheint wenn Produkte den Mindestbestand unterschreiten (per CONTEXT.md: "Banner auf dem Kassen-Screen, sichtbar ohne Adminzugang").

```tsx
import { useLiveQuery } from 'dexie-react-hooks';
import { db, SHOP_ID } from '../../db/index.js';

export function LowStockBanner() {
  const lowStockProducts = useLiveQuery(
    () => db.products
      .where('[shopId+active]')
      .equals([SHOP_ID, 1])
      .filter(p => p.minStock > 0 && p.stock <= p.minStock)
      .toArray(),
    []
  );

  if (!lowStockProducts || lowStockProducts.length === 0) return null;

  return (
    <div className="bg-amber-100 border-b border-amber-300 px-4 py-2 text-amber-800 text-sm">
      <span className="font-medium">Mindestbestand unterschritten:</span>{' '}
      {lowStockProducts.map(p => `${p.name} (${p.stock})`).join(', ')}
    </div>
  );
}
```

Opt-in-Modell: Nur Produkte mit `minStock > 0` werden ueberwacht (per CONTEXT.md).

**2. POSScreen.tsx — LowStockBanner einbinden:**

Importiere `LowStockBanner` und rendere es ganz oben im POS-Screen-Layout, VOR dem Header/ArticleGrid:

```tsx
import { LowStockBanner } from './LowStockBanner.js';
```

Im Return-JSX des POSScreen, als erstes Element im aeusseren Container:
```tsx
<LowStockBanner />
```

**3. client/src/features/admin/reports/DailyReport.tsx (NEU):**

Tagesuebersicht nach dem Gottesdienst (POS-08). Zeigt fuer den aktuellen Tag:
- Anzahl Verkaeufe
- Gesamtumsatz (formatEur)
- Gesamtspenden (formatEur)

Daten aus Dexie (lokal, offline verfuegbar — per RESEARCH.md Open Question 1):

```tsx
import { useMemo, useState } from 'react';
import { useLiveQuery } from 'dexie-react-hooks';
import { db, SHOP_ID } from '../../../db/index.js';
import { formatEur } from '../../pos/utils.js';
import { startOfDay, endOfDay, subDays, format } from 'date-fns';
import { de } from 'date-fns/locale';
```

- Default: heutiger Tag. Buttons fuer "Gestern" und ein Datums-Picker (`<input type="date">`) fuer andere Tage.
- `useLiveQuery` auf `db.sales.where('createdAt').between(dayStart, dayEnd).filter(s => s.shopId === SHOP_ID).toArray()`
- Berechne: `count`, `totalCents = sum(sale.totalCents)`, `donationCents = sum(sale.donationCents)`
- Drei grosse Kacheln/Cards nebeneinander:
  - Kachel 1: Anzahl Verkaeufe (Zahl gross, Label klein)
  - Kachel 2: Gesamtumsatz (formatEur, sky-500 Farbe)
  - Kachel 3: Spenden (formatEur, green-500 Farbe)
- Darunter: einfache Tabelle mit den einzelnen Verkaeufen des Tages (Uhrzeit, Summe, Spende)

Styling: Tailwind, touch-optimiert, cards mit `rounded-xl shadow-sm p-6`.

**4. AdminScreen.tsx — DailyReport einbinden:**

Importiere DailyReport und rendere im "reports"-Tab:

```tsx
import { DailyReport } from './reports/DailyReport.js';
```

Im Tab-Content fuer `tab === 'reports'`: `<DailyReport />`

(Monats-/Jahresberichte mit Charts kommen in Plan 03)
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - client/src/features/pos/LowStockBanner.tsx existiert und enthaelt `minStock > 0 && p.stock <= p.minStock`
    - client/src/features/pos/POSScreen.tsx importiert `LowStockBanner`
    - client/src/features/pos/POSScreen.tsx enthaelt `<LowStockBanner`
    - client/src/features/admin/reports/DailyReport.tsx existiert und enthaelt `useLiveQuery`
    - client/src/features/admin/reports/DailyReport.tsx enthaelt `db.sales.where`
    - client/src/features/admin/reports/DailyReport.tsx enthaelt `formatEur`
    - client/src/features/admin/reports/DailyReport.tsx enthaelt `donationCents`
    - client/src/features/admin/AdminScreen.tsx importiert `DailyReport`
    - TypeScript-Build fehlerfrei
  </acceptance_criteria>
  <done>
    Mindestbestand-Banner erscheint im POS-Screen bei Unterschreitung (WAR-06). Tagesuebersicht im Admin-Berichte-Tab zeigt Anzahl Verkaeufe, Gesamtumsatz und Gesamtspenden aus lokalen Dexie-Daten (POS-08). Datumswechsel moeglich.
  </done>
</task>

</tasks>

<verification>
- TypeScript-Build Client fehlerfrei: `cd client && npx tsc --noEmit`
- Produkt-CRUD komplett: Anlegen mit allen Feldern, Bearbeiten, Deaktivieren
- Bestandskorrektur schreibt STOCK_ADJUST in Outbox
- Mindestbestand-Banner auf POS-Screen sichtbar
- Tagesuebersicht zeigt drei Kennzahlen
</verification>

<success_criteria>
- Neue Produkte koennen angelegt werden mit Artikelnummer, Name, EK-Preis, VK-Preis, MwSt-Satz, Mindestbestand (WAR-01)
- Bestehende Produkte koennen bearbeitet werden (WAR-02)
- Produkte koennen deaktiviert/aktiviert werden, deaktivierte erscheinen nicht im Kassen-Grid (WAR-03)
- Bestandskorrektur mit Delta und Grund, schreibt Outbox-Entry fuer Sync (WAR-05)
- Mindestbestand-Banner auf POS-Screen bei stock <= minStock und minStock > 0 (WAR-06)
- Tagesuebersicht zeigt Anzahl Verkaeufe, Gesamtumsatz, Gesamtspenden (POS-08)
</success_criteria>

<output>
After completion, create `.planning/phases/03-warenwirtschaft-berichte/03-02-SUMMARY.md`
</output>
