---
phase: 03-warenwirtschaft-berichte
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - client/src/features/admin/reports/MonthlyReport.tsx
  - client/src/features/admin/reports/ReportChart.tsx
  - client/src/features/admin/settings/SettingsForm.tsx
  - client/src/features/admin/AdminScreen.tsx
  - server/src/services/mailer.ts
  - server/src/services/reportTemplate.ts
  - server/src/scheduler/reportScheduler.ts
  - server/src/index.ts
  - server/package.json
  - client/package.json
autonomous: true
requirements: [REP-01, REP-02, REP-03, REP-04]

must_haves:
  truths:
    - "Spendenuebersicht kumuliert pro Monat und Jahr sichtbar"
    - "Umsatzbericht mit Gesamtumsatz, Marge, Spenden und Top-5-Artikeln"
    - "Balkendiagramm zeigt Monatsvergleich fuer Umsatz und Spenden"
    - "E-Mail-Adresse fuer Berichtsversand ist im Settings-Tab konfigurierbar"
    - "Automatischer Mail-Versand monatlich und jaehrlich konfigurierbar"
    - "HTML-Mail mit formatierter Tabelle und Styling wird versendet"
  artifacts:
    - path: "client/src/features/admin/reports/MonthlyReport.tsx"
      provides: "Monats-/Jahresberichte mit Dropdown-Auswahl"
    - path: "client/src/features/admin/reports/ReportChart.tsx"
      provides: "Recharts-basiertes Balkendiagramm"
    - path: "client/src/features/admin/settings/SettingsForm.tsx"
      provides: "Settings-Formular mit E-Mail-Config und Toggles"
    - path: "server/src/services/mailer.ts"
      provides: "Nodemailer-Transport mit SMTP-Env-Vars"
    - path: "server/src/services/reportTemplate.ts"
      provides: "HTML-Template fuer Berichts-Mails"
    - path: "server/src/scheduler/reportScheduler.ts"
      provides: "Cron-Jobs fuer monatlichen und jaehrlichen Versand"
  key_links:
    - from: "server/src/scheduler/reportScheduler.ts"
      to: "server/src/services/mailer.ts"
      via: "sendReport() Aufruf im Cron-Task"
      pattern: "sendReport"
    - from: "server/src/scheduler/reportScheduler.ts"
      to: "server/src/db/schema.ts"
      via: "settings-Tabelle fuer E-Mail-Config"
      pattern: "settings"
    - from: "client/src/features/admin/reports/MonthlyReport.tsx"
      to: "server/src/routes/reports.ts"
      via: "fetch /api/reports/monthly und /api/reports/yearly"
      pattern: "api/reports"
    - from: "client/src/features/admin/settings/SettingsForm.tsx"
      to: "server/src/routes/settings.ts"
      via: "fetch /api/settings"
      pattern: "api/settings"
---

<objective>
Monats-/Jahresberichte mit Charts, E-Mail-Konfiguration und automatischer Mail-Versand.

Purpose: Verwalterin kann Umsatz- und Spendenberichte einsehen, mit Balkendiagrammen vergleichen und automatische Berichts-Mails konfigurieren.
Output: Report-UI mit Recharts, Settings-Formular, Nodemailer-Service, Cron-Scheduler.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/03-warenwirtschaft-berichte/03-CONTEXT.md
@.planning/phases/03-warenwirtschaft-berichte/03-RESEARCH.md
@.planning/phases/03-warenwirtschaft-berichte/03-01-SUMMARY.md
@.planning/phases/03-warenwirtschaft-berichte/03-02-SUMMARY.md

<interfaces>
<!-- Aus Plan 01 verfuegbare Server-Routes -->

From server/src/routes/reports.ts:
```typescript
// GET /api/reports/monthly?shopId=xxx&year=2026&month=3
// Returns: { summary: { sale_count, total_cents, donation_cents }, topArticles: [...] }

// GET /api/reports/yearly?shopId=xxx&year=2026
// Returns: { year: number, months: [{ month, sale_count, total_cents, donation_cents }] }
```

From server/src/routes/settings.ts:
```typescript
// GET /api/settings?shopId=xxx — Returns: [{ key, value, shopId }]
// PUT /api/settings — Body: { key, value, shopId } — Upsert
```

From server/src/db/schema.ts:
```typescript
export const settings = sqliteTable('settings', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),
  shopId: text('shop_id').notNull(),
});
```

From client/src/features/pos/utils.ts:
```typescript
export function formatEur(cents: number): string;
```

From client/src/features/admin/AdminScreen.tsx (nach Plan 02):
```typescript
// Tab 'reports' zeigt DailyReport — erweitern um MonthlyReport
// Tab 'settings' zeigt Platzhalter — ersetzen durch SettingsForm
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recharts installieren + Monats-/Jahresberichte-UI mit Charts</name>
  <files>
    client/package.json,
    client/src/features/admin/reports/MonthlyReport.tsx,
    client/src/features/admin/reports/ReportChart.tsx,
    client/src/features/admin/AdminScreen.tsx
  </files>
  <read_first>
    client/package.json,
    client/src/features/admin/AdminScreen.tsx,
    client/src/features/admin/reports/DailyReport.tsx,
    client/src/features/pos/utils.ts,
    client/src/db/index.ts,
    .planning/phases/03-warenwirtschaft-berichte/03-RESEARCH.md
  </read_first>
  <action>
**0. Recharts installieren:**
```bash
cd client && npm install recharts@^3
```

**1. client/src/features/admin/reports/ReportChart.tsx (NEU):**

Recharts-Wrapper fuer Balkendiagramm (per CONTEXT.md: "Vergleiche zu anderen Monaten/Jahren, Balkendiagramm oder Liniendiagramm").

```tsx
import { BarChart, Bar, XAxis, YAxis, Tooltip, Legend, ResponsiveContainer } from 'recharts';
import { formatEur } from '../../pos/utils.js';

interface MonthData {
  month: string;  // "Jan", "Feb" etc.
  umsatz: number; // Cent-Integer
  spenden: number;
}

interface ReportChartProps {
  data: MonthData[];
}

export function ReportChart({ data }: ReportChartProps) {
  return (
    <ResponsiveContainer width="100%" height={300}>
      <BarChart data={data}>
        <XAxis dataKey="month" />
        <YAxis tickFormatter={(v: number) => `${(v / 100).toFixed(0)}€`} />
        <Tooltip formatter={(v: number) => formatEur(v)} />
        <Legend />
        <Bar dataKey="umsatz" fill="#38bdf8" name="Umsatz" />
        <Bar dataKey="spenden" fill="#86efac" name="Spenden" />
      </BarChart>
    </ResponsiveContainer>
  );
}
```

Farben: sky-400 (#38bdf8) fuer Umsatz, green-300 (#86efac) fuer Spenden — konsistent mit Projekt-Farbschema.

**2. client/src/features/admin/reports/MonthlyReport.tsx (NEU):**

Monats- und Jahresberichte mit Dropdown-Auswahl (per CONTEXT.md: "Monat/Jahr-Auswahl per Dropdown").

- Zwei Dropdowns: Jahr (aktuelles Jahr +/- 2) und Monat (1-12, deutsch: "Januar" bis "Dezember")
- Beim Aendern: `fetch('/api/reports/monthly?shopId=${SHOP_ID}&year=${year}&month=${month}')` fuer Monatsdaten
- Beim Aendern des Jahres: `fetch('/api/reports/yearly?shopId=${SHOP_ID}&year=${year}')` fuer Jahreschart
- Zeigt:
  1. **Monats-Zusammenfassung** als Cards: Anzahl Verkaeufe, Gesamtumsatz (formatEur), Spenden (formatEur)
  2. **Top-5-Artikel** als Tabelle: Name, Menge, Umsatz (formatEur)
  3. **Jahresverlauf** als Balkendiagramm via `<ReportChart data={yearlyData} />`
- Offline-Hinweis: Wenn `!navigator.onLine`, zeige "Berichte sind nur online verfuegbar" statt fetch

Monats-Labels aus date-fns:
```typescript
import { format } from 'date-fns';
import { de } from 'date-fns/locale';
const monthNames = Array.from({ length: 12 }, (_, i) => format(new Date(2026, i, 1), 'MMMM', { locale: de }));
```

Umwandlung Yearly-API-Response zu Chart-Daten:
```typescript
const chartData: MonthData[] = monthNames.map((name, i) => {
  const monthData = yearlyResponse.months.find(m => m.month === i + 1);
  return {
    month: name.slice(0, 3), // "Jan", "Feb" etc.
    umsatz: monthData?.total_cents ?? 0,
    spenden: monthData?.donation_cents ?? 0,
  };
});
```

Fetches via `useEffect` bei Dropdown-Aenderung. Lade-Zustand mit "Laden..." Text.

Styling: Tailwind, cards `rounded-xl shadow-sm p-6`, Tabelle mit `border-collapse`.

**3. AdminScreen.tsx updaten — Reports-Tab erweitern:**

Der Reports-Tab soll sowohl DailyReport als auch MonthlyReport zeigen. Zwei Unter-Tabs oder einfach untereinander:
- Oben: DailyReport (Tagesuebersicht — immer sichtbar, auch offline)
- Darunter: Trennlinie + MonthlyReport (Monats-/Jahresberichte — nur online)

```tsx
import { MonthlyReport } from './reports/MonthlyReport.js';
```

Im `tab === 'reports'` Block:
```tsx
<DailyReport />
<hr className="my-6 border-slate-200" />
<MonthlyReport />
```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - client/package.json enthaelt `"recharts"` in dependencies
    - client/src/features/admin/reports/ReportChart.tsx existiert und enthaelt `BarChart` und `ResponsiveContainer`
    - client/src/features/admin/reports/ReportChart.tsx enthaelt `formatEur`
    - client/src/features/admin/reports/MonthlyReport.tsx existiert und enthaelt `fetch.*api/reports/monthly`
    - client/src/features/admin/reports/MonthlyReport.tsx enthaelt `fetch.*api/reports/yearly`
    - client/src/features/admin/reports/MonthlyReport.tsx enthaelt `navigator.onLine`
    - client/src/features/admin/reports/MonthlyReport.tsx enthaelt `topArticles` oder `Top-5`
    - client/src/features/admin/AdminScreen.tsx importiert `MonthlyReport`
    - TypeScript-Build fehlerfrei
  </acceptance_criteria>
  <done>
    Spendenuebersicht kumuliert pro Monat sichtbar (REP-01). Umsatzbericht mit Gesamtumsatz, Spenden und Top-5-Artikeln (REP-02). Balkendiagramm zeigt Jahresverlauf Umsatz vs. Spenden. Offline-Hinweis wenn kein Netz.
  </done>
</task>

<task type="auto">
  <name>Task 2: Nodemailer-Service + Cron-Scheduler + Settings-UI</name>
  <files>
    server/package.json,
    server/src/services/mailer.ts,
    server/src/services/reportTemplate.ts,
    server/src/scheduler/reportScheduler.ts,
    server/src/index.ts,
    client/src/features/admin/settings/SettingsForm.tsx,
    client/src/features/admin/AdminScreen.tsx
  </files>
  <read_first>
    server/package.json,
    server/src/index.ts,
    server/src/routes/reports.ts,
    server/src/routes/settings.ts,
    server/src/db/schema.ts,
    server/src/db/index.ts,
    client/src/features/admin/AdminScreen.tsx,
    client/src/db/index.ts,
    docker-compose.yml,
    .planning/phases/03-warenwirtschaft-berichte/03-RESEARCH.md
  </read_first>
  <action>
**0. Backend-Dependencies installieren:**
```bash
cd server && npm install nodemailer @fastify/schedule toad-scheduler && npm install -D @types/nodemailer
```

**1. server/src/services/mailer.ts (NEU):**

Nodemailer-Transport mit SMTP-Env-Vars (per CONTEXT.md: "Nodemailer ueber SMTP, Konfiguration via Umgebungsvariablen").

```typescript
import nodemailer from 'nodemailer';

let transport: nodemailer.Transporter | null = null;

export function isMailConfigured(): boolean {
  return !!(process.env.SMTP_HOST && process.env.SMTP_USER);
}

function getTransport(): nodemailer.Transporter {
  if (!transport) {
    transport = nodemailer.createTransport({
      host: process.env.SMTP_HOST,
      port: Number(process.env.SMTP_PORT ?? 587),
      secure: process.env.SMTP_SECURE === 'true',
      auth: {
        user: process.env.SMTP_USER,
        pass: process.env.SMTP_PASS,
      },
    });
  }
  return transport;
}

export async function sendReport(to: string, subject: string, htmlBody: string): Promise<void> {
  if (!isMailConfigured()) {
    throw new Error('SMTP nicht konfiguriert');
  }
  await getTransport().sendMail({
    from: process.env.SMTP_FROM ?? 'Fairstand <noreply@fairstand.godsapp.de>',
    to,
    subject,
    html: htmlBody,
  });
}
```

Lazy Transport-Initialisierung — kein Crash wenn SMTP-Vars fehlen. `isMailConfigured()` prueft Voraussetzungen.

**2. server/src/services/reportTemplate.ts (NEU):**

HTML-Template fuer Berichts-Mails (per CONTEXT.md: "HTML-Mail mit Styling, formatierte Tabelle, Farben, Logo").

```typescript
function escapeHtml(str: string): string {
  return str.replace(/&/g, '&amp;').replace(/</g, '&lt;').replace(/>/g, '&gt;').replace(/"/g, '&quot;');
}

function formatEur(cents: number): string {
  return (cents / 100).toFixed(2).replace('.', ',') + ' EUR';
}

interface ReportData {
  monthLabel: string;
  saleCount: number;
  totalCents: number;
  donationCents: number;
  topArticles: Array<{ name: string; total_qty: number; revenue_cents: number }>;
}

export function buildMonthlyReportHtml(data: ReportData): string {
  const topRows = data.topArticles.map(a => `
    <tr>
      <td style="padding: 8px; border: 1px solid #e0f2fe;">${escapeHtml(a.name)}</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${a.total_qty}</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(a.revenue_cents)}</td>
    </tr>
  `).join('');

  return `<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"></head>
<body style="font-family: -apple-system, BlinkMacSystemFont, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
  <h1 style="color: #0ea5e9; margin-bottom: 24px;">Fairstand Monatsbericht ${escapeHtml(data.monthLabel)}</h1>
  <table style="width: 100%; border-collapse: collapse; margin-bottom: 24px;">
    <tr style="background: #f0f9ff;">
      <th style="text-align: left; padding: 8px; border: 1px solid #e0f2fe;">Kennzahl</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Wert</th>
    </tr>
    <tr>
      <td style="padding: 8px; border: 1px solid #e0f2fe;">Anzahl Verkaeufe</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${data.saleCount}</td>
    </tr>
    <tr>
      <td style="padding: 8px; border: 1px solid #e0f2fe;">Gesamtumsatz</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(data.totalCents)}</td>
    </tr>
    <tr>
      <td style="padding: 8px; border: 1px solid #e0f2fe;">Spenden</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(data.donationCents)}</td>
    </tr>
  </table>
  ${data.topArticles.length > 0 ? `
  <h2 style="color: #0ea5e9; margin-bottom: 16px;">Top 5 Artikel</h2>
  <table style="width: 100%; border-collapse: collapse;">
    <tr style="background: #f0f9ff;">
      <th style="text-align: left; padding: 8px; border: 1px solid #e0f2fe;">Artikel</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Menge</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Umsatz</th>
    </tr>
    ${topRows}
  </table>
  ` : ''}
  <p style="color: #94a3b8; font-size: 12px; margin-top: 32px;">Automatisch generiert vom Fairstand Kassensystem</p>
</body>
</html>`;
}

export function buildYearlyReportHtml(year: number, months: Array<{ month: number; sale_count: number; total_cents: number; donation_cents: number }>): string {
  const monthNames = ['Januar','Februar','Maerz','April','Mai','Juni','Juli','August','September','Oktober','November','Dezember'];
  const totalSales = months.reduce((s, m) => s + m.sale_count, 0);
  const totalCents = months.reduce((s, m) => s + m.total_cents, 0);
  const totalDonations = months.reduce((s, m) => s + m.donation_cents, 0);

  const monthRows = months.map(m => `
    <tr>
      <td style="padding: 8px; border: 1px solid #e0f2fe;">${monthNames[m.month - 1]}</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${m.sale_count}</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(m.total_cents)}</td>
      <td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(m.donation_cents)}</td>
    </tr>
  `).join('');

  return `<!DOCTYPE html>
<html lang="de">
<head><meta charset="UTF-8"></head>
<body style="font-family: -apple-system, BlinkMacSystemFont, sans-serif; max-width: 600px; margin: 0 auto; padding: 20px;">
  <h1 style="color: #0ea5e9; margin-bottom: 24px;">Fairstand Jahresbericht ${year}</h1>
  <table style="width: 100%; border-collapse: collapse; margin-bottom: 24px;">
    <tr style="background: #f0f9ff;">
      <th style="text-align: left; padding: 8px; border: 1px solid #e0f2fe;">Gesamt</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Wert</th>
    </tr>
    <tr><td style="padding: 8px; border: 1px solid #e0f2fe;">Verkaeufe</td><td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${totalSales}</td></tr>
    <tr><td style="padding: 8px; border: 1px solid #e0f2fe;">Umsatz</td><td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(totalCents)}</td></tr>
    <tr><td style="padding: 8px; border: 1px solid #e0f2fe;">Spenden</td><td style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">${formatEur(totalDonations)}</td></tr>
  </table>
  <h2 style="color: #0ea5e9; margin-bottom: 16px;">Monatsuebersicht</h2>
  <table style="width: 100%; border-collapse: collapse;">
    <tr style="background: #f0f9ff;">
      <th style="text-align: left; padding: 8px; border: 1px solid #e0f2fe;">Monat</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Verkaeufe</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Umsatz</th>
      <th style="text-align: right; padding: 8px; border: 1px solid #e0f2fe;">Spenden</th>
    </tr>
    ${monthRows}
  </table>
  <p style="color: #94a3b8; font-size: 12px; margin-top: 32px;">Automatisch generiert vom Fairstand Kassensystem</p>
</body>
</html>`;
}
```

**3. server/src/scheduler/reportScheduler.ts (NEU):**

Cron-Jobs fuer monatlichen und jaehrlichen Versand (per CONTEXT.md: "Optionaler automatischer Versand: monatlich und/oder jaehrlich").

```typescript
import type { FastifyInstance } from 'fastify';
import { AsyncTask, CronJob } from 'toad-scheduler';
import { eq, sql } from 'drizzle-orm';
import { db } from '../db/index.js';
import { settings, sales } from '../db/schema.js';
import { sendReport, isMailConfigured } from '../services/mailer.js';
import { buildMonthlyReportHtml, buildYearlyReportHtml } from '../services/reportTemplate.js';
```

Implementiere als Fastify-Plugin-Funktion (NICHT mit fastify-plugin wrappen — einfache async function):

```typescript
export async function reportScheduler(fastify: FastifyInstance) {
  // Monatlich: 1. des Monats, 07:00 UTC (= 08:00 MEZ / 09:00 MESZ)
  const monthlyTask = new AsyncTask('monthly-report', async () => {
    if (!isMailConfigured()) return;
    // Settings aus DB lesen
    const emailSetting = db.select().from(settings).where(eq(settings.key, 'report_email')).get();
    const monthlyEnabled = db.select().from(settings).where(eq(settings.key, 'report_monthly')).get();
    if (!emailSetting || monthlyEnabled?.value !== 'true') return;

    // Vormonat berechnen
    const now = new Date();
    const year = now.getMonth() === 0 ? now.getFullYear() - 1 : now.getFullYear();
    const month = now.getMonth() === 0 ? 12 : now.getMonth(); // getMonth() ist 0-basiert
    const monthStart = new Date(year, month - 1, 1).getTime();
    const monthEnd = new Date(year, month, 1).getTime();
    const shopId = 'st-secundus-hennstedt';

    const summary = db.all(sql`
      SELECT COUNT(*) as sale_count, COALESCE(SUM(total_cents), 0) as total_cents,
             COALESCE(SUM(donation_cents), 0) as donation_cents
      FROM sales WHERE shop_id = ${shopId} AND created_at >= ${monthStart} AND created_at < ${monthEnd}
    `);

    const topArticles = db.all(sql`
      SELECT json_extract(item.value, '$.name') as name,
             SUM(CAST(json_extract(item.value, '$.quantity') AS INTEGER)) as total_qty,
             SUM(CAST(json_extract(item.value, '$.quantity') AS INTEGER) * CAST(json_extract(item.value, '$.salePrice') AS INTEGER)) as revenue_cents
      FROM sales, json_each(sales.items) as item
      WHERE sales.shop_id = ${shopId} AND sales.created_at >= ${monthStart} AND sales.created_at < ${monthEnd}
      GROUP BY json_extract(item.value, '$.productId') ORDER BY total_qty DESC LIMIT 5
    `);

    const monthNames = ['Januar','Februar','Maerz','April','Mai','Juni','Juli','August','September','Oktober','November','Dezember'];
    const html = buildMonthlyReportHtml({
      monthLabel: `${monthNames[month - 1]} ${year}`,
      saleCount: Number((summary[0] as any)?.sale_count ?? 0),
      totalCents: Number((summary[0] as any)?.total_cents ?? 0),
      donationCents: Number((summary[0] as any)?.donation_cents ?? 0),
      topArticles: (topArticles as any[]).map(a => ({ name: String(a.name), total_qty: Number(a.total_qty), revenue_cents: Number(a.revenue_cents) })),
    });

    await sendReport(emailSetting.value, `Fairstand Monatsbericht ${monthNames[month - 1]} ${year}`, html);
    fastify.log.info(`Monatsbericht ${monthNames[month - 1]} ${year} an ${emailSetting.value} gesendet`);
  });

  fastify.scheduler.addCronJob(new CronJob(
    { cronExpression: '0 7 1 * *' },
    monthlyTask,
    { preventOverrun: true }
  ));

  // Jaehrlich: 1. Januar, 08:00 UTC
  const yearlyTask = new AsyncTask('yearly-report', async () => {
    if (!isMailConfigured()) return;
    const emailSetting = db.select().from(settings).where(eq(settings.key, 'report_email')).get();
    const yearlyEnabled = db.select().from(settings).where(eq(settings.key, 'report_yearly')).get();
    if (!emailSetting || yearlyEnabled?.value !== 'true') return;

    const year = new Date().getFullYear() - 1; // Vorjahr
    const yearStart = new Date(year, 0, 1).getTime();
    const yearEnd = new Date(year + 1, 0, 1).getTime();
    const shopId = 'st-secundus-hennstedt';

    const months = db.all(sql`
      SELECT CAST(strftime('%m', datetime(created_at / 1000, 'unixepoch')) AS INTEGER) as month,
             COUNT(*) as sale_count, COALESCE(SUM(total_cents), 0) as total_cents,
             COALESCE(SUM(donation_cents), 0) as donation_cents
      FROM sales WHERE shop_id = ${shopId} AND created_at >= ${yearStart} AND created_at < ${yearEnd}
      GROUP BY strftime('%m', datetime(created_at / 1000, 'unixepoch')) ORDER BY month
    `);

    const html = buildYearlyReportHtml(year, (months as any[]).map(m => ({
      month: Number(m.month), sale_count: Number(m.sale_count),
      total_cents: Number(m.total_cents), donation_cents: Number(m.donation_cents),
    })));

    await sendReport(emailSetting.value, `Fairstand Jahresbericht ${year}`, html);
    fastify.log.info(`Jahresbericht ${year} an ${emailSetting.value} gesendet`);
  });

  fastify.scheduler.addCronJob(new CronJob(
    { cronExpression: '0 8 1 1 *' },
    yearlyTask,
    { preventOverrun: true }
  ));

  // Startup-Log
  if (isMailConfigured()) {
    fastify.log.info('Mail-Versand aktiv — SMTP konfiguriert');
  } else {
    fastify.log.warn('SMTP nicht konfiguriert — automatischer Mail-Versand deaktiviert');
  }
}
```

**4. server/src/index.ts — Scheduler registrieren:**

Fuege imports hinzu:
```typescript
import fastifySchedule from '@fastify/schedule';
import { reportScheduler } from './scheduler/reportScheduler.js';
```

Nach den Route-Registrierungen:
```typescript
await fastify.register(fastifySchedule);
await fastify.register(reportScheduler);
```

**5. client/src/features/admin/settings/SettingsForm.tsx (NEU):**

Settings-Formular fuer E-Mail-Konfiguration (per CONTEXT.md: "Konfigurierbare E-Mail-Adresse, eine einzelne").

Felder:
- `report_email` — E-Mail-Adresse (Text-Input, type="email")
- `report_monthly` — Toggle/Checkbox: "Monatlichen Bericht per Mail senden"
- `report_yearly` — Toggle/Checkbox: "Jaehrlichen Bericht per Mail senden"

Lade Settings beim Mount via `fetch('/api/settings?shopId=${SHOP_ID}')`. Speichere jede Aenderung einzeln via `fetch('/api/settings', { method: 'PUT', body: { key, value, shopId: SHOP_ID } })`.

SMTP-Status-Anzeige: Zeige einen Hinweis "Mail-Versand ist serverseitig nicht konfiguriert" wenn kein SMTP. Dafuer: neuen minimalen Endpoint `GET /api/settings/smtp-status` auf dem Server hinzufuegen, der `{ configured: boolean }` zurueckgibt (basierend auf `isMailConfigured()`). Oder einfacher: Wenn Settings gespeichert werden und der PUT 200 zurueckgibt, ist alles OK — SMTP-Fehler treten erst beim Senden auf.

Einfachere Variante (empfohlen): Keinen Extra-Endpoint. Stattdessen Hinweis-Text: "Die SMTP-Konfiguration erfolgt ueber die Server-Umgebungsvariablen. E-Mail-Versand funktioniert nur wenn SMTP_HOST und SMTP_USER im Docker-Container gesetzt sind."

Styling: Tailwind, Formular-Inputs `h-12 text-lg`, Toggle als Checkbox mit `w-6 h-6` fuer Touch. Speichern-Button `bg-sky-500 text-white h-12 rounded-lg`.

**6. AdminScreen.tsx — Settings-Tab einbinden:**

```tsx
import { SettingsForm } from './settings/SettingsForm.js';
```

Im `tab === 'settings'` Block: `<SettingsForm />`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit && cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - server/package.json enthaelt `"nodemailer"` und `"@fastify/schedule"` und `"toad-scheduler"` in dependencies
    - client/package.json enthaelt `"recharts"` in dependencies
    - server/src/services/mailer.ts existiert und enthaelt `nodemailer.createTransport`
    - server/src/services/mailer.ts enthaelt `isMailConfigured`
    - server/src/services/reportTemplate.ts existiert und enthaelt `buildMonthlyReportHtml`
    - server/src/services/reportTemplate.ts enthaelt `buildYearlyReportHtml`
    - server/src/services/reportTemplate.ts enthaelt `escapeHtml`
    - server/src/scheduler/reportScheduler.ts existiert und enthaelt `cronExpression`
    - server/src/scheduler/reportScheduler.ts enthaelt `monthly-report` und `yearly-report`
    - server/src/scheduler/reportScheduler.ts enthaelt `report_email` und `report_monthly` und `report_yearly`
    - server/src/index.ts enthaelt `fastifySchedule` und `reportScheduler`
    - client/src/features/admin/settings/SettingsForm.tsx existiert und enthaelt `report_email`
    - client/src/features/admin/settings/SettingsForm.tsx enthaelt `fetch.*api/settings`
    - client/src/features/admin/AdminScreen.tsx importiert `SettingsForm`
    - TypeScript-Build beider Packages fehlerfrei
  </acceptance_criteria>
  <done>
    Monats-/Jahresberichte mit Recharts-Balkendiagramm sichtbar (REP-01, REP-02). E-Mail-Adresse und Versand-Toggles im Settings-Tab konfigurierbar (REP-03). Automatischer Mail-Versand via Cron (monatlich 1. des Monats, jaehrlich 1. Januar) mit HTML-Mails (REP-04). SMTP-Startup-Warnung wenn nicht konfiguriert.
  </done>
</task>

</tasks>

<verification>
- TypeScript-Build beider Packages fehlerfrei
- Recharts im Client installiert, ReportChart rendert BarChart
- MonthlyReport fetcht von /api/reports/monthly und /api/reports/yearly
- Settings-Formular speichert und laedt via /api/settings
- Nodemailer-Transport mit Lazy-Init und isMailConfigured()-Guard
- Cron-Jobs registriert in server/src/index.ts via @fastify/schedule
- HTML-Mail-Templates mit escapeHtml fuer alle dynamischen Werte
</verification>

<success_criteria>
- Spendenuebersicht kumuliert pro Monat und Jahr sichtbar (REP-01)
- Umsatzbericht mit Gesamtumsatz, Spenden, Top-5-Artikeln und Balkendiagramm (REP-02)
- E-Mail-Adresse im Settings-Tab konfigurierbar (REP-03)
- Automatischer monatlicher und jaehrlicher Mail-Versand via Cron, deaktivierbar (REP-04)
- HTML-Mails mit formatierter Tabelle und Styling
</success_criteria>

<output>
After completion, create `.planning/phases/03-warenwirtschaft-berichte/03-03-SUMMARY.md`
</output>
