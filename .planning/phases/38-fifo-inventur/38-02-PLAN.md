---
phase: 38-fifo-inventur
plan: 02
type: execute
wave: 2
depends_on: [38-01]
files_modified:
  - server/src/routes/reports.ts
autonomous: true
requirements: [INVENT-01, INVENT-03]

must_haves:
  truths:
    - "CSV-Export zeigt FIFO-basierten Bestandswert pro Artikel (nicht aktueller EK x Bestand)"
    - "PDF-Export zeigt FIFO-basierten Bestandswert pro Artikel"
    - "XLSX-Export zeigt FIFO-basierten Bestandswert pro Artikel"
    - "Alle drei Exports nutzen computeFifoInventory statt eigenstaendiger Datenbankabfragen"
    - "total_stock_value_cents in allen Exports ist FIFO-basiert"
  artifacts:
    - path: "server/src/routes/reports.ts"
      provides: "inventory-csv, inventory-pdf, inventory-xlsx nutzen computeFifoInventory"
      contains: "computeFifoInventory"
  key_links:
    - from: "server/src/routes/reports.ts inventory-csv Handler"
      to: "computeFifoInventory"
      via: "direkter Funktionsaufruf"
      pattern: "await computeFifoInventory"
---

<objective>
Inventur-Exports auf FIFO umstellen: inventory-csv, inventory-pdf und inventory-xlsx nutzen die in Plan 01 erstellte computeFifoInventory-Funktion statt eigener Datenbankabfragen.

Purpose: Konsistenz zwischen UI-Anzeige und Download-Exports. Alle vier Ausgabewege zeigen denselben FIFO-Bestandswert.
Output: Drei Export-Handler in reports.ts refaktoriert — duplizierte DB-Abfragen entfernt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/38-fifo-inventur/38-01-SUMMARY.md

<interfaces>
<!-- computeFifoInventory Signatur aus Plan 01 -->
```typescript
type FifoLot = { ek_cents: number; quantity: number; moved_at: number };
type FifoItem = {
  id: string;
  article_number: string;
  name: string;
  current_stock: number;
  current_ek_cents: number;
  sold_qty: number;
  withdrawn_qty: number;
  revenue_cents: number;
  cost_cents: number;
  withdrawal_cost_cents: number;
  stock_value_cents: number;
  remaining_lots: FifoLot[];
  price_periods: PricePeriod[];
};
type FifoInventoryResult = {
  items: FifoItem[];
  total_stock_value_cents: number;
  total_purchased_cents: number;
};

async function computeFifoInventory(shopId: string, year: number): Promise<FifoInventoryResult>
```

<!-- Aktuell: inventory-csv Zeilen 547-668, inventory-xlsx Zeilen 671-784, inventory-pdf Zeilen 921-1100+ -->
<!-- Jeder dieser Handler hat seine eigene duplizierte inventoryResult + stockValueResult Query -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: inventory-csv auf computeFifoInventory umstellen</name>
  <files>server/src/routes/reports.ts</files>
  <read_first>
    - server/src/routes/reports.ts Zeilen 546-668 (inventory-csv Handler)
  </read_first>
  <action>
Den `/reports/inventory-csv` Handler (Zeilen ~547-668) refaktorieren:

**Vorher:** eigene inventoryResult-Query + stockValueResult-Query (~50 Zeilen SQL)

**Nachher:**
```typescript
fastify.get('/reports/inventory-csv', async (request, reply) => {
  const session = (request as any).session as { shopId: string };
  const shopId = session.shopId;
  const { year } = request.query as { year?: string };
  if (!year) return reply.status(400).send({ error: 'year required' });

  const y = Number(year);
  if (isNaN(y)) return reply.status(400).send({ error: 'year must be a number' });

  const { items, total_stock_value_cents: totalStockValueCents } = await computeFifoInventory(shopId, y);

  // ... ab hier: Bilanz-Berechnung, CSV-Ausgabe wie vorher
```

In der CSV-Daten-Ausgabe die Bestandswert-Spalte anpassen:
- Vorher: `item.current_stock * item.current_ek_cents / 100`
- Nachher: `item.stock_value_cents / 100`

Alles andere (Bilanz-Zeilen, Spalten-Header, stringify-Config) bleibt unverändert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p server/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "computeFifoInventory" server/src/routes/reports.ts` gibt mindestens 4 zurück (Definition + 3 Handler-Aufrufe nach allen 3 Export-Tasks)
    - `grep -n "stock \* purchase_price" server/src/routes/reports.ts` gibt keine Treffer mehr in den inventory-csv/pdf/xlsx Handlern
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>inventory-csv Handler nutzt computeFifoInventory, Bestandswert-Spalte zeigt item.stock_value_cents.</done>
</task>

<task type="auto">
  <name>Task 2: inventory-xlsx und inventory-pdf auf computeFifoInventory umstellen</name>
  <files>server/src/routes/reports.ts</files>
  <read_first>
    - server/src/routes/reports.ts Zeilen 671-784 (inventory-xlsx Handler)
    - server/src/routes/reports.ts Zeilen 921-1100 (inventory-pdf Handler)
  </read_first>
  <action>
**inventory-xlsx Handler (~Zeilen 671-784):**

Gleiche Umstellung wie bei CSV: eigene DB-Queries durch `computeFifoInventory(shopId, y)` ersetzen.

In der wsData-Befüllung die Bestandswert-Spalte aktualisieren:
- Vorher: `Number((item.current_stock * item.current_ek_cents / 100).toFixed(2))`
- Nachher: `Number((item.stock_value_cents / 100).toFixed(2))`

Bilanzzeile mit `totalStockValueCents` bleibt (kommt jetzt FIFO-basiert aus computeFifoInventory).

**inventory-pdf Handler (~Zeilen 921-1100+):**

Gleiche Umstellung: eigene DB-Queries durch `computeFifoInventory(shopId, yearNum)` ersetzen.

Im shopName-Lookup: separate Abfrage für `shops.name` bleibt erhalten (die braucht computeFifoInventory nicht), nur die inventoryResult + stockValueResult Queries entfernen.

In der PDF-Tabellen-Befüllung (`rowValues` Array):
- Letzter Wert (Bestandswert-Spalte): von `item.current_stock * item.current_ek_cents` zu `item.stock_value_cents` ändern

Bestehende PDF-Spaltenstruktur (colHeaders, colWidths, colX) bleibt unverändert.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p server/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "await computeFifoInventory" server/src/routes/reports.ts` gibt mindestens 4 zurück (1x /inventory + 3x Exports)
    - `grep -n "current_stock \* item.current_ek_cents\|stock \* purchase_price" server/src/routes/reports.ts` gibt keine Treffer mehr (alte Bestandswert-Berechnung entfernt)
    - TypeScript kompiliert ohne Fehler
  </acceptance_criteria>
  <done>Alle drei Export-Handler (CSV, XLSX, PDF) nutzen computeFifoInventory und zeigen FIFO-basierten Bestandswert. Duplizierte DB-Abfragen entfernt.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client→API | shopId aus Session, year aus Query-Param |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-38-04 | Tampering | year in Export-Endpoints | mitigate | isNaN-Check nach Number(year) in allen Handlern hinzufügen (wie in sales-csv bereits vorhanden) |
| T-38-05 | Information Disclosure | Export-Endpoints geben Inventurdaten zurück | accept | Session-Authentifizierung schützt alle /reports/* Routen |
</threat_model>

<verification>
```bash
# 1. TypeScript kompiliert
cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p server/tsconfig.json

# 2. Alle 4 Handler nutzen computeFifoInventory
grep -c "await computeFifoInventory" server/src/routes/reports.ts
# Erwartetes Ergebnis: 4

# 3. Alte stock*purchase_price Berechnung nicht mehr in Export-Handlern
grep -n "current_stock \* item.current_ek_cents" server/src/routes/reports.ts && echo "FEHLER" || echo "OK"

# 4. stockValueResult Query nicht mehr in Export-Handlern
grep -c "stockValueResult" server/src/routes/reports.ts
# Erwartetes Ergebnis: 0 (alle durch computeFifoInventory ersetzt)
```
</verification>

<success_criteria>
- computeFifoInventory wird in allen 4 Inventur-Endpoints aufgerufen (1x /inventory + 3x Exports)
- Alle Exports zeigen FIFO-basierten Bestandswert (item.stock_value_cents)
- Keine doppelten DB-Abfragen mehr in Export-Handlern
- TypeScript kompiliert ohne Fehler
- isNaN-Check für year Parameter in allen Export-Endpoints
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/38-fifo-inventur/38-02-SUMMARY.md` erstellen.
</output>
