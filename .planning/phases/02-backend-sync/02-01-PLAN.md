---
phase: 02-backend-sync
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/db/schema.ts
  - server/src/db/index.ts
  - server/src/routes/sync.ts
  - server/src/index.ts
  - server/migrations/0000_initial.sql
  - server/Dockerfile
autonomous: true
requirements:
  - WAR-04
  - OFF-03
  - OFF-04

must_haves:
  truths:
    - "POST /api/sync nimmt OutboxEntries entgegen und antwortet mit { processed, errors }"
    - "SALE_COMPLETE-Event inseriert den Sale idempotent (INSERT OR IGNORE auf id)"
    - "SALE_COMPLETE-Event reduziert den Stock als Delta (nie Absolutwert)"
    - "Produkte aus Sale-Items werden per Upsert angelegt, falls auf dem Server noch unbekannt"
    - "Migration laeuft automatisch beim Container-Start — kein manueller SQL-Schritt"
  artifacts:
    - path: "server/src/db/schema.ts"
      provides: "Drizzle-Tabellendefinitionen: products, sales, outboxEvents"
      exports: ["products", "sales", "outboxEvents"]
    - path: "server/src/db/index.ts"
      provides: "DB-Singleton via better-sqlite3 + drizzle()"
      exports: ["db"]
    - path: "server/src/routes/sync.ts"
      provides: "POST /api/sync Endpoint mit Zod-Validierung"
      exports: ["syncRoutes"]
    - path: "server/migrations/0000_initial.sql"
      provides: "Initiale SQLite-Migration (generiert von drizzle-kit)"
      contains: "CREATE TABLE"
  key_links:
    - from: "server/src/index.ts"
      to: "server/src/routes/sync.ts"
      via: "fastify.register(syncRoutes, { prefix: '/api' })"
      pattern: "syncRoutes"
    - from: "server/src/routes/sync.ts"
      to: "server/src/db/index.ts"
      via: "import { db } from '../db/index.js'"
      pattern: "db\\.transaction"
    - from: "server/Dockerfile"
      to: "server/migrations/"
      via: "CMD sh -c 'npx drizzle-kit migrate && node dist/index.js'"
      pattern: "drizzle-kit migrate"
---

<objective>
Drizzle-Schema, DB-Singleton und POST /api/sync Endpoint auf dem Fastify-Server implementieren. Migration wird automatisch beim Container-Start angewendet. Verkaufsdaten landen idempotent in der Server-Datenbank, Warenbestand wird als Delta reduziert.

Purpose: Server-seitige Grundlage fuer die Sync-Engine. Ohne diesen Endpoint kann der Client keine Outbox-Eintraege uebertragen.
Output: Funktionierender POST /api/sync Endpoint der SALE_COMPLETE-Events verarbeitet und idempotent in SQLite speichert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/02-backend-sync/02-RESEARCH.md

<interfaces>
<!-- Source of Truth fuer Typen: client/src/db/schema.ts -->
<!-- Das Drizzle-Schema spiegelt exakt diese Strukturen -->

Dexie-Typen (Client — als Referenz fuer Drizzle-Schema-Design):

  Product:
    id: string (UUID)
    shopId: string
    articleNumber: string
    name: string
    category: string
    purchasePrice: number  (Cent-Integer)
    salePrice: number      (Cent-Integer)
    vatRate: number        (7 oder 19)
    stock: number
    active: boolean
    updatedAt: number      (Unix-Timestamp ms, LWW)

  SaleItem:
    productId: string
    articleNumber: string
    name: string
    salePrice: number      (Cent-Integer, Snapshot)
    quantity: number

  Sale:
    id: string             (crypto.randomUUID())
    shopId: string
    items: SaleItem[]
    totalCents: number
    paidCents: number
    changeCents: number
    donationCents: number
    createdAt: number      (Unix-Timestamp ms)
    syncedAt?: number

  OutboxEntry (was Client sendet):
    operation: 'SALE_COMPLETE' | 'STOCK_ADJUST'
    payload: unknown       (bei SALE_COMPLETE: Sale-Objekt)
    shopId: string
    createdAt: number
    attempts: number

Bestehende Fastify-Struktur (server/src/index.ts):
  - healthRoutes registriert mit: fastify.register(healthRoutes, { prefix: '/api' })
  - syncRoutes analog registrieren

Docker-Compose-Konfiguration (bereits gesetzt):
  - volumes: sqlite-data:/app/data
  - environment: CORS_ORIGIN=https://fairstand.godsapp.de
  - DB_PATH nicht explizit gesetzt => Fallback './data/fairstand.db' greift

drizzle.config.ts zeigt auf:
  - schema: './src/db/schema.ts'
  - out: './migrations'
  - dbCredentials.url: process.env.DB_PATH ?? './data/fairstand.db'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Drizzle-Schema + DB-Singleton + Migration generieren</name>
  <files>server/src/db/schema.ts, server/src/db/index.ts, server/migrations/0000_initial.sql</files>

  <read_first>
    - server/drizzle.config.ts (zeigt auf ./src/db/schema.ts und ./migrations/)
    - client/src/db/schema.ts (Source of Truth — exakt spiegeln)
    - server/package.json (bestaetigt: drizzle-orm, drizzle-kit, better-sqlite3 bereits installiert)
  </read_first>

  <action>
1. server/src/db/schema.ts erstellen:

```typescript
import { sqliteTable, text, integer } from 'drizzle-orm/sqlite-core';

export const products = sqliteTable('products', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  articleNumber: text('article_number').notNull(),
  name: text('name').notNull(),
  category: text('category').notNull().default(''),
  purchasePrice: integer('purchase_price').notNull(),
  salePrice: integer('sale_price').notNull(),
  vatRate: integer('vat_rate').notNull(),
  stock: integer('stock').notNull().default(0),
  active: integer('active', { mode: 'boolean' }).notNull().default(true),
  updatedAt: integer('updated_at').notNull(),
});

export const sales = sqliteTable('sales', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  items: text('items', { mode: 'json' }).notNull(),
  totalCents: integer('total_cents').notNull(),
  paidCents: integer('paid_cents').notNull(),
  changeCents: integer('change_cents').notNull(),
  donationCents: integer('donation_cents').notNull(),
  createdAt: integer('created_at').notNull(),
  syncedAt: integer('synced_at'),
});

export const outboxEvents = sqliteTable('outbox_events', {
  id: integer('id').primaryKey({ autoIncrement: true }),
  shopId: text('shop_id').notNull(),
  operation: text('operation').notNull(),
  payload: text('payload', { mode: 'json' }).notNull(),
  processedAt: integer('processed_at'),
  createdAt: integer('created_at').notNull(),
});
```

Kein $type() auf JSON-Feldern — drizzle gibt bei mode:'json' ohnehin unknown zurueck, TypeScript-Cast erfolgt in der Route.

2. server/src/db/index.ts erstellen:

```typescript
import { drizzle } from 'drizzle-orm/better-sqlite3';
import Database from 'better-sqlite3';
import * as schema from './schema.js';

const sqlite = new Database(process.env.DB_PATH ?? './data/fairstand.db');
export const db = drizzle({ client: sqlite, schema });
```

WICHTIG: new Database(path) ist synchron — kein await. better-sqlite3 ist ein synchrones Native-Addon.
Bei "type": "module" in package.json: import Database from 'better-sqlite3' funktioniert korrekt ohne createRequire.

3. Migration generieren (im server/-Verzeichnis ausfuehren):

```bash
cd /Users/simonluthe/Documents/fairstand/server && npx drizzle-kit generate
```

Das erzeugt server/migrations/0000_initial.sql und server/migrations/meta/. Beide Verzeichnisse committen.
drizzle-kit generate benoetigt keine laufende DB — liest nur schema.ts.
Falls der Befehl eine DB-URL erwartet: DB_PATH=./data/fairstand.db npx drizzle-kit generate
  </action>

  <verify>
    <automated>ls /Users/simonluthe/Documents/fairstand/server/migrations/*.sql 2>/dev/null | head -1 | grep -q sql &amp;&amp; echo "Migration-Datei vorhanden" &amp;&amp; grep -q "CREATE TABLE" /Users/simonluthe/Documents/fairstand/server/migrations/*.sql &amp;&amp; echo "CREATE TABLE in Migration gefunden"</automated>
  </verify>

  <acceptance_criteria>
    - server/src/db/schema.ts exportiert products, sales, outboxEvents (pruefbar via: grep -c "export const" server/src/db/schema.ts ergibt 3)
    - server/src/db/index.ts exportiert db (grep "export const db" server/src/db/index.ts gibt Treffer)
    - server/migrations/0000_initial.sql existiert mit CREATE TABLE products, CREATE TABLE sales, CREATE TABLE outbox_events
    - Keine TypeScript-Fehler: cd server && npx tsc --noEmit gibt keinen Output
  </acceptance_criteria>

  <done>Drizzle-Schema mit drei Tabellen definiert, DB-Singleton exportiert, initiale Migration als SQL-Datei generiert und im Repository vorhanden.</done>
</task>

<task type="auto">
  <name>Task 2: POST /api/sync Endpoint + Dockerfile-Migration</name>
  <files>server/src/routes/sync.ts, server/src/index.ts, server/Dockerfile</files>

  <read_first>
    - server/src/index.ts (zeigt wie healthRoutes registriert ist — gleiche Pattern fuer syncRoutes)
    - server/src/db/schema.ts (aus Task 1 — exportierte Tabellen-Objekte fuer Insert-Operationen)
    - server/src/db/index.ts (aus Task 1 — db-Import-Pfad)
    - server/Dockerfile (muss migrations/ und drizzle.config.ts ins Production-Image kopieren)
  </read_first>

  <action>
1. server/src/routes/sync.ts erstellen:

```typescript
import type { FastifyInstance } from 'fastify';
import { z } from 'zod';
import { eq, sql } from 'drizzle-orm';
import { db } from '../db/index.js';
import { sales, products, outboxEvents } from '../db/schema.js';

const SaleItemSchema = z.object({
  productId: z.string(),
  articleNumber: z.string(),
  name: z.string(),
  salePrice: z.number().int(),
  quantity: z.number().int().positive(),
});

const SaleSchema = z.object({
  id: z.string(),
  shopId: z.string(),
  items: z.array(SaleItemSchema),
  totalCents: z.number().int(),
  paidCents: z.number().int(),
  changeCents: z.number().int(),
  donationCents: z.number().int(),
  createdAt: z.number().int(),
  syncedAt: z.number().int().optional(),
});

const OutboxEntrySchema = z.object({
  operation: z.enum(['SALE_COMPLETE', 'STOCK_ADJUST']),
  payload: z.unknown(),
  shopId: z.string(),
  createdAt: z.number().int(),
});

const SyncBatchSchema = z.object({
  entries: z.array(OutboxEntrySchema),
});

export async function syncRoutes(fastify: FastifyInstance) {
  fastify.post('/sync', async (request, reply) => {
    const result = SyncBatchSchema.safeParse(request.body);
    if (!result.success) {
      return reply.status(400).send({ error: result.error.flatten() });
    }

    let processed = 0;
    const errors: Array<{ index: number; message: string }> = [];

    for (let i = 0; i < result.data.entries.length; i++) {
      const entry = result.data.entries[i];
      try {
        if (entry.operation === 'SALE_COMPLETE') {
          const saleResult = SaleSchema.safeParse(entry.payload);
          if (!saleResult.success) {
            errors.push({ index: i, message: 'Ungültiger Sale-Payload: ' + JSON.stringify(saleResult.error.flatten()) });
            continue;
          }
          const sale = saleResult.data;

          db.transaction(() => {
            // 1. Sale idempotent einfuegen (INSERT OR IGNORE via onConflictDoNothing)
            db.insert(sales).values({
              id: sale.id,
              shopId: sale.shopId,
              items: sale.items,
              totalCents: sale.totalCents,
              paidCents: sale.paidCents,
              changeCents: sale.changeCents,
              donationCents: sale.donationCents,
              createdAt: sale.createdAt,
              syncedAt: Date.now(),
            }).onConflictDoNothing().run();

            // 2. Produkte aus Items upserten — Pitfall 5 abdecken:
            //    Produkt kann auf dem Server noch unbekannt sein (nur lokal in Dexie angelegt)
            for (const item of sale.items) {
              db.insert(products).values({
                id: item.productId,
                shopId: sale.shopId,
                articleNumber: item.articleNumber,
                name: item.name,
                category: '',
                purchasePrice: 0,
                salePrice: item.salePrice,
                vatRate: 7,
                stock: 0,
                active: true,
                updatedAt: sale.createdAt,
              }).onConflictDoNothing().run();

              // Stock als Delta reduzieren — nie Absolutwert (OFF-04)
              db.update(products)
                .set({ stock: sql`${products.stock} - ${item.quantity}` })
                .where(eq(products.id, item.productId))
                .run();
            }

            // 3. OutboxEvent auf dem Server protokollieren
            db.insert(outboxEvents).values({
              shopId: entry.shopId,
              operation: entry.operation,
              payload: entry.payload,
              processedAt: Date.now(),
              createdAt: entry.createdAt,
            }).run();
          })();

          processed++;
        }
        // STOCK_ADJUST: Phase 3 — noch nicht implementiert
      } catch (err) {
        errors.push({ index: i, message: String(err) });
      }
    }

    return reply.send({ processed, errors });
  });
}
```

2. server/src/index.ts anpassen — syncRoutes registrieren:

```typescript
import Fastify from 'fastify';
import cors from '@fastify/cors';
import { healthRoutes } from './routes/health.js';
import { syncRoutes } from './routes/sync.js';

const fastify = Fastify({ logger: true });

await fastify.register(cors, {
  origin: process.env.CORS_ORIGIN ?? '*',
});

await fastify.register(healthRoutes, { prefix: '/api' });
await fastify.register(syncRoutes, { prefix: '/api' });

const port = Number(process.env.PORT ?? 3000);
await fastify.listen({ port, host: '0.0.0.0' });
```

3. server/Dockerfile ersetzen — migrations/ und drizzle.config.ts ins Image kopieren, Migration beim Start ausfuehren:

```dockerfile
FROM node:20-alpine AS build
WORKDIR /app
COPY package*.json ./
RUN npm ci
COPY . .
RUN npm run build

FROM node:20-alpine AS production
WORKDIR /app
COPY package*.json ./
RUN npm ci --omit=dev
COPY --from=build /app/dist ./dist
COPY --from=build /app/migrations ./migrations
COPY --from=build /app/drizzle.config.ts ./drizzle.config.ts
RUN mkdir -p /app/data
EXPOSE 3000
CMD ["sh", "-c", "npx drizzle-kit migrate && node dist/index.js"]
```

WICHTIG: Ohne COPY --from=build /app/migrations ./migrations schlaegt drizzle-kit migrate mit
"Error: No migration files found" fehl (Pitfall 1 aus RESEARCH.md).
Die drizzle.config.ts wird benoetigt damit drizzle-kit die migrations/-Ordner-Adresse kennt.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server &amp;&amp; npx tsc --noEmit &amp;&amp; echo "TypeScript OK"</automated>
  </verify>

  <acceptance_criteria>
    - npx tsc --noEmit im server/-Verzeichnis gibt keinen Fehler aus
    - server/src/routes/sync.ts existiert und exportiert syncRoutes (grep "export async function syncRoutes" server/src/routes/sync.ts gibt Treffer)
    - server/src/index.ts importiert und registriert syncRoutes (grep "syncRoutes" server/src/index.ts gibt zwei Treffer: import + register)
    - server/Dockerfile enthaelt "COPY --from=build /app/migrations ./migrations" und "drizzle-kit migrate" im CMD
    - POST /api/sync mit leerem entries-Array gibt { processed: 0, errors: [] } zurueck (testbar mit: npm run dev in server/ und curl -X POST http://localhost:3000/api/sync -H 'Content-Type: application/json' -d '{"entries":[]}')
  </acceptance_criteria>

  <done>Sync-Endpoint vollstaendig implementiert, in Fastify registriert, Dockerfile migriert Datenbank automatisch beim Container-Start.</done>
</task>

</tasks>

<verification>
Gesamtverifikation Phase 02-01:

1. TypeScript-Build fehlerfrei: cd server && npx tsc --noEmit
2. Migration-Datei vorhanden: ls server/migrations/*.sql
3. Endpoint erreichbar: curl -X POST http://localhost:3000/api/sync -H 'Content-Type: application/json' -d '{"entries":[]}' gibt {"processed":0,"errors":[]}
4. Idempotenz-Test: Denselben Sale zweimal senden — processed=1 beim ersten, processed=1 beim zweiten (kein Fehler, kein Duplikat in DB)
5. Delta-Test: Sale mit 2x Produkt A senden — Stock in server-DB sinkt um 2, nicht absolut
</verification>

<success_criteria>
- POST /api/sync akzeptiert SALE_COMPLETE-Events und antwortet mit { processed: N, errors: [] }
- Doppelter POST mit gleichem Sale-id fuehrt nicht zu doppeltem DB-Eintrag
- Stock-Werte in products-Tabelle spiegeln kumulierte Deltas (nie Absolutwerte)
- npm run build im server/-Verzeichnis schlaegt nicht fehl
- Dockerfile kopiert migrations/ und fuehrt drizzle-kit migrate vor dem Server-Start aus
</success_criteria>

<output>
Nach Abschluss .planning/phases/02-backend-sync/02-01-SUMMARY.md erstellen.
</output>
