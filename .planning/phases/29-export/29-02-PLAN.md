---
phase: 29-export
plan: 02
type: execute
wave: 2
depends_on:
  - "29-01"
files_modified:
  - client/src/features/admin/reports/DailyReport.tsx
  - client/src/features/admin/reports/MonthlyReport.tsx
  - client/src/features/admin/reports/SaleDetailModal.tsx
autonomous: true
requirements:
  - EXP-01
  - EXP-02
  - EXP-03

must_haves:
  truths:
    - "DailyReport zeigt einen 'CSV-Export' Button — Klick lädt verkaufshistorie-YYYY-MM-DD.csv herunter"
    - "MonthlyReport Inventur-Tab zeigt zwei Buttons 'Inventur CSV' und 'Inventur PDF' für den aktiven Jahr"
    - "SaleDetailModal zeigt einen 'PDF-Beleg' Button — Klick lädt beleg-XXXXXXXX-YYYY-MM-DD.pdf herunter"
    - "Während des Downloads zeigt der Button einen Loading-State (Spinner oder deaktiviert)"
    - "Fehler beim Download werden als Alert angezeigt"
  artifacts:
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "CSV-Download-Button in Tagesübersicht"
      contains: "sales-csv"
    - path: "client/src/features/admin/reports/MonthlyReport.tsx"
      provides: "CSV + PDF-Download-Buttons im Inventur-Tab"
      contains: "inventory-csv, inventory-pdf"
    - path: "client/src/features/admin/reports/SaleDetailModal.tsx"
      provides: "PDF-Beleg-Button in Verkaufsdetail"
      contains: "receipt-pdf"
  key_links:
    - from: "client/src/features/admin/reports/DailyReport.tsx"
      to: "/api/reports/sales-csv"
      via: "fetch + blob download"
      pattern: "sales-csv.*from.*to"
    - from: "client/src/features/admin/reports/MonthlyReport.tsx"
      to: "/api/reports/inventory-csv, /api/reports/inventory-pdf"
      via: "fetch + blob download"
      pattern: "inventory-csv.*year|inventory-pdf.*year"
    - from: "client/src/features/admin/reports/SaleDetailModal.tsx"
      to: "/api/sales/:id/receipt-pdf"
      via: "fetch + blob download"
      pattern: "receipt-pdf.*sale\\.id"
---

<objective>
Download-Buttons in DailyReport, MonthlyReport (Inventur-Tab) und SaleDetailModal verdrahten, die die Backend-Endpoints aus Plan 01 ansprechen.

Purpose: Komplettiert die Export-Funktion aus Nutzersicht — Backend-Endpoints allein sind nicht erreichbar ohne Frontend-Trigger.
Output: Drei Komponenten mit Download-Buttons, fetch+blob-Pattern, Loading-States, Error-Handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/29-export/29-CONTEXT.md
@.planning/phases/29-export/29-01-SUMMARY.md
</context>

<interfaces>
<!-- Bestehende Patterns aus den drei Komponenten -->

Aus DailyReport.tsx:
```typescript
// rangeStart, rangeEnd sind unix-ms Zahlen (aus useMemo)
// authFetch ist bereits importiert (nutze es für den Download-Fetch)
// Tailwind 4 Button-Pattern (bestehend in der Datei):
// className="px-3 py-2 rounded-lg text-sm font-medium bg-..." 
```

Aus MonthlyReport.tsx:
```typescript
// year (State) ist bereits vorhanden — für inventory-csv und inventory-pdf
// activeTab === 'inventur' — nur dann Buttons anzeigen
// inventoryData ist bereits geladen (enthält items und total_stock_value_cents)
```

Aus SaleDetailModal.tsx:
```typescript
// sale: Sale — sale.id ist vorhanden
// authFetch ist bereits importiert
```

Download-Helper-Pattern (inline, kein separate Datei nötig):
```typescript
async function downloadFile(url: string, filename: string, setLoading: (v: boolean) => void) {
  setLoading(true);
  try {
    const res = await authFetch(url);
    if (!res.ok) throw new Error(`Download fehlgeschlagen: ${res.status}`);
    const blob = await res.blob();
    const a = document.createElement('a');
    a.href = URL.createObjectURL(blob);
    a.download = filename;
    a.click();
    URL.revokeObjectURL(a.href);
  } catch (err) {
    alert('Download fehlgeschlagen. Bitte erneut versuchen.');
    console.error(err);
  } finally {
    setLoading(false);
  }
}
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: DailyReport CSV-Export-Button</name>
  <files>client/src/features/admin/reports/DailyReport.tsx</files>
  <read_first>
    - client/src/features/admin/reports/DailyReport.tsx (vollständig — rangeStart/rangeEnd-Berechnung und bestehende Button-Styles prüfen)
  </read_first>
  <action>
1. State hinzufügen (direkt nach den bestehenden useState-Aufrufen):
   ```typescript
   const [downloadingCsv, setDownloadingCsv] = useState(false);
   ```

2. Download-Funktion als lokale Arrow-Function im Komponenten-Body (nach den useMemo-Blöcken):
   ```typescript
   async function handleCsvDownload() {
     setDownloadingCsv(true);
     try {
       const res = await authFetch(`/api/reports/sales-csv?from=${rangeStart}&to=${rangeEnd}`);
       if (!res.ok) throw new Error(`${res.status}`);
       const blob = await res.blob();
       const date = new Date().toISOString().slice(0, 10);
       const a = document.createElement('a');
       a.href = URL.createObjectURL(blob);
       a.download = `verkaufshistorie-${date}.csv`;
       a.click();
       URL.revokeObjectURL(a.href);
     } catch (err) {
       alert('CSV-Download fehlgeschlagen. Bitte erneut versuchen.');
       console.error('CSV download error:', err);
     } finally {
       setDownloadingCsv(false);
     }
   }
   ```

3. Button in die JSX einfügen — Platzierung: In der Toolbar-Zeile wo die Range-Buttons (heute/gestern/woche/monat) stehen, am rechten Ende der Zeile (mit `ml-auto` oder `flex-1 justify-end`):
   ```tsx
   <button
     onClick={handleCsvDownload}
     disabled={downloadingCsv || sales.length === 0}
     className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-emerald-600 text-white hover:bg-emerald-700 disabled:opacity-50 disabled:cursor-not-allowed"
   >
     {downloadingCsv ? (
       <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
     ) : (
       <span>↓</span>
     )}
     CSV
   </button>
   ```

4. Import ergänzen falls nicht vorhanden: `useState` muss in React-Import enthalten sein (bereits vorhanden).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "handleCsvDownload\|downloadingCsv" client/src/features/admin/reports/DailyReport.tsx` — beide gefunden
    - `grep -n "sales-csv" client/src/features/admin/reports/DailyReport.tsx` — URL vorhanden
    - `grep -n "verkaufshistorie" client/src/features/admin/reports/DailyReport.tsx` — Dateiname vorhanden
    - TypeScript-Check sauber für DailyReport.tsx
  </acceptance_criteria>
  <done>CSV-Button in DailyReport verdrahtet, Loading-State, Error-Handling, korrekter Dateiname</done>
</task>

<task type="auto">
  <name>Task 2: MonthlyReport Inventur-Buttons (CSV + PDF) und SaleDetailModal Beleg-PDF</name>
  <files>client/src/features/admin/reports/MonthlyReport.tsx, client/src/features/admin/reports/SaleDetailModal.tsx</files>
  <read_first>
    - client/src/features/admin/reports/MonthlyReport.tsx (vollständig — activeTab, year State und Inventur-Tab-Bereich finden)
    - client/src/features/admin/reports/SaleDetailModal.tsx (vollständig — Button-Bereich und authFetch-Import prüfen)
  </read_first>
  <action>
**MonthlyReport.tsx:**

1. States ergänzen (nach den bestehenden useState-Aufrufen):
   ```typescript
   const [downloadingInventurCsv, setDownloadingInventurCsv] = useState(false);
   const [downloadingInventurPdf, setDownloadingInventurPdf] = useState(false);
   ```

2. Download-Funktionen im Komponenten-Body:
   ```typescript
   async function handleInventurCsvDownload() {
     setDownloadingInventurCsv(true);
     try {
       const res = await authFetch(`/api/reports/inventory-csv?year=${year}`);
       if (!res.ok) throw new Error(`${res.status}`);
       const blob = await res.blob();
       const a = document.createElement('a');
       a.href = URL.createObjectURL(blob);
       a.download = `inventur-${year}.csv`;
       a.click();
       URL.revokeObjectURL(a.href);
     } catch (err) {
       alert('CSV-Download fehlgeschlagen. Bitte erneut versuchen.');
       console.error('Inventur CSV error:', err);
     } finally {
       setDownloadingInventurCsv(false);
     }
   }

   async function handleInventurPdfDownload() {
     setDownloadingInventurPdf(true);
     try {
       const res = await authFetch(`/api/reports/inventory-pdf?year=${year}`);
       if (!res.ok) throw new Error(`${res.status}`);
       const blob = await res.blob();
       const a = document.createElement('a');
       a.href = URL.createObjectURL(blob);
       a.download = `inventur-${year}.pdf`;
       a.click();
       URL.revokeObjectURL(a.href);
     } catch (err) {
       alert('PDF-Download fehlgeschlagen. Bitte erneut versuchen.');
       console.error('Inventur PDF error:', err);
     } finally {
       setDownloadingInventurPdf(false);
     }
   }
   ```

3. Buttons im Inventur-Tab-Bereich (wo `activeTab === 'inventur'`) einfügen — direkt über oder unter der Inventur-Tabelle, als Zeile mit zwei Buttons:
   ```tsx
   {activeTab === 'inventur' && inventoryData && (
     <div className="flex gap-2 mb-4">
       <button
         onClick={handleInventurCsvDownload}
         disabled={downloadingInventurCsv}
         className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-emerald-600 text-white hover:bg-emerald-700 disabled:opacity-50 disabled:cursor-not-allowed"
       >
         {downloadingInventurCsv ? (
           <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
         ) : <span>↓</span>}
         Inventur CSV
       </button>
       <button
         onClick={handleInventurPdfDownload}
         disabled={downloadingInventurPdf}
         className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-blue-600 text-white hover:bg-blue-700 disabled:opacity-50 disabled:cursor-not-allowed"
       >
         {downloadingInventurPdf ? (
           <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
         ) : <span>↓</span>}
         Inventur PDF
       </button>
     </div>
   )}
   ```

4. authFetch-Import prüfen — falls nicht vorhanden: `import { authFetch } from '../../../utils/authFetch.js';` (oder den bestehenden Pfad aus der Datei anpassen)

**SaleDetailModal.tsx:**

1. State ergänzen:
   ```typescript
   const [downloadingPdf, setDownloadingPdf] = useState(false);
   ```

2. Download-Funktion im Komponenten-Body:
   ```typescript
   async function handleReceiptPdfDownload() {
     setDownloadingPdf(true);
     try {
       const res = await authFetch(`/api/sales/${sale.id}/receipt-pdf`);
       if (!res.ok) throw new Error(`${res.status}`);
       const blob = await res.blob();
       const date = new Date().toISOString().slice(0, 10);
       const a = document.createElement('a');
       a.href = URL.createObjectURL(blob);
       a.download = `beleg-${sale.id.slice(0, 8).toUpperCase()}-${date}.pdf`;
       a.click();
       URL.revokeObjectURL(a.href);
     } catch (err) {
       alert('PDF-Download fehlgeschlagen. Bitte erneut versuchen.');
       console.error('Receipt PDF error:', err);
     } finally {
       setDownloadingPdf(false);
     }
   }
   ```

3. PDF-Button in die Modal-Footer-Zeile einfügen (neben oder unter den bestehenden Storno/Löschen-Buttons). Nur sichtbar wenn Sale NICHT storniert ist (`!sale.cancelledAt`):
   ```tsx
   {!sale.cancelledAt && (
     <button
       onClick={handleReceiptPdfDownload}
       disabled={downloadingPdf}
       className="flex items-center gap-1.5 px-3 py-2 rounded-lg text-sm font-medium bg-slate-600 text-white hover:bg-slate-700 disabled:opacity-50 disabled:cursor-not-allowed"
     >
       {downloadingPdf ? (
         <span className="inline-block h-3.5 w-3.5 rounded-full border-2 border-white border-t-transparent animate-spin" />
       ) : <span>↓</span>}
       PDF-Beleg
     </button>
   )}
   ```
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "inventory-csv\|inventory-pdf" client/src/features/admin/reports/MonthlyReport.tsx` — beide URLs vorhanden
    - `grep -n "downloadingInventurCsv\|downloadingInventurPdf" client/src/features/admin/reports/MonthlyReport.tsx` — beide States vorhanden
    - `grep -n "receipt-pdf" client/src/features/admin/reports/SaleDetailModal.tsx` — URL vorhanden
    - `grep -n "downloadingPdf" client/src/features/admin/reports/SaleDetailModal.tsx` — State vorhanden
    - `grep -n "inventur-.*\.csv\|inventur-.*\.pdf" client/src/features/admin/reports/MonthlyReport.tsx` — Dateinamen vorhanden
    - TypeScript-Check sauber für beide Dateien
  </acceptance_criteria>
  <done>Inventur CSV/PDF-Buttons in MonthlyReport, PDF-Beleg-Button in SaleDetailModal, alle mit Loading-State und Error-Handling</done>
</task>

</tasks>

<verification>
- `grep -rn "sales-csv\|inventory-csv\|inventory-pdf\|receipt-pdf" client/src/features/admin/reports/` — alle vier Download-URLs in Komponenten vorhanden
- `grep -n "animate-spin" client/src/features/admin/reports/DailyReport.tsx client/src/features/admin/reports/MonthlyReport.tsx client/src/features/admin/reports/SaleDetailModal.tsx` — Loading-Spinner in allen drei Dateien
- `npx tsc --noEmit -p client/tsconfig.json` — kein TypeScript-Fehler
</verification>

<success_criteria>
- DailyReport: CSV-Button (grün) in Toolbar, lädt verkaufshistorie-{datum}.csv
- MonthlyReport Inventur-Tab: CSV-Button (grün) + PDF-Button (blau), laden inventur-{year}.csv/.pdf
- SaleDetailModal: PDF-Beleg-Button (grau/slate), nur bei nicht-stornierten Sales, lädt beleg-{id}-{datum}.pdf
- Alle Buttons haben Loading-State (Spinner) und Error-Handling (alert)
- TypeScript fehlerfrei
</success_criteria>

<output>
Nach Abschluss erstelle `.planning/phases/29-export/29-02-SUMMARY.md`
</output>
