---
phase: 22-postgresql-migration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - server/Dockerfile
  - server/drizzle.config.ts
  - server/src/db/schema.ts
  - server/src/db/index.ts
autonomous: true
requirements:
  - PG-01
  - PG-02
  - PG-03

must_haves:
  truths:
    - "docker-compose.yml startet einen postgres:16-alpine Container mit named Volume postgres-data"
    - "server-Container hat depends_on postgres mit condition: service_healthy"
    - "DATABASE_URL ist in docker-compose.yml als Umgebungsvariable gesetzt (postgresql://fairstand:${POSTGRES_PASSWORD}@postgres:5432/fairstand)"
    - "server/src/db/schema.ts importiert ausschliesslich aus drizzle-orm/pg-core (kein sqlite-core)"
    - "server/src/db/index.ts nutzt Pool aus 'pg' und drizzle aus 'drizzle-orm/node-postgres'"
    - "drizzle.config.ts hat dialect: 'postgresql' und DATABASE_URL statt DB_PATH"
  artifacts:
    - path: "docker-compose.yml"
      provides: "PostgreSQL-Service + Volume + server depends_on"
      contains: "postgres:16-alpine"
    - path: "server/src/db/schema.ts"
      provides: "pgTable Definitionen für alle 6 Tabellen"
      contains: "pgTable"
    - path: "server/src/db/index.ts"
      provides: "Drizzle-Instanz mit Pool"
      contains: "drizzle-orm/node-postgres"
  key_links:
    - from: "docker-compose.yml server.environment"
      to: "server/src/db/index.ts Pool connectionString"
      via: "DATABASE_URL env var"
      pattern: "DATABASE_URL"
    - from: "server/src/db/index.ts"
      to: "server/src/db/schema.ts"
      via: "import * as schema"
      pattern: "import.*schema"
---

<objective>
Infrastruktur und Datenbankschicht auf PostgreSQL umstellen: Docker-Compose bekommt einen postgres:16-alpine Container mit persistentem Volume, der Server-Container verbindet sich per Pool statt SQLite-Datei, und alle 6 Drizzle-Schema-Definitionen werden von sqliteTable auf pgTable migriert.

Purpose: Ohne diesen Plan schlagen alle nachfolgenden Pläne fehl — sie bauen auf dem neuen Schema und der neuen DB-Connection auf.
Output: Funktionsfähige PostgreSQL-Infrastruktur; server/src/db/index.ts und schema.ts sind PostgreSQL-ready.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/22-postgresql-migration/22-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Docker-Compose + Dockerfile + drizzle.config.ts auf PostgreSQL umstellen</name>
  <files>docker-compose.yml, server/Dockerfile, server/drizzle.config.ts</files>
  <read_first>
    - docker-compose.yml (aktueller Stand: sqlite-data Volume, DB_PATH env, kein postgres Service)
    - server/Dockerfile (aktueller Stand: drizzle-kit migrate + node start)
    - server/drizzle.config.ts (aktueller Stand: dialect sqlite, DB_PATH)
  </read_first>
  <action>
**docker-compose.yml** komplett umschreiben:

1. Neuen Service `postgres` hinzufügen ÜBER dem `server` Service:
```yaml
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: fairstand
      POSTGRES_USER: fairstand
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
      POSTGRES_INITDB_ARGS: "-E UTF8"
    volumes:
      - postgres-data:/var/lib/postgresql/data
    networks:
      - traefik
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U fairstand"]
      interval: 5s
      timeout: 5s
      retries: 10
      # Max Wartezeit: 10 Versuche × 5s = 50s bis PostgreSQL als healthy gilt
```

2. Im `server` Service:
   - `volumes` Eintrag `- sqlite-data:/app/data` BEHALTEN (wird für das manuelle Migrationsskript in Plan 03 noch gebraucht — SQLite-Datei muss lesbar sein)
   - Neue Umgebungsvariable hinzufügen: `DATABASE_URL: postgresql://fairstand:${POSTGRES_PASSWORD}@postgres:5432/fairstand`
   - `DB_PATH` Zeile ENTFERNEN (existiert aktuell nicht explizit in env, aber falls vorhanden entfernen)
   - `depends_on` Block hinzufügen:
```yaml
    depends_on:
      postgres:
        condition: service_healthy
```

3. Im `volumes` Block am Ende:
   - `sqlite-data:` BEHALTEN (für Datenmigrations-Backup)
   - `postgres-data:` hinzufügen

**server/Dockerfile** — Folgendes ändern:

Im Production-Stage CMD:
```
CMD ["sh", "-c", "npx drizzle-kit migrate && node dist/index.js"]
```
bleibt, aber drizzle.config.ts muss im Production-Image DATABASE_URL lesen.

Im build-Stage nach `COPY --from=build /app/drizzle.config.ts ./drizzle.config.ts` sicherstellen dass die Datei kopiert wird — das ist bereits der Fall, keine Änderung nötig wenn drizzle.config.ts angepasst ist.

**server/drizzle.config.ts** komplett ersetzen:
```typescript
import { defineConfig } from 'drizzle-kit';

export default defineConfig({
  schema: './src/db/schema.ts',
  out: './migrations',
  dialect: 'postgresql',
  dbCredentials: { url: process.env.DATABASE_URL ?? '' },
});
```
  </action>
  <verify>
    <automated>grep -n "postgres:16-alpine" /Users/simonluthe/Documents/fairstand/docker-compose.yml && grep -n "DATABASE_URL" /Users/simonluthe/Documents/fairstand/docker-compose.yml && grep -n "service_healthy" /Users/simonluthe/Documents/fairstand/docker-compose.yml && grep -n "postgres-data" /Users/simonluthe/Documents/fairstand/docker-compose.yml && grep "dialect.*postgresql" /Users/simonluthe/Documents/fairstand/server/drizzle.config.ts</automated>
  </verify>
  <acceptance_criteria>
    - docker-compose.yml enthält `postgres:16-alpine`
    - docker-compose.yml enthält `DATABASE_URL: postgresql://fairstand:${POSTGRES_PASSWORD}@postgres:5432/fairstand`
    - docker-compose.yml enthält `condition: service_healthy`
    - docker-compose.yml enthält `postgres-data:` in volumes (als named volume)
    - docker-compose.yml Health Check: `interval: 5s`, `retries: 10` (max 50s Wartezeit)
    - server/drizzle.config.ts enthält `dialect: 'postgresql'`
    - server/drizzle.config.ts enthält `DATABASE_URL` (nicht mehr DB_PATH)
  </acceptance_criteria>
  <done>Docker-Compose startet PostgreSQL-Container mit Health Check (max 50s Wartezeit), Server wartet auf gesunde DB, drizzle.config.ts kennt PostgreSQL-Dialect</done>
</task>

<task type="auto">
  <name>Task 2: Drizzle Schema (schema.ts) und DB-Connection (index.ts) auf PostgreSQL migrieren</name>
  <files>server/src/db/schema.ts, server/src/db/index.ts</files>
  <read_first>
    - server/src/db/schema.ts (aktuell: sqliteTable, integer mode:boolean, integer autoIncrement, text mode:json)
    - server/src/db/index.ts (aktuell: drizzle-orm/better-sqlite3, Database from better-sqlite3)
    - server/package.json (prüfen welche Abhängigkeiten schon vorhanden sind)
  </read_first>
  <action>
**server/package.json** — `pg` als Dependency hinzufügen und `@types/pg` als devDependency:
```json
"pg": "^8.13",
```
```json
"@types/pg": "^8",
```
`better-sqlite3` und `@types/better-sqlite3` in diesem Schritt NOCH NICHT entfernen — sie werden in Plan 03 als separates Standalone-Skript außerhalb der Server-Runtime verwendet. Plan 03 regelt die Entfernung vollständig.

Dann `npm install` im server-Verzeichnis ausführen.

**server/src/db/index.ts** komplett ersetzen:
```typescript
import { Pool } from 'pg';
import { drizzle } from 'drizzle-orm/node-postgres';
import * as schema from './schema.js';

const pool = new Pool({
  connectionString: process.env.DATABASE_URL,
  max: 20,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 5000,
});

export const db = drizzle({ client: pool, schema });

// Pool für Fastify onClose Hook exportieren
export { pool };
```

Dann in **server/src/index.ts** den Fastify `onClose` Hook hinzufügen. NACH `const fastify = Fastify({ logger: true });` und VOR der ersten `fastify.register(cors, ...)` Zeile einfügen:
```typescript
import { pool } from './db/index.js';
```
(zum bestehenden Import-Block hinzufügen)

Und nach `await fastify.register(fastifySchedule);` einfügen:
```typescript
fastify.addHook('onClose', async () => {
  await pool.end();
});
```

**server/src/db/schema.ts** komplett ersetzen — alle 6 Tabellen auf pgTable umstellen:

```typescript
import { pgTable, text, integer, boolean, serial, jsonb } from 'drizzle-orm/pg-core';

export const products = pgTable('products', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  articleNumber: text('article_number').notNull(),
  name: text('name').notNull(),
  category: text('category').notNull().default(''),
  purchasePrice: integer('purchase_price').notNull(),
  salePrice: integer('sale_price').notNull(),
  vatRate: integer('vat_rate').notNull(),
  stock: integer('stock').notNull().default(0),
  minStock: integer('min_stock').notNull().default(0),
  active: boolean('active').notNull().default(true),
  imageUrl: text('image_url'),
  updatedAt: integer('updated_at').notNull(),
});

export const sales = pgTable('sales', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  items: jsonb('items').notNull(),
  totalCents: integer('total_cents').notNull(),
  paidCents: integer('paid_cents').notNull(),
  changeCents: integer('change_cents').notNull(),
  donationCents: integer('donation_cents').notNull(),
  type: text('type'),
  createdAt: integer('created_at').notNull(),
  syncedAt: integer('synced_at'),
  cancelledAt: integer('cancelled_at'),
});

export const settings = pgTable('settings', {
  key: text('key').primaryKey(),
  value: text('value').notNull(),
  shopId: text('shop_id').notNull(),
});

export const outboxEvents = pgTable('outbox_events', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  operation: text('operation').notNull(),
  payload: jsonb('payload').notNull(),
  processedAt: integer('processed_at'),
  createdAt: integer('created_at').notNull(),
});

export const shops = pgTable('shops', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull().unique(),
  name: text('name').notNull(),
  pin: text('pin').notNull(),
  createdAt: integer('created_at').notNull(),
});

export const categories = pgTable('categories', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  name: text('name').notNull(),
  sortOrder: integer('sort_order').notNull().default(0),
  createdAt: integer('created_at').notNull(),
});
```

Wichtige Typdifferenzen zum alten Schema:
- `active`: war `integer('active', { mode: 'boolean' })` → jetzt `boolean('active')`
- `outboxEvents.id`: war `integer('id').primaryKey({ autoIncrement: true })` → jetzt `serial('id').primaryKey()`
- `sales.items`: war `text('items', { mode: 'json' })` → jetzt `jsonb('items')`
- `outboxEvents.payload`: war `text('payload', { mode: 'json' })` → jetzt `jsonb('payload')`
  </action>
  <verify>
    <automated>grep "pgTable" /Users/simonluthe/Documents/fairstand/server/src/db/schema.ts | wc -l && grep "drizzle-orm/node-postgres" /Users/simonluthe/Documents/fairstand/server/src/db/index.ts && grep "boolean('active')" /Users/simonluthe/Documents/fairstand/server/src/db/schema.ts && grep "serial('id')" /Users/simonluthe/Documents/fairstand/server/src/db/schema.ts && grep "jsonb('items')" /Users/simonluthe/Documents/fairstand/server/src/db/schema.ts && cd /Users/simonluthe/Documents/fairstand/server && npm run build 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - server/src/db/schema.ts enthält `import { pgTable` (kein sqliteTable mehr)
    - server/src/db/schema.ts enthält `boolean('active')` (nicht mehr integer mit mode:boolean)
    - server/src/db/schema.ts enthält `serial('id').primaryKey()` in outboxEvents
    - server/src/db/schema.ts enthält `jsonb('items')` in sales
    - server/src/db/index.ts enthält `import { Pool } from 'pg'`
    - server/src/db/index.ts enthält `drizzle-orm/node-postgres`
    - server/src/index.ts enthält `pool.end()` in onClose Hook
    - `npm run build` im server-Verzeichnis schlägt nicht fehl mit TypeScript-Fehlern im db/-Verzeichnis
  </acceptance_criteria>
  <done>Alle 6 Schema-Tabellen sind pgTable-Definitionen, DB-Connection nutzt pg Pool, TypeScript-Build läuft durch</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

1. `grep -r "sqliteTable\|better-sqlite3\|drizzle-orm/better-sqlite3" server/src/db/` gibt keine Treffer zurück
2. `grep "pgTable" server/src/db/schema.ts | wc -l` gibt 6 zurück (6 Tabellen)
3. `grep "postgres:16-alpine" docker-compose.yml` findet den Service
4. `cd server && npm run build` kompiliert ohne Fehler
</verification>

<success_criteria>
- Docker-Compose hat postgres:16-alpine Service mit named Volume `postgres-data`, Health Check (interval: 5s, retries: 10, max 50s Wartezeit), und server `depends_on: condition: service_healthy`
- Alle 6 Schema-Tabellen verwenden pgTable aus drizzle-orm/pg-core
- DB-Connection nutzt pg Pool mit DATABASE_URL
- TypeScript-Build läuft fehlerfrei durch
- drizzle.config.ts hat dialect: 'postgresql'
</success_criteria>

<output>
Nach Completion `.planning/phases/22-postgresql-migration/22-01-SUMMARY.md` erstellen.
</output>
