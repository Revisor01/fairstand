---
phase: 22-postgresql-migration
plan: 03
type: execute
wave: 2
depends_on:
  - 22-01
files_modified:
  - server/src/db/migrate-from-sqlite.ts
  - server/src/index.ts
  - server/package.json
autonomous: true
requirements:
  - PG-04
  - PG-05

must_haves:
  truths:
    - "server/src/db/migrate-from-sqlite.ts existiert und transferiert alle 5 Tabellen (shops, categories, products, sales, settings) in korrekter Reihenfolge"
    - "Migrationsskript ist idempotent — laeuft es ein zweites Mal, tut es nichts"
    - "better-sqlite3 und @types/better-sqlite3 sind aus package.json entfernt"
    - "Kein Import von better-sqlite3 verbleibt in server/src/ (ausser migrate-from-sqlite.ts vor Entfernung nach Daten-Transfer)"
    - "server/src/index.ts ruft migrateFromSQLiteIfNeeded() mit await auf beim Start"
  artifacts:
    - path: "server/src/db/migrate-from-sqlite.ts"
      provides: "Datenmigrationsskript SQLite nach PostgreSQL"
      contains: "migrateFromSQLiteIfNeeded"
    - path: "server/package.json"
      provides: "Bereinigte Dependencies ohne better-sqlite3"
  key_links:
    - from: "server/src/index.ts"
      to: "server/src/db/migrate-from-sqlite.ts"
      via: "import { migrateFromSQLiteIfNeeded } from './db/migrate-from-sqlite.js'"
      pattern: "migrateFromSQLiteIfNeeded"
    - from: "server/src/db/migrate-from-sqlite.ts"
      to: "SQLite-Datei auf Disk"
      via: "process.env.SQLITE_BACKUP_PATH || './data/fairstand.db'"
      pattern: "SQLITE_BACKUP_PATH"
---

<objective>
Datenmigrationsskript schreiben das bestehende SQLite-Daten nach PostgreSQL überträgt, und better-sqlite3 aus package.json entfernen. Das Skript läuft beim Server-Start einmalig und ist idempotent.

Purpose: Produktionsdaten (Shops, Produkte, Verkaufshistorie) dürfen beim Wechsel auf PostgreSQL nicht verloren gehen.
Output: migrate-from-sqlite.ts als einmalig ausführbares Migrationstool, package.json ohne better-sqlite3.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/22-postgresql-migration/22-RESEARCH.md
@.planning/phases/22-postgresql-migration/22-01-SUMMARY.md
</context>

<interfaces>
<!-- Aus Plan 01: Schema-Tabellen nach pgTable-Migration -->
<!-- server/src/db/schema.ts exports: products, sales, settings, outboxEvents, shops, categories -->
<!-- Alle IDs sind text('id').primaryKey() ausser outboxEvents (serial) -->
<!-- sales.items ist jsonb — Insert erwartet JavaScript-Object/Array, keine JSON-String -->
<!-- products.active ist boolean — Insert erwartet boolean, nicht 0/1 -->

<!-- SQLite Spaltennamen (snake_case) vs. Drizzle-ORM Feldnamen (camelCase): -->
<!-- shop_id → shopId, article_number → articleNumber, etc. -->
<!-- Bei .prepare('SELECT * FROM shops').all() kommen SQLite snake_case Spaltennamen -->
<!-- Beim Insert in Drizzle werden camelCase Keys aus dem Schema erwartet -->
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: migrate-from-sqlite.ts schreiben und in server/src/index.ts einbinden</name>
  <files>server/src/db/migrate-from-sqlite.ts, server/src/index.ts</files>
  <read_first>
    - server/src/db/schema.ts (Spaltennamen nach Plan 01 — pgTable mit camelCase Keys)
    - server/src/index.ts (Startsequenz, Position für await migrateFromSQLiteIfNeeded())
    - server/src/db/seed.ts (Referenz für korrekte Drizzle Insert-Syntax nach Plan 02)
  </read_first>
  <action>
**Neue Datei server/src/db/migrate-from-sqlite.ts erstellen:**

```typescript
import fs from 'fs';
import Database from 'better-sqlite3';
import { db } from './index.js';
import { shops, categories, products, sales, settings } from './schema.js';

const SQLITE_PATH = process.env.SQLITE_BACKUP_PATH || './data/fairstand.db';

export async function migrateFromSQLiteIfNeeded(): Promise<void> {
  // 1. Prüfe ob SQLite-Backup existiert
  if (!fs.existsSync(SQLITE_PATH)) {
    console.log('[Migration] Kein SQLite-Backup gefunden — Migration übersprungen');
    return;
  }

  // 2. Idempotenz: Prüfe ob PostgreSQL bereits Daten enthält
  const existingShops = await db.select().from(shops);
  if (existingShops.length > 0) {
    console.log('[Migration] PostgreSQL bereits befüllt — Migration übersprungen');
    return;
  }

  console.log(`[Migration] Starte SQLite→PostgreSQL Migration von: ${SQLITE_PATH}`);
  const sqlite = new Database(SQLITE_PATH, { readonly: true });

  try {
    // Reihenfolge wichtig: Foreign Keys werden von PostgreSQL enforced
    // shops → categories → products → sales → settings

    // Shops migrieren
    const sqliteShops = sqlite.prepare('SELECT * FROM shops').all() as any[];
    for (const row of sqliteShops) {
      await db.insert(shops).values({
        id: row.id,
        shopId: row.shop_id,
        name: row.name,
        pin: row.pin,
        createdAt: Number(row.created_at),
      });
    }
    console.log(`[Migration] Shops: ${sqliteShops.length} migriert`);

    // Categories migrieren
    const sqliteCategories = sqlite.prepare('SELECT * FROM categories').all() as any[];
    for (const row of sqliteCategories) {
      await db.insert(categories).values({
        id: row.id,
        shopId: row.shop_id,
        name: row.name,
        sortOrder: Number(row.sort_order ?? 0),
        createdAt: Number(row.created_at),
      });
    }
    console.log(`[Migration] Kategorien: ${sqliteCategories.length} migriert`);

    // Products migrieren
    const sqliteProducts = sqlite.prepare('SELECT * FROM products').all() as any[];
    for (const row of sqliteProducts) {
      await db.insert(products).values({
        id: row.id,
        shopId: row.shop_id,
        articleNumber: row.article_number,
        name: row.name,
        category: row.category ?? '',
        purchasePrice: Number(row.purchase_price),
        salePrice: Number(row.sale_price),
        vatRate: Number(row.vat_rate),
        stock: Number(row.stock ?? 0),
        minStock: Number(row.min_stock ?? 0),
        active: Boolean(row.active),  // SQLite speichert 0/1 — PostgreSQL erwartet boolean
        imageUrl: row.image_url ?? null,
        updatedAt: Number(row.updated_at),
      });
    }
    console.log(`[Migration] Produkte: ${sqliteProducts.length} migriert`);

    // Sales migrieren
    const sqliteSales = sqlite.prepare('SELECT * FROM sales').all() as any[];
    for (const row of sqliteSales) {
      // SQLite speichert items als JSON-String, PostgreSQL jsonb erwartet geparsten Wert
      const items = typeof row.items === 'string' ? JSON.parse(row.items) : row.items;
      await db.insert(sales).values({
        id: row.id,
        shopId: row.shop_id,
        items,
        totalCents: Number(row.total_cents),
        paidCents: Number(row.paid_cents),
        changeCents: Number(row.change_cents),
        donationCents: Number(row.donation_cents),
        type: row.type ?? null,
        createdAt: Number(row.created_at),
        syncedAt: row.synced_at ? Number(row.synced_at) : null,
        cancelledAt: row.cancelled_at ? Number(row.cancelled_at) : null,
      });
    }
    console.log(`[Migration] Verkäufe: ${sqliteSales.length} migriert`);

    // Settings migrieren
    const sqliteSettings = sqlite.prepare('SELECT * FROM settings').all() as any[];
    for (const row of sqliteSettings) {
      await db.insert(settings).values({
        key: row.key,
        value: row.value,
        shopId: row.shop_id,
      });
    }
    console.log(`[Migration] Einstellungen: ${sqliteSettings.length} migriert`);

    console.log('[Migration] SQLite→PostgreSQL Migration abgeschlossen');
  } catch (error) {
    console.error('[Migration] Fehler bei Migration:', error);
    throw error;
  } finally {
    sqlite.close();
  }
}
```

**server/src/index.ts** — migrateFromSQLiteIfNeeded() einbinden:

1. Import am Ende des Import-Blocks hinzufügen:
```typescript
import { migrateFromSQLiteIfNeeded } from './db/migrate-from-sqlite.js';
```

2. NACH `await ensureShopSeeded();` in der Startsequenz aufrufen — NEIN, umgekehrt:
   `migrateFromSQLiteIfNeeded()` ZUERST aufrufen (befüllt Shops), dann `ensureShopSeeded()` (legt Shop an wenn nicht vorhanden).

   Die Zeile `await ensureShopSeeded();` durch diese zwei Zeilen ersetzen:
```typescript
await migrateFromSQLiteIfNeeded();
await ensureShopSeeded();
```
  </action>
  <verify>
    <automated>test -f /Users/simonluthe/Documents/fairstand/server/src/db/migrate-from-sqlite.ts && echo "FILE EXISTS" && grep "migrateFromSQLiteIfNeeded" /Users/simonluthe/Documents/fairstand/server/src/db/migrate-from-sqlite.ts | wc -l && grep "jsonb_array_elements\|json_each\|SQLITE_BACKUP_PATH\|readonly.*true" /Users/simonluthe/Documents/fairstand/server/src/db/migrate-from-sqlite.ts && grep "migrateFromSQLiteIfNeeded" /Users/simonluthe/Documents/fairstand/server/src/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - server/src/db/migrate-from-sqlite.ts existiert
    - migrate-from-sqlite.ts enthält `export async function migrateFromSQLiteIfNeeded()`
    - migrate-from-sqlite.ts enthält Idempotenz-Check: `existingShops.length > 0`
    - migrate-from-sqlite.ts enthält `Boolean(row.active)` (0/1 → boolean Konvertierung)
    - migrate-from-sqlite.ts enthält `JSON.parse(row.items)` (JSON-String → Object für jsonb)
    - migrate-from-sqlite.ts enthält alle 5 Tabellen: shops, categories, products, sales, settings
    - server/src/index.ts enthält `await migrateFromSQLiteIfNeeded();` VOR `await ensureShopSeeded();`
  </acceptance_criteria>
  <done>Migrationsskript ist vollständig, deckt alle 5 Tabellen ab, ist idempotent, konvertiert SQLite-Typen korrekt, und wird beim Server-Start aufgerufen</done>
</task>

<task type="auto">
  <name>Task 2: better-sqlite3 aus package.json entfernen und Build verifizieren</name>
  <files>server/package.json</files>
  <read_first>
    - server/package.json (aktueller Stand nach Plan 01 — pg wurde hinzugefügt, better-sqlite3 noch vorhanden)
    - server/src/db/migrate-from-sqlite.ts (braucht better-sqlite3 — daher BLEIBT es als devDependency bis zum Produktionseinsatz, oder in dependencies für den Container)
  </read_first>
  <action>
**Wichtiger Hinweis:** better-sqlite3 wird im Migrationsskript (migrate-from-sqlite.ts) noch gebraucht — das Skript liest die SQLite-Datei beim ersten Start in der Produktion. Es kann NICHT vollständig entfernt werden solange das Migrationsskript existiert.

Strategie: better-sqlite3 von `dependencies` in `devDependencies` verschieben? Nein — der Produktions-Container braucht es für den einmaligen Migrationslauf.

Strategie (korrekt): better-sqlite3 BLEIBT als Dependency bis nach dem erfolgreichen Produktions-Migrationslauf. PG-05 ("better-sqlite3 ist komplett entfernt") wird NACH dem Datentransfer erfüllt, indem:
1. Migrationsskript ausgeführt wurde (Daten in PostgreSQL)
2. migrate-from-sqlite.ts gelöscht wird
3. better-sqlite3 aus package.json entfernt wird

Für jetzt: Den aktuellen Stand dokumentieren und TypeScript-Fehler verhindern. better-sqlite3 BLEIBT in dependencies. Import in migrate-from-sqlite.ts ist korrekt.

Was in diesem Task zu tun ist:
1. Prüfen ob `@types/better-sqlite3` in devDependencies ist (muss dort sein für TypeScript in der Datei)
2. Sicherstellen dass `pg` und `@types/pg` korrekt in package.json eingetragen sind (aus Plan 01)
3. TypeScript-Build ausführen: `cd server && npm run build`
4. Prüfen ob `grep -r "better-sqlite3" server/src/` NUR migrate-from-sqlite.ts als Treffer hat

Falls better-sqlite3 oder @types/better-sqlite3 fehlen (npm install wurde nicht ausgeführt):
```bash
cd server && npm install
```

Vollständigen Build testen:
```bash
cd server && npm run build 2>&1
```

Wenn Build erfolgreich: Ergebnis ist "TypeScript kompiliert, better-sqlite3 nur noch in migrate-from-sqlite.ts, kein anderer Code referenziert SQLite mehr".

**Dokumentiere im SUMMARY:** better-sqlite3 verbleibt absichtlich in package.json bis nach dem Produktions-Migrationslauf. Nach erfolgreichem Datentransfer: migrate-from-sqlite.ts löschen, better-sqlite3 aus package.json entfernen, `npm install`, neu deployen. Dies ist kein vergessener Schritt sondern Absicht.
  </action>
  <verify>
    <automated>grep -r "better-sqlite3" /Users/simonluthe/Documents/fairstand/server/src/ --include="*.ts" -l && echo "Files with better-sqlite3 imports (should be ONLY migrate-from-sqlite.ts)" && cd /Users/simonluthe/Documents/fairstand/server && npm run build 2>&1 | tail -10</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "better-sqlite3" server/src/ --include="*.ts" -l` gibt NUR `server/src/db/migrate-from-sqlite.ts` zurück
    - `cd server && npm run build` kompiliert ohne Fehler
    - server/package.json enthält `"pg": "^8.13"` in dependencies
    - server/package.json enthält `"@types/pg": "^8"` in devDependencies
  </acceptance_criteria>
  <done>TypeScript-Build ist gruen, better-sqlite3 ist nur noch in migrate-from-sqlite.ts referenziert, alle anderen Dateien sind SQLite-frei</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

1. `grep -r "better-sqlite3\|drizzle-orm/better-sqlite3\|sqliteTable\|sqlite-core" server/src/ --include="*.ts" -l` gibt NUR `server/src/db/migrate-from-sqlite.ts` zurück
2. `test -f server/src/db/migrate-from-sqlite.ts && echo EXISTS` gibt EXISTS zurück
3. `grep "migrateFromSQLiteIfNeeded" server/src/index.ts` findet den Aufruf
4. `cd server && npm run build` kompiliert fehlerfrei

Phase 22 vollständig wenn alle 3 Pläne abgeschlossen sind und:
- Docker-Compose startet postgres + server
- Server läuft gegen PostgreSQL statt SQLite
- Migrations-Skript kann bestehende SQLite-Daten übertragen
</verification>

<success_criteria>
- migrate-from-sqlite.ts überträgt alle 5 Tabellen in FK-korrekter Reihenfolge
- Idempotenz: Zweiter Aufruf tut nichts wenn PostgreSQL bereits Daten hat
- boolean und JSON-Typ-Konvertierung korrekt (SQLite 0/1 → PG boolean, JSON-String → jsonb)
- TypeScript-Build gruen
- better-sqlite3 ist nur noch in migrate-from-sqlite.ts vorhanden (dokumentiert als bewusste Entscheidung)
</success_criteria>

<output>
Nach Completion `.planning/phases/22-postgresql-migration/22-03-SUMMARY.md` erstellen.
</output>
