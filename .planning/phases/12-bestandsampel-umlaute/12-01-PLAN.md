---
phase: 12-bestandsampel-umlaute
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/features/admin/products/ProductList.tsx
  - client/src/features/admin/reports/DailyReport.tsx
  - client/src/features/admin/reports/MonthlyReport.tsx
autonomous: true
requirements:
  - AMP-01
  - TXT-01

must_haves:
  truths:
    - "Jede Artikelkachel in ArticleGrid trägt einen farbigen Punkt (●) — grün/gelb/rot — direkt sichtbar ohne Text lesen zu müssen"
    - "Jede Zeile in ProductList trägt denselben farbigen Punkt neben dem Bestandstext"
    - "Kein sichtbarer Text in der App enthält ae/oe/ue als Umlaut-Ersatz"
  artifacts:
    - path: "client/src/features/pos/ArticleGrid.tsx"
      provides: "Bestandsampel-Dot in Artikelkacheln"
    - path: "client/src/features/admin/products/ProductList.tsx"
      provides: "Bestandsampel-Dot in Produktliste"
    - path: "client/src/features/admin/reports/DailyReport.tsx"
      provides: "Korrekte Umlaute: Tagesübersicht, Verkäufe"
    - path: "client/src/features/admin/reports/MonthlyReport.tsx"
      provides: "Korrekte Umlaute: Verkäufe"
  key_links:
    - from: "ArticleGrid.tsx Kachel"
      to: "product.stock + product.minStock"
      via: "inline Farb-Logik per ternary"
      pattern: "stock.*minStock.*emerald.*amber.*rose"
    - from: "ProductList.tsx Zeile"
      to: "product.stock + product.minStock"
      via: "inline Farb-Logik per ternary"
      pattern: "stock.*minStock.*emerald.*amber.*rose"
---

<objective>
Farbige Bestandsampel (●) in Artikelkacheln und Produktliste einbauen, und alle Umlaut-Fehler (ae→ä, oe→ö, ue→ü) in sichtbaren UI-Texten korrigieren.

Purpose: Mitarbeiterinnen erkennen Bestandsstatus auf einen Blick ohne Zahlen lesen zu müssen. Die App spricht korrektes Deutsch.
Output: ArticleGrid + ProductList mit Ampel-Dot, DailyReport + MonthlyReport mit korrekten Umlauten.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Aus ArticleGrid.tsx — bestehende Farblogik für Bestand (wird um Ampel-Dot erweitert) -->
<!--
product.stock <= 0                                            → 'text-rose-500'   → Ampel: rose
product.minStock > 0 && product.stock <= product.minStock    → 'text-amber-600'  → Ampel: amber
sonst                                                         → 'text-slate-400'  → Ampel: emerald

Neue Farbregeln (TXT-01-konforme Spezifikation):
- Grün  (emerald-500): stock > 0 && (minStock === 0 || stock > minStock)
- Gelb  (amber-500):   stock > 0 && minStock > 0 && stock <= minStock
- Rot   (rose-500):    stock === 0
-->

<!-- Aus ProductList.tsx — isLowStock-Variable bereits vorhanden -->
<!--
const isLowStock = product.minStock > 0 && product.stock <= product.minStock;
// Ampel soll direkt neben "Bestand: X" angezeigt werden
-->

<!-- Bekannte Umlaut-Fehler in sichtbaren Texten: -->
<!--
DailyReport.tsx:59   → "Tagesuebersicht"  → "Tagesübersicht"
DailyReport.tsx:95   → "Verkaeufe"        → "Verkäufe"
DailyReport.tsx:107  → JSX-Kommentar "Einzelne Verkaeufe" (nicht sichtbar — trotzdem bereinigen)
DailyReport.tsx:151  → "Keine Verkaeufe an diesem Tag." → "Keine Verkäufe an diesem Tag."
MonthlyReport.tsx:141 → "Verkaeufe"       → "Verkäufe"
-->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bestandsampel-Dot in ArticleGrid und ProductList (AMP-01)</name>
  <files>client/src/features/pos/ArticleGrid.tsx, client/src/features/admin/products/ProductList.tsx</files>
  <action>
In beiden Dateien eine kleine Hilfsfunktion oder inline-Expression einbauen, die basierend auf `product.stock` und `product.minStock` eine Tailwind-Farbe für einen Unicode-Punkt (●) zurückgibt:

Farbregeln:
- `stock === 0` → `text-rose-500` (rot)
- `stock > 0 && minStock > 0 && stock <= minStock` → `text-amber-500` (gelb)
- `stock > 0 && (minStock === 0 || stock > minStock)` → `text-emerald-500` (grün)

**ArticleGrid.tsx:**
Den Ampel-Dot `<span>●</span>` mit der entsprechenden Farbklasse in jede Kachel einbauen. Platzierung: rechts oben in der Kachel (absolute position oder flex-row mit dem Bestandstext). Empfohlen: kleines `<span className="text-[10px] leading-none {farbe}">●</span>` direkt vor oder nach dem Bestandstext im unteren Bereich der Kachel — konsistent mit dem bestehenden Bestandstext-Block (Zeile 111-121). Keine neue eigene Zeile nötig; ein `gap-1`-flex-Wrapper um Dot + Bestandstext reicht.

**ProductList.tsx:**
In der Produktliste ist der Bestandstext bereits in einer `<span>` (Zeile 224-225). Den Ampel-Dot als `<span className="text-[10px] leading-none {farbe}">●</span>` direkt vor "Bestand:" in einem `flex items-center gap-1`-Wrapper einbauen. Die bestehende `isLowStock`-Variable kann beibehalten werden — die neue Ampellogik ersetzt aber die bisherige `text-red-600`-Färbung des Bestandstexts nicht, sie ergänzt sie lediglich um den Dot.

**Wichtig:** Keine Tests schreiben (rein visuell). Kein neues Interface, kein separater Hook — inline ist ausreichend.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -n "emerald\|amber\|rose" client/src/features/pos/ArticleGrid.tsx | grep "●\|text-\[10px\]\|Dot\|dot\|ampel" || grep -n "●" client/src/features/pos/ArticleGrid.tsx && grep -n "●" client/src/features/admin/products/ProductList.tsx</automated>
  </verify>
  <done>
    - ArticleGrid.tsx enthält `●` mit emerald/amber/rose Farbklasse in jeder Kachel
    - ProductList.tsx enthält `●` mit emerald/amber/rose Farbklasse je Produktzeile
    - Farblogik deckt alle drei Zustände ab: stock===0 (rose), 0&lt;stock≤minStock (amber), stock&gt;minStock oder minStock=0 (emerald)
  </done>
</task>

<task type="auto">
  <name>Task 2: Umlaut-Korrekturen in sichtbaren UI-Texten (TXT-01)</name>
  <files>client/src/features/admin/reports/DailyReport.tsx, client/src/features/admin/reports/MonthlyReport.tsx</files>
  <action>
Folgende sichtbaren Texte (und JSX-Kommentare) korrigieren:

**DailyReport.tsx:**
- Zeile 59: `"Tagesuebersicht"` → `"Tagesübersicht"`
- Zeile 95: `"Verkaeufe"` → `"Verkäufe"`
- Zeile 107: JSX-Kommentar `{/* Einzelne Verkaeufe */}` → `{/* Einzelne Verkäufe */}`
- Zeile 151: `"Keine Verkaeufe an diesem Tag."` → `"Keine Verkäufe an diesem Tag."`

**MonthlyReport.tsx:**
- Zeile 141: `"Verkaeufe"` → `"Verkäufe"`

Danach alle .tsx-Dateien in `client/src/` auf weitere sichtbare ae/oe/ue-Umlaut-Ersatzschreibweisen prüfen:
`grep -rn "Verkaeufe\|Tagesuebersicht\|Kassenuebersicht\|Pruefen\|Bestaetigen\|Aenderung\|Loeschen\|Erhoehen\|Groesse\|Bestaende\|Kaeufe\|Zurueck" client/src/ --include="*.tsx"`

Falls dabei weitere sichtbare Texte (keine Funktionsnamen, keine Variablennamen, keine Kommentare im nicht-JSX-Code) gefunden werden, diese ebenfalls korrigieren und in der Auflistung im SUMMARY dokumentieren.

Funktionsnamen wie `handleAllesZurueck` werden NICHT umbenannt (Breaking Change, unnötig).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -rn "Verkaeufe\|Tagesuebersicht\|Kassenuebersicht\|Aenderung\|Loeschen\|Erhoehen\|Groesse\|Bestaende\|Kaeufe" client/src/ --include="*.tsx" | grep -v "//" | grep -v "handle\|function\|const\|let\|var"</automated>
  </verify>
  <done>
    - Der grep-Befehl gibt keine Treffer mehr für die korrigierten Begriffe in sichtbaren Texten zurück
    - DailyReport zeigt "Tagesübersicht" und "Verkäufe" in der Überschrift und als Statistik-Label
    - MonthlyReport zeigt "Verkäufe" als Statistik-Label
  </done>
</task>

</tasks>

<verification>
Nach beiden Tasks:
1. `grep -n "●" client/src/features/pos/ArticleGrid.tsx` gibt Treffer zurück
2. `grep -n "●" client/src/features/admin/products/ProductList.tsx` gibt Treffer zurück
3. `grep -rn "Verkaeufe\|Tagesuebersicht" client/src/ --include="*.tsx"` gibt keine Treffer in sichtbaren Texten zurück
4. TypeScript-Compilation: `cd client && npx tsc --noEmit` gibt keine Fehler
</verification>

<success_criteria>
- Jede Artikelkachel (ArticleGrid) und jede Produktzeile (ProductList) zeigt einen farbigen ●-Punkt: grün wenn Bestand ausreichend, gelb wenn niedrig (≤ minStock), rot wenn 0
- "Tagesübersicht", "Verkäufe" sind korrekt mit Umlauten in allen Report-Komponenten
- Keine sichtbaren ae/oe/ue-Umlaut-Ersatzschreibweisen verbleiben in TSX-Texten
- TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/12-bestandsampel-umlaute/12-01-SUMMARY.md` erstellen.
</output>
