---
phase: 16-ui-stabilit-t-bestand
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/pos/ArticleCard.tsx
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/features/pos/LowStockBanner.tsx
autonomous: true
requirements:
  - UIX-01
  - BST-01

must_haves:
  truths:
    - "Beim Scrollen im Artikel-Grid wird kein Artikel versehentlich in den Warenkorb gelegt"
    - "Artikelkacheln mit niedrigem Bestand haben einen farbigen linken Rahmen (amber)"
    - "Ausverkaufte Artikel haben einen roten linken Rahmen"
    - "Der Bestandsampel-Dot ist text-xs (nicht mehr text-[10px]) und damit besser lesbar"
    - "Der LowStockBanner ist auffälliger mit Warnicon, Zähler und stärkerem Rahmen"
  artifacts:
    - path: "client/src/features/pos/ArticleCard.tsx"
      provides: "Einzelne Produktkachel mit Pointer-Movement-Threshold und Bestandsstatus-Styling"
      exports: ["ArticleCard"]
    - path: "client/src/features/pos/ArticleGrid.tsx"
      provides: "Grid-Layout mit Category-Tabs; delegiert Kacheln an ArticleCard"
      contains: "ArticleCard"
    - path: "client/src/features/pos/LowStockBanner.tsx"
      provides: "Prominenter Banner mit Icon, Zähler und border-b-2"
  key_links:
    - from: "client/src/features/pos/ArticleGrid.tsx"
      to: "client/src/features/pos/ArticleCard.tsx"
      via: "filteredProducts.map → <ArticleCard>"
      pattern: "ArticleCard"
    - from: "client/src/features/pos/ArticleCard.tsx"
      to: "onAddToCart prop"
      via: "onPointerUp mit dx/dy < 8 Prüfung"
      pattern: "onPointerUp"
---

<objective>
Zwei visuelle und interaktive Qualitätsprobleme in der Kassensicht beheben: das versehentliche Antippen von Artikeln beim Scrollen (UIX-01) und zu unauffällige Bestandswarnungen (BST-01).

Purpose: Mitarbeiterinnen sollen beim Scrollen keine Artikel unabsichtlich in den Warenkorb legen. Kritisch niedrige Bestände müssen sofort auffallen — ohne in die Verwaltung navigieren zu müssen.
Output: Neue ArticleCard.tsx-Komponente mit Pointer-Movement-Threshold, aktualisiertes ArticleGrid.tsx das ArticleCard nutzt, verbesserter LowStockBanner.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/16-ui-stabilit-t-bestand/16-RESEARCH.md

@client/src/features/pos/ArticleGrid.tsx
@client/src/features/pos/LowStockBanner.tsx
</context>

<interfaces>
<!-- Aus client/src/features/pos/ArticleGrid.tsx (aktuell): -->
```typescript
interface ArticleGridProps {
  onAddToCart: (product: Product) => void;
}
// useLiveQuery gibt Product[] | undefined zurück
// getShopId() wirft wenn kein Shop gesetzt — kein try-catch nötig hier
// filteredProducts.map() iteriert aktuell inline über <button> Elemente
```

<!-- Aus client/src/db/index.ts (relevante Felder von Product): -->
```typescript
// Product hat u.a.: id, name, salePrice, stock, minStock, imageUrl, active, shopId, category
```
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ArticleCard.tsx erstellen mit Pointer-Movement-Threshold (UIX-01)</name>
  <files>client/src/features/pos/ArticleCard.tsx, client/src/features/pos/ArticleCard.test.tsx</files>
  <behavior>
    - Test 1: onAddToCart wird aufgerufen wenn pointerdown + pointerup an derselben Position (0px Bewegung)
    - Test 2: onAddToCart wird aufgerufen wenn pointerdown + pointerup mit 7px Bewegung (unter Threshold)
    - Test 3: onAddToCart wird NICHT aufgerufen wenn Bewegung > 8px (Scroll-Geste)
    - Test 4: onAddToCart wird NICHT aufgerufen wenn product.stock <= 0 (auch bei 0px Bewegung)
    - Test 5: Nach pointercancel wird startPos auf null gesetzt — nächster pointerup löst nichts aus
  </behavior>
  <action>
    Neue Datei `client/src/features/pos/ArticleCard.tsx` erstellen.

    Die Komponente kapselt eine einzelne Produktkachel mit:
    - `useRef<{ x: number; y: number } | null>(null)` für `startPos` (per D-UIX-01: kein useState — kein Re-Render)
    - `onPointerDown`: Setzt `startPos.current = { x: e.clientX, y: e.clientY }`
    - `onPointerUp`: Berechnet `dx = Math.abs(e.clientX - startPos.current.x)`, `dy = Math.abs(...)`, setzt `startPos.current = null`, ruft `onAddToCart(product)` nur wenn `dx < 8 && dy < 8 && product.stock > 0`
    - `onPointerCancel`: Setzt `startPos.current = null` (verhindert Ghost-Tap nach iOS Scroll-Übernahme)
    - `disabled={product.stock <= 0}`

    Kachel-Styling mit farbigem linken Rahmen je nach Bestandsstatus (per BST-01):
    ```
    isOutOfStock (stock <= 0):  'opacity-50 cursor-not-allowed border-l-4 border-rose-400'
    isLowStock (minStock > 0 && stock <= minStock):  'border-l-4 border-amber-400 active:bg-amber-50'
    normal:  'active:bg-sky-50'
    ```
    Basis-Klassen: `bg-white shadow-sm rounded-xl min-h-[80px] p-4 flex flex-col justify-between items-start transition-colors text-left`

    Bestandsampel-Dot: `text-[10px]` → `text-xs` (per BST-01 — bessere Lesbarkeit)

    Bestehender Inhalt aus ArticleGrid.tsx 1:1 übernehmen:
    - Produktbild-Block (imageUrl-Prüfung)
    - Produktname-Span
    - Preis + Bestandsampel-Row (inklusive Dot und Bestandstext)

    Interface:
    ```typescript
    interface ArticleCardProps {
      product: Product;
      onAddToCart: (product: Product) => void;
    }
    ```

    Testdatei `ArticleCard.test.tsx`: Tests mit `@testing-library/react` und `vi.fn()` für onAddToCart.
    Pointer-Events simulieren mit `fireEvent.pointerDown(el, { clientX: 0, clientY: 0 })` und `fireEvent.pointerUp(el, { clientX: ..., clientY: ... })`.
    Prüfen ob onAddToCart aufgerufen wurde oder nicht mit `expect(mockFn).toHaveBeenCalledTimes(...)`.

    WICHTIG: Kategorie-Tabs in ArticleGrid NICHT anfassen — die behalten `onPointerDown` wie bisher.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npm run test -- ArticleCard</automated>
  </verify>
  <done>
    - ArticleCard.tsx existiert und wird von TypeScript fehlerfrei kompiliert
    - Alle 5 Tests in ArticleCard.test.tsx bestehen
    - Pointer-Movement-Threshold ist implementiert: dx/dy Prüfung, pointercancel Handler, stock-Prüfung
  </done>
</task>

<task type="auto">
  <name>Task 2: ArticleGrid.tsx auf ArticleCard umstellen + LowStockBanner verbessern (BST-01)</name>
  <files>client/src/features/pos/ArticleGrid.tsx, client/src/features/pos/LowStockBanner.tsx</files>
  <action>
    **ArticleGrid.tsx:**
    Den inline `<button>` Block im `filteredProducts.map()` durch `<ArticleCard>` ersetzen:
    ```tsx
    import { ArticleCard } from './ArticleCard.js';
    // ...
    {filteredProducts.map((product: Product) => (
      <ArticleCard
        key={product.id}
        product={product}
        onAddToCart={onAddToCart}
      />
    ))}
    ```
    Die bisherigen inline Event-Handler, className-Blöcke und Bestandsampel-JSX aus ArticleGrid.tsx entfernen — alles liegt jetzt in ArticleCard.tsx.
    Die übrige Struktur (Kategorie-Tabs, Grid-Container, Lade-Spinner, Leer-Zustand) bleibt unverändert.

    **LowStockBanner.tsx:**
    Den Banner prominenter machen (per BST-01 — aktuell zu schmaler Streifen):
    ```tsx
    <div className="bg-amber-50 border-b-2 border-amber-400 px-4 py-3 text-amber-900 text-sm flex items-start gap-2">
      <span className="text-amber-500 text-base shrink-0">⚠</span>
      <div>
        <span className="font-semibold">
          Mindestbestand unterschritten ({lowStockProducts.length}):
        </span>{' '}
        {lowStockProducts.map(p => `${p.name} (${p.stock})`).join(', ')}
      </div>
    </div>
    ```
    Änderungen gegenüber vorher:
    - `bg-amber-100` → `bg-amber-50` (leichter Ton)
    - `border-b` → `border-b-2` (stärkerer Rahmen)
    - `border-amber-300` → `border-amber-400`
    - `py-2` → `py-3` (mehr Raum)
    - `text-amber-800` → `text-amber-900`
    - Hinzugefügt: `flex items-start gap-2` Wrapper + `⚠` Icon-Span + Zähler in Klammern
    - `font-medium` → `font-semibold`
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
    - `npm run build` läuft fehlerfrei durch
    - ArticleGrid.tsx enthält keine inline Produkt-Button-Logik mehr (nur noch `&lt;ArticleCard&gt;`)
    - LowStockBanner.tsx hat `border-b-2`, `⚠`-Icon und Zähler in Klammern
    - Kein TypeScript-Fehler in der gesamten Build-Ausgabe
  </done>
</task>

</tasks>

<verification>
Gesamtprüfung nach Abschluss beider Tasks:

1. `cd /Users/simonluthe/Documents/fairstand/client && npm run test` — alle Tests grün
2. `cd /Users/simonluthe/Documents/fairstand/client && npm run build` — kein Fehler
3. Manuelle Prüfung (iPad/Chrome DevTools Mobile): Durch Artikel-Grid scrollen → kein Artikel landet im Warenkorb beim Scrollen
4. Manuelle Prüfung: Artikel mit stock <= minStock hat amber-Rahmen links, Ausverkauft hat rose-Rahmen links
5. Manuelle Prüfung: LowStockBanner zeigt Warnicon und Zähler wenn Artikel unter Mindestbestand
</verification>

<success_criteria>
- UIX-01: Beim Scrollen (> 8px Fingerbewegung) wird kein Artikel in den Warenkorb gelegt
- UIX-01: Gezieltes Antippen (< 8px Bewegung) legt Artikel korrekt in den Warenkorb
- BST-01: Kacheln mit niedrigem Bestand haben amber-Rahmen (border-l-4 border-amber-400)
- BST-01: Ausverkaufte Kacheln haben roten Rahmen (border-l-4 border-rose-400)
- BST-01: Bestandsampel-Dot ist text-xs (nicht mehr text-[10px])
- BST-01: LowStockBanner hat Warnicon (⚠), Zähler und border-b-2
- Build läuft fehlerfrei durch
</success_criteria>

<output>
Nach Abschluss `.planning/phases/16-ui-stabilit-t-bestand/16-01-SUMMARY.md` erstellen mit:
- Was wurde gebaut (ArticleCard-Extraktion, Pointer-Threshold, Banner-Verbesserung)
- Welche Entscheidungen getroffen wurden (useRef statt useState, 8px Threshold, ArticleCard als eigene Datei)
- Dateien erstellt/geändert
- Bekannte Einschränkungen (manuelle iPad-Verifikation erforderlich)
</output>
