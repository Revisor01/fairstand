---
phase: 17-datenverwaltung-sync
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/admin/products/ProductForm.tsx
  - client/src/features/admin/products/ProductList.tsx
  - client/src/sync/useSyncStatus.ts
  - client/src/sync/triggers.ts
  - client/src/features/pos/POSScreen.tsx
autonomous: true
requirements:
  - VRW-02
  - SYN-01

must_haves:
  truths:
    - "Beim Anlegen oder Bearbeiten eines Produkts kann ein Bild direkt im Formular ausgewählt und hochgeladen werden"
    - "Eine Bild-Vorschau erscheint sofort nach Auswahl der Datei (vor dem Speichern)"
    - "Schlägt ein Sync fehl, erscheint ein farbiger Status-Punkt im POS-Header"
    - "Wenn Outbox-Einträge >= 5 Versuche haben (dauerhafter Fehler), zeigt die Kasse eine Warnung"
    - "Die Sync-Engine versucht alle 30 Sekunden erneut zu flushen wenn online"
    - "Outbox-Einträge mit attempts >= 5 können zurückgesetzt werden"
  artifacts:
    - path: "client/src/sync/useSyncStatus.ts"
      provides: "useSyncStatus()-Hook mit SyncStatus-Typ"
      exports: ["useSyncStatus", "SyncStatus"]
    - path: "client/src/features/pos/POSScreen.tsx"
      provides: "Sync-Status-Punkt im POS-Header"
      contains: "useSyncStatus"
  key_links:
    - from: "client/src/features/pos/POSScreen.tsx"
      to: "client/src/sync/useSyncStatus.ts"
      via: "useSyncStatus() Hook-Aufruf"
      pattern: "useSyncStatus"
    - from: "client/src/features/admin/products/ProductForm.tsx"
      to: "server POST /api/products/:id/image"
      via: "fetch mit FormData nach handleSave"
      pattern: "api/products.*image"
---

<objective>
Bild-Upload direkt in ProductForm integrieren (mit Vorschau via URL.createObjectURL) und Sync-Robustheit durch useSyncStatus-Hook + 30s-Retry + POS-Status-Badge verbessern.

Purpose: Weniger Schritte beim Bildzuweisung, sichtbares Feedback wenn Verkäufe nicht übertragen werden.
Output: Bildupload mit Preview im ProductForm, "Bild"-Button in ProductList entfernt, useSyncStatus-Hook, periodischer Retry in triggers.ts, Sync-Badge in POS-Header.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md

@client/src/features/admin/products/ProductForm.tsx
@client/src/features/admin/products/ProductList.tsx
@client/src/sync/engine.ts
@client/src/sync/triggers.ts
@client/src/features/pos/POSScreen.tsx

<interfaces>
<!-- Bestehender Image-Upload-Endpunkt (server/src/routes/products.ts) -->
<!-- POST /api/products/:id/image — multipart/form-data, Feld 'image' -->
<!-- Rückgabe: { ok: true, imageUrl: '/api/images/filename.ext' } -->

<!-- handleImageUpload in ProductList.tsx (Zeilen 103-118) — wird entfernt/vereinfacht -->
async function handleImageUpload(productId: string, file: File | undefined)
// FormData.append('image', file) → fetch POST /api/products/:id/image
// Bei Erfolg: db.products.update(productId, { imageUrl })

<!-- POSScreen.tsx Header (Zeilen 144-183) -->
<!-- Sync-Badge wird in <div className="flex items-center gap-3"> eingefügt (neben Warenkorb-Button) -->

<!-- OutboxEntry.attempts: number — Feld existiert in client/src/db/schema.ts -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bildupload mit Preview in ProductForm + Bild-Button aus ProductList entfernen</name>
  <files>
    client/src/features/admin/products/ProductForm.tsx,
    client/src/features/admin/products/ProductList.tsx
  </files>
  <action>
**client/src/features/admin/products/ProductForm.tsx:**

**Hinweis:** Plan 01 ändert ebenfalls ProductForm.tsx (Kategorie-Dropdown). Diese Datei berücksichtigt den Stand NACH Plan 01 (Dropdown ist bereits vorhanden). Falls Plan 01 nicht zuerst ausgeführt wurde, prüfen ob `dbCategories` und `useLiveQuery` bereits importiert sind — wenn nicht, die bestehende Kategorie-Freitext-Implementierung belassen und nur die Bild-Upload-Erweiterungen hinzufügen.

Änderungen in ProductForm.tsx:

1. State hinzufügen:
```typescript
const [imagePreview, setImagePreview] = useState<string | null>(
  product?.imageUrl ?? null
);
const [pendingImageFile, setPendingImageFile] = useState<File | null>(null);
```

2. `handleSave()` anpassen: NACH dem bestehenden `db.products.add(productData)` bzw. `db.products.update(...)` und NACH dem fire-and-forget Server-POST, aber VOR `onClose()`, das Bild hochladen wenn `pendingImageFile` vorhanden:

Für neues Produkt (isEdit === false): Das Bild hochladen NACHDEM der Server-POST erfolgreich war. Server-POST von fire-and-forget auf `await` umstellen wenn `pendingImageFile` vorhanden:
```typescript
// Wenn Bild vorhanden: Server-POST awaiten (nicht fire-and-forget)
if (navigator.onLine) {
  const serverOk = await fetch('/api/products', {
    method: 'POST',
    headers: { 'Content-Type': 'application/json' },
    body: JSON.stringify(productData),
  }).then(r => r.ok).catch(() => false);

  if (serverOk && pendingImageFile) {
    const formData = new FormData();
    formData.append('image', pendingImageFile);
    const imgRes = await fetch(`/api/products/${productData.id}/image`, {
      method: 'POST',
      body: formData,
    }).catch(() => null);
    if (imgRes?.ok) {
      const { imageUrl } = await imgRes.json() as { imageUrl: string };
      await db.products.update(productData.id, { imageUrl });
    }
    // Fehler beim Bild-Upload: kein Fehler, Produkt ist trotzdem gespeichert
  } else if (!pendingImageFile) {
    // Kein Bild: fire-and-forget bleibt wie vorher
    fetch('/api/products', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify(productData),
    }).catch(err => console.warn('Server-Sync fehlgeschlagen:', err));
  }
}
```

Für bestehendes Produkt (isEdit === true): Nach dem Update fire-and-forget-POST bleibt, plus danach Bild hochladen:
```typescript
if (navigator.onLine && pendingImageFile) {
  const formData = new FormData();
  formData.append('image', pendingImageFile);
  const imgRes = await fetch(`/api/products/${product.id}/image`, {
    method: 'POST',
    body: formData,
  }).catch(() => null);
  if (imgRes?.ok) {
    const { imageUrl } = await imgRes.json() as { imageUrl: string };
    await db.products.update(product.id, { imageUrl });
  }
}
```

3. Bild-Upload-Bereich ans Ende des weißen Cards einfügen (vor dem schließenden `</div>` des `bg-white rounded-xl shadow-sm p-4`-Containers, nach dem Mindestbestand-Feld):
```tsx
<div className="flex flex-col gap-2">
  <label className="text-sm font-medium text-slate-600">Produktbild</label>
  {imagePreview && (
    <img
      src={imagePreview}
      alt=""
      className="w-24 h-24 object-cover rounded-xl border border-slate-200"
    />
  )}
  <label className="flex items-center justify-center h-12 border-2 border-dashed border-slate-200 rounded-xl cursor-pointer hover:border-sky-400 text-sm text-slate-500 transition-colors">
    {imagePreview ? 'Bild ändern' : 'Bild auswählen'}
    <input
      type="file"
      accept="image/jpeg,image/png,image/webp"
      className="sr-only"
      onChange={e => {
        const f = e.target.files?.[0];
        if (f) {
          setPendingImageFile(f);
          setImagePreview(URL.createObjectURL(f));
        }
      }}
    />
  </label>
  {imagePreview && (
    <button
      type="button"
      onPointerDown={() => {
        setImagePreview(null);
        setPendingImageFile(null);
      }}
      className="text-xs text-rose-500 hover:text-rose-700 text-left"
    >
      Bild entfernen
    </button>
  )}
</div>
```

**client/src/features/admin/products/ProductList.tsx:**
Den "Bild"-Button aus der Produktliste entfernen (die `<label>`-Element mit `type="file"` Input, Zeilen 247-255 in der aktuellen Version). Die `handleImageUpload`-Funktion kann ebenfalls entfernt werden, da sie jetzt in ProductForm lebt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript-Kompilierung ohne Fehler. ProductForm hat imagePreview-State und Bild-Upload-Bereich. ProductList hat keinen "Bild"-Button mehr. `grep -n "pendingImageFile" client/src/features/admin/products/ProductForm.tsx` gibt Treffer. `grep -n "Bild" client/src/features/admin/products/ProductList.tsx` gibt keinen Label-Button-Treffer mehr.
  </done>
</task>

<task type="auto">
  <name>Task 2: useSyncStatus-Hook + 30s-Retry in triggers.ts + Sync-Badge in POSScreen</name>
  <files>
    client/src/sync/useSyncStatus.ts,
    client/src/sync/triggers.ts,
    client/src/features/pos/POSScreen.tsx
  </files>
  <action>
**client/src/sync/useSyncStatus.ts:** Neue Datei erstellen:
```typescript
import { useLiveQuery } from 'dexie-react-hooks';
import { db, getShopId } from '../db/index.js';

export type SyncStatus = 'synced' | 'pending' | 'failed';

export function useSyncStatus(): SyncStatus {
  const outbox = useLiveQuery(
    () => {
      try {
        return db.outbox.where('shopId').equals(getShopId()).toArray();
      } catch {
        return Promise.resolve([]);
      }
    },
    []
  );
  if (!outbox || outbox.length === 0) return 'synced';
  if (outbox.some(e => (e.attempts ?? 0) >= 5)) return 'failed';
  return 'pending';
}

// Alle failed Einträge zurücksetzen (attempts auf 0) damit nächster flushOutbox() sie nochmal versucht
export async function resetFailedEntries(): Promise<void> {
  try {
    const shopId = getShopId();
    const failed = await db.outbox
      .where('shopId')
      .equals(shopId)
      .filter(e => (e.attempts ?? 0) >= 5)
      .toArray();
    for (const entry of failed) {
      await db.outbox.update(entry.id!, { attempts: 0 });
    }
  } catch {
    // Kein Shop gesetzt — ignorieren
  }
}
```

**client/src/sync/triggers.ts:** Periodischen Retry hinzufügen. Nach den bestehenden Event-Listenern (nach dem `visibilitychange`-Listener):
```typescript
// Periodischer Retry alle 30 Sekunden wenn online (SYN-01)
setInterval(() => {
  if (navigator.onLine) void flushOutbox();
}, 30_000);
```

**client/src/features/pos/POSScreen.tsx:** Sync-Status-Badge im POS-Header einfügen.

1. Imports ergänzen:
```typescript
import { useSyncStatus, resetFailedEntries } from '../../sync/useSyncStatus.js';
```

2. Hook aufrufen (im Funktionskörper von POSScreen, neben den bestehenden State-Deklarationen):
```typescript
const syncStatus = useSyncStatus();
```

3. Im POS-Header (`<div className="flex items-center gap-3">`), VOR dem Warenkorb-Button, Sync-Badge einfügen:
```tsx
{/* Sync-Status-Badge */}
{syncStatus !== 'synced' && (
  <button
    onPointerDown={() => {
      if (syncStatus === 'failed') {
        void resetFailedEntries().then(() => {
          // flushOutbox wird durch online-Event oder nächsten 30s-Tick getriggert
        });
      }
    }}
    title={
      syncStatus === 'failed'
        ? 'Sync fehlgeschlagen — antippen zum Wiederholen'
        : 'Synchronisierung läuft...'
    }
    className="flex items-center gap-1.5 px-2 py-1 rounded-lg text-xs font-medium transition-colors min-h-[44px]"
  >
    <span className={`w-2.5 h-2.5 rounded-full ${
      syncStatus === 'failed' ? 'bg-rose-400 animate-pulse' : 'bg-amber-300 animate-pulse'
    }`} />
    <span className={syncStatus === 'failed' ? 'text-rose-200' : 'text-sky-100'}>
      {syncStatus === 'failed' ? 'Sync fehlg.' : 'Syncing...'}
    </span>
  </button>
)}
```

Wenn `syncStatus === 'synced'`: kein Badge sichtbar (kein leeres Element, kein Platzhalter).

**Hinweis zu useEffect für Toast:** Kein separater Toast-useEffect nötig — der Badge im Header ist das primäre Feedback. Einfacher ist besser (per Einfachheits-Constraint aus CLAUDE.md).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    TypeScript-Kompilierung ohne Fehler. `client/src/sync/useSyncStatus.ts` existiert und exportiert `useSyncStatus` und `resetFailedEntries`. `grep -n "setInterval" client/src/sync/triggers.ts` zeigt den 30s-Retry. `grep -n "useSyncStatus" client/src/features/pos/POSScreen.tsx` zeigt den Hook-Aufruf und Badge.
  </done>
</task>

</tasks>

<verification>
- `grep -n "pendingImageFile" /Users/simonluthe/Documents/fairstand/client/src/features/admin/products/ProductForm.tsx` — zeigt State-Deklaration
- `grep -n "URL.createObjectURL" /Users/simonluthe/Documents/fairstand/client/src/features/admin/products/ProductForm.tsx` — zeigt Preview-Logik
- `grep -rn "Bild" /Users/simonluthe/Documents/fairstand/client/src/features/admin/products/ProductList.tsx | grep "label\|input\|file"` — keine Treffer mehr (Button entfernt)
- `ls /Users/simonluthe/Documents/fairstand/client/src/sync/useSyncStatus.ts` — Datei existiert
- `grep -n "setInterval" /Users/simonluthe/Documents/fairstand/client/src/sync/triggers.ts` — zeigt 30s-Retry
- `grep -n "syncStatus" /Users/simonluthe/Documents/fairstand/client/src/features/pos/POSScreen.tsx` — zeigt Badge
- Client TypeScript: `cd client && npx tsc --noEmit` ohne Fehler
</verification>

<success_criteria>
1. ProductForm hat Bild-Preview (URL.createObjectURL) und Upload-Bereich
2. Neues Produkt: Server-POST wird awaited wenn Bild vorhanden; Bild-Upload danach
3. Bestehendes Produkt: Bild kann im Bearbeiten-Formular geändert werden
4. "Bild"-Button in ProductList ist entfernt
5. useSyncStatus.ts existiert, exportiert SyncStatus-Typ, useSyncStatus() und resetFailedEntries()
6. triggers.ts hat 30s-Interval für periodischen Retry
7. POSScreen zeigt Sync-Badge wenn Status !== 'synced', mit Antippen-zum-Wiederholen bei 'failed'
8. Alle TypeScript-Kompilierungen ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/17-datenverwaltung-sync/17-02-SUMMARY.md` erstellen.
</output>
