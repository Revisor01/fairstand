---
phase: 27-preis-history-bestandsverlauf
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/db/schema.ts
  - server/migrations/ (generated by drizzle-kit)
autonomous: true
requirements:
  - PRICE-01
  - INV-04
must_haves:
  truths:
    - "Tabelle price_histories existiert in der DB mit Spalten id, shop_id, product_id, field, old_value, new_value, changed_at"
    - "Tabelle stock_movements existiert in der DB mit Spalten id, shop_id, product_id, type, quantity, reference_sale_id, reason, moved_at"
    - "Drizzle-Migration wurde generiert und kann via npm run migrate angewendet werden"
  artifacts:
    - path: "server/src/db/schema.ts"
      provides: "priceHistories + stockMovements Tabellen-Definitionen exportiert"
      contains: "export const priceHistories"
    - path: "server/migrations/0005_add_price_history.sql"
      provides: "CREATE TABLE price_histories Migration"
    - path: "server/migrations/0006_add_stock_movements.sql"
      provides: "CREATE TABLE stock_movements Migration"
  key_links:
    - from: "server/src/db/schema.ts"
      to: "server/src/routes/products.ts"
      via: "priceHistories Import in Logging-Routes"
      pattern: "import.*priceHistories.*from.*schema"
    - from: "server/src/db/schema.ts"
      to: "server/src/routes/sync.ts"
      via: "stockMovements Import in Sync-Routes"
      pattern: "import.*stockMovements.*from.*schema"
---

<objective>
Erweitert das Drizzle-Schema um zwei neue Audit-Tabellen (`price_histories`, `stock_movements`) und generiert die PostgreSQL-Migrationen. Diese Tabellen sind das Fundament für das Logging in den nachfolgenden Plänen.

Purpose: Ohne diese Tabellen können weder Preisänderungen noch Bestandsbewegungen protokolliert werden — alle nachfolgenden Pläne hängen von diesem Schema ab.
Output: Zwei neue Drizzle-Tabellen in schema.ts + zwei Migration-SQL-Dateien
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende schema.ts Exports — Executor braucht diese als Referenz für konsistente Patterns -->

Aus server/src/db/schema.ts (aktueller Stand, alle Tabellen):
```typescript
import { pgTable, text, integer, bigint, boolean, serial, jsonb, uniqueIndex } from 'drizzle-orm/pg-core';

// Bestehende Pattern: text-PKs für IDs, bigint für timestamps (mode: 'number'), integer für cents/stock
export const products = pgTable('products', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  purchasePrice: integer('purchase_price').notNull(),
  salePrice: integer('sale_price').notNull(),
  stock: integer('stock').notNull().default(0),
  updatedAt: bigint('updated_at', { mode: 'number' }).notNull(),
  // ... weitere Spalten
});

export const sales = pgTable('sales', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  createdAt: bigint('created_at', { mode: 'number' }).notNull(),
  // ...
});

export const outboxEvents = pgTable('outbox_events', {
  id: serial('id').primaryKey(),  // serial für auto-increment IDs
  shopId: text('shop_id').notNull(),
  // ...
});
```

Migrations-Zähler: Letzte Migration ist idx=4 (0004_settings_composite_unique). Nächste Migration wird 0005 sein.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Schema-Erweiterung — priceHistories + stockMovements Tabellen</name>
  <files>server/src/db/schema.ts</files>
  <read_first>
    - server/src/db/schema.ts — aktuellen Schema-Stand lesen, um konsistente Patterns zu verwenden
  </read_first>
  <action>
Füge zwei neue Tabellen am Ende von `server/src/db/schema.ts` hinzu:

**1. Tabelle `price_histories`:**
```typescript
export const priceHistories = pgTable('price_histories', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  field: text('field').notNull(), // 'purchase_price' | 'sale_price'
  oldValue: integer('old_value').notNull(),
  newValue: integer('new_value').notNull(),
  changedAt: bigint('changed_at', { mode: 'number' }).notNull(),
});
```

**2. Tabelle `stock_movements`:**
```typescript
export const stockMovements = pgTable('stock_movements', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  type: text('type').notNull(), // 'sale' | 'adjustment' | 'correction' | 'return' | 'hard_delete'
  quantity: integer('quantity').notNull(), // negativ = Ausgang, positiv = Eingang
  referenceSaleId: text('reference_sale_id'), // FK auf sales.id, nullable
  reason: text('reason'), // für STOCK_ADJUST, hard_delete etc., nullable
  movedAt: bigint('moved_at', { mode: 'number' }).notNull(),
});
```

Der `serial` Import ist bereits in schema.ts vorhanden — prüfen, ansonsten ergänzen. Beide Tabellen folgen exakt dem bestehenden Pattern: `serial` PK für auto-increment, `text` für IDs und Enums, `integer` für Cents/Mengen, `bigint` mit `mode: 'number'` für Timestamps.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && grep -n "export const priceHistories" src/db/schema.ts && grep -n "export const stockMovements" src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "export const priceHistories" server/src/db/schema.ts` findet genau einen Treffer
    - `grep "export const stockMovements" server/src/db/schema.ts` findet genau einen Treffer
    - `grep "referenceSaleId" server/src/db/schema.ts` findet einen Treffer (nullable Spalte vorhanden)
    - `grep "hard_delete" server/src/db/schema.ts` findet einen Treffer im Kommentar (Typ-Enum dokumentiert)
    - TypeScript-Kompilierung ohne Fehler: `cd server && npx tsc --noEmit` gibt keinen Fehler für schema.ts
  </acceptance_criteria>
  <done>Beide Tabellen in schema.ts exportiert, TypeScript kompiliert fehlerfrei</done>
</task>

<task type="auto">
  <name>Task 2: Drizzle-Migrationen generieren</name>
  <files>server/migrations/ (neue SQL-Dateien und meta/_journal.json Update)</files>
  <read_first>
    - server/drizzle.config.ts — Migration-Konfiguration lesen
    - server/migrations/meta/_journal.json — aktuellen Migrations-Index lesen (letzte idx=4)
    - server/package.json — npm scripts prüfen (generate, migrate)
  </read_first>
  <action>
Im Verzeichnis `server/` die Drizzle-Migrations generieren:

```bash
cd /Users/simonluthe/Documents/fairstand/server
npx drizzle-kit generate
```

Dieser Befehl liest `drizzle.config.ts`, vergleicht das aktuelle Schema mit dem letzten Migrations-Stand und erzeugt automatisch neue SQL-Dateien für `price_histories` und `stock_movements`.

**Erwartetes Ergebnis:**
- `server/migrations/0005_*.sql` — CREATE TABLE price_histories
- `server/migrations/0006_*.sql` — CREATE TABLE stock_movements
- `server/migrations/meta/_journal.json` — aktualisiert mit idx=5 und idx=6

**Falls drizzle-kit eine einzelne Datei für beide Tabellen generiert** (0005_*.sql mit beiden CREATE TABLEs), ist das auch korrekt — drizzle-kit kann beides zusammenfassen.

**Nicht manuell SQL schreiben** — drizzle-kit kennt den Schema-Diff und generiert korrekte SQL.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && ls migrations/*.sql | grep -E "000[56]" && cat migrations/meta/_journal.json | grep -c '"idx"'</automated>
  </verify>
  <acceptance_criteria>
    - Mindestens eine neue .sql-Datei mit Nummer 0005 oder höher existiert in `server/migrations/`
    - `cat server/migrations/meta/_journal.json | grep '"idx"'` zeigt mindestens 6 Einträge (idx 0-5 oder 0-6)
    - Die neue(n) SQL-Datei(en) enthalten `CREATE TABLE "price_histories"` (exakter String)
    - Die neue(n) SQL-Datei(en) enthalten `CREATE TABLE "stock_movements"` (exakter String)
    - `npx drizzle-kit generate` gibt keinen Fehler aus (Exit-Code 0)
  </acceptance_criteria>
  <done>Migrationen generiert, price_histories und stock_movements CREATE TABLE Statements in SQL-Dateien vorhanden</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

```bash
cd /Users/simonluthe/Documents/fairstand/server
# 1. Schema korrekt
grep -c "export const price" src/db/schema.ts  # erwartet: 1
grep -c "export const stock" src/db/schema.ts   # erwartet: 1 (stockMovements)

# 2. Migrationen vorhanden
ls migrations/000[56]*.sql

# 3. TypeScript kompiliert
npx tsc --noEmit 2>&1 | grep -c "error"  # erwartet: 0
```
</verification>

<success_criteria>
- Beide Tabellen (`price_histories`, `stock_movements`) in schema.ts mit korrekten Typen exportiert
- Drizzle-Migrationen generiert (SQL-Dateien + _journal.json aktualisiert)
- TypeScript kompiliert ohne Fehler
- Alle nachfolgenden Pläne können `priceHistories` und `stockMovements` aus schema.ts importieren
</success_criteria>

<output>
Nach Abschluss `.planning/phases/27-preis-history-bestandsverlauf/27-01-SUMMARY.md` erstellen mit:
- Exakten Namen der generierten Migration-Dateien
- Exportierten Tabellennamen (für Import in späteren Plänen)
- TypeScript-Kompilierungsstatus
</output>
