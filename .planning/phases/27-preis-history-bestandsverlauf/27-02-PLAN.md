---
phase: 27-preis-history-bestandsverlauf
plan: 02
type: execute
wave: 2
depends_on:
  - 27-01
files_modified:
  - server/src/routes/products.ts
  - server/src/routes/sales.ts
autonomous: true
requirements:
  - PRICE-01
must_haves:
  truths:
    - "Nach einer Preisänderung via POST /api/products enthält price_histories einen Eintrag mit altem Preis, neuem Preis, field und changedAt"
    - "Nach DELETE /api/sales/:id (Hard-Delete eines nicht-stornierten Verkaufs) enthält stock_movements für jeden restockten Artikel einen Eintrag mit type='hard_delete'"
  artifacts:
    - path: "server/src/routes/products.ts"
      provides: "price_history Logging in POST /products bei purchasePrice oder salePrice Änderung"
      contains: "priceHistories"
    - path: "server/src/routes/sales.ts"
      provides: "stock_movements Logging in DELETE /sales/:id für Restock"
      contains: "stockMovements"
  key_links:
    - from: "server/src/routes/products.ts"
      to: "price_histories Tabelle"
      via: "db.insert(priceHistories) innerhalb der onConflictDoUpdate-Transaktion"
      pattern: "insert.*priceHistories"
    - from: "server/src/routes/sales.ts"
      to: "stock_movements Tabelle"
      via: "db.insert(stockMovements) im DELETE /sales/:id Transaction-Block"
      pattern: "insert.*stockMovements"
---

<objective>
Ergänzt `products.ts` und `sales.ts` um App-Layer Logging: Jede Preisänderung über POST /products wird in `price_histories` protokolliert. Jeder Hard-Delete eines Verkaufs (mit Restock) wird in `stock_movements` protokolliert.

Purpose: PRICE-01 — jede EK/VK-Änderung muss automatisch geloggt werden. Der Hard-Delete-Endpoint in sales.ts ist eine bekannte Lücke (Research Pitfall 3), die hier geschlossen wird.
Output: Zwei modifizierte Route-Dateien mit Audit-Logging
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/27-preis-history-bestandsverlauf/27-01-SUMMARY.md

<interfaces>
<!-- Typen aus 27-01 — diese werden importiert -->

Aus server/src/db/schema.ts (nach 27-01):
```typescript
export const priceHistories = pgTable('price_histories', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  field: text('field').notNull(), // 'purchase_price' | 'sale_price'
  oldValue: integer('old_value').notNull(),
  newValue: integer('new_value').notNull(),
  changedAt: bigint('changed_at', { mode: 'number' }).notNull(),
});

export const stockMovements = pgTable('stock_movements', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  type: text('type').notNull(), // 'sale' | 'adjustment' | 'correction' | 'return' | 'hard_delete'
  quantity: integer('quantity').notNull(), // negativ = Ausgang, positiv = Eingang
  referenceSaleId: text('reference_sale_id'), // nullable
  reason: text('reason'), // nullable
  movedAt: bigint('moved_at', { mode: 'number' }).notNull(),
});
```

Bestehende products.ts Schlüssel-Stellen:
- Zeile 5: `import { products } from '../db/schema.js';` — hier `priceHistories` ergänzen
- Zeile 63-94: `db.insert(products).values(...).onConflictDoUpdate(...)` — LWW-Upsert ohne Transaktion
- Zeile 102-111: PATCH /products/:id/deactivate — kein Preis-Logging nötig (nur active-Flag)

Bestehende sales.ts Schlüssel-Stellen:
- Zeile 4: `import { sales, products } from '../db/schema.js';` — hier `stockMovements` ergänzen
- Zeile 66-81: `db.transaction(...)` mit Restock-Loop — hier stock_movements INSERT einfügen
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Preis-History Logging in products.ts (POST /products)</name>
  <files>server/src/routes/products.ts</files>
  <read_first>
    - server/src/routes/products.ts — vollständige Datei lesen, besonders POST /products (Zeilen 53-95)
    - server/src/db/schema.ts — prüfen dass priceHistories exportiert ist (aus 27-01)
  </read_first>
  <action>
**Import erweitern** (Zeile 5):
```typescript
import { products, priceHistories } from '../db/schema.js';
```

**POST /products umschreiben** — den bestehenden `db.insert(...).onConflictDoUpdate(...)` Block in eine Transaktion einwickeln und davor Preis-Logging einfügen:

Ersetze den Block von `await db.insert(products).values({...}).onConflictDoUpdate({...});` mit:

```typescript
await db.transaction(async (tx) => {
  // Bestehenden Preis lesen für Logging (vor dem Upsert)
  const [existing] = await tx.select({
    purchasePrice: products.purchasePrice,
    salePrice: products.salePrice,
  }).from(products).where(eq(products.id, p.id)).limit(1);

  // Preis-History Logging — nur wenn Produkt existiert UND Preis sich ändert
  if (existing) {
    if (existing.purchasePrice !== p.purchasePrice) {
      await tx.insert(priceHistories).values({
        shopId: p.shopId,
        productId: p.id,
        field: 'purchase_price',
        oldValue: existing.purchasePrice,
        newValue: p.purchasePrice,
        changedAt: Date.now(),
      });
    }
    if (existing.salePrice !== p.salePrice) {
      await tx.insert(priceHistories).values({
        shopId: p.shopId,
        productId: p.id,
        field: 'sale_price',
        oldValue: existing.salePrice,
        newValue: p.salePrice,
        changedAt: Date.now(),
      });
    }
  }

  // LWW-Upsert — unverändert aus dem bestehenden Code
  await tx.insert(products).values({
    id: p.id,
    shopId: p.shopId,
    articleNumber: p.articleNumber,
    name: p.name,
    category: p.category,
    purchasePrice: p.purchasePrice,
    salePrice: p.salePrice,
    vatRate: p.vatRate,
    stock: p.stock,
    minStock: p.minStock,
    active: p.active,
    imageUrl: p.imageUrl ?? null,
    updatedAt: p.updatedAt,
  }).onConflictDoUpdate({
    target: products.id,
    set: {
      articleNumber: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.article_number ELSE ${products.articleNumber} END`,
      name: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.name ELSE ${products.name} END`,
      category: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.category ELSE ${products.category} END`,
      purchasePrice: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.purchase_price ELSE ${products.purchasePrice} END`,
      salePrice: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.sale_price ELSE ${products.salePrice} END`,
      vatRate: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.vat_rate ELSE ${products.vatRate} END`,
      stock: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.stock ELSE ${products.stock} END`,
      minStock: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.min_stock ELSE ${products.minStock} END`,
      active: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.active ELSE ${products.active} END`,
      imageUrl: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.image_url ELSE ${products.imageUrl} END`,
      updatedAt: sql`CASE WHEN excluded.updated_at > ${products.updatedAt} THEN excluded.updated_at ELSE ${products.updatedAt} END`,
    },
  });
});
```

**Wichtig:** Logging nur wenn `existing` vorhanden (neues Produkt = kein Preis-Log, da kein "alter Preis"). Der LWW-Upsert-Inhalt bleibt 1:1 erhalten — nur in `tx.insert/update` umbenannt.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && grep -n "priceHistories" src/routes/products.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "priceHistories" server/src/routes/products.ts` findet mindestens 2 Treffer (Import + insert)
    - `grep "db.transaction" server/src/routes/products.ts` findet genau 1 Treffer im POST /products Handler
    - `grep "field: 'purchase_price'" server/src/routes/products.ts` findet genau 1 Treffer
    - `grep "field: 'sale_price'" server/src/routes/products.ts` findet genau 1 Treffer
    - `npx tsc --noEmit` in `server/` gibt 0 Fehler aus
  </acceptance_criteria>
  <done>POST /products loggt Preisänderungen atomar in price_histories, neues Produkt erzeugt keinen Log-Eintrag</done>
</task>

<task type="auto">
  <name>Task 2: Stock-Movement Logging in sales.ts (DELETE /sales/:id)</name>
  <files>server/src/routes/sales.ts</files>
  <read_first>
    - server/src/routes/sales.ts — vollständige Datei lesen, besonders DELETE /sales/:id (Zeilen 50-88)
    - server/src/db/schema.ts — prüfen dass stockMovements exportiert ist (aus 27-01)
  </read_first>
  <action>
**Import erweitern** (Zeile 4):
```typescript
import { sales, products, stockMovements } from '../db/schema.js';
```

**DELETE /sales/:id Transaktion erweitern** — innerhalb des bestehenden `db.transaction(async (tx) => {...})` Blocks, nach dem Restock-Loop und VOR dem `tx.delete(sales)`, einen stock_movements INSERT einfügen:

```typescript
await db.transaction(async (tx) => {
  // If sale is not cancelled, restock products
  if (!sale.cancelledAt) {
    for (const item of saleItems) {
      const [prod] = await tx.select().from(products).where(eq(products.id, item.productId)).limit(1);
      if (!prod || prod.shopId !== shopId) continue;
      await tx.update(products)
        .set({ stock: sql`${products.stock} + ${Number(item.quantity)}` })
        .where(eq(products.id, item.productId));
      // Stock-Movement für Hard-Delete-Restock protokollieren
      await tx.insert(stockMovements).values({
        shopId,
        productId: item.productId,
        type: 'hard_delete',
        quantity: Number(item.quantity), // positiv = Eingang (Restock)
        referenceSaleId: id,
        reason: 'Hard-Delete: Verkauf vollständig gelöscht',
        movedAt: Date.now(),
      });
    }
    stockAdjusted = true;
  }

  // Delete the sale
  await tx.delete(sales).where(eq(sales.id, id));
});
```

**Erläuterung:** `quantity` ist positiv weil der Bestand durch den Hard-Delete zurückgebucht wird (Eingang). `referenceSaleId` referenziert den gelöschten Verkauf — damit ist der Bewegungsgrund nachvollziehbar auch wenn die Sale-Zeile danach weg ist.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand/server && grep -n "stockMovements" src/routes/sales.ts && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "stockMovements" server/src/routes/sales.ts` findet mindestens 2 Treffer (Import + insert)
    - `grep "hard_delete" server/src/routes/sales.ts` findet genau 1 Treffer (type: 'hard_delete')
    - `grep "referenceSaleId: id" server/src/routes/sales.ts` findet genau 1 Treffer
    - Der INSERT steht INNERHALB des if (!sale.cancelledAt) Blocks — kein Restock-Log für bereits stornierte Sales
    - `npx tsc --noEmit` in `server/` gibt 0 Fehler aus
  </acceptance_criteria>
  <done>DELETE /sales/:id loggt Restock-Bewegungen mit type='hard_delete' in stock_movements, nur wenn Verkauf nicht bereits storniert war</done>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:

```bash
cd /Users/simonluthe/Documents/fairstand/server

# 1. Logging vorhanden
grep -c "priceHistories" src/routes/products.ts  # >= 2
grep -c "stockMovements" src/routes/sales.ts      # >= 2

# 2. TypeScript fehlerfrei
npx tsc --noEmit 2>&1 | grep -c "error TS"  # erwartet: 0

# 3. Strukturprüfung
grep "db.transaction" src/routes/products.ts     # 1 Treffer (POST Handler)
grep "hard_delete" src/routes/sales.ts           # 1 Treffer
```
</verification>

<success_criteria>
- POST /products loggt EK/VK-Änderungen atomar in price_histories (nur bei existierenden Produkten, nur bei tatsächlicher Änderung)
- DELETE /sales/:id loggt Restock als stock_movements Einträge mit type='hard_delete' (nur wenn Verkauf nicht storniert)
- TypeScript kompiliert fehlerfrei
- Kein Eingriff in andere Route-Handler (PATCH /deactivate, PATCH /activate, POST /image bleiben unverändert)
</success_criteria>

<output>
Nach Abschluss `.planning/phases/27-preis-history-bestandsverlauf/27-02-SUMMARY.md` erstellen mit:
- Geänderten Dateien und Art der Änderung
- Logging-Pattern (wann wird geloggt, wann nicht)
- TypeScript-Status
</output>
