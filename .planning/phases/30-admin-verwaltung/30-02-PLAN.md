---
phase: 30-admin-verwaltung
plan: 02
type: execute
wave: 2
depends_on:
  - 30-01
files_modified:
  - client/src/features/admin/AdminScreen.tsx
  - client/src/features/admin/reports/MonthlyReport.tsx
  - client/src/features/admin/reports/InventurTab.tsx
  - client/src/features/admin/products/ProductList.tsx
  - client/src/features/admin/products/StockAdjustModal.tsx
  - client/src/hooks/api/useProducts.ts
autonomous: true
requirements:
  - ADMIN-01
  - ADMIN-02
  - ADMIN-03
must_haves:
  truths:
    - "AdminScreen zeigt Tab 'Inventur' nach Tab 'Berichte'"
    - "Inventur-Tab zeigt dieselbe Tabelle und Export-Buttons wie bisher in MonthlyReport"
    - "MonthlyReport zeigt keinen Inventur-Tab mehr — nur noch Monat und Jahr"
    - "Jede Produktzeile in ProductList hat einen roten Löschen-Button"
    - "Löschen-Button zeigt Bestätigungsdialog mit Artikelname und Warnung"
    - "Nach erfolgreichem Löschen verschwindet das Produkt aus der Liste"
    - "Fehler 409 vom Server zeigt Toast-ähnliche Fehlermeldung in ProductList"
    - "StockAdjustModal zeigt 'Bestand anpassen' statt 'Bestandskorrektur'"
  artifacts:
    - path: "client/src/features/admin/reports/InventurTab.tsx"
      provides: "Extrahierte Inventur-Komponente"
      contains: "export function InventurTab"
    - path: "client/src/hooks/api/useProducts.ts"
      provides: "useDeleteProduct Mutation"
      contains: "useDeleteProduct"
    - path: "client/src/features/admin/AdminScreen.tsx"
      provides: "Inventur-Tab in Navigation"
      contains: "'inventur'"
  key_links:
    - from: "client/src/features/admin/AdminScreen.tsx"
      to: "client/src/features/admin/reports/InventurTab.tsx"
      via: "import + {tab === 'inventur' && <InventurTab />}"
      pattern: "InventurTab"
    - from: "client/src/features/admin/products/ProductList.tsx"
      to: "client/src/hooks/api/useProducts.ts"
      via: "useDeleteProduct().mutateAsync(productId)"
      pattern: "useDeleteProduct"
---

<objective>
Frontend-Änderungen für alle drei ADMIN-Anforderungen: Inventur als eigener Tab, Artikel-Löschen mit Bestätigung, Umbenennung "Bestandskorrektur" → "Bestand anpassen".

Purpose: Mitarbeiterinnen finden Inventur direkt ohne Umweg über Berichte, können falsch angelegte Artikel löschen, und lesen korrekte deutsche Bezeichnungen.
Output: InventurTab.tsx (extrahiert), AdminScreen mit neuem Tab, ProductList mit Delete-Button, StockAdjustModal umbenannt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/30-admin-verwaltung/30-01-SUMMARY.md

<interfaces>
<!-- Extrahiert aus AdminScreen.tsx -->

AdminTab Type (Zeile 13):
```typescript
type AdminTab = 'products' | 'reports' | 'settings' | 'import' | 'categories' | 'shops';
```

tabs-Array (Zeile 19-25):
```typescript
const tabs: { key: AdminTab; label: string; icon: typeof Package }[] = [
  { key: 'products', label: 'Produkte', icon: Package },
  { key: 'reports', label: 'Berichte', icon: BarChart3 },
  { key: 'import', label: 'Import', icon: Upload },
  { key: 'categories', label: 'Kategorien', icon: Tags },
  { key: 'settings', label: 'Einstellungen', icon: Settings },
];
```

Tab-Rendering-Muster im JSX:
```typescript
{tab === 'reports' && (
  <>
    <DailyReport />
    <hr className="my-6 border-slate-200" />
    <MonthlyReport />
  </>
)}
```

<!-- Extrahiert aus useProducts.ts — bestehende Mutation als Vorlage -->

useToggleProductActive Muster:
```typescript
export function useToggleProductActive() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async ({ productId, active }: { productId: string; active: boolean }) => {
      const action = active ? 'activate' : 'deactivate';
      const res = await authFetch(`/api/products/${productId}/${action}`, { method: 'PATCH' });
      if (!res.ok) throw new Error('Status konnte nicht geändert werden');
      return { productId, active };
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['products', getShopId()] });
    },
  });
}
```

<!-- Inventur-Logik aus MonthlyReport.tsx — zu extrahieren -->

Interfaces (Zeile 56-76):
```typescript
interface EkBreakdownEntry { ek_cents: number; qty: number; }
interface InventoryItem {
  id: string; article_number: string; name: string;
  current_stock: number; current_ek_cents: number;
  sold_qty: number; revenue_cents: number; cost_cents: number;
  ek_breakdown: EkBreakdownEntry[];
}
interface InventoryResponse { year: number; items: InventoryItem[]; total_stock_value_cents: number; }
```

API-Endpunkte (Zeile 131, 141, 157):
- `GET /api/reports/inventory?year={year}` → InventoryResponse
- `GET /api/reports/inventory-csv?year={year}` → CSV-Blob
- `GET /api/reports/inventory-pdf?year={year}` → PDF-Blob
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: InventurTab.tsx extrahieren + MonthlyReport bereinigen</name>
  <files>
    client/src/features/admin/reports/InventurTab.tsx
    client/src/features/admin/reports/MonthlyReport.tsx
  </files>
  <read_first>
    - client/src/features/admin/reports/MonthlyReport.tsx (vollständige Datei — Inventur-Logik ab Zeile 88 bis Ende)
    - client/src/features/pos/utils.js (formatEur Import-Pfad)
    - client/src/features/auth/serverAuth.js (authFetch Import-Pfad)
    - client/src/db/index.js (getShopId Import-Pfad)
  </read_first>
  <action>
**Schritt 1: Neue Datei `InventurTab.tsx` erstellen**

Extrahiere die gesamte Inventur-Logik aus MonthlyReport. Die Komponente bekommt `year: number` als Prop (Jahresfilter kommt von außen):

```typescript
import React, { useState, useEffect } from 'react';
import { authFetch } from '../../auth/serverAuth.js';
import { getShopId } from '../../../db/index.js';
import { formatEur } from '../../pos/utils.js';

interface EkBreakdownEntry {
  ek_cents: number;
  qty: number;
}

interface InventoryItem {
  id: string;
  article_number: string;
  name: string;
  current_stock: number;
  current_ek_cents: number;
  sold_qty: number;
  revenue_cents: number;
  cost_cents: number;
  ek_breakdown: EkBreakdownEntry[];
}

interface InventoryResponse {
  year: number;
  items: InventoryItem[];
  total_stock_value_cents: number;
}

interface InventurTabProps {
  year: number;
}

export function InventurTab({ year }: InventurTabProps) {
  // State: inventoryData, loadingInventory, expandedProducts, downloadingInventurCsv, downloadingInventurPdf
  // — exakt übernehmen aus MonthlyReport (Zeilen 89-93)

  // useEffect: authFetch /api/reports/inventory?year={year} bei year-Änderung
  // — exakt übernehmen aus MonthlyReport (Zeilen 127-136), OHNE activeTab-Check

  // handleInventurCsvDownload und handleInventurPdfDownload
  // — exakt übernehmen aus MonthlyReport (Zeilen 138-174)

  // JSX: der "Inventur-Tab"-Block aus MonthlyReport (Zeilen 321-440)
  // — als top-level return, kein {activeTab === 'inventur' && (...)} wrapper mehr

  // WICHTIG: getShopId() im API-Call NICHT vergessen (bestehender Code hat keinen shopId-Param,
  // aber prüfe ob /api/reports/inventory den shopId aus der Session bezieht — wenn ja, kein Param nötig)
}
```

Implementiere die vollständige Komponente mit allen State-Variablen, Effects und dem vollständigen JSX aus MonthlyReport (Inventur-Tab-Block).

**Schritt 2: MonthlyReport.tsx bereinigen**

Entferne aus MonthlyReport.tsx:
1. State-Variablen: `inventoryData`, `loadingInventory`, `expandedProducts`, `downloadingInventurCsv`, `downloadingInventurPdf`
2. useEffect für Inventur-Laden (Zeilen 127-136)
3. Funktionen `handleInventurCsvDownload` und `handleInventurPdfDownload`
4. Interfaces: `EkBreakdownEntry`, `InventoryItem`, `InventoryResponse` (da sie in InventurTab.tsx leben)
5. Tab-Option `{ key: 'inventur', label: 'Inventur' }` aus dem Tab-Array (Zeilen 219-221)
6. Den `{activeTab === 'inventur' && (...)}` Block (Zeilen 321-440)
7. Den `useState`-Typ für `activeTab`: von `'monat' | 'jahr' | 'inventur'` zu `'monat' | 'jahr'`

Das Jahresfilter-Dropdown und den Monatsfilter-Dropdown in MonthlyReport beibehalten — sie werden weiterhin für Monat/Jahr-Tab gebraucht.
  </action>
  <verify>
    <automated>grep -n "export function InventurTab" /Users/simonluthe/Documents/fairstand/client/src/features/admin/reports/InventurTab.tsx && grep -n "inventur" /Users/simonluthe/Documents/fairstand/client/src/features/admin/reports/MonthlyReport.tsx | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - `InventurTab.tsx` existiert und enthält `export function InventurTab`
    - `InventurTab.tsx` enthält `authFetch('/api/reports/inventory`
    - `InventurTab.tsx` enthält `handleInventurCsvDownload` und `handleInventurPdfDownload`
    - `MonthlyReport.tsx` enthält NICHT mehr `activeTab === 'inventur'`
    - `MonthlyReport.tsx` enthält NICHT mehr `inventoryData`
    - `MonthlyReport.tsx` enthält `'monat' | 'jahr'` als activeTab-Typ (kein 'inventur')
  </acceptance_criteria>
  <done>
    InventurTab.tsx vollständig mit der extrahierten Inventur-Logik. MonthlyReport.tsx ohne Inventur — nur noch Monat/Jahr-Tabs.
  </done>
</task>

<task type="auto">
  <name>Task 2: AdminScreen Inventur-Tab + ProductList Löschen + StockAdjustModal Umbenennung</name>
  <files>
    client/src/features/admin/AdminScreen.tsx
    client/src/features/admin/products/ProductList.tsx
    client/src/features/admin/products/StockAdjustModal.tsx
    client/src/hooks/api/useProducts.ts
  </files>
  <read_first>
    - client/src/features/admin/AdminScreen.tsx (vollständige Datei — Tab-System, Zeilen 1-129)
    - client/src/features/admin/products/ProductList.tsx (vollständige Datei — Aktionsbutton-Bereich, Zeilen 189-223)
    - client/src/features/admin/products/StockAdjustModal.tsx (Zeile 49 und 120 — "Bestandskorrektur")
    - client/src/hooks/api/useProducts.ts (vollständige Datei — Mutations-Muster)
  </read_first>
  <action>
**1. AdminScreen.tsx — Inventur-Tab hinzufügen (per D-01: ADMIN-01)**

Import ergänzen:
```typescript
import { InventurTab } from './reports/InventurTab.js';
import { ClipboardList } from 'lucide-react';
```

AdminTab-Type erweitern:
```typescript
type AdminTab = 'products' | 'reports' | 'inventur' | 'settings' | 'import' | 'categories' | 'shops';
```

tabs-Array: Neuen Eintrag NACH 'reports' einfügen:
```typescript
{ key: 'reports', label: 'Berichte', icon: BarChart3 },
{ key: 'inventur', label: 'Inventur', icon: ClipboardList },  // NEU
```

Jahresfilter-State für InventurTab — direkt in AdminScreen verwalten:
```typescript
const currentYear = new Date().getFullYear();
const [inventurYear, setInventurYear] = useState(currentYear);
const yearOptions = [currentYear - 2, currentYear - 1, currentYear, currentYear + 1, currentYear + 2];
```

Im JSX-Block für Tab-Inhalt ergänzen:
```typescript
{tab === 'inventur' && (
  <div className="flex flex-col gap-4">
    <div className="flex items-center justify-between flex-wrap gap-2">
      <h2 className="text-lg font-semibold text-sky-800">Inventur</h2>
      <select
        value={inventurYear}
        onChange={e => setInventurYear(Number(e.target.value))}
        className="h-11 border border-slate-200 rounded-lg px-3 text-sm focus:outline-none focus:border-sky-400 bg-white"
      >
        {yearOptions.map(y => (
          <option key={y} value={y}>{y}</option>
        ))}
      </select>
    </div>
    <InventurTab year={inventurYear} />
  </div>
)}
```

**2. useProducts.ts — useDeleteProduct Mutation hinzufügen (per D-01: ADMIN-02)**

Am Ende der Datei einfügen:
```typescript
export function useDeleteProduct() {
  const queryClient = useQueryClient();
  return useMutation({
    mutationFn: async (productId: string) => {
      const res = await authFetch(`/api/products/${productId}`, {
        method: 'DELETE',
      });
      if (res.status === 409) {
        const data = await res.json() as { error: string };
        throw new Error(data.error ?? 'Artikel kann nicht gelöscht werden.');
      }
      if (!res.ok) throw new Error('Artikel konnte nicht gelöscht werden.');
    },
    onSuccess: () => {
      queryClient.invalidateQueries({ queryKey: ['products', getShopId()] });
    },
  });
}
```

**3. ProductList.tsx — Löschen-Button mit Bestätigungsdialog (per D-01: ADMIN-02)**

Import ergänzen:
```typescript
import { Pencil, BarChart3, Package, Eye, EyeOff, Plus, Trash2 } from 'lucide-react';
import { useProducts, useToggleProductActive, useDeleteProduct } from '../../../hooks/api/useProducts.js';
```

State für Bestätigungsdialog hinzufügen (in ProductList-Funktion):
```typescript
const [deleteTarget, setDeleteTarget] = useState<Product | null>(null);
const [deleteError, setDeleteError] = useState<string | null>(null);
const [deleting, setDeleting] = useState(false);
const deleteProduct = useDeleteProduct();
```

Lösch-Handler:
```typescript
async function handleDeleteConfirm() {
  if (!deleteTarget) return;
  setDeleting(true);
  setDeleteError(null);
  try {
    await deleteProduct.mutateAsync(deleteTarget.id);
    setDeleteTarget(null);
  } catch (err) {
    setDeleteError(err instanceof Error ? err.message : 'Fehler beim Löschen.');
  } finally {
    setDeleting(false);
  }
}
```

Löschen-Button in der Aktionsbutton-Zeile (NACH dem Deaktivieren/Aktivieren-Button):
```typescript
<button
  onPointerDown={() => { setDeleteTarget(product); setDeleteError(null); }}
  title="Löschen"
  className="bg-red-100 active:bg-red-300 text-red-700 min-w-[44px] min-h-[44px] flex items-center justify-center rounded-lg transition-colors"
>
  <Trash2 size={18} />
</button>
```

Bestätigungsdialog — als Overlay über der gesamten Produktliste, NACH dem `<div className="flex flex-col gap-2">` Block (als Geschwister-Element, nicht Kind):
```typescript
{deleteTarget && (
  <div className="fixed inset-0 z-50 flex items-center justify-center bg-black/40 p-4">
    <div className="bg-white rounded-2xl shadow-xl p-6 max-w-sm w-full flex flex-col gap-4">
      <h3 className="text-lg font-bold text-slate-800">Artikel löschen?</h3>
      <p className="text-sm text-slate-600">
        <span className="font-semibold">{deleteTarget.name}</span> wird unwiderruflich gelöscht.
        Dieser Vorgang kann nicht rückgängig gemacht werden.
      </p>
      <p className="text-xs text-slate-400">
        Artikel mit Verkaufshistorie können nicht gelöscht werden.
      </p>
      {deleteError && (
        <div className="bg-rose-50 text-rose-700 px-4 py-3 rounded-xl text-sm">
          {deleteError}
        </div>
      )}
      <div className="flex gap-3">
        <button
          onPointerDown={() => { setDeleteTarget(null); setDeleteError(null); }}
          disabled={deleting}
          className="flex-1 bg-slate-100 active:bg-slate-200 text-slate-700 font-semibold py-3 rounded-xl min-h-[48px] transition-colors"
        >
          Abbrechen
        </button>
        <button
          onPointerDown={handleDeleteConfirm}
          disabled={deleting}
          className="flex-1 bg-red-500 active:bg-red-700 disabled:opacity-50 text-white font-semibold py-3 rounded-xl min-h-[48px] transition-colors"
        >
          {deleting ? 'Löschen...' : 'Unwiderruflich löschen'}
        </button>
      </div>
    </div>
  </div>
)}
```

**4. StockAdjustModal.tsx — "Bestandskorrektur" → "Bestand anpassen" (per D-01: ADMIN-03)**

Ändere an zwei Stellen:
- Zeile 49: `<h2 className="text-lg font-semibold text-sky-800">Bestandskorrektur</h2>` → `Bestand anpassen`
- Zeile 120 (Button-Text): `'Bestand korrigieren'` → `'Bestand anpassen'`
  </action>
  <verify>
    <automated>grep -n "'inventur'" /Users/simonluthe/Documents/fairstand/client/src/features/admin/AdminScreen.tsx && grep -n "useDeleteProduct" /Users/simonluthe/Documents/fairstand/client/src/hooks/api/useProducts.ts && grep -n "Bestand anpassen" /Users/simonluthe/Documents/fairstand/client/src/features/admin/products/StockAdjustModal.tsx && grep -n "Trash2" /Users/simonluthe/Documents/fairstand/client/src/features/admin/products/ProductList.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `AdminScreen.tsx` enthält `'inventur'` im AdminTab-Type und im tabs-Array
    - `AdminScreen.tsx` enthält `<InventurTab year={inventurYear} />`
    - `AdminScreen.tsx` enthält `import { InventurTab }` von InventurTab.js
    - `useProducts.ts` enthält `export function useDeleteProduct`
    - `useProducts.ts` enthält `authFetch` mit `method: 'DELETE'` und `/api/products/${productId}`
    - `ProductList.tsx` enthält `Trash2` import und `deleteTarget` State
    - `ProductList.tsx` enthält `fixed inset-0 z-50` (Bestätigungsdialog)
    - `StockAdjustModal.tsx` enthält NICHT mehr "Bestandskorrektur" (grep gibt 0 Treffer)
    - `StockAdjustModal.tsx` enthält "Bestand anpassen" (mindestens 2 Treffer: Heading + Button)
  </acceptance_criteria>
  <done>
    - Inventur-Tab in AdminScreen navigierbar mit Jahresfilter
    - Löschen-Button in jeder Produktzeile mit Bestätigungsdialog
    - 409-Fehler vom Server erscheint als Fehlermeldung im Bestätigungsdialog
    - StockAdjustModal zeigt "Bestand anpassen" in Heading und Button
  </done>
</task>

</tasks>

<verification>
TypeScript-Check für Client:
```bash
cd /Users/simonluthe/Documents/fairstand/client && npx tsc --noEmit
```

Manuelle Funktions-Checks (nach lokalem Start):
1. AdminScreen öffnen → Tab "Inventur" erscheint nach "Berichte"
2. Inventur-Tab zeigt Tabelle mit Jahresfilter und Export-Buttons
3. Berichte-Tab zeigt nur noch Monat/Jahr (kein Inventur-Sub-Tab mehr)
4. Produktliste: roter Trash-Button an jeder Zeile
5. Trash-Button tippen → Bestätigungsdialog mit Artikelname erscheint
6. Abbrechen → Dialog schließt
7. Produkt öffnen → Bestand anpassen → Heading und Button-Text korrekt
</verification>

<success_criteria>
- ADMIN-01: `InventurTab.tsx` existiert, AdminScreen hat Tab 'inventur', MonthlyReport hat keinen Inventur-Tab mehr
- ADMIN-02: `useDeleteProduct` in useProducts.ts, Trash2-Button in ProductList.tsx, Bestätigungsdialog mit 409-Fehlerbehandlung
- ADMIN-03: "Bestandskorrektur" kommt in StockAdjustModal.tsx nicht mehr vor (grep liefert nichts)
- TypeScript compiliert ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/30-admin-verwaltung/30-02-SUMMARY.md` erstellen.
</output>
