---
phase: 30-admin-verwaltung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/products.ts
  - server/src/db/migrations/0006_add_article_number_unique.sql
  - server/src/db/schema.ts
autonomous: true
requirements:
  - ADMIN-02
must_haves:
  truths:
    - "DELETE /api/products/:id gibt 200 zurück wenn kein Verkauf existiert"
    - "DELETE /api/products/:id gibt 409 zurück wenn Verkaufshistorie vorhanden ist"
    - "DELETE /api/products/:id gibt 403 zurück wenn Produkt zu anderem Shop gehört"
    - "Artikelnummer ist pro Shop unique — doppeltes Anlegen gibt 409"
  artifacts:
    - path: "server/src/routes/products.ts"
      provides: "DELETE-Endpoint + Unique-Check-Fehlerbehandlung"
      contains: "DELETE /products/:id"
    - path: "server/src/db/migrations/0006_add_article_number_unique.sql"
      provides: "Unique-Constraint article_number+shop_id"
      contains: "CREATE UNIQUE INDEX"
  key_links:
    - from: "server/src/routes/products.ts"
      to: "server/src/db/schema.ts (sales-Tabelle)"
      via: "db.select().from(sales).where(eq(sales.shopId, ...) + JSON-contains-Prüfung"
      pattern: "sales.*shopId"
    - from: "server/src/routes/products.ts"
      to: "server/src/db/schema.ts (products-Tabelle)"
      via: "db.delete(products).where(eq(products.id, id))"
      pattern: "db.delete"
---

<objective>
Backend-Voraussetzungen für Artikel-Löschen schaffen: DELETE-Endpoint mit Verkaufshistorie-Check und Unique-Constraint auf Artikelnummern.

Purpose: Plan 02 (Frontend) kann den Endpoint aufrufen. Ohne diese Grundlage ist die Delete-Schaltfläche im UI wertlos.
Output: Neuer DELETE /api/products/:id Endpoint, Migration für Unique-Constraint, POST /api/products gibt 409 bei Duplikat-Artikelnummer.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende Muster aus server/src/routes/products.ts -->

Ownership-Check-Muster (von PATCH /products/:id/deactivate):
```typescript
const [product] = await db.select().from(products).where(eq(products.id, id)).limit(1);
if (!product) return reply.status(404).send({ error: 'Produkt nicht gefunden' });
if (product.shopId !== session.shopId) {
  return reply.status(403).send({ error: 'Zugriff verweigert' });
}
```

Broadcast-Muster nach Mutation:
```typescript
reply.send({ ok: true });
broadcast({ type: 'products_changed', shopId: session.shopId });
```

DB-Schema sales-Tabelle (schema.ts):
```typescript
export const sales = pgTable('sales', {
  id: text('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  items: jsonb('items').notNull(),          // Array mit {productId, ...}
  cancelledAt: bigint('cancelled_at', { mode: 'number' }),
  ...
});
```

products-Tabelle hat KEINEN unique-Constraint auf article_number+shop_id (aktuell).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Migration — Unique-Constraint article_number+shop_id</name>
  <files>server/src/db/migrations/0006_add_article_number_unique.sql</files>
  <read_first>
    - server/src/db/migrations/0005_add_categories.sql (Migrations-Format verstehen)
    - server/src/db/schema.ts (products-Tabelle, aktuelle Constraints)
    - server/src/db/index.ts (Migrations-Ausführung verstehen)
  </read_first>
  <action>
Erstelle Migration `0006_add_article_number_unique.sql` mit folgendem exakten Inhalt:

```sql
-- Migration 0006: Unique-Constraint für Artikelnummer pro Shop
-- Verhindert Duplikate beim Anlegen von Artikeln mit derselben Artikelnummer im gleichen Shop
CREATE UNIQUE INDEX IF NOT EXISTS products_article_number_shop_id_idx
  ON products (article_number, shop_id);
```

Aktualisiere anschließend `server/src/db/schema.ts` — die `products`-Tabelle bekommt einen uniqueIndex:

1. Import ergänzen: `uniqueIndex` zu den pg-core-Imports hinzufügen (bereits importiert in settings-Tabelle — prüfen)
2. In der `products`-Tabellendefinition einen zweiten Parameter hinzufügen:

```typescript
export const products = pgTable('products', {
  // ... bestehende Felder unverändert ...
}, (table) => ({
  articleNumberShopIdx: uniqueIndex('products_article_number_shop_id_idx').on(table.articleNumber, table.shopId),
}));
```

WICHTIG: Der Index-Name `products_article_number_shop_id_idx` muss in Migration und Schema identisch sein.
  </action>
  <verify>
    <automated>grep -n "products_article_number_shop_id_idx" /Users/simonluthe/Documents/fairstand/server/src/db/migrations/0006_add_article_number_unique.sql && grep -n "articleNumberShopIdx" /Users/simonluthe/Documents/fairstand/server/src/db/schema.ts</automated>
  </verify>
  <done>
    - Datei `0006_add_article_number_unique.sql` existiert mit `CREATE UNIQUE INDEX IF NOT EXISTS products_article_number_shop_id_idx`
    - `schema.ts` enthält `articleNumberShopIdx: uniqueIndex('products_article_number_shop_id_idx')`
  </done>
</task>

<task type="auto">
  <name>Task 2: DELETE /api/products/:id Endpoint mit Verkaufshistorie-Check</name>
  <files>server/src/routes/products.ts</files>
  <read_first>
    - server/src/routes/products.ts (vollständige Datei — bestehende Muster für Ownership-Check, Broadcast)
    - server/src/db/schema.ts (sales-Tabelle, items-Struktur: jsonb Array mit {productId, ...})
  </read_first>
  <action>
Füge in `server/src/routes/products.ts` folgende Änderungen durch:

**1. Import ergänzen** (oben bei den drizzle-Imports):
```typescript
import { eq, sql, and } from 'drizzle-orm';
```
(Falls `and` noch nicht importiert ist — `sql` ist bereits vorhanden)

Außerdem `sales` aus schema importieren:
```typescript
import { products, priceHistories, sales } from '../db/schema.js';
```

**2. Neuen DELETE-Endpoint** am Ende der `productRoutes`-Funktion einfügen, vor der schließenden Klammer:

```typescript
// DELETE /products/:id — Hartes Löschen (nur ohne Verkaufshistorie möglich)
fastify.delete('/products/:id', async (request, reply) => {
  const { id } = request.params as { id: string };
  const session = (request as any).session as { shopId: string };

  // Ownership-Check
  const [product] = await db.select().from(products).where(eq(products.id, id)).limit(1);
  if (!product) return reply.status(404).send({ error: 'Produkt nicht gefunden' });
  if (product.shopId !== session.shopId) {
    return reply.status(403).send({ error: 'Zugriff verweigert' });
  }

  // Verkaufshistorie-Check: Artikel darf keine Verkäufe haben (items ist jsonb Array)
  // Prüfung: sales mit shopId des Shops, die diesen Artikel in items enthalten
  const salesWithProduct = await db
    .select({ id: sales.id })
    .from(sales)
    .where(
      and(
        eq(sales.shopId, session.shopId),
        sql`${sales.items}::jsonb @> ${JSON.stringify([{ productId: id }])}::jsonb`
      )
    )
    .limit(1);

  if (salesWithProduct.length > 0) {
    return reply.status(409).send({
      error: 'Artikel kann nicht gelöscht werden — es existieren Verkäufe mit diesem Artikel. Bitte deaktivieren statt löschen.'
    });
  }

  // Hard-Delete aus DB + zugehörige price_histories und stock_movements löschen
  await db.transaction(async (tx) => {
    await tx.delete(priceHistories).where(eq(priceHistories.productId, id));
    // stock_movements importieren falls nötig
    await tx.delete(products).where(eq(products.id, id));
  });

  reply.send({ ok: true });
  broadcast({ type: 'products_changed', shopId: session.shopId });
});
```

**3. `stockMovements` ebenfalls importieren** (für den Cleanup in der Transaction):
```typescript
import { products, priceHistories, sales, stockMovements } from '../db/schema.js';
```

Und in der Transaction:
```typescript
await tx.delete(stockMovements).where(eq(stockMovements.productId, id));
```
(einfügen zwischen priceHistories-Delete und products-Delete)

**4. Fehlerbehandlung für Unique-Constraint-Verletzung** im POST /products Endpoint:
Umschließe den bestehenden `await db.transaction(...)` Block mit try/catch und prüfe auf unique-Fehler:

```typescript
try {
  await db.transaction(async (tx) => {
    // ... bestehender Code unverändert ...
  });
} catch (err: unknown) {
  // PostgreSQL unique-Verletzung: code 23505
  if (err && typeof err === 'object' && 'code' in err && (err as { code: string }).code === '23505') {
    return reply.status(409).send({ error: 'Artikelnummer bereits vorhanden — bitte eine andere Artikelnummer wählen.' });
  }
  throw err;
}
```
  </action>
  <verify>
    <automated>grep -n "fastify.delete" /Users/simonluthe/Documents/fairstand/server/src/routes/products.ts && grep -n "status(409)" /Users/simonluthe/Documents/fairstand/server/src/routes/products.ts && grep -n "stockMovements" /Users/simonluthe/Documents/fairstand/server/src/routes/products.ts</automated>
  </verify>
  <done>
    - `products.ts` enthält `fastify.delete('/products/:id', ...)`
    - Endpoint gibt 409 zurück wenn `salesWithProduct.length > 0`
    - Endpoint gibt 404 zurück wenn Produkt nicht existiert
    - Endpoint gibt 403 zurück wenn shopId nicht übereinstimmt
    - POST /products hat try/catch mit 409-Handling für Unique-Constraint-Fehler (code `23505`)
    - stockMovements und priceHistories werden vor Produkt-Delete bereinigt
  </done>
</task>

</tasks>

<verification>
Backend-Endpoint manuell per curl testbar (nach lokalem Start):

```bash
# Produkt ohne Verkäufe: erwartet 200
curl -X DELETE http://localhost:3000/api/products/{id} -H "Authorization: Bearer {token}"

# Produkt mit Verkäufen: erwartet 409
curl -X DELETE http://localhost:3000/api/products/{id-mit-verkauf} -H "Authorization: Bearer {token}"
```

TypeScript-Fehler prüfen:
```bash
cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit
```
</verification>

<success_criteria>
- `server/src/routes/products.ts` enthält `fastify.delete('/products/:id', ...)` mit Ownership-Check und Verkaufshistorie-Check
- Migration `0006_add_article_number_unique.sql` existiert mit `CREATE UNIQUE INDEX IF NOT EXISTS products_article_number_shop_id_idx`
- `schema.ts` products-Tabelle hat `articleNumberShopIdx: uniqueIndex(...)`
- POST /products gibt 409 bei doppelter Artikelnummer (Unique-Constraint-Handling)
- TypeScript compiliert ohne Fehler
</success_criteria>

<output>
Nach Abschluss `.planning/phases/30-admin-verwaltung/30-01-SUMMARY.md` erstellen.
</output>
