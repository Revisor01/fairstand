---
phase: 36-ek-preiswarnung-beim-import
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/admin/import/ReviewTable.tsx
  - client/src/features/admin/import/ImportScreen.tsx
autonomous: true
requirements:
  - IMP-01

must_haves:
  truths:
    - "Beim Import einer Rechnung mit geändertem EK erscheint ein sichtbarer Hinweis in der Prüfzeile"
    - "Der Hinweis zeigt den alten (gespeicherten) und neuen (importierten) EK-Wert"
    - "Zeilen ohne EK-Abweichung erhalten keinen Hinweis"
    - "Neue Artikel (status='new') erhalten keinen EK-Vergleich (kein gespeicherter Wert vorhanden)"
  artifacts:
    - path: "client/src/features/admin/import/ReviewTable.tsx"
      provides: "EK-Warnzeile im ReviewTable wenn storedPurchasePriceCents abweicht"
      contains: "storedPurchasePriceCents"
    - path: "client/src/features/admin/import/ImportScreen.tsx"
      provides: "storedPurchasePriceCents aus gematchtem Produkt in MatchedRow eintragen"
      contains: "match?.purchasePrice"
  key_links:
    - from: "client/src/features/admin/import/ImportScreen.tsx"
      to: "client/src/features/admin/import/ReviewTable.tsx"
      via: "MatchedRow.storedPurchasePriceCents prop"
      pattern: "storedPurchasePriceCents"
---

<objective>
EK-Preiswarnung im PDF-Import-Review anzeigen.

Purpose: Mitarbeiterinnen sehen beim Prüfen des Imports sofort welche Artikel einen geänderten Einkaufspreis haben — ohne manuellen Vergleich.
Output: ReviewTable zeigt bei Artikeln mit EK-Abweichung eine Warnzeile mit altem und neuem EK-Wert.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Bestehende MatchedRow-Struktur aus ReviewTable.tsx -->
```typescript
export interface MatchedRow {
  lineNumber: number;
  quantity: number;
  articleNumber: string;
  name: string;
  purchasePriceCents: number;      // importierter EK aus PDF
  evpCents: number | null;
  vatRate: 7 | 19;
  parseWarning?: string;
  status: 'known' | 'new';
  existingProductId?: string;
  checked: boolean;
}
```

<!-- Matching-Logik in ImportScreen.tsx (Zeilen 93-101) -->
```typescript
const matchedRows: MatchedRow[] = data.rows.map(row => {
  const match = productIndex.get(row.articleNumber.toLowerCase().trim());
  return {
    ...row,
    status: match ? 'known' : 'new',
    existingProductId: match?.id,
    checked: true,
  };
});
```

<!-- Product-Typ hat purchasePrice in Cents -->
<!-- match?.purchasePrice gibt gespeicherten EK in Cents -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: MatchedRow um storedPurchasePriceCents erweitern + Matching anpassen</name>
  <files>client/src/features/admin/import/ReviewTable.tsx, client/src/features/admin/import/ImportScreen.tsx</files>
  <read_first>
    - client/src/features/admin/import/ReviewTable.tsx — vollständig lesen (Interface + Rendering)
    - client/src/features/admin/import/ImportScreen.tsx — Zeilen 93-103 (matchedRows-Mapping)
  </read_first>
  <action>
**Schritt 1 — ReviewTable.tsx:**

`MatchedRow`-Interface um optionales Feld erweitern:
```typescript
storedPurchasePriceCents?: number;  // gespeicherter EK des gematchten Produkts (nur bei status='known')
```

**Schritt 2 — ImportScreen.tsx:**

Im `matchedRows`-Mapping `storedPurchasePriceCents` aus dem gematchten Produkt befüllen:
```typescript
const matchedRows: MatchedRow[] = data.rows.map(row => {
  const match = productIndex.get(row.articleNumber.toLowerCase().trim());
  return {
    ...row,
    status: match ? 'known' : 'new',
    existingProductId: match?.id,
    storedPurchasePriceCents: match?.purchasePrice,  // NEU
    checked: true,
  };
});
```

**Schritt 3 — ReviewTable.tsx, Warnzeile:**

Direkt nach der bestehenden `parseWarning`-Zeile (Zeile 148-154) eine zweite bedingte Zeile für EK-Abweichung einfügen. Die Bedingung: `row.status === 'known'` UND `row.storedPurchasePriceCents !== undefined` UND `row.storedPurchasePriceCents !== row.purchasePriceCents`.

Warnzeile-JSX (colSpan=9, gleiche Struktur wie parseWarning-Zeile aber Farbe amber-600/amber-100):
```tsx
{row.status === 'known' &&
  row.storedPurchasePriceCents !== undefined &&
  row.storedPurchasePriceCents !== row.purchasePriceCents && (
  <tr key={`ekwarn-${index}`} className="bg-amber-50">
    <td colSpan={9} className="px-4 py-2 text-amber-700 text-xs flex items-center gap-1.5">
      <span>⚠️</span>
      <span>
        EK geändert: bisher{' '}
        <strong>{toEurStr(row.storedPurchasePriceCents)} €</strong>
        {' '}→ neu{' '}
        <strong>{toEurStr(row.purchasePriceCents)} €</strong>
      </span>
    </td>
  </tr>
)}
```

Hinweis: `toEurStr` ist bereits in ReviewTable.tsx vorhanden und kann direkt verwendet werden.

Wichtig: Das Fragment-Wrapper-Pattern `<>...</>` für jede Zeile ist bereits etabliert (Zeilen 61-155 in ReviewTable.tsx) — das neue `tr` kommt innerhalb dieses Fragments nach dem parseWarning-Block.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npm run build --workspace=client 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - grep -n "storedPurchasePriceCents" client/src/features/admin/import/ReviewTable.tsx | wc -l gibt mindestens 2 (Interface + Rendering)
    - grep -n "storedPurchasePriceCents" client/src/features/admin/import/ImportScreen.tsx | wc -l gibt mindestens 1 (Mapping)
    - grep "ekwarn" client/src/features/admin/import/ReviewTable.tsx zeigt den key der Warnzeile
    - npm run build gibt keinen TypeScript-Fehler aus
  </acceptance_criteria>
  <done>
    - MatchedRow hat optionales Feld storedPurchasePriceCents
    - Matching in ImportScreen füllt storedPurchasePriceCents aus match?.purchasePrice
    - ReviewTable zeigt Warnzeile bei EK-Abweichung mit altem und neuem Wert
    - Neue Artikel (status='new') erhalten keine Warnzeile
    - Zeilen ohne Abweichung erhalten keine Warnzeile
    - TypeScript-Build ist fehlerfrei
  </done>
</task>

</tasks>

<verification>
1. TypeScript-Build: `cd /Users/simonluthe/Documents/fairstand && npm run build --workspace=client` — kein Fehler
2. Interface-Check: `grep "storedPurchasePriceCents" client/src/features/admin/import/ReviewTable.tsx` — mindestens 2 Treffer (Interface + Conditional)
3. Mapping-Check: `grep "storedPurchasePriceCents" client/src/features/admin/import/ImportScreen.tsx` — mindestens 1 Treffer
</verification>

<success_criteria>
- Bekannte Artikel mit geändertem EK zeigen in der Import-Prüfansicht eine Warnzeile mit altem und neuem EK-Wert
- Artikel ohne Änderung und neue Artikel zeigen keine EK-Warnzeile
- Build ist fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/36-ek-preiswarnung-beim-import/36-01-SUMMARY.md` erstellen.
</output>
