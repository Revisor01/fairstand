---
phase: 10-ui-redesign
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/features/admin/AdminScreen.tsx
autonomous: true
requirements:
  - UI-04
  - UI-05

must_haves:
  truths:
    - "Artikel-Grid hat mehr Luft zwischen den Kacheln und mehr Innenabstand"
    - "Artikel-Kacheln haben etwas mehr Padding"
    - "Die Tab-Navigation im Admin-Bereich verwendet Pill-Tabs statt voller Blöcke"
    - "Der Admin-Content-Bereich hat mehr Padding"
  artifacts:
    - path: "client/src/features/pos/ArticleGrid.tsx"
      provides: "Luftigeres Grid-Layout"
      contains: "gap-4"
    - path: "client/src/features/admin/AdminScreen.tsx"
      provides: "Pill-Tabs statt Block-Tabs"
      contains: "rounded-full"
  key_links:
    - from: "ArticleGrid.tsx"
      to: "Produkt-Kacheln"
      via: "grid gap- und p- Klassen"
      pattern: "gap-4"
    - from: "AdminScreen.tsx"
      to: "Tab-Buttons"
      via: "rounded-full Pill-Stil"
      pattern: "rounded-full"
---

<objective>
Spacing-Verbesserungen im Artikel-Grid und Umstieg auf Pill-Tabs in der Admin-Navigation. Keine inhaltlichen Änderungen — nur Tailwind-Klassen anpassen.

Purpose: Die App wirkt luftiger und professioneller ohne das Farbschema oder die Struktur zu verändern.
Output: ArticleGrid.tsx (Spacing), AdminScreen.tsx (Tabs + Spacing)
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/10-ui-redesign/10-01-SUMMARY.md

@client/src/features/pos/ArticleGrid.tsx
@client/src/features/admin/AdminScreen.tsx
</context>

<interfaces>
<!-- Nur die relevanten Stellen, die geändert werden. Kein Codebase-Durchsuchen nötig. -->

Aus ArticleGrid.tsx (Zeilen die geändert werden):
```tsx
// VORHER (Zeile 72):
<div className="flex-1 overflow-y-auto p-4">
// NACHHER:
<div className="flex-1 overflow-y-auto p-6">

// VORHER (Zeile 78):
<div className="grid grid-cols-[repeat(auto-fill,minmax(140px,1fr))] gap-3">
// NACHHER:
<div className="grid grid-cols-[repeat(auto-fill,minmax(140px,1fr))] gap-4">

// VORHER (Zeile 85):
className={`bg-white shadow-sm rounded-xl min-h-[80px] p-3 ...`}
// NACHHER:
className={`bg-white shadow-sm rounded-xl min-h-[80px] p-4 ...`}
```

Aus AdminScreen.tsx (Tab-Nav wird zu Pill-Tabs):
```tsx
// VORHER: full-width flex blocks mit bg-sky-500 active:
<nav className="bg-white border-b border-sky-200 flex">
  <button className={`flex-1 py-4 text-sm font-semibold ...`}>

// NACHHER: scrollbare Pill-Tabs in einem Padding-Container:
<nav className="bg-white border-b border-sky-100 px-4 py-3">
  <div className="flex gap-2 overflow-x-auto">
    <button className={`px-4 py-2 rounded-full text-sm font-semibold whitespace-nowrap min-h-[44px] transition-colors ${
      tab === 'products' ? 'bg-sky-400 text-white' : 'bg-sky-100 text-sky-700 hover:bg-sky-200'
    }`}>

// Content-Padding: p-4 → p-6
<main className="flex-1 p-6">
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: ArticleGrid — mehr Whitespace (UI-04)</name>
  <files>client/src/features/pos/ArticleGrid.tsx</files>
  <action>
Drei gezielte Klassen-Änderungen in ArticleGrid.tsx (per Entscheidung UI-04 — nur Spacing, keine Farben oder Schriftgrößen):

1. Grid-Container-Padding: `p-4` → `p-6`
   - Zeile mit `flex-1 overflow-y-auto p-4`

2. Grid-Lücken: `gap-3` → `gap-4`
   - Zeile mit `grid grid-cols-[repeat(auto-fill,minmax(140px,1fr))] gap-3`

3. Kachel-Padding: `p-3` → `p-4`
   - Zeile mit `bg-white shadow-sm rounded-xl min-h-[80px] p-3`

Sonst nichts ändern. Keine Farben, keine Schriftgrößen, kein struktureller Umbau.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -n "gap-4\|p-6\|p-4" client/src/features/pos/ArticleGrid.tsx | head -20</automated>
  </verify>
  <done>
- `gap-4` im Grid vorhanden (statt gap-3)
- Äußeres Grid-Padding ist `p-6` (statt p-4)
- Kachel-Padding ist `p-4` (statt p-3)
- TypeScript kompiliert sauber
  </done>
</task>

<task type="auto">
  <name>Task 2: AdminScreen — Pill-Tabs und mehr Content-Padding (UI-05)</name>
  <files>client/src/features/admin/AdminScreen.tsx</files>
  <action>
Zwei Änderungen in AdminScreen.tsx (per Entscheidung UI-05 — kompaktere, übersichtlichere Admin-Navigation):

**1. Tab-Navigation zu Pill-Tabs umbauen:**

Ersetze das aktuelle `<nav>` Block komplett:

```tsx
{/* VORHER: Volle Breite, gleichmäßige Blöcke */}
<nav className="bg-white border-b border-sky-200 flex">
  <button className={`relative flex-1 py-4 text-sm font-semibold transition-colors ${
    tab === 'products' ? 'bg-sky-500 text-white' : 'text-sky-700 hover:bg-sky-50'
  }`}>
    Produkte
    {lowStockCount > 0 && (
      <span className="inline-flex items-center justify-center bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 ml-1">
        {lowStockCount}
      </span>
    )}
  </button>
  {/* ... weitere Tabs ... */}
</nav>
```

```tsx
{/* NACHHER: Pill-Tabs in scrollbarem Container */}
<nav className="bg-white border-b border-sky-100 px-4 py-3">
  <div className="flex gap-2 overflow-x-auto">
    <button
      onPointerDown={() => setTab('products')}
      className={`relative flex items-center gap-1 px-4 py-2 rounded-full text-sm font-semibold whitespace-nowrap min-h-[44px] transition-colors ${
        tab === 'products'
          ? 'bg-sky-400 text-white'
          : 'bg-sky-100 text-sky-700 hover:bg-sky-200'
      }`}
    >
      Produkte
      {lowStockCount > 0 && (
        <span className="inline-flex items-center justify-center bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5">
          {lowStockCount}
        </span>
      )}
    </button>
    <button
      onPointerDown={() => setTab('reports')}
      className={`px-4 py-2 rounded-full text-sm font-semibold whitespace-nowrap min-h-[44px] transition-colors ${
        tab === 'reports' ? 'bg-sky-400 text-white' : 'bg-sky-100 text-sky-700 hover:bg-sky-200'
      }`}
    >
      Berichte
    </button>
    <button
      onPointerDown={() => setTab('import')}
      className={`px-4 py-2 rounded-full text-sm font-semibold whitespace-nowrap min-h-[44px] transition-colors ${
        tab === 'import' ? 'bg-sky-400 text-white' : 'bg-sky-100 text-sky-700 hover:bg-sky-200'
      }`}
    >
      Import
    </button>
    <button
      onPointerDown={() => setTab('settings')}
      className={`px-4 py-2 rounded-full text-sm font-semibold whitespace-nowrap min-h-[44px] transition-colors ${
        tab === 'settings' ? 'bg-sky-400 text-white' : 'bg-sky-100 text-sky-700 hover:bg-sky-200'
      }`}
    >
      Einstellungen
    </button>
  </div>
</nav>
```

**2. Content-Padding erhöhen:**
- `<main className="flex-1 p-4">` → `<main className="flex-1 p-6">`

Tab-Logik (setTab, aktive Anzeige, lowStockCount-Badge) bleibt funktional identisch.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -n "rounded-full\|p-6" client/src/features/admin/AdminScreen.tsx | head -20</automated>
  </verify>
  <done>
- Tab-Buttons haben `rounded-full` statt `flex-1`
- Content `<main>` hat `p-6`
- Alle 4 Tabs (Produkte, Berichte, Import, Einstellungen) vorhanden
- lowStockCount-Badge auf "Produkte"-Tab erhalten
- TypeScript kompiliert sauber
  </done>
</task>

</tasks>

<verification>
TypeScript-Kompilierung ohne Fehler:
```bash
cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json
```

Grep-Checks:
```bash
grep -n "gap-4" client/src/features/pos/ArticleGrid.tsx
grep -n "rounded-full" client/src/features/admin/AdminScreen.tsx
```
</verification>

<success_criteria>
1. ArticleGrid: `gap-4`, `p-6` (Container), `p-4` (Kacheln) statt gap-3/p-4/p-3
2. AdminScreen: Tabs verwenden `rounded-full` (Pill-Stil)
3. AdminScreen: `<main>` hat `p-6`
4. TypeScript-Kompilierung sauber
</success_criteria>

<output>
Nach Abschluss `.planning/phases/10-ui-redesign/10-02-SUMMARY.md` erstellen.
</output>
