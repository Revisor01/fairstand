---
phase: 37-ek-wareneingaenge-bestandsanpassung
plan: 03
type: execute
wave: 2
depends_on:
  - 37-01
files_modified:
  - server/src/routes/import.ts
  - server/src/index.ts
  - client/src/features/admin/import/ImportScreen.tsx
autonomous: true
requirements:
  - EINGANG-02

must_haves:
  truths:
    - "POST /api/stock/adjust Endpoint existiert und schreibt restock-Bewegung in stock_movements"
    - "ImportScreen sendet purchasePriceCents aus der Import-Zeile an /api/stock/adjust"
    - "Bei bekanntem Artikel mit geändertem EK wird price_histories aktualisiert"
    - "stock_movements.type ist 'restock' (nicht 'adjustment') für PDF-Import-Buchungen"
  artifacts:
    - path: "server/src/routes/import.ts"
      provides: "POST /api/stock/adjust Endpoint + restock-Bewegungslogik"
      contains: "stock/adjust"
    - path: "client/src/features/admin/import/ImportScreen.tsx"
      provides: "purchasePriceCents im Buchungs-Request"
      contains: "purchasePriceCents"
  key_links:
    - from: "client/src/features/admin/import/ImportScreen.tsx"
      to: "POST /api/stock/adjust"
      via: "authFetch('/api/stock/adjust', { productId, delta, purchasePriceCents, reason, shopId })"
      pattern: "purchasePriceCents"
    - from: "server/src/routes/import.ts"
      to: "stockMovements"
      via: "db.transaction INSERT mit type='restock' und purchasePriceCents"
      pattern: "restock"
    - from: "server/src/routes/import.ts"
      to: "priceHistories"
      via: "INSERT wenn EK geändert (importEK !== adjustProd.purchasePrice)"
      pattern: "priceHistories"
---

<objective>
Neuer `/api/stock/adjust` Endpoint in `import.ts`, der Wareneingänge als `restock`-Bewegung mit EK-Preis erfasst. ImportScreen sendet `purchasePriceCents` aus der PDF-Zeile. Bei EK-Änderung wird `price_histories` aktualisiert.

Purpose: PDF-Import ist das Hauptwerkzeug für Wareneingänge — jeder Import muss die EK-Preise korrekt in stock_movements speichern damit FIFO-Inventur (Phase 38) korrekt rechnen kann.
Output: Neuer Endpoint, erweiterter ImportScreen-Commit, automatische price_history-Aktualisierung.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ek-wareneingaenge-bestandsanpassung/37-CONTEXT.md
</context>

<interfaces>
<!-- Aktueller ImportScreen handleCommit (client/src/features/admin/import/ImportScreen.tsx): -->
```typescript
// Bestandsbuchung direkt an Server
await authFetch('/api/stock/adjust', {
  method: 'POST',
  body: JSON.stringify({
    productId,
    delta: row.quantity,
    reason: 'Import Rechnung',
    shopId: getShopId(),
  }),
});
```

<!-- Import-Zeilen-Struktur (ParsedInvoiceRow): -->
```typescript
interface ParsedInvoiceRow {
  lineNumber: number;
  quantity: number;
  articleNumber: string;
  name: string;
  purchasePriceCents: number;  // EK aus PDF — das ist der Wert den wir speichern wollen
  evpCents: number | null;
  vatRate: 7 | 19;
  parseWarning?: string;
}
```

<!-- stockMovements-Tabelle nach Plan 01 (neue Spalte): -->
```typescript
purchasePriceCents: integer('purchase_price_cents'),  // nullable
```

<!-- priceHistories-Tabelle (bereits existiert): -->
```typescript
export const priceHistories = pgTable('price_histories', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  field: text('field').notNull(), // 'purchase_price' | 'sale_price'
  oldValue: integer('old_value').notNull(),
  newValue: integer('new_value').notNull(),
  changedAt: bigint('changed_at', { mode: 'number' }).notNull(),
});
```

<!-- Produkt-Typ (für EK-Vergleich): -->
```typescript
products.purchasePrice  // integer, in Cents
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: POST /api/stock/adjust Endpoint in import.ts erstellen</name>
  <files>
    server/src/routes/import.ts
    server/src/index.ts
  </files>
  <read_first>
    - server/src/routes/import.ts — aktueller Stand (nur /import/parse vorhanden)
    - server/src/db/schema.ts — stockMovements, priceHistories, products Tabellen
    - server/src/index.ts — Route-Registrierungen (zur Prüfung ob import.ts bereits registriert ist)
  </read_first>
  <action>
In `server/src/routes/import.ts` einen neuen Endpoint `POST /stock/adjust` hinzufügen:

**Imports hinzufügen** (oben in der Datei):
```typescript
import { z } from 'zod';
import { eq, sql } from 'drizzle-orm';
import { db } from '../db/index.js';
import { products, stockMovements, priceHistories } from '../db/schema.js';
import { broadcast } from './websocket.js';
```

**Zod-Schema**:
```typescript
const StockAdjustImportSchema = z.object({
  productId: z.string(),
  delta: z.number().int().positive(), // Import ist immer Eingang — nur positiv erlaubt
  purchasePriceCents: z.number().int().positive(), // EK aus Rechnung — Pflicht für Import
  reason: z.string().optional(),
  shopId: z.string(),
});
```

**Route** (nach der `/import/parse` Route, innerhalb der `importRoutes`-Funktion):
```typescript
fastify.post('/stock/adjust', async (request, reply) => {
  const session = (request as any).session as { shopId: string };
  const result = StockAdjustImportSchema.safeParse(request.body);
  if (!result.success) {
    return reply.status(400).send({ error: result.error.flatten() });
  }
  const adj = result.data;

  // shopId-Ownership-Check
  if (adj.shopId !== session.shopId) {
    return reply.status(403).send({ error: 'Zugriff verweigert: shopId stimmt nicht überein' });
  }

  await db.transaction(async (tx) => {
    // Ownership-Check: Produkt muss zu diesem Shop gehören
    const [adjustProd] = await tx.select().from(products).where(eq(products.id, adj.productId)).limit(1);
    if (!adjustProd || adjustProd.shopId !== adj.shopId) {
      return reply.status(404).send({ error: 'Produkt nicht gefunden oder gehört nicht zu diesem Shop' });
    }

    // 1. Bestand erhöhen
    await tx.update(products)
      .set({ stock: sql`${products.stock} + ${adj.delta}`, updatedAt: sql`${Date.now()}` })
      .where(eq(products.id, adj.productId));

    // 2. restock-Bewegung mit EK-Preis speichern
    await tx.insert(stockMovements).values({
      shopId: adj.shopId,
      productId: adj.productId,
      type: 'restock',
      quantity: adj.delta,
      reason: adj.reason ?? 'Import Rechnung',
      purchasePriceCents: adj.purchasePriceCents,
      movedAt: Date.now(),
    });

    // 3. Wenn EK geändert: price_histories + Produkt-EK aktualisieren
    if (adj.purchasePriceCents !== adjustProd.purchasePrice) {
      await tx.insert(priceHistories).values({
        shopId: adj.shopId,
        productId: adj.productId,
        field: 'purchase_price',
        oldValue: adjustProd.purchasePrice,
        newValue: adj.purchasePriceCents,
        changedAt: Date.now(),
      });
      await tx.update(products)
        .set({ purchasePrice: adj.purchasePriceCents, updatedAt: sql`${Date.now()}` })
        .where(eq(products.id, adj.productId));
    }
  });

  broadcast({ type: 'stock_changed', shopId: adj.shopId });
  return reply.status(200).send({ ok: true });
});
```

**index.ts:** Keine Änderung nötig — `importRoutes` ist bereits registriert mit `prefix: '/api'` (Zeile 75). Die neue Route wird automatisch als `/api/stock/adjust` erreichbar.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -n "stock/adjust\|restock\|purchasePriceCents" server/src/routes/import.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "stock/adjust" server/src/routes/import.ts` findet die Route-Definition `fastify.post('/stock/adjust', ...)`
    - `grep "restock" server/src/routes/import.ts` findet den type-Wert im INSERT
    - `grep "purchasePriceCents" server/src/routes/import.ts` findet Schema-Definition und INSERT
    - `grep "priceHistories" server/src/routes/import.ts` findet den EK-Update-Block
    - TypeScript-Kompilierung fehlerfrei: `cd server && npx tsc --noEmit`
    - index.ts wurde NICHT verändert (importRoutes ist bereits registriert)
  </acceptance_criteria>
  <done>POST /api/stock/adjust erstellt; schreibt restock-Bewegung mit EK-Preis; aktualisiert price_histories wenn EK geändert</done>
</task>

<task type="auto">
  <name>Task 2: ImportScreen sendet purchasePriceCents beim Commit</name>
  <files>client/src/features/admin/import/ImportScreen.tsx</files>
  <read_first>
    - client/src/features/admin/import/ImportScreen.tsx — aktueller handleCommit
  </read_first>
  <action>
In `client/src/features/admin/import/ImportScreen.tsx` die `handleCommit`-Funktion anpassen:

Der bestehende `/api/stock/adjust`-Aufruf (Zeile ~160-168) bekommt `purchasePriceCents` aus `row.purchasePriceCents`:

```typescript
// Bestandsbuchung direkt an Server
await authFetch('/api/stock/adjust', {
  method: 'POST',
  body: JSON.stringify({
    productId,
    delta: row.quantity,
    purchasePriceCents: row.purchasePriceCents, // neu — EK aus PDF-Zeile
    reason: 'Import Rechnung',
    shopId: getShopId(),
  }),
});
```

Das ist die einzige Änderung in dieser Datei. `row.purchasePriceCents` ist bereits in `ParsedInvoiceRow` definiert und in `matchedRows` verfügbar — kein zusätzlicher State nötig.

WICHTIG: Keine anderen Änderungen am ImportScreen — kein UI, keine Logik außer dem Payload-Feld.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -n "purchasePriceCents" client/src/features/admin/import/ImportScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep "purchasePriceCents" client/src/features/admin/import/ImportScreen.tsx` findet genau eine Zeile im handleCommit-Block
    - Der Wert kommt aus `row.purchasePriceCents` (nicht hardcoded oder aus anderem State)
    - TypeScript-Kompilierung fehlerfrei: `cd client && npx tsc --noEmit`
    - Keine anderen Änderungen im ImportScreen (nur die eine Payload-Zeile)
  </acceptance_criteria>
  <done>ImportScreen sendet purchasePriceCents beim Commit; Server empfängt EK-Preis aus PDF-Rechnung</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → POST /api/stock/adjust | delta, purchasePriceCents, productId kommen vom Client |
| Import-Endpoint → DB | Produktzugehörigkeit muss vor Write geprüft werden |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-05 | Tampering | productId in /api/stock/adjust | mitigate | Ownership-Check: `adjustProd.shopId !== adj.shopId` vor allen Schreiboperationen |
| T-37-06 | Tampering | delta negativ oder null im Import | mitigate | Zod-Schema: `z.number().int().positive()` — nur positive Ganzzahlen erlaubt |
| T-37-07 | Tampering | purchasePriceCents = 0 oder negativ | mitigate | Zod-Schema: `z.number().int().positive()` — kein EK von 0 oder negativ erlaubt |
| T-37-08 | Elevation of Privilege | shopId im Body weicht von Session.shopId ab | mitigate | Expliziter shopId-Vergleich: `adj.shopId !== session.shopId` → 403 |
</threat_model>

<verification>
```bash
cd /Users/simonluthe/Documents/fairstand
# Server-Endpoint
grep -n "stock/adjust\|restock\|purchasePriceCents\|priceHistories" server/src/routes/import.ts

# Client-Payload
grep -n "purchasePriceCents" client/src/features/admin/import/ImportScreen.tsx

# TypeScript
cd server && npx tsc --noEmit 2>&1 | head -20
cd ../client && npx tsc --noEmit 2>&1 | head -20
```
</verification>

<success_criteria>
- POST /api/stock/adjust ist unter /api/stock/adjust erreichbar (importRoutes bereits mit /api-Prefix registriert)
- Erfolgreicher Commit schreibt restock-Bewegung mit type='restock' und purchasePriceCents in DB
- Bei EK-Änderung wird ein price_histories-Eintrag erstellt und Produkt-EK aktualisiert
- ImportScreen sendet purchasePriceCents aus der PDF-Zeile
- Kein TypeScript-Fehler in server/ und client/
</success_criteria>

<output>
Nach Abschluss `.planning/phases/37-ek-wareneingaenge-bestandsanpassung/37-03-SUMMARY.md` erstellen.
</output>
