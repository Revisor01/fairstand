---
phase: 37-ek-wareneingaenge-bestandsanpassung
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/db/schema.ts
autonomous: true
requirements:
  - EINGANG-01
  - ANPASS-02

must_haves:
  truths:
    - "stockMovements-Tabelle hat eine nullable Spalte purchase_price_cents (integer)"
    - "Der Kommentar in schema.ts listet 'restock' als gültigen type-Wert auf"
    - "drizzle-kit migrate läuft beim nächsten Container-Start durch"
  artifacts:
    - path: "server/src/db/schema.ts"
      provides: "Erweitertes stockMovements-Schema mit purchase_price_cents"
      contains: "purchasePriceCents"
  key_links:
    - from: "server/src/db/schema.ts"
      to: "PostgreSQL stock_movements Tabelle"
      via: "npx drizzle-kit migrate (automatisch beim Container-Start)"
      pattern: "purchasePriceCents.*integer"
---

<objective>
Erweiterung des DB-Schemas: Neue Spalte `purchase_price_cents` (integer, nullable) in der `stock_movements`-Tabelle und Dokumentation des neuen Bewegungstyps `restock`.

Purpose: Ohne diese Spalte können Wareneingänge keinen EK-Preis tragen. Alle Pläne in Phase 37 bauen auf diesem Schema auf.
Output: Erweitertes schema.ts, Migration wird automatisch beim nächsten Docker-Build/Start ausgeführt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/37-ek-wareneingaenge-bestandsanpassung/37-CONTEXT.md
</context>

<interfaces>
<!-- Aktuelles stockMovements-Schema (aus server/src/db/schema.ts): -->
```typescript
export const stockMovements = pgTable('stock_movements', {
  id: serial('id').primaryKey(),
  shopId: text('shop_id').notNull(),
  productId: text('product_id').notNull(),
  type: text('type').notNull(), // 'sale' | 'adjustment' | 'correction' | 'return' | 'hard_delete'
  quantity: integer('quantity').notNull(),
  referenceSaleId: text('reference_sale_id'),
  reason: text('reason'),
  movedAt: bigint('moved_at', { mode: 'number' }).notNull(),
});
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: purchase_price_cents zu stockMovements hinzufügen</name>
  <files>server/src/db/schema.ts</files>
  <read_first>
    - server/src/db/schema.ts — vollständige aktuelle Schema-Definitionen lesen
  </read_first>
  <action>
In `server/src/db/schema.ts` das `stockMovements`-Table-Objekt um eine neue Spalte erweitern:

```typescript
purchasePriceCents: integer('purchase_price_cents'),
```

Die Spalte muss:
- Typ `integer('purchase_price_cents')` sein (kein `.notNull()` — explizit nullable für negative Korrekturen und Verkäufe)
- Nach der bestehenden `reason`-Spalte und vor `movedAt` eingefügt werden
- Den Kommentar im `type`-Feld von `'sale' | 'adjustment' | 'correction' | 'return' | 'hard_delete'` auf `'sale' | 'adjustment' | 'correction' | 'return' | 'hard_delete' | 'restock'` erweitern

KEINE anderen Tabellen anfassen. KEINE Migration-Datei manuell erstellen — Drizzle erzeugt diese automatisch über `drizzle-kit migrate` (läuft im Dockerfile beim Container-Start: `npx drizzle-kit migrate && node dist/index.js`).
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && grep -n "purchasePriceCents\|purchase_price_cents" server/src/db/schema.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "purchasePriceCents" server/src/db/schema.ts` findet genau eine Zeile mit `integer('purchase_price_cents')`
    - Die Zeile hat KEIN `.notNull()` (nullable)
    - `grep "restock" server/src/db/schema.ts` findet den aktualisierten Kommentar
    - TypeScript-Kompilierung wirft keinen Fehler: `cd server && npx tsc --noEmit`
  </acceptance_criteria>
  <done>schema.ts enthält purchase_price_cents als nullable integer Spalte und 'restock' im type-Kommentar</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| API → DB | Werte für purchase_price_cents kommen aus API-Payloads (clientseitig) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-37-01 | Tampering | purchase_price_cents in stock_movements | mitigate | Wert wird nur von authentifizierten Sessions geschrieben (Session-Check in sync.ts bereits vorhanden); negativer EK wird in sync.ts auf null gesetzt (Plan 02) |
| T-37-02 | Information Disclosure | stock_movements via stock-movements API | accept | Bewegungen sind nur für den eigenen Shop sichtbar (shopId-Check bereits in priceHistory.ts implementiert) |
</threat_model>

<verification>
```bash
cd /Users/simonluthe/Documents/fairstand
grep -n "purchasePriceCents\|purchase_price_cents\|restock" server/src/db/schema.ts
cd server && npx tsc --noEmit 2>&1 | head -20
```
Erwartet: Spalte gefunden, kein TypeScript-Fehler.
</verification>

<success_criteria>
- server/src/db/schema.ts enthält `purchasePriceCents: integer('purchase_price_cents')` (nullable)
- Kommentar beim `type`-Feld nennt `'restock'` als gültigen Wert
- `npx tsc --noEmit` im server/-Verzeichnis fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/37-ek-wareneingaenge-bestandsanpassung/37-01-SUMMARY.md` erstellen.
</output>
