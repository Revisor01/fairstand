---
phase: 18-quick-wins-security
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - server/src/routes/auth.ts
  - server/src/lib/sessions.ts
  - server/src/index.ts
  - server/src/routes/sync.ts
  - server/src/routes/products.ts
  - server/src/routes/categories.ts
  - server/src/routes/reports.ts
  - server/src/routes/settings.ts
  - client/src/sync/engine.ts
  - client/src/features/auth/serverAuth.ts
  - server/package.json
autonomous: true
requirements: [SEC-02, SEC-03]
user_setup: []

must_haves:
  truths:
    - "Nach 5 falschen PIN-Versuchen von derselben IP innerhalb einer Minute antwortet der Server mit 429"
    - "Ein gültiger Token mit falscher shopId im Request-Body wird mit 403 abgelehnt"
    - "Ein Request ohne Authorization-Header an geschützte Endpoints gibt 401 zurück"
    - "Nach erfolgreichem PIN-Login sendet der Client bei jedem API-Request den Token als Authorization-Header"
  artifacts:
    - path: "server/src/lib/sessions.ts"
      provides: "In-Memory Session-Store: Map<token, {shopId, createdAt}> mit createSession/validateSession/deleteSession"
      exports: ["createSession", "validateSession", "deleteSession"]
    - path: "server/src/routes/auth.ts"
      provides: "POST /api/auth/pin mit @fastify/rate-limit (5/min/IP) + createSession() statt randomUUID direkt"
      contains: "rate-limit"
    - path: "server/src/index.ts"
      provides: "Auth-Middleware-Hook: preHandler für alle /api/* Routen außer /api/auth/* und /api/health"
      contains: "validateSession"
  key_links:
    - from: "client/src/sync/engine.ts"
      to: "server/src/routes/sync.ts"
      via: "Authorization: Bearer <token> Header in fetch()"
      pattern: "Authorization"
    - from: "server/src/routes/auth.ts"
      to: "server/src/lib/sessions.ts"
      via: "createSession(token, shopId) nach erfolgreichem PIN-Check"
      pattern: "createSession"
    - from: "server/src/index.ts"
      to: "server/src/lib/sessions.ts"
      via: "validateSession(token) in preHandler-Hook"
      pattern: "validateSession"
---

<objective>
Zwei Security-Features: Rate-Limiting auf dem PIN-Endpoint verhindert Brute-Force-Angriffe. Server-seitige Session-Validierung stellt sicher dass ein Token nur auf Daten der zugehörigen shopId zugreifen kann.

Purpose: 4-stelliger PIN hat 10.000 Kombinationen — ohne Rate-Limiting ist er in Sekunden erschöpft. ShopId-Isolation beruht derzeit auf Client-Ehrlichkeit — kein Server-Enforcement.
Output: @fastify/rate-limit auf /api/auth/pin + In-Memory-Session-Store + shopId-Validierungs-Middleware + Client sendet Authorization-Header
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: @fastify/rate-limit installieren + Session-Store + Auth-Middleware</name>
  <files>
    server/package.json,
    server/src/lib/sessions.ts,
    server/src/routes/auth.ts,
    server/src/index.ts
  </files>
  <read_first>
    - server/package.json (aktuelle Dependencies, Versions-Kontext)
    - server/src/routes/auth.ts (aktueller PIN-Handler — kein Token-Management, nur randomUUID)
    - server/src/index.ts (aktueller Stand nach Plan 01 — CORS-Middleware-Block)
  </read_first>
  <action>
**Schritt 1: @fastify/rate-limit installieren**

Im Verzeichnis `server/` ausführen:
```bash
npm install @fastify/rate-limit
```

**Schritt 2: server/src/lib/sessions.ts erstellen (neu)**

```typescript
// In-Memory Session Store
// Token → { shopId, createdAt }
// Kein DB-Overhead; Sessions überleben keinen Server-Neustart (akzeptabel — Client logged automatisch re-ein)

interface Session {
  shopId: string;
  createdAt: number;
}

const sessions = new Map<string, Session>();

// Session lifetime: 30 Tage (wie idb-keyval auf Client, kein expliziter Ablauf)
// Für jetzt: Sessions bleiben bis Server-Neustart

export function createSession(token: string, shopId: string): void {
  sessions.set(token, { shopId, createdAt: Date.now() });
}

export function validateSession(token: string): Session | null {
  return sessions.get(token) ?? null;
}

export function deleteSession(token: string): void {
  sessions.delete(token);
}

// Sichtbar für /api/auth/logout (optional, für sauberes Logout)
export function getSessionCount(): number {
  return sessions.size;
}
```

**Schritt 3: server/src/routes/auth.ts erweitern**

`@fastify/rate-limit` in auth.ts als Route-spezifische Option setzen, NICHT global. `createSession()` aus sessions.ts aufrufen:

```typescript
import type { FastifyInstance } from 'fastify';
import { z } from 'zod';
import { eq } from 'drizzle-orm';
import { db } from '../db/index.js';
import { shops } from '../db/schema.js';
import { hashPin } from '../lib/crypto.js';
import { createSession } from '../lib/sessions.js';

const PinAuthSchema = z.object({
  pin: z.string().min(4).max(8),
});

export async function authRoutes(fastify: FastifyInstance) {
  fastify.post('/auth/pin', {
    config: {
      rateLimit: {
        max: 5,
        timeWindow: '1 minute',
        errorResponseBuilder: () => ({
          error: 'Zu viele Versuche. Bitte 1 Minute warten.',
        }),
      },
    },
  }, async (request, reply) => {
    const result = PinAuthSchema.safeParse(request.body);
    if (!result.success) return reply.status(400).send({ error: 'Ungültige Anfrage' });

    const { pin } = result.data;
    const pinHash = await hashPin(pin);

    const shop = db.select().from(shops).where(eq(shops.pin, pinHash)).get();
    if (!shop) return reply.status(401).send({ error: 'Falscher PIN' });

    const token = crypto.randomUUID();
    createSession(token, shop.shopId);

    return reply.send({
      shopId: shop.shopId,
      shopName: shop.name,
      token,
    });
  });
}
```

**Schritt 4: server/src/index.ts — Rate-Limit Plugin registrieren + Auth-Middleware**

Bestehende CORS-Blöcke aus Plan 01 bleiben unverändert. Ergänzungen:

```typescript
import rateLimit from '@fastify/rate-limit';
import { validateSession } from './lib/sessions.js';

// Nach CORS-Registrierung:
await fastify.register(rateLimit, {
  global: false, // Nur auf explizit markierten Routen aktiv
});

// Auth-Middleware: alle /api/* Routen außer /api/auth/* und /api/health
fastify.addHook('preHandler', async (request, reply) => {
  const url = request.url;
  // Öffentliche Endpoints: Auth (Login) + Health
  if (url.startsWith('/api/auth/') || url.startsWith('/api/health')) {
    return;
  }

  const authHeader = request.headers['authorization'];
  if (!authHeader || !authHeader.startsWith('Bearer ')) {
    return reply.status(401).send({ error: 'Authentifizierung erforderlich' });
  }

  const token = authHeader.slice(7); // "Bearer " entfernen
  const session = validateSession(token);
  if (!session) {
    return reply.status(401).send({ error: 'Ungültiger oder abgelaufener Token' });
  }

  // Session an Request anhängen für shopId-Validierung in Routen
  (request as any).session = session;
});
```

Die Import-Zeilen oben ergänzen die bestehenden Imports in index.ts.
  </action>
  <verify>
    <automated>ls /Users/simonluthe/Documents/fairstand/server/src/lib/sessions.ts && grep -n "createSession\|validateSession" /Users/simonluthe/Documents/fairstand/server/src/routes/auth.ts</automated>
  </verify>
  <done>sessions.ts existiert mit createSession/validateSession/deleteSession. auth.ts importiert createSession und ruft es nach erfolgreichem PIN-Check auf. index.ts registriert rate-limit global:false und hat preHandler-Hook.</done>
  <acceptance_criteria>
    - `ls server/src/lib/sessions.ts` gibt die Datei zurück (existiert)
    - `grep "createSession" server/src/routes/auth.ts` gibt mindestens 2 Treffer (Import + Aufruf)
    - `grep "rateLimit" server/src/routes/auth.ts` zeigt den config.rateLimit Block
    - `grep "global: false" server/src/index.ts` zeigt die Rate-Limit-Registrierung
    - `grep "preHandler" server/src/index.ts` zeigt den Auth-Hook
    - `grep "@fastify/rate-limit" server/package.json` zeigt die neue Dependency
    - `cd /Users/simonluthe/Documents/fairstand/server && npx tsc --noEmit` gibt keinen Fehler
  </acceptance_criteria>
</task>

<task type="auto" tdd="false">
  <name>Task 2: ShopId-Validierung in sync.ts + products.ts + categories.ts + reports.ts + settings.ts und Authorization-Header im Client</name>
  <files>
    server/src/routes/sync.ts,
    server/src/routes/products.ts,
    server/src/routes/categories.ts,
    server/src/routes/reports.ts,
    server/src/routes/settings.ts,
    client/src/sync/engine.ts,
    client/src/features/auth/serverAuth.ts
  </files>
  <read_first>
    - server/src/routes/sync.ts (Zeilen 62-80 — wie shopId derzeit aus Payload kommt, kein Auth-Check)
    - server/src/routes/products.ts (erste 50 Zeilen — GET /api/products?shopId=xxx Pattern)
    - server/src/routes/categories.ts (erste 30 Zeilen — GET /api/categories?shopId=xxx Pattern)
    - server/src/routes/reports.ts (erste 30 Zeilen — GET /reports/monthly?shopId=xxx Pattern)
    - server/src/routes/settings.ts (erste 30 Zeilen — shopId aus Query oder Body)
    - client/src/sync/engine.ts (flushOutbox und downloadProducts — fetch ohne Auth-Header)
    - client/src/features/auth/serverAuth.ts (StoredSession interface mit token-Feld)
  </read_first>
  <action>
**Strategie:** Der preHandler-Hook in index.ts (Task 1) hängt `request.session = { shopId, createdAt }` an jeden authentifizierten Request. Die Routen lesen `session.shopId` und vergleichen mit dem `shopId` aus Query-Params oder Request-Body. Bei Mismatch: 403.

**SERVER: ShopId-Validierung in den Routen**

Hilfsfunktion als TypeScript-Pattern (kein separates Modul nötig, inline in jeder Route):

```typescript
// Am Anfang jedes Handlers wo shopId aus Query/Body kommt:
const session = (request as any).session as { shopId: string };
const requestedShopId = /* shopId aus Query oder Payload */;
if (session.shopId !== requestedShopId) {
  return reply.status(403).send({ error: 'Zugriff verweigert: falsche shopId' });
}
```

**server/src/routes/sync.ts — POST /api/sync:**
In `syncRoutes`, im Handler für POST /api/sync, nach Zod-Validierung:
```typescript
const session = (request as any).session as { shopId: string };
// Alle Entries müssen zum Shop der Session gehören
for (const entry of result.data.entries) {
  if (entry.shopId !== session.shopId) {
    return reply.status(403).send({ error: 'Zugriff verweigert: shopId stimmt nicht überein' });
  }
}
```
Dieser Check kommt nach `SyncBatchSchema.safeParse` und vor der `for`-Schleife.

**server/src/routes/products.ts — GET /api/products und PUT/POST:**
Im GET-Handler: `const session = (request as any).session as { shopId: string }; const shopId = session.shopId;` — `shopId` aus Query-Params ignorieren, aus Session lesen. Analog für POST (Produkt anlegen): `shopId` aus Session statt aus Request-Body.

**server/src/routes/categories.ts — alle Handler:**
Gleiche Strategie: shopId aus Session statt aus Query/Body lesen.

**server/src/routes/reports.ts — GET /reports/monthly, /reports/yearly, /reports/daily:**
Im jeweiligen Handler: `const session = (request as any).session as { shopId: string }; const shopId = session.shopId;` — Query-Param shopId ignorieren.

**server/src/routes/settings.ts — GET/PUT /settings:**
shopId aus Session lesen statt aus Query/Body.

**CLIENT: Authorization-Header hinzufügen**

`client/src/features/auth/serverAuth.ts` — neue Hilfsfunktion:
```typescript
// Auth-Header für API-Requests (Token aus gespeicherter Session)
export async function getAuthHeaders(): Promise<Record<string, string>> {
  const session = await getStoredSession();
  if (!session?.token) return { 'Content-Type': 'application/json' };
  return {
    'Content-Type': 'application/json',
    'Authorization': `Bearer ${session.token}`,
  };
}
```

`client/src/sync/engine.ts` — alle `fetch()` Aufrufe mit Auth-Header:
```typescript
import { getAuthHeaders, getStoredSession } from '../features/auth/serverAuth.js';

// flushOutbox — POST /api/sync:
res = await fetch('/api/sync', {
  method: 'POST',
  headers: await getAuthHeaders(),
  body: JSON.stringify({ entries: pending }),
});

// downloadProducts — GET /api/products:
const headers = await getAuthHeaders();
const res = await fetch(`/api/products?shopId=${shopId}`, { headers });

// downloadCategories — GET /api/categories:
const res = await fetch(`/api/categories?shopId=${shopId}`, { headers });
```

Hinweis: `getAuthHeaders()` gibt `{ 'Content-Type': 'application/json', 'Authorization': 'Bearer ...' }` zurück. Bei GET-Requests den Content-Type weglassen oder der Browser ignoriert ihn ohnehin.

Alle anderen Client-Calls (ProductForm fetch, AdminScreen CRUD) analog mit `await getAuthHeaders()` in den jeweiligen `fetch()`-Aufrufen. Diese liegen in:
- `client/src/features/admin/ProductForm.tsx` (oder ähnlich)
- `client/src/features/admin/AdminScreen.tsx`
- `client/src/features/reports/MonthlyReport.tsx`
- `client/src/features/reports/DailyReport.tsx`

Für alle fetch-Aufrufe in client/src die gegen `/api/` gehen und noch keinen Auth-Header haben, `getAuthHeaders()` verwenden. Prüfe mit: `grep -rn "fetch('/api" client/src --include="*.ts" --include="*.tsx"`.
  </action>
  <verify>
    <automated>grep -rn "getAuthHeaders\|Authorization.*Bearer" /Users/simonluthe/Documents/fairstand/client/src/sync/engine.ts</automated>
  </verify>
  <done>engine.ts verwendet getAuthHeaders() in allen fetch-Aufrufen. Server-Routen (sync.ts, products.ts, categories.ts) lesen shopId aus request.session statt aus Client-Payloads.</done>
  <acceptance_criteria>
    - `grep "session.shopId" server/src/routes/sync.ts` gibt mindestens 1 Treffer (403-Check)
    - `grep "getAuthHeaders" client/src/sync/engine.ts` gibt mindestens 2 Treffer (flushOutbox + downloadProducts)
    - `grep "getAuthHeaders" client/src/features/auth/serverAuth.ts` zeigt die neue Export-Funktion
    - `grep "Authorization" client/src/sync/engine.ts` zeigt den Bearer-Header
    - `grep -rn "fetch.*api/" client/src --include="*.ts" --include="*.tsx" | grep -v "getAuthHeaders\|Authorization"` sollte keine ungeschützten Calls mehr zeigen (außer /api/auth/pin welches bewusst public ist)
    - `cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit` (root-level falls vorhanden) ODER `cd server && npx tsc --noEmit && cd ../client && npx tsc --noEmit` gibt keine Fehler
  </acceptance_criteria>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
1. `grep "rateLimit" server/src/routes/auth.ts` → zeigt config.rateLimit Block
2. `grep "validateSession" server/src/index.ts` → zeigt preHandler-Hook
3. `grep "session.shopId" server/src/routes/sync.ts` → zeigt 403-Validierung
4. `grep "getAuthHeaders" client/src/sync/engine.ts` → zeigt Auth-Header in fetch-Calls
5. TypeScript-Build fehlerfrei
</verification>

<success_criteria>
- POST /api/auth/pin: nach 5 Versuchen von gleicher IP → 429 (Rate-Limit aktiv, global:false)
- POST /api/sync ohne Authorization-Header → 401
- POST /api/sync mit gültigem Token aber falscher shopId in Entries → 403
- GET /api/products ohne Token → 401
- GET /api/health → 200 (Whitelist greift)
- Client sendet Authorization: Bearer <token> bei allen /api/* Requests außer /api/auth/pin
</success_criteria>

<output>
Nach Abschluss `.planning/phases/18-quick-wins-security/18-03-SUMMARY.md` erstellen.
</output>
