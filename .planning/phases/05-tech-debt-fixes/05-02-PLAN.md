---
phase: 05-tech-debt-fixes
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - client/src/sync/engine.ts
  - client/src/features/admin/products/ProductList.tsx
autonomous: true
requirements: [TD-03]

must_haves:
  truths:
    - "Frischer Client kann Produktdaten vom Server laden"
    - "Download-Sync ueberschreibt lokale Daten nur wenn Server-Daten neuer sind (LWW)"
    - "Nutzer kann manuell Daten laden oder es passiert automatisch beim App-Start"
  artifacts:
    - path: "client/src/sync/engine.ts"
      provides: "downloadProducts-Funktion fuer Server→Client Sync"
      contains: "downloadProducts"
    - path: "client/src/features/admin/products/ProductList.tsx"
      provides: "Daten laden Button fuer manuellen Download-Sync"
      contains: "downloadProducts"
  key_links:
    - from: "client/src/sync/engine.ts"
      to: "GET /api/products?shopId=xxx"
      via: "fetch-Aufruf in downloadProducts"
      pattern: "fetch.*api/products"
    - from: "client/src/sync/engine.ts"
      to: "client/src/db/schema.ts"
      via: "Dexie bulkPut mit LWW-Vergleich"
      pattern: "products\\.put\\|products\\.bulkPut"
---

<objective>
Download-Sync implementieren: Frische Clients koennen Produktdaten vom Server laden. Bisher ist Sync nur Upload (Outbox → Server). Jetzt wird der Rueckkanal gebaut.

Purpose: Neues iPad oder geloeschte App-Daten koennen sofort wieder mit Produktdaten versorgt werden — ohne manuelles Nachpflegen.
Output: downloadProducts-Funktion in engine.ts, automatischer Trigger bei App-Start + manueller Button.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-tech-debt-fixes/05-01-SUMMARY.md

@client/src/sync/engine.ts
@client/src/db/schema.ts
@client/src/db/index.ts
@client/src/features/admin/products/ProductList.tsx
@server/src/routes/products.ts

<interfaces>
<!-- Server-Endpoint existiert bereits -->
From server/src/routes/products.ts (Zeilen 24-29):
```typescript
// GET /products?shopId=xxx — alle Produkte eines Shops
fastify.get('/products', async (request, reply) => {
  const { shopId } = request.query as { shopId: string };
  if (!shopId) return reply.status(400).send({ error: 'shopId required' });
  const rows = db.select().from(products).where(eq(products.shopId, shopId)).all();
  return reply.send(rows);
});
```
// Response ist Array von Product-Objekten mit snake_case Keys (Drizzle default):
// { id, shop_id, article_number, name, category, purchase_price, sale_price, vat_rate, stock, min_stock, active, updated_at }

From client/src/db/schema.ts:
```typescript
export interface Product {
  id: string;
  shopId: string;
  articleNumber: string;
  name: string;
  category: string;
  purchasePrice: number;
  salePrice: number;
  vatRate: number;
  stock: number;
  minStock: number;
  active: boolean;
  updatedAt: number;
}
```

From client/src/db/index.ts:
```typescript
export const db = new FairstandDB();
export const SHOP_ID = 'st-secundus-hennstedt';
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: downloadProducts-Funktion in engine.ts</name>
  <files>client/src/sync/engine.ts</files>
  <read_first>
    - client/src/sync/engine.ts (vollstaendig — flushOutbox Funktion, Imports)
    - client/src/db/schema.ts (Product Interface auf Zeile 3-16)
    - client/src/db/index.ts (db + SHOP_ID Export)
    - server/src/routes/products.ts (GET /products Response-Format Zeile 24-29)
  </read_first>
  <action>
**Neue Export-Funktion `downloadProducts` am Ende von engine.ts hinzufuegen:**

WICHTIG: Der Server antwortet mit Drizzle-Spaltennamen (snake_case): `shop_id`, `article_number`, `purchase_price`, `sale_price`, `vat_rate`, `min_stock`, `updated_at`. Diese muessen auf das Client-seitige Product-Interface (camelCase) gemappt werden.

1. Import ergaenzen am Anfang der Datei:
```typescript
import { SHOP_ID } from '../db/index.js';
import type { Product } from '../db/index.js';
```

2. Interface fuer Server-Response definieren (snake_case):
```typescript
interface ServerProduct {
  id: string;
  shop_id: string;
  article_number: string;
  name: string;
  category: string;
  purchase_price: number;
  sale_price: number;
  vat_rate: number;
  stock: number;
  min_stock: number;
  active: boolean | number; // SQLite boolean kann 0/1 sein
  updated_at: number;
}
```

3. downloadProducts-Funktion:
```typescript
export async function downloadProducts(): Promise<number> {
  const res = await fetch(`/api/products?shopId=${SHOP_ID}`);
  if (!res.ok) throw new Error(`Download fehlgeschlagen: ${res.status}`);
  const serverProducts: ServerProduct[] = await res.json();

  let upserted = 0;
  for (const sp of serverProducts) {
    const mapped: Product = {
      id: sp.id,
      shopId: sp.shop_id,
      articleNumber: sp.article_number,
      name: sp.name,
      category: sp.category,
      purchasePrice: sp.purchase_price,
      salePrice: sp.sale_price,
      vatRate: sp.vat_rate,
      stock: sp.stock,
      minStock: sp.min_stock,
      active: Boolean(sp.active),
      updatedAt: sp.updated_at,
    };

    // LWW: Nur ueberschreiben wenn Server-Daten neuer
    const existing = await db.products.get(mapped.id);
    if (!existing || mapped.updatedAt > existing.updatedAt) {
      await db.products.put(mapped);
      upserted++;
    }
  }
  return upserted;
}
```

4. Automatischen Download bei App-Start triggern — am Ende der Datei:
```typescript
// Download-Sync bei App-Start wenn online
if (typeof window !== 'undefined' && navigator.onLine) {
  downloadProducts().catch(() => {});
}
```

Returnwert `number` ermoeglicht dem UI anzuzeigen wie viele Produkte aktualisiert wurden.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "downloadProducts" client/src/sync/engine.ts (Funktion existiert)
    - grep -q "api/products" client/src/sync/engine.ts (Server-Endpoint aufgerufen)
    - grep -q "updatedAt > existing.updatedAt" client/src/sync/engine.ts (LWW-Vergleich)
    - grep -q "ServerProduct" client/src/sync/engine.ts (Snake-Case Mapping)
    - grep -q "navigator.onLine" client/src/sync/engine.ts (Auto-Trigger bei Start)
  </acceptance_criteria>
  <done>
    - downloadProducts fetcht alle Produkte vom Server fuer den aktuellen Shop
    - Snake-Case → CamelCase Mapping korrekt
    - LWW-Vergleich: Nur neuere Produkte ueberschreiben lokale Daten
    - Automatischer Download bei App-Start wenn online
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

<task type="auto">
  <name>Task 2: Manueller "Daten laden" Button in ProductList</name>
  <files>client/src/features/admin/products/ProductList.tsx</files>
  <read_first>
    - client/src/features/admin/products/ProductList.tsx (vollstaendig — Header-Bereich Zeile 84-93)
    - client/src/sync/engine.ts (downloadProducts Export — aus Task 1)
  </read_first>
  <action>
**Manuellen Download-Button in ProductList.tsx hinzufuegen:**

1. Import am Anfang ergaenzen:
```typescript
import { downloadProducts } from '../../../sync/engine.js';
```

2. Neuen State fuer Download-Status hinzufuegen (nach Zeile 14):
```typescript
const [syncing, setSyncing] = useState(false);
const [syncResult, setSyncResult] = useState<string | null>(null);
```

3. Handler-Funktion erstellen (nach handleToggleActive):
```typescript
async function handleDownloadSync() {
  if (!navigator.onLine) {
    setSyncResult('Keine Internetverbindung');
    return;
  }
  setSyncing(true);
  setSyncResult(null);
  try {
    const count = await downloadProducts();
    setSyncResult(`${count} Produkt${count !== 1 ? 'e' : ''} aktualisiert`);
  } catch {
    setSyncResult('Sync fehlgeschlagen');
  } finally {
    setSyncing(false);
  }
}
```

4. Button im Header-Bereich hinzufuegen (Zeile 85-93, zwischen h2 und "Neues Produkt"-Button). Die bestehende `<div className="flex items-center justify-between">` wird erweitert — rechte Seite bekommt eine Button-Gruppe:
```tsx
<div className="flex items-center justify-between">
  <h2 className="text-lg font-semibold text-sky-800">Produkte</h2>
  <div className="flex items-center gap-2">
    <button
      onPointerDown={handleDownloadSync}
      disabled={syncing}
      className="bg-slate-100 hover:bg-slate-200 active:bg-slate-300 text-slate-700 font-medium px-4 py-2 rounded-lg h-11 text-sm transition-colors disabled:opacity-50"
    >
      {syncing ? 'Laden...' : 'Daten laden'}
    </button>
    <button
      onPointerDown={openNewForm}
      className="bg-sky-500 hover:bg-sky-600 active:bg-sky-700 text-white font-semibold px-4 py-2 rounded-lg h-11 text-sm transition-colors"
    >
      + Neues Produkt
    </button>
  </div>
</div>
```

5. Sync-Ergebnis-Anzeige unter dem Header (nach dem Kategorie-Filter, vor der Produktliste):
```tsx
{syncResult && (
  <div className="bg-sky-50 text-sky-700 text-sm px-4 py-2 rounded-lg">
    {syncResult}
  </div>
)}
```

Ergebnis verschwindet NICHT automatisch — bleibt bis naechster Sync oder Navigation. Das ist gewollt: Nutzer sieht Feedback.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit --project client/tsconfig.json 2>&1 | head -30</automated>
  </verify>
  <acceptance_criteria>
    - grep -q "downloadProducts" client/src/features/admin/products/ProductList.tsx (Import vorhanden)
    - grep -q "Daten laden" client/src/features/admin/products/ProductList.tsx (Button-Text)
    - grep -q "handleDownloadSync" client/src/features/admin/products/ProductList.tsx (Handler-Funktion)
    - grep -q "syncResult" client/src/features/admin/products/ProductList.tsx (Ergebnis-Anzeige)
  </acceptance_criteria>
  <done>
    - "Daten laden" Button im ProductList-Header sichtbar
    - Button zeigt "Laden..." waehrend Sync laeuft (disabled)
    - Ergebnis wird angezeigt: "X Produkte aktualisiert" oder Fehlermeldung
    - Offline-Check verhindert sinnlosen Fetch-Versuch
    - TypeScript kompiliert fehlerfrei
  </done>
</task>

</tasks>

<verification>
- `npx tsc --noEmit --project client/tsconfig.json` — Client kompiliert fehlerfrei
- `grep "downloadProducts" client/src/sync/engine.ts` — Funktion existiert
- `grep "Daten laden" client/src/features/admin/products/ProductList.tsx` — Button vorhanden
- `grep "api/products" client/src/sync/engine.ts` — Server-Endpoint aufgerufen
</verification>

<success_criteria>
1. downloadProducts-Funktion fetcht Produkte vom Server und upserted in Dexie mit LWW
2. Automatischer Download bei App-Start wenn online
3. Manueller "Daten laden" Button in der Produktverwaltung
4. Snake-Case → CamelCase Mapping fuer Server-Response
5. Feedback-Anzeige fuer Sync-Ergebnis
6. TypeScript kompiliert fehlerfrei
</success_criteria>

<output>
After completion, create `.planning/phases/05-tech-debt-fixes/05-02-SUMMARY.md`
</output>
