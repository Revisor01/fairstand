---
phase: 07-server-sync-multi-laden
plan: 02
type: execute
wave: 2
depends_on:
  - "07-01"
files_modified:
  - client/src/features/auth/serverAuth.ts
  - client/src/features/auth/useAuth.ts
  - client/src/db/index.ts
  - client/src/db/schema.ts
autonomous: true
requirements:
  - SHOP-03
  - SHOP-04
  - SYNC-02

must_haves:
  truths:
    - "Nach PIN-Eingabe wird POST /api/auth/pin aufgerufen — bei Erfolg wird shopId + token lokal gespeichert"
    - "Beim App-Start mit gültigem lokalem Session-Token überspringt die App den PIN-Dialog und öffnet direkt"
    - "shopId ist dynamisch (aus serverAuth) — keine hardcodierte SHOP_ID-Konstante mehr in client/src/db/index.ts"
    - "Dexie-DB wird auf Version 3 gehoben — beim Upgrade werden alle lokalen Produkte gelöscht (Hard Reset per D-01)"
    - "getShopId() gibt die aktuell angemeldete shopId zurück oder wirft wenn kein Shop gesetzt"
  artifacts:
    - path: "client/src/features/auth/serverAuth.ts"
      provides: "Server-PIN-Auth + Session-Persistenz in idb-keyval"
      exports: ["serverLogin", "getStoredSession", "clearSession", "updateActivity", "isSessionValid"]
    - path: "client/src/db/index.ts"
      provides: "db-Export ohne SHOP_ID + getShopId() Funktion"
      exports: ["db", "getShopId", "setShopId"]
    - path: "client/src/db/schema.ts"
      provides: "Dexie Version 3 mit Upgrade-Funktion (Hard Reset)"
  key_links:
    - from: "client/src/features/auth/useAuth.ts"
      to: "client/src/features/auth/serverAuth.ts"
      via: "serverLogin(), getStoredSession(), clearSession()"
      pattern: "serverLogin|getStoredSession"
    - from: "client/src/db/index.ts"
      to: "client/src/features/auth/serverAuth.ts"
      via: "getShopId() liest aus Modul-Variable die durch setShopId() gesetzt wird"
      pattern: "getShopId|setShopId"
---

<objective>
Client-seitige Auth-Architektur: serverAuth.ts ersetzt pinAuth.ts, SHOP_ID wird dynamisch, Dexie-DB Hard-Reset auf v3.

Purpose: Jede Komponente, die bisher `SHOP_ID` importiert hat, bekommt stattdessen `getShopId()`. Auth-Flow prüft Server statt lokalen Hash. Nach diesem Plan kann Plan 03 die Sync-Engine und alle UI-Komponenten auf das neue System umstellen.
Output: serverAuth.ts mit Server-PIN-Auth; db/index.ts ohne SHOP_ID-Konstante; Dexie Version 3 mit Hard-Reset-Upgrade; useAuth nutzt serverAuth.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-server-sync-multi-laden/07-01-SUMMARY.md

<interfaces>
<!-- Bestehende Client-Dateien — Executor liest diese vor dem Ändern -->

Aus client/src/features/auth/pinAuth.ts (WIRD ERSETZT durch serverAuth.ts):
```typescript
// Bestehende Exports die useAuth.ts verwendet:
export async function verifyPin(pin: string): Promise<boolean>
export async function setupPin(pin: string): Promise<void>
export async function updateActivity(): Promise<void>
export async function isSessionValid(): Promise<boolean>
export async function hasPinSetup(): Promise<boolean>
export async function logout(): Promise<void>
// idb-keyval Keys: 'pinHash', 'lastActivity'
```

Aus client/src/features/auth/useAuth.ts (WIRD GEÄNDERT):
```typescript
type AuthState = 'checking' | 'setup' | 'locked' | 'unlocked';
export function useAuth() {
  // Gibt zurück: { state, unlock, setup, lock }
  // unlock(pin) → boolean
  // App.tsx erwartet dieses Interface — NICHT verändern
}
```

Aus client/src/db/index.ts (WIRD GEÄNDERT):
```typescript
export const db = new FairstandDB();
export const SHOP_ID = 'st-secundus-hennstedt'; // ← ENTFERNEN
// Neue Exports nötig: getShopId(), setShopId()
```

Aus client/src/db/schema.ts (Dexie-Version-Bump):
```typescript
// Aktuelle Version: 2
// Version 3: Upgrade-Funktion löscht alle Produkte (Hard Reset)
this.version(2).stores({...}).upgrade(...);
// NEU:
this.version(3).stores({
  products: 'id, shopId, category, active, [shopId+active]',
  sales: 'id, shopId, createdAt, syncedAt',
  outbox: '++id, shopId, createdAt, operation',
}).upgrade(async tx => {
  await tx.table('products').clear(); // Hard Reset: alte Produkte löschen
});
```

Aus server/src/routes/auth.ts (07-01-SUMMARY lesen für konkretes Response-Format):
```typescript
// POST /api/auth/pin Response:
// { shopId: string, shopName: string, token: string }
// 401 bei falschem PIN: { error: 'Falscher PIN' }
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: serverAuth.ts erstellen + db/index.ts auf getShopId() umstellen</name>
  <files>
    client/src/features/auth/serverAuth.ts
    client/src/db/index.ts
  </files>
  <read_first>
    - client/src/features/auth/pinAuth.ts (vollständig — Vorgänger-Datei, wird durch serverAuth.ts ersetzt)
    - client/src/db/index.ts (vollständig — SHOP_ID wird entfernt, getShopId/setShopId hinzugefügt)
  </read_first>
  <action>
**A) client/src/features/auth/serverAuth.ts** (neue Datei — pinAuth.ts NICHT löschen, Plan 03 passt Imports an):

```typescript
import { get, set, del } from 'idb-keyval';

// Session-Daten die lokal gespeichert werden
export interface StoredSession {
  shopId: string;
  shopName: string;
  token: string;
  lastActivity: number; // Unix-Timestamp ms
}

// POST /api/auth/pin aufrufen und Session lokal speichern
export async function serverLogin(pin: string): Promise<StoredSession | null> {
  try {
    const res = await fetch('/api/auth/pin', {
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      body: JSON.stringify({ pin }),
    });
    if (!res.ok) return null;
    const data = await res.json() as { shopId: string; shopName: string; token: string };
    const session: StoredSession = {
      shopId: data.shopId,
      shopName: data.shopName,
      token: data.token,
      lastActivity: Date.now(),
    };
    await set('session', session);
    return session;
  } catch {
    // Netzwerkfehler — offline-Fallback (getStoredSession prüft gespeicherte Session)
    return null;
  }
}

// Gespeicherte Session laden (für Offline-Fallback + App-Start-Check)
export async function getStoredSession(): Promise<StoredSession | null> {
  return await get<StoredSession>('session') ?? null;
}

// Session löschen (Logout)
export async function clearSession(): Promise<void> {
  await del('session');
}

// lastActivity aktualisieren (für Session-Timeout)
export async function updateActivity(): Promise<void> {
  const session = await getStoredSession();
  if (!session) return;
  await set('session', { ...session, lastActivity: Date.now() });
}

// Session-Timeout: 2 Stunden (120 Min) — identisch zu bisherigem pinAuth.ts
export async function isSessionValid(): Promise<boolean> {
  const session = await getStoredSession();
  if (!session) return false;
  return Date.now() - session.lastActivity < 120 * 60 * 1000;
}
```

**B) client/src/db/index.ts** — SHOP_ID entfernen, getShopId/setShopId hinzufügen:

```typescript
import { FairstandDB } from './schema.js';
export type { Product, Sale, SaleItem, CartItem, OutboxEntry } from './schema.js';

export const db = new FairstandDB();

// Dynamische Shop-ID (wird nach Login über setShopId gesetzt)
let _shopId: string | null = null;

export function setShopId(id: string): void {
  _shopId = id;
}

export function getShopId(): string {
  if (!_shopId) throw new Error('Shop nicht gesetzt — zuerst einloggen');
  return _shopId;
}
```

Wichtig: `export const SHOP_ID` vollständig entfernen. Die Variable `_shopId` ist null bis nach erfolgreichem Login.
  </action>
  <verify>
    <automated>grep -n "SHOP_ID" /Users/simonluthe/Documents/fairstand/client/src/db/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SHOP_ID" client/src/db/index.ts` gibt keinen Treffer (SHOP_ID vollständig entfernt)
    - `grep "export function getShopId" client/src/db/index.ts` gibt Treffer
    - `grep "export function setShopId" client/src/db/index.ts` gibt Treffer
    - `grep "export async function serverLogin" client/src/features/auth/serverAuth.ts` gibt Treffer
    - `grep "export async function getStoredSession" client/src/features/auth/serverAuth.ts` gibt Treffer
    - serverAuth.ts exportiert: serverLogin, getStoredSession, clearSession, updateActivity, isSessionValid
  </acceptance_criteria>
  <done>serverAuth.ts erstellt; db/index.ts ohne SHOP_ID; getShopId()/setShopId() verfügbar</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: useAuth.ts auf serverAuth umstellen + Dexie Version 3 Hard Reset</name>
  <files>
    client/src/features/auth/useAuth.ts
    client/src/db/schema.ts
  </files>
  <read_first>
    - client/src/features/auth/useAuth.ts (vollständig — wird umgeschrieben)
    - client/src/db/schema.ts (vollständig — Version 3 wird hinzugefügt)
    - client/src/features/auth/serverAuth.ts (aus Task 1 — neue Auth-Funktionen)
    - client/src/db/index.ts (aus Task 1 — setShopId muss nach Login aufgerufen werden)
  </read_first>
  <action>
**A) client/src/features/auth/useAuth.ts** — komplett neu schreiben (gleiche Signatur, neue Logik):

```typescript
import { useState, useEffect, useCallback } from 'react';
import {
  serverLogin,
  getStoredSession,
  clearSession,
  updateActivity,
  isSessionValid,
} from './serverAuth.js';
import { setShopId } from '../../db/index.js';

type AuthState = 'checking' | 'setup' | 'locked' | 'unlocked';

export function useAuth() {
  const [state, setState] = useState<AuthState>('checking');

  useEffect(() => {
    (async () => {
      // Gespeicherte Session prüfen
      const valid = await isSessionValid();
      if (valid) {
        const session = await getStoredSession();
        if (session) {
          setShopId(session.shopId); // Shop-Kontext setzen
          setState('unlocked');
          return;
        }
      }
      // Kein Setup-Mode mehr (server-seitig konfiguriert) — direkt zu locked
      setState('locked');
    })();
  }, []);

  // Activity-Timer: bei Benutzerinteraktion lastActivity aktualisieren
  useEffect(() => {
    if (state !== 'unlocked') return;
    const handler = () => updateActivity();
    window.addEventListener('pointerdown', handler);
    return () => window.removeEventListener('pointerdown', handler);
  }, [state]);

  const unlock = useCallback(async (pin: string): Promise<boolean> => {
    // Versuche Server-Auth (online)
    if (navigator.onLine) {
      const session = await serverLogin(pin);
      if (session) {
        setShopId(session.shopId);
        setState('unlocked');
        return true;
      }
      return false;
    }
    // Offline-Fallback: gespeicherte Session prüfen (Laden hat sich bereits eingeloggt)
    const stored = await getStoredSession();
    if (!stored) return false;
    // Offline: PIN kann nicht geprüft werden — nur wenn Session noch nicht abgelaufen war
    const valid = await isSessionValid();
    if (valid) {
      setShopId(stored.shopId);
      setState('unlocked');
      return true;
    }
    return false;
  }, []);

  // setup wird nicht mehr benötigt (server-seitig konfiguriert), bleibt aber für App.tsx-Kompatibilität
  const setup = useCallback(async (_pin: string): Promise<void> => {
    // No-op: PIN-Setup erfolgt nur server-seitig durch Admin
  }, []);

  const lock = useCallback(async () => {
    await clearSession();
    setState('locked');
  }, []);

  return { state, unlock, setup, lock };
}
```

Wichtig: `state === 'setup'` wird von App.tsx noch gerendert, aber useAuth gibt diesen Zustand nicht mehr zurück (setState('locked') statt 'setup'). PinScreen mode="setup" wird nie gezeigt — das ist korrekt per CONTEXT.md-Entscheidung.

**B) client/src/db/schema.ts** — Version 3 mit Hard Reset hinzufügen:

Nach der bestehenden version(2)-Definition folgendes hinzufügen:

```typescript
this.version(3).stores({
  products: 'id, shopId, category, active, [shopId+active]',
  sales: 'id, shopId, createdAt, syncedAt',
  outbox: '++id, shopId, createdAt, operation',
}).upgrade(async tx => {
  // Hard Reset: alle lokalen Produkte löschen
  // Beim v2.0-Update lädt die App alles neu vom Server
  await tx.table('products').clear();
});
```

Die Stores-Definition ist identisch zu Version 2 — nur das Upgrade löscht Produkte. Das sorgt dafür, dass beim Update von v1.x auf v2.0 keine veralteten Produkte ohne shopId-Bezug im lokalen Cache bleiben.
  </action>
  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --project client/tsconfig.json --noEmit 2>&1 | grep -E "error TS" | head -20</automated>
  </verify>
  <acceptance_criteria>
    - `grep "import.*pinAuth" client/src/features/auth/useAuth.ts` gibt keinen Treffer (kein Import von pinAuth mehr)
    - `grep "serverLogin\|getStoredSession" client/src/features/auth/useAuth.ts` gibt Treffer
    - `grep "setShopId" client/src/features/auth/useAuth.ts` gibt Treffer (wird nach Login aufgerufen)
    - `grep "version(3)" client/src/db/schema.ts` gibt Treffer
    - `grep "products.*clear" client/src/db/schema.ts` gibt Treffer (Hard Reset)
    - TypeScript-Kompilierung Client ohne Fehler in useAuth.ts und schema.ts
  </acceptance_criteria>
  <done>useAuth verwendet serverAuth; Dexie Version 3 löscht alte Produkte bei Update; kein Setup-Mode mehr</done>
</task>

</tasks>

<verification>
```bash
# Keine SHOP_ID mehr in db/index.ts
grep "SHOP_ID" client/src/db/index.ts

# serverAuth korrekt exportiert
grep "export async function" client/src/features/auth/serverAuth.ts

# useAuth importiert kein pinAuth mehr
grep "pinAuth" client/src/features/auth/useAuth.ts

# Dexie Version 3 vorhanden
grep "version(3)" client/src/db/schema.ts

# TypeScript Client-Check
npx tsc --project client/tsconfig.json --noEmit
```
</verification>

<success_criteria>
- serverAuth.ts implementiert mit serverLogin(), getStoredSession(), clearSession(), updateActivity(), isSessionValid()
- db/index.ts: SHOP_ID entfernt, getShopId()/setShopId() exportiert
- useAuth.ts: ruft serverLogin() beim unlock auf, setzt shopId via setShopId()
- Dexie Version 3: clear() auf products-Tabelle beim Upgrade
- TypeScript-Kompilierung beider geänderter Dateien ohne Fehler
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/07-server-sync-multi-laden/07-02-SUMMARY.md` erstellen.
</output>
