---
phase: 07-server-sync-multi-laden
plan: 03
type: execute
wave: 3
depends_on:
  - "07-01"
  - "07-02"
files_modified:
  - client/src/App.tsx
  - client/src/sync/engine.ts
  - client/src/features/pos/ArticleGrid.tsx
  - client/src/features/admin/products/ProductList.tsx
autonomous: true
requirements:
  - SYNC-03
  - SYNC-04
  - SHOP-03

must_haves:
  truths:
    - "Beim App-Start (online) werden Produkte automatisch vom Server geladen — keine lokale seedIfEmpty() mehr"
    - "Offline + leere DB: App zeigt 'Keine Verbindung' statt Seed-Daten"
    - "Nach erfolgreichem Outbox-Sync wird automatisch downloadProducts() ausgeführt"
    - "ArticleGrid und ProductList nutzen getShopId() statt hardcodierter SHOP_ID-Konstante"
    - "engine.ts importiert getShopId() statt SHOP_ID"
  artifacts:
    - path: "client/src/App.tsx"
      provides: "App ohne seedIfEmpty, mit Server-Download nach Login"
      contains: "downloadProducts"
    - path: "client/src/sync/engine.ts"
      provides: "Sync-Engine mit dynamischer shopId"
      contains: "getShopId()"
  key_links:
    - from: "client/src/App.tsx"
      to: "client/src/sync/engine.ts downloadProducts()"
      via: "useEffect nach state === 'unlocked' + navigator.onLine"
      pattern: "downloadProducts"
    - from: "client/src/sync/engine.ts flushOutbox()"
      to: "client/src/sync/engine.ts downloadProducts()"
      via: "nach erfolgreichem Outbox-Flush: downloadProducts().catch()"
      pattern: "downloadProducts.*catch"
---

<objective>
Alle Client-Dateien mit SHOP_ID-Import auf getShopId() umstellen; seedIfEmpty() entfernen; Sync-Engine nach Flush downloaden.

Purpose: Plan 02 hat die Infrastruktur geschaffen (getShopId, serverAuth). Dieser Plan verdrahtet alle Consumer. Nach diesem Plan ist Phase 7 vollständig — alle 8 Requirements erfüllt.
Output: App.tsx ohne seedIfEmpty; engine.ts mit dynamischer shopId; ArticleGrid + ProductList ohne SHOP_ID-Import; Offline-Meldung wenn DB leer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/07-server-sync-multi-laden/07-01-SUMMARY.md
@.planning/phases/07-server-sync-multi-laden/07-02-SUMMARY.md

<interfaces>
<!-- Neue Interfaces aus Plan 01 + 02 — Executor braucht keine weitere Exploration -->

Aus client/src/db/index.ts (nach Plan 02):
```typescript
export const db = new FairstandDB();
// KEIN SHOP_ID mehr
export function setShopId(id: string): void  // setzt _shopId Modul-Variable
export function getShopId(): string           // wirft wenn null
```

Aus client/src/sync/engine.ts (AKTUELL — wird in Task 1 geändert):
```typescript
import { db, SHOP_ID } from '../db/index.js';
// downloadProducts() nutzt SHOP_ID in der URL → muss getShopId() werden
// flushOutbox() löscht Outbox-Einträge → danach downloadProducts() aufrufen
export async function flushOutbox(): Promise<void>
export async function downloadProducts(): Promise<number>
```

Aus client/src/App.tsx (AKTUELL — wird in Task 2 geändert):
```typescript
import { seedIfEmpty } from './db/seed.js'; // ← ENTFERNEN
// useEffect ruft seedIfEmpty().catch(console.error) → ENTFERNEN
// Nach state === 'unlocked': downloadProducts() wenn online aufrufen
```

Aus client/src/features/pos/ArticleGrid.tsx (AKTUELL):
```typescript
import { db, SHOP_ID } from '../../db/index.js';
// db.products.where('shopId').equals(SHOP_ID) → equals(getShopId())
```

Aus client/src/features/admin/products/ProductList.tsx (AKTUELL):
```typescript
import { db, SHOP_ID } from '../../../db/index.js';
// db.products.where('shopId').equals(SHOP_ID) → equals(getShopId())
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: engine.ts auf getShopId() umstellen + Download nach Flush</name>
  <files>client/src/sync/engine.ts</files>
  <read_first>
    - client/src/sync/engine.ts (vollständig — zwei Änderungen: SHOP_ID → getShopId(), nach Flush downloadProducts())
    - client/src/db/index.ts (nach Plan 02 — getShopId ist jetzt vorhanden)
  </read_first>
  <action>
In client/src/sync/engine.ts zwei Änderungen:

**1. Import-Zeile ändern:**
```typescript
// ALT:
import { db, SHOP_ID } from '../db/index.js';
// NEU:
import { db, getShopId } from '../db/index.js';
```

**2. downloadProducts()-Funktion: SHOP_ID → getShopId():**
```typescript
// ALT:
const res = await fetch(`/api/products?shopId=${SHOP_ID}`);
// NEU:
const res = await fetch(`/api/products?shopId=${getShopId()}`);
```

**3. flushOutbox(): Nach Erfolg downloadProducts() aufrufen:**
Nach dem Block `await db.outbox.bulkDelete(ids);` und dem Sale-syncedAt-Block am Ende des try-Blocks, BEVOR `flushing = false` im finally:

```typescript
// Download-Sync nach erfolgreichem Upload: aktuellste Daten holen
downloadProducts().catch(() => {}); // fire-and-forget
```

**4. Startup-Download (letzte 3 Zeilen der Datei): SHOP_ID-Abhängigkeit entfernen:**
```typescript
// ALT:
if (typeof window !== 'undefined' && navigator.onLine) {
  downloadProducts().catch(() => {});
}
// NEU: Entfernen — App.tsx übernimmt den Download nach Login (Plan 03 Task 2)
```

Die letzten 3 Zeilen komplett entfernen. App.tsx ruft downloadProducts() nach erfolgreichem Login auf.
  </action>
  <verify>
    <automated>grep -n "SHOP_ID" /Users/simonluthe/Documents/fairstand/client/src/sync/engine.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep "SHOP_ID" client/src/sync/engine.ts` gibt keinen Treffer
    - `grep "getShopId" client/src/sync/engine.ts` gibt mindestens 1 Treffer (im fetch-URL)
    - `grep "downloadProducts.*catch" client/src/sync/engine.ts` gibt mindestens 1 Treffer (nach flush)
    - Die automatische Startup-Download-Zeile am Dateiende ist entfernt
  </acceptance_criteria>
  <done>engine.ts nutzt getShopId(); nach Outbox-Flush wird downloadProducts() getriggert</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: App.tsx + ArticleGrid + ProductList auf neue Architektur umstellen</name>
  <files>
    client/src/App.tsx
    client/src/features/pos/ArticleGrid.tsx
    client/src/features/admin/products/ProductList.tsx
  </files>
  <read_first>
    - client/src/App.tsx (vollständig — seedIfEmpty entfernen, downloadProducts nach Login)
    - client/src/features/pos/ArticleGrid.tsx (vollständig — SHOP_ID → getShopId())
    - client/src/features/admin/products/ProductList.tsx (vollständig — SHOP_ID → getShopId())
    - client/src/sync/engine.ts (nach Task 1 — downloadProducts ist dort exportiert)
  </read_first>
  <action>
**A) client/src/App.tsx** — drei Änderungen:

1. Import entfernen: `import { seedIfEmpty } from './db/seed.js';`
2. Import hinzufügen: `import { downloadProducts } from './sync/engine.js';`
3. Im useEffect: `seedIfEmpty().catch(console.error);` entfernen und durch Download-Logic ersetzen:

```typescript
// ALT (im useEffect):
seedIfEmpty().catch(console.error);

// NEU: Kein seedIfEmpty mehr
// Download passiert in UnlockedApp (nach Auth)
```

4. In der UnlockedApp-Komponente einen useEffect hinzufügen der beim Mount (= nach erfolgreichem Login) Produkte lädt:

```typescript
function UnlockedApp({ onLock }: { onLock: () => void }) {
  const [activeView, setActiveView] = useState<'pos' | 'admin'>('pos');
  const lowStockCount = useLowStockCount();

  // Beim ersten Rendern nach Login: Produkte vom Server laden wenn online
  useEffect(() => {
    if (navigator.onLine) {
      downloadProducts().catch(() => {});
    }
  }, []); // Nur beim Mount — nicht bei jedem Re-Render

  // ... Rest unverändert
}
```

5. Im App-useEffect den Storage-Persist-Code behalten, nur seedIfEmpty-Zeile entfernen. Der useEffect wird dann:

```typescript
useEffect(() => {
  if (navigator.storage?.persist) {
    navigator.storage.persist().then(granted => {
      if (!granted) {
        console.warn('[Fairstand] Storage-Persistenz abgelehnt — Datenverlust bei Speicherdruck möglich');
      }
    });
  }
  // seedIfEmpty() komplett entfernt
}, []);
```

**B) client/src/features/pos/ArticleGrid.tsx** — Import + Verwendung:

```typescript
// ALT:
import { db, SHOP_ID } from '../../db/index.js';
// NEU:
import { db, getShopId } from '../../db/index.js';
```

```typescript
// ALT im useLiveQuery:
db.products.where('shopId').equals(SHOP_ID)
// NEU:
db.products.where('shopId').equals(getShopId())
```

Hinweis: getShopId() kann theoretisch werfen wenn kein Shop gesetzt ist. ArticleGrid wird aber nur gerendert wenn state === 'unlocked' (nach Login), also ist shopId immer gesetzt. Kein try-catch nötig.

**C) client/src/features/admin/products/ProductList.tsx** — Import + zwei Verwendungen:

```typescript
// ALT:
import { db, SHOP_ID } from '../../../db/index.js';
// NEU:
import { db, getShopId } from '../../../db/index.js';
```

```typescript
// ALT im useLiveQuery:
db.products.where('shopId').equals(SHOP_ID)
// NEU:
db.products.where('shopId').equals(getShopId())
```

Offline-Meldung wenn DB leer: ProductList und ArticleGrid zeigen bereits "Keine Produkte verfügbar" bzw. "Noch keine Produkte angelegt." wenn das Query-Ergebnis leer ist. Das ist ausreichend für den Offline-Fall — kein neuer UI-Code nötig.
  </action>
  <verify>
    <automated>grep -rn "SHOP_ID" /Users/simonluthe/Documents/fairstand/client/src/ 2>/dev/null | grep -v "node_modules" | grep -v ".d.ts"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "SHOP_ID" client/src/` gibt KEINEN Treffer (vollständig eliminiert aus allen Client-Dateien)
    - `grep "seedIfEmpty" client/src/App.tsx` gibt keinen Treffer
    - `grep "downloadProducts" client/src/App.tsx` gibt Treffer (wird nach Login aufgerufen)
    - `grep "getShopId" client/src/features/pos/ArticleGrid.tsx` gibt Treffer
    - `grep "getShopId" client/src/features/admin/products/ProductList.tsx` gibt Treffer
    - TypeScript-Kompilierung ohne Fehler: `npx tsc --project client/tsconfig.json --noEmit`
  </acceptance_criteria>
  <done>SHOP_ID vollständig aus dem Client entfernt; App lädt Produkte nach Login; alle Components nutzen getShopId()</done>
</task>

</tasks>

<verification>
```bash
# SHOP_ID vollständig aus Client entfernt
grep -rn "SHOP_ID" client/src/

# seedIfEmpty nicht mehr in App.tsx
grep "seedIfEmpty" client/src/App.tsx

# engine.ts verwendet getShopId
grep "getShopId" client/src/sync/engine.ts

# Vollständiger TypeScript-Check
npx tsc --project client/tsconfig.json --noEmit
npx tsc --project server/tsconfig.json --noEmit
```
</verification>

<success_criteria>
- Kein einziger `SHOP_ID`-Import mehr in client/src/ (grep findet nichts)
- App.tsx: seedIfEmpty entfernt, downloadProducts nach Login aufgerufen
- engine.ts: getShopId() in der fetch-URL, downloadProducts() nach Flush
- ArticleGrid und ProductList: getShopId() statt SHOP_ID
- TypeScript: Beide tsconfig.json ohne Fehler
- Phase 7 komplett: Alle 8 Requirements adressiert (SYNC-01–04, SHOP-01–04)
</success_criteria>

<output>
Nach Abschluss: `.planning/phases/07-server-sync-multi-laden/07-03-SUMMARY.md` erstellen.
</output>
