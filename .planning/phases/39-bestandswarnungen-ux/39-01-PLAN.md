---
phase: 39-bestandswarnungen-ux
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - client/src/features/pos/LowStockBanner.tsx
  - client/src/features/pos/StockAlertButton.tsx
  - client/src/features/pos/POSScreen.tsx
autonomous: true
requirements:
  - WARN-01
  - WARN-02

must_haves:
  truths:
    - "Im POS-Header ist ein Glocken-Icon sichtbar — ohne Badge wenn kein Artikel unter Mindestbestand"
    - "Sobald mindestens ein Artikel den Mindestbestand unterschreitet, zeigt das Glocken-Icon einen roten Badge-Zaehler"
    - "Klick auf die Glocke oeffnet eine aufgeraeumt Warnliste mit einem Eintrag pro Artikel (Name, Bestand, Mindestbestand)"
    - "Die Warnliste ist nach Dringlichkeit sortiert (niedrigster relativer Bestand zuerst)"
    - "Die bisherige LowStockBanner-Leiste am oberen Rand des POS-Screens ist entfernt"
  artifacts:
    - path: "client/src/features/pos/StockAlertButton.tsx"
      provides: "Bell-Icon mit Badge + Popover-Warnliste"
      exports: ["StockAlertButton"]
    - path: "client/src/features/pos/LowStockBanner.tsx"
      provides: "Leere Datei (deprecated) oder vollstaendig entfernt"
  key_links:
    - from: "client/src/features/pos/POSScreen.tsx"
      to: "StockAlertButton"
      via: "Import und Einbau im Header-Div neben ShoppingCart-Button"
    - from: "StockAlertButton"
      to: "useLowStockProducts()"
      via: "Hook-Import aus client/src/hooks/useLowStockCount.ts"
---

<objective>
Bestandswarnungen im POS-Header von der ausgeklappten Banner-Zeile auf ein Bell-Icon mit Badge-Zaehler und Klick-Popover umstellen.

Purpose: Der bisherige LowStockBanner listet alle Artikel als langen Fliesstext — bei vielen Artikeln mit minStock > 0 fuellt das den kompletten Header. Mit dem neuen Bell-Icon bleibt der Header kompakt; die Details sind per Klick abrufbar.

Output: StockAlertButton.tsx (neue Komponente), angepasstes POSScreen.tsx (LowStockBanner raus, StockAlertButton rein), LowStockBanner.tsx bereinigt.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/39-bestandswarnungen-ux/39-CONTEXT.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: StockAlertButton erstellen und LowStockBanner ersetzen</name>
  <files>
    client/src/features/pos/StockAlertButton.tsx,
    client/src/features/pos/LowStockBanner.tsx,
    client/src/features/pos/POSScreen.tsx
  </files>

  <read_first>
    - client/src/features/pos/POSScreen.tsx — Header-Struktur, wo LowStockBanner und Settings-Button sitzen, wie lowStockCount-Badge aktuell gerendert wird (Zeilen 214-272)
    - client/src/features/pos/LowStockBanner.tsx — bisherige Implementierung die entfernt wird
    - client/src/hooks/useLowStockCount.ts — useLowStockProducts() Hook, Rueckgabe-Typ
  </read_first>

  <action>
**1. StockAlertButton.tsx erstellen**

Neue Datei `client/src/features/pos/StockAlertButton.tsx`:

```tsx
import { useState, useRef, useEffect } from 'react';
import { Bell } from 'lucide-react';
import { useLowStockProducts } from '../../hooks/useLowStockCount.js';

export function StockAlertButton() {
  const products = useLowStockProducts();
  const [open, setOpen] = useState(false);
  const ref = useRef<HTMLDivElement>(null);

  // Sortierung: niedrigster relativer Bestand zuerst
  // (stock / minStock aufsteigend; 0 ganz oben)
  const sorted = [...products].sort((a, b) => {
    const ratioA = a.minStock > 0 ? a.stock / a.minStock : Infinity;
    const ratioB = b.minStock > 0 ? b.stock / b.minStock : Infinity;
    return ratioA - ratioB;
  });

  // Klick ausserhalb schliesst Popover
  useEffect(() => {
    if (!open) return;
    function handleOutside(e: MouseEvent) {
      if (ref.current && !ref.current.contains(e.target as Node)) {
        setOpen(false);
      }
    }
    document.addEventListener('mousedown', handleOutside);
    return () => document.removeEventListener('mousedown', handleOutside);
  }, [open]);

  // Wenn keine Warnungen: Glocke ohne Badge, kein Klick-Handler noetig
  if (products.length === 0) {
    return (
      <div className="min-h-[44px] min-w-[44px] flex items-center justify-center opacity-40">
        <Bell size={22} />
      </div>
    );
  }

  return (
    <div ref={ref} className="relative">
      <button
        onPointerDown={() => setOpen(prev => !prev)}
        className="relative min-h-[44px] min-w-[44px] flex items-center justify-center rounded-xl active:bg-sky-500 transition-colors"
        aria-label={`${products.length} Artikel unter Mindestbestand`}
      >
        <Bell size={22} />
        <span className="absolute -top-1 -right-1 bg-red-500 text-white text-xs font-bold rounded-full w-5 h-5 flex items-center justify-center">
          {products.length}
        </span>
      </button>

      {open && (
        <div className="absolute right-0 top-full mt-2 w-72 bg-white rounded-xl shadow-xl border border-slate-100 z-50 overflow-hidden">
          <div className="px-4 py-2 bg-amber-50 border-b border-amber-100">
            <span className="text-sm font-semibold text-amber-800">
              Mindestbestand unterschritten
            </span>
          </div>
          <ul className="divide-y divide-slate-50 max-h-72 overflow-y-auto">
            {sorted.map(p => (
              <li key={p.id} className="px-4 py-2.5 flex items-center justify-between gap-2">
                <span className="text-sm text-slate-800 truncate">{p.name}</span>
                <span className="shrink-0 text-xs text-slate-500">
                  <span className="font-semibold text-red-600">{p.stock}</span>
                  {' / min '}
                  {p.minStock}
                </span>
              </li>
            ))}
          </ul>
        </div>
      )}
    </div>
  );
}
```

**2. POSScreen.tsx anpassen**

- Import von `LowStockBanner` entfernen
- Import von `StockAlertButton` hinzufuegen: `import { StockAlertButton } from './StockAlertButton.js';`
- Zeile `<LowStockBanner />` (vor dem Header, Zeile ~215) entfernen
- Im Header-Bereich (`.flex.items-center.gap-2`-Div): `StockAlertButton` VOR dem ShoppingCart-Button einfuegen:
  ```tsx
  <StockAlertButton />
  {!shouldShowSidebar && (
    <button ... ShoppingCart ... />
  )}
  ```
- Den bestehenden `lowStockCount`-Badge am Settings-Button (Zeilen 255-259) BEHALTEN — er zeigt immer noch die Warnzahl am Admin-Toggle-Button an (bestehende Logik, andere Semantik als Bell-Button)
- `lowStockCount` Prop aus `POSScreenProps` und Verwendung kann bleiben (wird von App.tsx uebergeben, stoert nicht)

**3. LowStockBanner.tsx bereinigen**

Datei auf minimalen Export reduzieren, damit bestehende Imports nicht brechen falls irgendwo referenziert. Alternativ: Datei loeschen wenn keine anderen Imports existieren.

Pruefe per grep ob LowStockBanner noch woanders importiert wird ausser POSScreen.tsx:
```bash
grep -r "LowStockBanner" client/src --include="*.tsx" --include="*.ts" -l
```
Wenn nur POSScreen.tsx: Datei loeschen. Wenn weitere: Datei auf leeren Re-Export reduzieren oder entsprechende Imports ebenfalls entfernen.
  </action>

  <verify>
    <automated>cd /Users/simonluthe/Documents/fairstand && npx tsc --noEmit -p client/tsconfig.json 2>&1 | head -30</automated>
  </verify>

  <acceptance_criteria>
    - `client/src/features/pos/StockAlertButton.tsx` existiert und exportiert `StockAlertButton`
    - `grep -n "LowStockBanner" client/src/features/pos/POSScreen.tsx` gibt keine Treffer
    - `grep -n "StockAlertButton" client/src/features/pos/POSScreen.tsx` zeigt Import und Verwendung im JSX
    - `grep -n "Bell" client/src/features/pos/StockAlertButton.tsx` zeigt Bell-Import aus lucide-react
    - `grep -n "useLowStockProducts" client/src/features/pos/StockAlertButton.tsx` zeigt Hook-Aufruf
    - TypeScript-Kompilierung laeuft fehlerfrei durch
  </acceptance_criteria>

  <done>
    Bell-Icon mit Badge ist im POS-Header sichtbar (nur Badge wenn minStock-Warnungen vorhanden). Klick oeffnet Popover mit sortierter Warnliste. Die bisherige Banner-Leiste ist aus dem POS-Screen entfernt. TypeScript-Kompilierung ist fehlerfrei.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Beschreibung |
|----------|--------------|
| API → Client | Produktdaten (inkl. stock/minStock) kommen vom Server via TanStack Query |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-39-01 | Information Disclosure | StockAlertButton (Popover) | accept | Bestandsdaten sind shop-intern, kein PII; Popover ist nur auf dem PIN-gesperrten Geraet sichtbar |
| T-39-02 | Tampering | useLowStockProducts | accept | Reiner Read-Hook, keine Schreiboperation; Daten kommen vom Server und sind dort validiert |
</threat_model>

<verification>
1. `npx tsc --noEmit -p client/tsconfig.json` — keine Fehler
2. Im Browser POS-Screen oeffnen: Bell-Icon ist im Header sichtbar
3. Wenn kein Artikel minStock > 0 hat: Bell ohne Badge, gedimmt
4. Wenn Artikel minStock > 0 und stock <= minStock: roter Badge-Zaehler erscheint
5. Klick auf Bell: Popover oeffnet mit Liste (Name, Bestand, Min-Wert)
6. Klick ausserhalb: Popover schliesst sich
7. Kein LowStockBanner-Streifen mehr am oberen Rand des POS-Screens sichtbar
</verification>

<success_criteria>
- Bell-Icon mit rotem Badge zeigt Anzahl Low-Stock-Artikel im POS-Header
- Kein Badge wenn keine Warnungen
- Klick-Popover zeigt vollstaendige Liste sortiert nach Dringlichkeit
- LowStockBanner vollstaendig aus POS-Screen entfernt
- TypeScript-Build fehlerfrei
</success_criteria>

<output>
Nach Abschluss `.planning/phases/39-bestandswarnungen-ux/39-01-SUMMARY.md` erstellen mit:
- Was wurde geaendert (welche Dateien, was genau)
- Neue Komponente StockAlertButton (Exports, Props)
- Entfernte Komponente LowStockBanner
</output>
