---
phase: 01-offline-kern-kasse
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - client/src/db/schema.ts
  - client/src/db/index.ts
  - client/src/db/seed.ts
  - client/src/features/auth/pinAuth.ts
  - client/src/features/auth/useAuth.ts
  - client/src/features/auth/PinScreen.tsx
  - client/src/App.tsx
  - client/src/main.tsx
autonomous: true
requirements:
  - OFF-01
  - OFF-02
  - OFF-05
  - AUTH-01
  - AUTH-02
  - AUTH-03
  - POS-01

must_haves:
  truths:
    - "Die App lädt vollständig offline — kein Netz nötig für Produktanzeige"
    - "PIN-Eingabe (6 Ziffern) schützt die App; nach 120 Minuten Inaktivität Auto-Logout"
    - "Die App ist als Home-Screen-Icon installierbar (PWA-Manifest korrekt)"
    - "IndexedDB enthält alle drei Tabellen (products, sales, outbox) mit shop_id"
    - "Seed-Daten aus Rechnung 2600988 sind in der DB vorhanden (33 Artikel)"
    - "navigator.storage.persist() wird beim App-Start aufgerufen"
  artifacts:
    - path: "client/src/db/schema.ts"
      provides: "Dexie-Schema mit Product, Sale, OutboxEntry und SaleItem Interfaces"
      contains: "shop_id"
      exports: ["db", "FairstandDB", "Product", "Sale", "OutboxEntry", "SaleItem", "CartItem"]
    - path: "client/src/db/seed.ts"
      provides: "33 Artikel aus Rechnung 2600988 als Seed-Funktion"
      contains: "shopId: 'st-secundus-hennstedt'"
    - path: "client/src/features/auth/pinAuth.ts"
      provides: "SHA-256-PIN-Hashing, setupPin, verifyPin, isSessionValid, updateActivity"
      contains: "crypto.subtle"
    - path: "client/src/features/auth/PinScreen.tsx"
      provides: "6-Ziffern-PIN-Eingabe-Komponente"
      contains: "pin.length === 6"
  key_links:
    - from: "client/src/db/index.ts"
      to: "FairstandDB (Dexie)"
      via: "export const db = new FairstandDB()"
      pattern: "new FairstandDB"
    - from: "client/src/main.tsx"
      to: "navigator.storage.persist()"
      via: "App-Start-Initialisierung"
      pattern: "storage.persist"
    - from: "client/src/App.tsx"
      to: "PinScreen / POS-Route"
      via: "useAuth Hook — sessionValid bestimmt angezeigte Komponente"
      pattern: "sessionValid"
---

<objective>
Dexie.js-Datenbankschema mit Offline-Architektur, PIN-Authentifizierung und PWA-Grundlage implementieren. Kein UI für den Kassen-Flow — nur das Datenfundament und die Auth-Schicht.

Purpose: Kassen-UI (Plan 01-03) braucht ein fertiges, typsicheres Dexie-Schema und eine funktionierende Auth-Schicht. Ohne dieses Fundament können keine Verkäufe atomar gespeichert werden.
Output: Lauffähige Dexie-DB mit Seed-Daten, PIN-Auth mit Session-Timeout, PWA-Manifest funktioniert.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-offline-kern-kasse/01-CONTEXT.md
@.planning/phases/01-offline-kern-kasse/01-RESEARCH.md
@.planning/phases/01-offline-kern-kasse/01-01-SUMMARY.md

<interfaces>
<!-- Bestehende Interfaces aus Plan 01-01 (Projektgerüst) -->
<!-- App.tsx und main.tsx werden in diesem Plan erweitert/ersetzt -->

Dexie-Schema (aus RESEARCH.md Pattern 1) — wird hier implementiert:
```typescript
interface Product {
  id: string;           // crypto.randomUUID()
  shopId: string;       // Multi-Tenant — immer setzen; AUTH-02
  articleNumber: string;
  name: string;
  category: string;
  purchasePrice: number; // Cent-Integer (EK-Preis nach Rabatt)
  salePrice: number;     // Cent-Integer (VK-Preis = EVP)
  vatRate: number;       // 7 oder 19
  stock: number;
  active: boolean;
  updatedAt: number;    // Unix-Timestamp ms für LWW-Sync
}

interface SaleItem {
  productId: string;
  articleNumber: string;
  name: string;
  salePrice: number;    // Cent-Integer — Snapshot zum Kaufzeitpunkt
  quantity: number;
}

interface CartItem extends SaleItem {
  // Identisch mit SaleItem — Cart-State vor Verkaufsabschluss
}

interface Sale {
  id: string;
  shopId: string;
  items: SaleItem[];    // JSON-Array (kein eigener Store)
  totalCents: number;
  paidCents: number;
  changeCents: number;
  donationCents: number;
  createdAt: number;    // Unix-Timestamp ms
  syncedAt?: number;    // undefined = noch nicht gesynct
}

interface OutboxEntry {
  id?: number;          // ++id auto-increment
  operation: 'SALE_COMPLETE' | 'STOCK_ADJUST';
  payload: unknown;
  shopId: string;
  createdAt: number;
  attempts: number;
}
```

Dexie-Store-Indizes (aus RESEARCH.md):
- products: 'id, shopId, category, active, [shopId+active]'
- sales: 'id, shopId, createdAt, syncedAt'
- outbox: '++id, shopId, createdAt, operation'

PIN-Auth Pattern (aus RESEARCH.md Pattern 4):
- hashPin: SHA-256 via crypto.subtle
- setupPin(pin): hash speichern in idb-keyval Schlüssel 'pinHash'
- verifyPin(pin): hash vergleichen
- updateActivity(): set('lastActivity', Date.now())
- isSessionValid(): Date.now() - lastActivity < 120 * 60 * 1000

Seed-Daten Quelle: Rechnung 2600988 (Süd-Nord-Kontor)
shopId für Phase 1: 'st-secundus-hennstedt'
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Dexie-Schema, DB-Singleton und Seed-Daten implementieren</name>
  <files>
    client/src/db/schema.ts,
    client/src/db/index.ts,
    client/src/db/seed.ts
  </files>
  <read_first>
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-RESEARCH.md (Pattern 1: Dexie-Schema mit shop_id, Pattern 3: Cent-Integer)
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-CONTEXT.md (Decisions: Seed-Daten aus Rechnung 2600988)
    - /Users/simonluthe/Documents/fairstand/client/src/App.tsx (aktueller Zustand des Placeholders)
  </read_first>
  <action>
**client/src/db/schema.ts** — Alle TypeScript-Interfaces und die FairstandDB-Klasse:

```typescript
import Dexie, { type EntityTable } from 'dexie';

export interface Product {
  id: string;
  shopId: string;
  articleNumber: string;
  name: string;
  category: string;
  purchasePrice: number;  // Cent-Integer
  salePrice: number;      // Cent-Integer
  vatRate: number;        // 7 oder 19
  stock: number;
  active: boolean;
  updatedAt: number;
}

export interface SaleItem {
  productId: string;
  articleNumber: string;
  name: string;
  salePrice: number;  // Cent-Integer, Snapshot
  quantity: number;
}

export type CartItem = SaleItem;

export interface Sale {
  id: string;
  shopId: string;
  items: SaleItem[];
  totalCents: number;
  paidCents: number;
  changeCents: number;
  donationCents: number;
  createdAt: number;
  syncedAt?: number;
}

export interface OutboxEntry {
  id?: number;
  operation: 'SALE_COMPLETE' | 'STOCK_ADJUST';
  payload: unknown;
  shopId: string;
  createdAt: number;
  attempts: number;
}

export class FairstandDB extends Dexie {
  products!: EntityTable<Product, 'id'>;
  sales!: EntityTable<Sale, 'id'>;
  outbox!: EntityTable<OutboxEntry, 'id'>;

  constructor() {
    super('fairstand-db');
    this.version(1).stores({
      products: 'id, shopId, category, active, [shopId+active]',
      sales: 'id, shopId, createdAt, syncedAt',
      outbox: '++id, shopId, createdAt, operation',
    });
  }
}
```

**client/src/db/index.ts** — Singleton-Export:
```typescript
import { FairstandDB } from './schema.js';
export { type Product, type Sale, type SaleItem, type CartItem, type OutboxEntry } from './schema.js';

export const db = new FairstandDB();
export const SHOP_ID = 'st-secundus-hennstedt';
```

**client/src/db/seed.ts** — 33 Artikel aus Rechnung 2600988.
Alle Preise als Cent-Integer (EVP × 100 = salePrice, EK-Preis nach Rabatt × 100 = purchasePrice).
Kategorien ableiten aus Bezeichnungen (Schokolade, Kaffee, Tee, Kunsthandwerk, Sonstiges).

Suche die tatsächlichen Rechnungsdaten: `ls /Users/simonluthe/Documents/fairstand/Süd-Nord-Kontor/`
Falls PDF vorhanden und lesbar, extrahiere die Artikel-Daten. Falls nur PDF und kein Textextrakt verfügbar, erstelle plausible Seed-Daten basierend auf typischen Süd-Nord-Kontor Produkten (Fairtrade-Schokolade, Kaffee, Tee, Kunsthandwerk aus Entwicklungsländern) mit realistischen Fairtrade-Preisen.

Seed-Funktion Struktur:
```typescript
import { db, SHOP_ID } from './index.js';

const SEED_PRODUCTS: Omit<import('./schema.js').Product, 'id' | 'shopId' | 'updatedAt'>[] = [
  // Beispielstruktur:
  {
    articleNumber: 'SN-12345',
    name: 'Fairtrade-Schokolade Zartbitter 100g',
    category: 'Schokolade',
    purchasePrice: 189,   // 1,89€ EK (nach Rabatt)
    salePrice: 299,       // 2,99€ EVP
    vatRate: 7,
    stock: 24,
    active: true,
  },
  // ... weitere Artikel
];

export async function seedIfEmpty(): Promise<void> {
  const count = await db.products.where('shopId').equals(SHOP_ID).count();
  if (count > 0) return; // Bereits geseedet

  await db.products.bulkAdd(
    SEED_PRODUCTS.map(p => ({
      ...p,
      id: crypto.randomUUID(),
      shopId: SHOP_ID,
      updatedAt: Date.now(),
    }))
  );
}
```

WICHTIG: Mindestens 10 Artikel aus verschiedenen Kategorien. Alle Preise als Cent-Integer. Keine Floating-Point-Werte (RESEARCH.md Anti-Pattern).

Falls tatsächliche PDF-Daten aus Süd-Nord-Kontor lesbar sind: Verwende echte Artikelnummern, Namen und Preise. Falls nicht: Erstelle 15-20 plausible Fairtrade-Artikel und vermerke im SUMMARY, dass die Seed-Daten noch mit der echten Rechnung abgeglichen werden müssen.
  </action>
  <verify>
    <automated>
      grep -q "shopId" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts &amp;&amp; grep -q "SALE_COMPLETE" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts &amp;&amp; grep -q "seedIfEmpty" /Users/simonluthe/Documents/fairstand/client/src/db/seed.ts &amp;&amp; grep -q "Cent" /Users/simonluthe/Documents/fairstand/client/src/db/seed.ts &amp;&amp; echo "Schema OK"
    </automated>
  </verify>
  <done>
    - schema.ts exportiert Product, Sale, SaleItem, CartItem, OutboxEntry und FairstandDB
    - Alle Tabellen haben shopId als Feld (AUTH-02)
    - index.ts exportiert db-Singleton und SHOP_ID = 'st-secundus-hennstedt'
    - seed.ts enthält mindestens 10 Artikel mit Cent-Integer-Preisen
    - seedIfEmpty() prüft vor dem Seeden ob bereits Daten vorhanden
  </done>
  <acceptance_criteria>
    - grep -c "shopId" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts | grep -v "^0$"
    - grep -q "SHOP_ID = 'st-secundus-hennstedt'" /Users/simonluthe/Documents/fairstand/client/src/db/index.ts
    - grep -q "crypto.randomUUID" /Users/simonluthe/Documents/fairstand/client/src/db/seed.ts
    - grep -q "count > 0" /Users/simonluthe/Documents/fairstand/client/src/db/seed.ts
    - grep -q "version(1)" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts
    - grep -q "shopId+active" /Users/simonluthe/Documents/fairstand/client/src/db/schema.ts
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: PIN-Auth-Schicht, useAuth-Hook und App-Routing mit PIN-Screen implementieren</name>
  <files>
    client/src/features/auth/pinAuth.ts,
    client/src/features/auth/useAuth.ts,
    client/src/features/auth/PinScreen.tsx,
    client/src/App.tsx,
    client/src/main.tsx
  </files>
  <read_first>
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-RESEARCH.md (Pattern 4: PIN-Auth mit idb-keyval, Pitfall 3: navigator.storage.persist())
    - /Users/simonluthe/Documents/fairstand/.planning/phases/01-offline-kern-kasse/01-CONTEXT.md (Decisions: 6-Ziffern-PIN, 120 Minuten Auto-Logout)
    - /Users/simonluthe/Documents/fairstand/client/src/App.tsx (aktueller Placeholder)
    - /Users/simonluthe/Documents/fairstand/client/src/main.tsx (aktueller Einstiegspunkt)
    - /Users/simonluthe/Documents/fairstand/client/src/db/index.ts (SHOP_ID)
  </read_first>
  <action>
**client/src/features/auth/pinAuth.ts** — Exakt nach RESEARCH.md Pattern 4:
```typescript
import { get, set } from 'idb-keyval';

async function hashPin(pin: string): Promise<string> {
  const encoder = new TextEncoder();
  const data = encoder.encode(pin);
  const hash = await crypto.subtle.digest('SHA-256', data);
  return Array.from(new Uint8Array(hash))
    .map(b => b.toString(16).padStart(2, '0'))
    .join('');
}

export async function setupPin(pin: string): Promise<void> {
  const hash = await hashPin(pin);
  await set('pinHash', hash);
}

export async function verifyPin(pin: string): Promise<boolean> {
  const stored = await get<string>('pinHash');
  if (!stored) return false;
  const hash = await hashPin(pin);
  return hash === stored;
}

export async function updateActivity(): Promise<void> {
  await set('lastActivity', Date.now());
}

export async function isSessionValid(): Promise<boolean> {
  const last = await get<number>('lastActivity');
  if (!last) return false;
  return Date.now() - last < 120 * 60 * 1000;
}

export async function hasPinSetup(): Promise<boolean> {
  const hash = await get<string>('pinHash');
  return Boolean(hash);
}

export async function logout(): Promise<void> {
  await set('lastActivity', 0);
}
```

**client/src/features/auth/useAuth.ts** — React-Hook für Session-Management:
```typescript
import { useState, useEffect, useCallback } from 'react';
import { isSessionValid, verifyPin, setupPin, updateActivity, hasPinSetup, logout } from './pinAuth.js';

type AuthState = 'checking' | 'setup' | 'locked' | 'unlocked';

export function useAuth() {
  const [state, setState] = useState<AuthState>('checking');

  useEffect(() => {
    (async () => {
      const hasPin = await hasPinSetup();
      if (!hasPin) { setState('setup'); return; }
      const valid = await isSessionValid();
      setState(valid ? 'unlocked' : 'locked');
    })();
  }, []);

  // Activity-Timer: bei Benutzerinteraktion lastActivity aktualisieren
  useEffect(() => {
    if (state !== 'unlocked') return;
    const handler = () => updateActivity();
    window.addEventListener('pointerdown', handler);
    return () => window.removeEventListener('pointerdown', handler);
  }, [state]);

  const unlock = useCallback(async (pin: string): Promise<boolean> => {
    const ok = await verifyPin(pin);
    if (ok) {
      await updateActivity();
      setState('unlocked');
    }
    return ok;
  }, []);

  const setup = useCallback(async (pin: string): Promise<void> => {
    await setupPin(pin);
    await updateActivity();
    setState('unlocked');
  }, []);

  const lock = useCallback(async () => {
    await logout();
    setState('locked');
  }, []);

  return { state, unlock, setup, lock };
}
```

**client/src/features/auth/PinScreen.tsx** — 6-Ziffern-PIN-Eingabe, Taschenrechner-Numpad:
- State: `pin` (string, max 6 Zeichen), `error` (string | null), `isSetup` (boolean aus Props)
- Anzeige: 6 Punkte-Kreise (gefüllt wenn Ziffer eingegeben), Numpad-Grid (1-9, löschen, 0, bestätigen)
- Bei isSetup=true: Erste Eingabe → PIN festlegen, zweite Eingabe → Bestätigung. Bei Mismatch: Fehler-Anzeige.
- Bei isSetup=false: PIN eingeben → verifyPin aufrufen. Bei Fehler: "Falscher PIN" anzeigen, PIN zurücksetzen.
- Tap-Targets: min 56×56px (h-14 w-14), sky-400 für Ziffern, sky-500 hover
- Touch-optimiert: `onPointerDown` statt `onClick`

```tsx
interface PinScreenProps {
  mode: 'setup' | 'unlock';
  onSetup: (pin: string) => Promise<void>;
  onUnlock: (pin: string) => Promise<boolean>;
}
```

**client/src/App.tsx** — Routing und Auth-Guard. Ersetzt Placeholder:
```tsx
import { useAuth } from './features/auth/useAuth.js';
import { PinScreen } from './features/auth/PinScreen.jsx';
import { seedIfEmpty } from './db/seed.js';
import { useEffect } from 'react';

export default function App() {
  const { state, unlock, setup, lock } = useAuth();

  useEffect(() => {
    // OFF-02/Pitfall 3: Storage-Persistenz sichern
    if (navigator.storage?.persist) {
      navigator.storage.persist().then(granted => {
        if (!granted) console.warn('[Fairstand] Storage-Persistenz abgelehnt — Datenverlust bei Speicherdruck möglich');
      });
    }
    // Seed-Daten laden
    seedIfEmpty().catch(console.error);
  }, []);

  if (state === 'checking') {
    return (
      <div className="min-h-screen bg-sky-50 flex items-center justify-center">
        <div className="text-sky-500 text-lg">Laden...</div>
      </div>
    );
  }

  if (state === 'setup') {
    return <PinScreen mode="setup" onSetup={setup} onUnlock={unlock} />;
  }

  if (state === 'locked') {
    return <PinScreen mode="unlock" onSetup={setup} onUnlock={unlock} />;
  }

  // state === 'unlocked' — Kassen-UI (kommt in Plan 01-03)
  return (
    <div className="min-h-screen bg-sky-50">
      <div className="p-4 bg-sky-400 text-white flex justify-between items-center">
        <h1 className="text-xl font-bold">Fairstand Kasse</h1>
        <button
          className="text-sm bg-sky-600 px-3 py-2 rounded min-h-[44px]"
          onPointerDown={() => lock()}
        >
          Sperren
        </button>
      </div>
      <p className="p-8 text-slate-600">Kassen-UI wird in Plan 01-03 implementiert.</p>
    </div>
  );
}
```

**client/src/main.tsx** — Unverändert lassen falls bereits korrekt. Falls Placeholder: StrictMode + ReactDOM.createRoot + `<App />`.

Nach Implementierung: `cd /Users/simonluthe/Documents/fairstand/client && npm run build` — muss ohne TypeScript-Fehler durchlaufen.
  </action>
  <verify>
    <automated>
      cd /Users/simonluthe/Documents/fairstand/client &amp;&amp; npm run build 2>&amp;1 | grep -E "error|Error|built in" | tail -5
    </automated>
  </verify>
  <done>
    - pinAuth.ts implementiert setupPin, verifyPin, isSessionValid, updateActivity, hasPinSetup, logout
    - useAuth.ts verwaltet AuthState ('checking' | 'setup' | 'locked' | 'unlocked')
    - PinScreen.tsx zeigt 6 Punkte-Kreise und Numpad, unterscheidet setup/unlock-Modus
    - App.tsx ruft navigator.storage.persist() auf und seedIfEmpty()
    - npm run build läuft ohne TypeScript-Fehler
  </done>
  <acceptance_criteria>
    - grep -q "crypto.subtle.digest" /Users/simonluthe/Documents/fairstand/client/src/features/auth/pinAuth.ts
    - grep -q "120 \* 60 \* 1000" /Users/simonluthe/Documents/fairstand/client/src/features/auth/pinAuth.ts
    - grep -q "storage.persist" /Users/simonluthe/Documents/fairstand/client/src/App.tsx
    - grep -q "seedIfEmpty" /Users/simonluthe/Documents/fairstand/client/src/App.tsx
    - grep -q "pin.length === 6\|pin.length==6" /Users/simonluthe/Documents/fairstand/client/src/features/auth/PinScreen.tsx
    - grep -q "'checking'\|\"checking\"" /Users/simonluthe/Documents/fairstand/client/src/features/auth/useAuth.ts
    - cd /Users/simonluthe/Documents/fairstand/client && npm run build 2>&1 | grep -v "^>" | grep -c "error" | grep "^0$"
  </acceptance_criteria>
</task>

</tasks>

<verification>
Nach Abschluss beider Tasks:
1. `npm run build` in client/ — kein TypeScript-Fehler
2. `grep -q "shopId" client/src/db/schema.ts` — pass (AUTH-02)
3. `grep -q "crypto.subtle" client/src/features/auth/pinAuth.ts` — pass (AUTH-01)
4. `grep -q "storage.persist" client/src/App.tsx` — pass (Pitfall 3)
5. `grep -q "seedIfEmpty" client/src/App.tsx` — pass (Seed-Daten)
6. dist/manifest.webmanifest aus Plan 01-01 noch vorhanden (OFF-05)
</verification>

<success_criteria>
- Dexie-Schema mit 3 Tabellen (products, sales, outbox), alle mit shopId (AUTH-02)
- PIN-Auth vollständig lokal via crypto.subtle + idb-keyval (AUTH-01)
- Session-Timeout 120 Minuten, Auto-Logout bei Inaktivität (per user decision)
- App zeigt PIN-Setup-Screen beim ersten Start, PIN-Unlock-Screen danach
- navigator.storage.persist() wird beim App-Start aufgerufen (Pitfall 3)
- seedIfEmpty() lädt Artikel aus Rechnung 2600988 beim ersten Start
- npm run build läuft fehlerfrei
</success_criteria>

<output>
Nach Abschluss SUMMARY erstellen unter:
.planning/phases/01-offline-kern-kasse/01-02-SUMMARY.md
</output>
